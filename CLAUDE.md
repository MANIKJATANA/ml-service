# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Working rules (always follow)

- **Record every decision.** Any change or non-trivial decision gets a dated entry in `decisions/` (see `decisions/README.md` for the format). Update the index there.
- **Keep this file current.** When architecture, commands, or conventions change, update CLAUDE.md in the same change.
- **Never commit or push on your own.** Make and verify changes, but do not run `git commit`, `git push`, or open PRs until the user explicitly asks. Leave changes staged/unstaged for them to review.
- **Self-review.** After making changes, review your own work and fix the issues you introduced before reporting done.
- **Review→fix loop (2×) after each phase.** After implementing a phase, run a *review agent → apply fixes* cycle **twice** — distinct focus per round (round 1: correctness / bugs / async / error-handling; round 2: edge cases / quality / simplification / test-coverage gaps) — verifying the gate (ruff + mypy + pytest + layering) is green after each round, **before** presenting the phase. Then stop for the user's review/approval before starting the next phase.
- **All DB schema changes go through migrations.** Any change to the database schema (tables, columns, indexes, constraints, types) must be a versioned migration file in the migrations folder — never an ad-hoc schema change made directly in application code. Application code may only assume the schema a migration has already established.
- **Every new env var goes in `.env.example`.** Whenever code introduces a new environment variable — a new `Settings`/pydantic-settings field, an `os.environ`/`os.getenv` read, or a `${VAR}` in `docker-compose.yml` — add it to `.env.example` in the **same** change, documented with its default or a placeholder (**never a real secret**). `.env.example` must always list the full env surface the code supports. Do **not** touch `.env` for this (it's gitignored and holds real values).
- **Never read `.env` files** (or any secrets files), and never store secrets in memory or in code.

## Repo shape: monorepo, 3 Docker images

This is **one repo** that builds **three images** (see [decisions/0003](decisions/0003-monorepo-structure.md)):

| Image | Path | Stack | Role |
|---|---|---|---|
| Frontend | `frontend/` | Next.js (Node) | UI |
| Backend (BE) | `services/backend/` | Python + FastAPI | The "core system": uploads, storage, notifications, distribution; calls the ML service's enrollment API and enqueues inference jobs |
| ML service | `services/ml_service/` | Python + FastAPI + workers | Face enrollment/inference, as specced below |

Python is managed with **`uv` in workspace mode** — one root `pyproject.toml` + one `uv.lock` shared by the Python members (`backend`, `ml_service`); shared Python code goes in `packages/`. The Next.js frontend is a separate Node package. BE is the only caller of the ML service; the ML service never calls BE.

The structure above is **scaffolded** (see [decisions/0004](decisions/0004-scaffold-monorepo.md)): each service is a runnable shell with `/healthz` + `/readyz` and a passing health test. The ML service additionally has its implemented `domain/` + `orchestration/` core (Phase 1, [decisions/0008](decisions/0008-domain-core-design.md)); the **backend build-out is underway** — architecture + scope locked in [decisions/0022](decisions/0022-backend-architecture-and-scope.md) (ports + adapters like the ML service, roll-our-own JWT, reads ML results from the shared DB; docs-first phases, reference in `services/backend/docs/`). **Phase 1 (foundations) has landed** ([decisions/0023](decisions/0023-backend-db-schema.md)): settings, the backend DB + its **own** Alembic chain (`schools`/`users`, migration `0001`, distinct version table `alembic_version_backend`), the ports/registry/container skeleton, structlog, and dep-probing `/readyz`. **Phase 2 (auth + RBAC) has landed** ([decisions/0024](decisions/0024-auth-jwt-and-rbac.md)): roll-our-own JWT (argon2 `PasswordHasher` + PyJWT `TokenService` ports, services stay crypto-free), the `Permission`/`ROLE_PERMISSIONS` RBAC behind one `PermissionResolver` seam, `get_current_user`/`require_permissions` deps, `/v1/auth/{login,refresh,change-password,me}`, migration `0002` (`users.must_change_password`) + case-insensitive email, and the platform-admin bootstrap CLI. **Phase 3 (onboarding + teacher limits) has landed** ([decisions/0025](decisions/0025-onboarding-and-teacher-limits.md)): the first protected routes — platform `POST/GET /v1/schools` + `POST /v1/schools/{id}/admins`, school-admin `POST/GET /v1/staff` — with tenant isolation deriving `school_id` from the token (never the URL), the `max_teachers` teacher cap in `OnboardingService`, `LimitExceededError`→409, `UserRepository.count/list_by_school_and_role`, a shared `UserResponse` (also for `/me`), and shared test doubles in `tests/backend_fakes.py` (no migration). **Phase 4 (students + ML enrollment) has landed** ([decisions/0026](decisions/0026-students-and-ml-enrollment.md)): staff-managed students where creating a student provisions a `students` profile (migration `0003`) **and** a linked `role=student` login (temp password); the reference photo is uploaded FE→Supabase via a backend-minted **signed upload URL** (30 MB FE-enforced; no bytes through the backend), then the backend fires **synchronous ML enrollment** and records `enrollment_status` (an ML outage never blocks account creation). Adds the `StudentRepository`/`ObjectStore` (supabase/local_fs)/`MlEnrollmentClient` (http/fake) ports + `UserRepository.delete`, `UpstreamError`→502, `POST /v1/students` (+ `/upload-url`, `/{id}/enroll`) + `GET`/`DELETE` behind `student:manage` (tenant from the token; two-write create uses compensating-delete, delete uses the `students.user_id` cascade), a shared `tenant_of`, and deps `httpx`+`supabase`. **Phase 5 (events + media + event-level processing) has landed** ([decisions/0027](decisions/0027-events-media-enqueue-status.md)) — and, per owner review, **spans both services with ML-written job status**: uploading a photo only **records** a backend `media` row (`pending`, migration `0004` adds `events`+`media`); a single **`POST /v1/events/{id}/process`** ("redistribute") enqueues **one event job `{school_id, event_id}`** (Redis `XADD`; the queue no longer carries per-photo jobs) and sets the event `processing_status=queued`. The **ML worker** consumes the event job and, via a new **`BackendEventStore`** (read-only-ish mirror `ml_service/db/backend_tables.py` over the shared DB), **reads the event's `media` roster AND writes the backend status columns directly** — event `processing` on pickup / `completed` when done, each photo `completed` as it finishes (skipping photos already `completed`), running the existing per-photo pipeline (which still writes `matches`+`media_detections`) on the rest. **The backend just reads its own `events`/`media` rows for job status — there is NO backend poller and no `media_detections`-presence signal for it** (Phase 6 adds `MlResultsReader`/`db/ml_read.py`, but for gallery *contents*, not job status). Backend ports `EventRepository`/`MediaRepository`/`EventJobProducer` (redis/inproc); ML gains `process_event`; routes behind `event:manage`/`media:upload`/`job:status:view`, tenant from the token; dep `redis>=5`. **This deliberately overrides earlier invariants with owner sign-off** — "no ML changes", "ML never reads the backend" (ML now reads *and writes* backend status columns), and the per-photo job contract ([decisions/0022](decisions/0022-backend-architecture-and-scope.md)) — and the two ML specs were updated to match. Deletes deferred (archive instead); result *contents* (galleries) are Phase 6 (below). **Phase 6 (galleries + download) has landed** ([decisions/0028](decisions/0028-galleries-and-download.md)): the distribution UX reads — the backend's first read of ML result *contents*, via one isolated coupling: `db/ml_read.py` (read-only `matches` Core table on its own `MetaData`, kept out of `Base.metadata` so backend Alembic never manages it) + a 3-method `MlResultsReader` port (`list_{event,student,media}_appearances`) returning pure `Appearance`s (join keys + `confidence`/`needs_review`). A `GalleryService` joins those "who-is-where" facts to the backend's OWN students/events/media rows (grouping in-Python) for the two views — event→students→photos + student→events→photos — plus media→appearances and the student `/me` self-scope; browse-all is the existing `GET /events/{id}/media`. Download mints a short-lived signed URL (`ObjectStore.create_signed_download_url`, `BE_DOWNLOAD_URL_TTL_S`) behind a `GalleryScope` dep — staff (`gallery:view_all`) any media in-school, a student (`gallery:view_own`) only media they appear in, else 404. Adds `StudentRepository.get_by_user_id` + `MediaRepository.list_by_ids`; routes `GET /v1/events/{id}/students`, `/v1/events/{id}/students/{sid}/media`, `/v1/students/{id}/events`, `/v1/students/{id}/media`, `/v1/media/{id}/appearances`, `/v1/media/{id}/download`, `/v1/me/{events,media}`; the perms were pre-seeded in Phase 2. **No migration, no ML change** (pure reads); the consumed `matches` columns get a Phase-7 `information_schema` contract test. **Phase 7 (hardening) has landed** ([decisions/0029](decisions/0029-hardening.md)) — pure hardening, still **no migration, no ML change**: backend Prometheus metrics at `GET /metrics` (`observability/metrics.py` + an `@app.middleware("http")` in `main.py`; labels are method / **route template** / status only — never a concrete `student_id`/`media_id` or the raw path, and unmatched requests collapse to one `__unmatched__` label; dep `prometheus-client`); conditional CORS via `BE_CORS_ORIGINS` (empty → no middleware); the Phase-6 promise redeemed — a `matches` contract test (`tests/adapters/test_ml_read_contract.py`): an always-on model-drift guard comparing `db/ml_read.py`'s `matches` to the ML service's authoritative `Match` ORM model, plus a gated `information_schema` live-schema check; and CI/compose parity — the backend layering grep added to CI (mirroring `scripts/check.ps1`), the backend image added to the `docker-build` job, a Postgres+Redis **`integration` job** that applies both Alembic chains and runs the gated DB/Redis tests, and the compose `backend` service gaining a `redis` dep + `BE_REDIS_URL`/`BE_QUEUE_STREAM`/`BE_CORS_ORIGINS`. Deferred (documented): OTel tracing, rate limiting, security headers. The backend is now v1 feature-complete + hardened; the **frontend build-out is underway** — Next.js 16 + React 19, architecture + design system locked in [decisions/0030](decisions/0030-frontend-architecture-and-design-system.md) (BFF + HttpOnly-cookie auth, Tailwind v4 `@theme` tokens synthesized from `frontend/design/*.DESIGN.md`, per-persona route groups, docs-first phases F0–F7). **F0 (foundations)** + **F1 (auth + app shell + primitive seed, [decisions/0031](decisions/0031-frontend-auth-and-shell.md))** have landed: the BFF auth handlers + `/api/v1` refresh-and-retry proxy + `proxy.ts` presence gate, login/change-password, the role-aware shell + `components/ui` seed; **F2 (platform admin — schools + admins, [decisions/0032](decisions/0032-frontend-platform-admin.md))** adds the `(platform)/schools` list/create + `[schoolId]` detail + add-admin, the first data `Table` + Radix `Dialog` + `Breadcrumb`, and an app-wide `SWRConfig` (its live smoke is pending — Docker was down); **F3 (staff + students + enrollment, [decisions/0033](decisions/0033-frontend-staff-and-students.md))** adds the `(school)` group's **staff** (Table + create-teacher `Dialog`, surfacing the **409** duplicate/**teacher-cap**) and **students** (Table w/ `StudentAvatar` + enrollment pill; create runs the **direct-to-Supabase** reference-photo upload → **synchronous ML enrollment**; `[studentId]` detail with re-enroll + delete), cashing in the one reserved **backend additive** — read-only `email` on the students read model (inner JOIN on `users`, **no migration**) — plus a `RoleGate` (staff is school-admin-only; teachers lack `staff:manage`), the `avatar`/`file-dropzone`/`progress-bar`/`confirm-dialog` primitives, and `lib/api/upload.ts` (mint-then-**XHR-PUT straight to Supabase**, memoized so a rejected-field resubmit doesn't re-upload; its live smoke — the Supabase PUT contract — is likewise pending Docker); **F4 (events + media upload + processing, [decisions/0034](decisions/0034-frontend-events-and-processing.md))** adds `(school)/events` list/create + `[eventId]` detail (edit `PATCH` / archive-restore) + `[eventId]/upload` **multi-file** uploader (a bounded-concurrency `useMediaUpload` pool; photos go **straight to Supabase** via mint→XHR-PUT→register) plus the **Process** action with **live status polling** (`useEventStatus` auto-stop `refreshInterval`; optimistic queued flip so a failed refetch can't strand the UI; the processing pill derived from counts; 400/502 as toasts) — **no backend change** (the Phase-5 contract was complete); **F5 (galleries + download, [decisions/0035](decisions/0035-frontend-galleries-and-download.md))** adds the distribution UX — `(school)/events/[eventId]/gallery` (Radix **Tabs**: All / By-student), a student **"Appears in"** section, and a deep-linkable `photos/[mediaId]` — via a lazy **masonry `PhotoGrid`** (`useInView` IntersectionObserver → per-tile signed-URL fetch through a shared **`SignedImage`** with 403 re-mint + fallback), a full-screen **`Lightbox`** (←/→/Esc, `aria-live` counter, deterministic focus, appearances), and per-tile **download** (`GET /media/{id}/download` → blob save), again **no backend change** (Phase-6 reads complete; new dep `@radix-ui/react-tabs`); **F6 (student self-view, [decisions/0036](decisions/0036-frontend-student-self-view.md))** adds the recipient's side — one consolidated **`(student)/me/events`** ("My Photos") page with an event `FilterChips` filter driving the reused **`PhotoGrid`** — with **appearances hidden for students** via a `showAppearances` prop (`PhotoGrid`→`Lightbox`) that gates the staff-only `/appearances` fetch *and* the panel (403 + privacy; defense-in-depth), download still entitlement-scoped, and `/me/*` reads reusing the F5 gallery schemas (no new types, **no backend change, no new dep**); **F7 (polish + hardening, [decisions/0037](decisions/0037-frontend-polish-and-hardening.md))** closes out the FE build-out — **`app/{error,not-found,global-error}.tsx`** (unified with `FullPageError` into one `FullPageMessage` with a real `<h1>`; the boundary uses `reset`, not the `unstable_`-prefixed `unstable_retry`), a **mobile nav drawer** (Radix Dialog; shared `NavList`/`UserFooter`; close-on-navigate + `matchMedia` resize-close), an **a11y sweep** (`FilterChips` → a roving-tabindex **radiogroup**; `role="alert"` on the 13 error `EmptyState`s + `role="status"` on `GridSkeleton`; full-page `<h1>`s), a **consistent-error-UX** audit (401→login, 403/409/502→toast, 404→in-page/router, render fault→`error.tsx`), the **Lightbox** mobile-panel scroll, the dead `ComingSoon` removed, and a **frontend README** — all **no backend change**. **The FE is now v1 feature-complete + hardened (F0–F7); live smoke across F2–F6 remains pending a running stack.** **The FE needs Node ≥ 20.9 (Next 16) — the shell defaults to too-old Node 18; use `nvm use 22`** (pinned in `frontend/.nvmrc`); its gate is `npm run lint` + `tsc --noEmit` + `next build`. **Post-v1, a product-improvement build track has begun** — the whole-product analysis lives in `product/00`–`03` (a durable knowledge base + the D/P/X lenses/targets + the phased **BP1–BP8** roadmap; owner-delegated prioritization, consent out of scope). **BP1 (Admin Command Center) has landed** ([decisions/0038](decisions/0038-product-build-BP1-admin-command-center.md)): a read-only **`GET /v1/dashboard`** (student/event/photo rollups + needs-attention signals) via grouped/EXISTS/filtered queries over the backend's own rows + the isolated `matches` seam (`MlResultsReader.count_needs_review`, only already-declared columns), composed by a pure `DashboardService`; new aggregate methods on the existing repos + a frozen `EventRollup`; a new **`dashboard:view`** perm (school_admin + teacher, tenant from the token); the "undistributed" alert counts only **active** events. FE: the placeholder `/dashboard` → a real command center (`StatCard` row + needs-attention alerts + first-run invitation) + **nav information-scent badges** via a shared `useDashboard` (SWR key `"dashboard"`, `enabled` gates off non-staff). **No migration, no ML change**; gated real-Postgres tests cover every new aggregate's SQL + tenant scoping. **BP2 (list data-richness & scale) has landed** ([decisions/0039](decisions/0039-product-build-BP2-list-richness.md)): the four admin lists gain **per-row counts + search/filter/sort** (still query-only, no migration/ML). A new isolated **`ListingService`** zips per-row counts to rows in-Python (no N+1) via new batched aggregates on the existing repos + the `matches` seam (`event_match_counts`/`student_appearance_counts`, only declared columns) + `EventMatchCounts`/`StudentAppearanceCounts`/`SchoolRollup`; list-variant schemas (`EventListItem`/`StudentListItem`/`SchoolWithRollupResponse`) keep write DTOs frozen; new **`GET /v1/schools/{id}/admins`** roster + additive `UserResponse.created_at`. FE adds a shared `SearchInput` + `useSort`/`SortableHead` (direction-aware, tie-stable) reusing the F5 `FilterChips`; students/events/schools/staff each get counts + search + (where apt) filter + the schools detail gains `StatCard` rollups + the admin roster. **BP3 (student receive experience) has landed** ([decisions/0040](decisions/0040-product-build-BP3-student-receive-experience.md)): the student `/me/events` gallery goes **Pinterest-grade**, **FE-only** (no backend/ML; one tiny new dep `client-zip`) — real **natural-aspect masonry** via a `variant` on the shared `PhotoGrid`/`PhotoTile` (`MasonryTile` for students with a restrained hover zoom + hover-download; `GridTile` unchanged for staff), a warm hero + a "new since last visit" cue, **download-all** as one client-zip (`useDownloadAll` — bounded concurrency, skip-failures, partial-result toast + `aria-live` progress), and a **global `prefers-reduced-motion` guard** in `globals.css` (closing a gap F7 claimed but never enforced). *(BP3's client-side `useNewSince` "new since" was **superseded by BP4's** authoritative server signal and removed.)* **BP4 (Distribution — the flagship) has landed** ([decisions/0041](decisions/0041-product-build-BP4-distribution.md)): real *delivery* — an authoritative, cross-device in-app **"new photos"** signal for students (**derived**: announced = `notified_at` set OR (`auto_notify` AND `completed_at`); unseen = no read OR `seen_at` < effective; composed in-Python, **never** a SQL join to the `matches` seam — no per-student rows, no backend worker, no ML change), plus staff **"Notify students"** + an **auto-announce** toggle + a **Notified·Seen roster**, and a **multi-channel notifier seam** (`NotificationChannel` port + `CompositeNotifier` over a `BE_NOTIFICATION_CHANNELS` comma-list — `log` now, email/whatsapp future drop-ins that compose together-or-one-at-a-time; best-effort, PII-free). **Migration `0005`** adds `events.auto_notify`/`notified_at` + `notification_reads`, and `set_processing` now keeps `completed_at` **set-forward** on redistribute (a deliberate 0027 tweak so an auto event doesn't un-announce mid-run). Honest limit (documented): **auto drives only the in-app signal** — outbound-on-auto needs a future worker. New `notification:send` perm; staff routes on `events.py`, student on `me.py`; FE student nav badge + `/me` banner (mark-seen on unmount) + staff `DistributionCard`. Migration verified up+down on real Postgres; **no new backend dep**. **BP5 (Trust & accuracy loop) has landed** ([decisions/0042](decisions/0042-product-build-BP5-trust-accuracy.md)): matching becomes a **correctable** loop via a **backend-owned `match_corrections` overlay** (verdict `confirmed`/`rejected`/`added`) keyed on the stable **`(media_id, student_id)`** — **no ML change, no cross-seam SQL join** (composed in-Python off the isolated `matches` seam). **Reject → hide + block download** (the security-sensitive *effective-appearance* gate: plain/confirmed/added → allow, rejected/never-present → 404, unit-tested truth table); students **self-serve "this isn't me"** (`POST /v1/me/media/{id}/not-me`, membership-checked); staff **confirm/reject/undo** + **report-a-miss** (add a missed student → they see + can download) + a **"Needs review (N)"** lane. Three pure overlay helpers (`effective_media_student_ids`/`effective_event_pairs`/`effective_student_pairs`, in `gallery_service.py`) are shared by `GalleryService` (all 6 reads + the download gate, batched — no N+1), the new `ReviewService`, **and** (revising BP4) `NotificationService` — so a rejected student is **never** notified/rostered and an `added` one **is**. Migration **`0006`** (`match_corrections`: `verdict` CHECK enum + `resolves_review` + `UNIQUE(media_id,student_id)` upsert, `corrected_by`→users SET NULL, no FK to ML `matches`); the BP1 dashboard "N to review" becomes **`count_needs_review(raw) − count_resolved()`** (clamped, per-pair so 1:1); the **BP2 list rollups stay raw ML** (documented divergence — galleries are the effective source of truth). New `match:review` perm (admin+teacher); staff `review.py` routes + student `me.py`; `MediaAppearanceResponse` gains `verdict` + nullable `confidence`. FE: a shared **`AppearanceEditor`** (one compact row per student — name + confidence + an inline **X** to remove; a dismissible **"Add students"** dropdown [Radix Popover, new dep `@radix-ui/react-popover`] with a searchable multi-select for report-a-miss) powers both the staff **photo detail** *and* the staff **gallery lightbox** (via `canManageAppearances` on `PhotoGrid`/`Lightbox` — so staff correct **any** photo's mapping inline, needs-review or not), the **event gallery** gains the review tab, and the **student `/me` lightbox** gets "This isn't me". **No ML change.** Migration verified up+down on a **throwaway** DB (never the dev DB); gated real-Postgres coverage for the corrections adapter; 2× review loop (R1 verified the entitlement gate has **no bypass**; R2 fixed coverage/a11y + the **F8 notification-overlay** gap with owner sign-off). **BP6 (Video end-to-end) has landed** ([decisions/0043](decisions/0043-product-build-BP6-video-end-to-end.md)): surfaces the already-built ML video pipeline in the UI, **FE-only** (**no backend/ML change, no migration, no new dep**). `SignedImage` gains a `kind`/`asPlayer` **video branch** — a full `<video controls>` in the lightbox + photo-detail, a muted `#t=0.1` first-frame **poster** + play badge in grid tiles — rendered off the **same signed URL** the `<img>` used (confirmed inline-served: `create_signed_url` sets **no `download` disposition/transform**, so Supabase serves it inline with range requests → it streams + seeks). `PhotoGrid` moves from `mediaIds: string[]` to **`items: GalleryItem[]` (`{id, mediaType}`)**, deriving index-aligned `mediaIds`/`mediaTypes` for the Lightbox; the **event uploader accepts video** (`mediaTypeOf` by MIME → registers the real `media_type`; `registerMedia`/`RegisterMediaRequest` already accepted `"video"`), while `uploadReferencePhoto` stays image-only. A video's appearances reuse the **BP5 overlay** unchanged. **Honest limits** (documented): video size bounded by the existing `BE_MAX_UPLOAD_MB` (30 MB) + Supabase bucket limit; no stored poster thumbnail (browser first-frame); lightbox arrows navigate over video seek. FE gate green (tsc + lint + build); 2× review loop (R1 traced inline-serve + `mediaTypes`/`mediaIds` alignment, reverted a layout-changing tile class; R2 media-aware a11y/copy). Deferred: the "who appears when" **timeline** (a new isolated `student_media_appearances` read + `GET /media/{id}/timeline`, corrections-overlaid). **BP7 (Onboarding & bulk) is underway, sliced into four approve-before-commit sub-phases** — **BP7a** setup checklist · **BP7b** reference-photo quality feedback (surface the already-returned-but-discarded ML enroll `detail`; 1-col migration, no ML change) · **BP7c** staff lifecycle + invite model (server-generated temp passwords shown-once + disable/enable [`users.status` already exists, no migration] + resend-invite) · **BP7d** CSV bulk student import (nullable `reference_photo_path` migration → name+email now / photo later, server-gen temp passwords, a bulk endpoint looping the reusable `create_student`). **BP7a (setup checklist) has landed** ([decisions/0044](decisions/0044-product-build-BP7a-setup-checklist.md)): a server-composed **`setup_checklist`** block (5 booleans) on `GET /v1/dashboard` + a guided **`SetupChecklistCard`** on the FE dashboard that supersedes the old `FirstRun` and retires once the school has distributed. **Query-only — no migration, no ML.** Only 2 signals are net-new: `has_staff` (the existing `UserRepository.count_by_school_and_role(TEACHER)`, wired via a new `users` dep on `DashboardService`) and `has_distributed` (a new `EventRepository.count_distributed` aggregate whose predicate mirrors BP4's event-level "announced" — `notified_at` set OR `auto_notify`+`completed_at` — status-agnostic, tenant-scoped); the other three (`has_enrolled_student`/`has_event`/`has_media`) compose off existing counts (`enrolled > 0`, not merely-added). FE: four **core** steps (enroll→event→upload→distribute) drive the "N of 4" progress + the primary CTA; "add a teacher" is **optional/last** (a solo school-admin never needs one). Gated real-Postgres coverage for `count_distributed`; 2× review loop (R1 no bugs; R2 fixed a D6 CTA-wording inconsistency, a D8 sub-AA contrast token on completed labels, and the primary CTA landing on the optional teacher step). **BP7b (reference-photo enrollment quality feedback) has landed** ([decisions/0045](decisions/0045-product-build-BP7b-enrollment-quality-feedback.md)): a failed enrollment now explains **why** instead of a bare "Failed". The ML enroll response already carried a per-photo `detail`/status that the backend parsed then **discarded**; `StudentService._run_enroll` now maps it to a closed **`EnrollmentFailureReason`** `{no_face, ml_unavailable, error}` (`multiple_faces` is **not** a failure — the ML enrolls the largest face), persists it (**migration `0007`**: nullable `students.enrollment_failure_reason` + CHECK, lockstep with the enum), **clears it on a successful (re-)enroll**, and exposes it on `StudentResponse` (inherited by `StudentListItem`). FE: a reason-specific title+fix **`EnrollmentFailureNote`** (`role="alert"`) on the student detail + a compact reason under the "Failed" pill on the list; graceful generic fallback when a `failed` row's reason is null. **No ML change, no new dep.** The cross-service `"no_face"` wire value is a pinned `_ML_STATUS_NO_FACE` constant + mapping tests. **Honest scope:** the fix action — replacing the reference photo **in place** — is **BP7d**; until then `no_face`/`error` copy steers to delete-and-re-add (Re-enroll retries the same photo, fixing only `ml_unavailable`). Migration verified up→down→up on a **throwaway** Postgres (`bp7b_migtest`, dropped; dev `app` DB untouched) + gated real-Postgres adapter round-trip; full backend suite **315 passed**; 2× review loop (R1 no bugs; R2 → the named constant, a `multiple_faces`→error contract pin, a route-level serialization assertion, sharper copy). **Also fixed (separate commit):** the backend ML-enroll HTTP timeout default 30s→60s (`BE_ML_HTTP_TIMEOUT_S`) — the ML service loads `buffalo_l` lazily on its first request, so a cold container's first synchronous enroll could exceed 30s and `ReadTimeout`. **BP7c (staff lifecycle + invite model) has landed** ([decisions/0046](decisions/0046-product-build-BP7c-staff-lifecycle-invite.md)): staff/admin provisioning becomes a real **invite model** + lifecycle — **NO migration** (`users.status` active/disabled already existed and auth already rejects a disabled account). Create-teacher/add-admin now **server-generate** the temp password (`secrets.token_urlsafe(12)`, CSPRNG) returned **exactly once** as **`ProvisionedUserResponse {user, temp_password}`** (`password` dropped from `CreateUserRequest`; the plaintext is never in list/get/me, never logged, only its hash persisted); **disable/enable** via `PATCH /v1/staff/{id}` + `PATCH /v1/schools/{sid}/admins/{id}` backed by a new `UserRepository.set_status` (real lockout — auth rejects at login, refresh, **and** the per-request `get_current_user` reload); **resend-invite** via `POST …/{id}/resend-invite` (regenerate + `must_change_password`, works on a disabled account too). One entitlement guard **`OnboardingService._require_managed_user`** — target must match the caller's school **and** the route's expected role, else **404 (not 403)** — structurally blocks cross-tenant, wrong-role, and self-action (the manager is always a different role than the managed). FE: a shared shown-once **`InviteResultDialog`** (Copy button + `copyToClipboard` fallback; plaintext only in component state) replaces the typed-password field, and each teacher/admin row gains **Resend invite** + **Enable/Disable** actions (per-row `aria-label`). **Scope calls (documented):** students stay caller-supplied → **BP7d** moves them to server-gen; the "N of M" teacher capacity is platform-side (BP2 school-detail rollup), the admin's own `/staff` shows a **count**; the last-active-admin lockout is **allowed** (platform-recoverable). Full backend suite **327 passed** + a gated real-Postgres `set_status` round-trip + a **disabled→login-401→re-enable→200** e2e; 2× review loop (R1 no bugs, security airtight; R2 → per-row aria-labels + resend-revalidate + 2 regression-guard tests). **BP7d (CSV bulk student import — the flagship) has landed** ([decisions/0047](decisions/0047-product-build-BP7d-csv-bulk-import.md)): a class imports from a **name+email CSV**. **Migration `0008`** makes `students.reference_photo_path` **nullable** so bulk students are created **photoless → `pending`** (no ML call); students join BP7c's **invite model** — `create_student` **server-generates** the temp password (a shared `services/credentials.generate_temp_password` now used by onboarding *and* students) with the photo **optional** → **`ProvisionedStudentResponse {student, temp_password}`** (shown once, hash-only). **`POST /v1/students/bulk`** (`student:manage`, tenant from token) loops the reusable two-write (`_provision_student`, compensating-delete) over **≤500** `{name,email}` rows **best-effort** — per-row `created`/`duplicate`/`invalid`(`domain.emails.validate_email`)/`error`, the batch never aborts, and **only `created` rows carry a `temp_password`+`student_id`**; `enroll_student` now **guards a photoless student** (400). FE: a dependency-free CSV parser (`lib/csv.ts` — quoted fields, `""` escapes, CRLF/LF, **BOM strip**, header detection), a 3-step **`BulkImportDialog`** (pick→preview→results) with a **"Download credentials"** CSV, and the single-create dialog drops the typed password (server-gen, shown once via the shared `InviteResultDialog`) while **keeping the reference photo required** (a photoless single student would be stuck until BP7d-2). **Owner scope: bulk-first** — the reference-photo **set/replace** (enrolls photoless/bulk students + closes BP7b's replace-a-bad-photo loop) is deferred to **BP7d-2**; there's also **no student password-resend** yet (lost credentials → delete/recreate), so the results dialog makes Download the emphasized action. **Migration `0008` verified up→down→up on a throwaway Postgres** (`bp7d_migtest`, dropped; dev `app` DB untouched) + a gated photoless round-trip; full backend suite **337 passed**; 2× review loop (R1 no blockers — best-effort loop/tenant/secret/CSV/migration all airtight; R2 → BOM strip, re-pick-same-file, SR phase announcements, Download-primary hierarchy, +2 regression tests). **BP7d-2 (reference-photo set/replace) has landed** ([decisions/0048](decisions/0048-product-build-BP7d2-reference-photo-replace.md)) — the deferred BP7d follow-up, **no migration** (column already nullable from `0008`), no ML change: a **`PUT /v1/students/{id}/reference-photo`** that sets/replaces a student's reference photo then (re-)enrolls — so a **photoless (bulk-imported) student can enroll** ("Add photo") and staff can **swap a bad photo to fix a failed enrollment** ("Replace photo", closing BP7b's loop). `StudentService.set_reference_photo` = tenant-scoped `get_student` (foreign→404 **before** the path guard) → the same `_require_tenant_photo_path` prefix guard → a new `StudentRepository.set_reference_photo` → `_run_enroll` (clears the failure reason on success) → `_reload` with a path-carrying fallback. FE: a `ReferencePhotoDialog` on the student detail (Add/Replace, upload-path memoized so a failed PUT doesn't re-upload), **Re-enroll hidden for a photoless student** (would 400), and the BP7b failure-note fix-copy now points at "Replace photo". Full backend suite **345 passed** + a gated real-Postgres `set_reference_photo` round-trip; 2× review loop (R1 no bugs — guard order/reason-semantics/adapter/FE-flow verified; R2 → stale-comment + fallback copy + the upload-path memo + a replace-on-enrolled test + a route-404 test). **BP7 (Onboarding & bulk) is now complete (a, b, c, d, d-2).** **BP8 (Ops & reliability) is underway, sliced into five approve-before-commit sub-phases** — **BP8a** failed-photo state + retry · **BP8b** access/download audit · **BP8c** rate limiting (+ security headers) · **BP8d** multi-replica enrollment (Redis-lock Option B, config-gated) · **BP8e** retention/erasure. **BP8a (failed-photo state + retry) has landed** ([decisions/0049](decisions/0049-product-build-BP8a-failed-photo-retry.md)): a photo the ML worker can't process used to look `pending` **forever** (silently skipped); now it's marked **`failed`** — visible + retryable. **Migration `0009`** widens `ck_media_processing_status` to add `'failed'` (the ML worker writes it, so it must apply **before** the worker deploys — documented); `InferenceService.process_event` marks a photo `failed` on a fetch/decode/other per-photo error via a new **`BackendEventStore.mark_media_failed`** (writes `failed`, no `completed_at`), while the systemic **`EmbeddingVersionMismatch` still aborts the whole event** (nack + alert) and marks **nothing** failed (the index is wrong, not the photo). Retry = **redistribute** — the worker's roster-skip stays `== completed` only (so a `failed` photo is re-attempted), and the backend's **`process_event` enqueue guard was widened** to refuse only when `pending + failed == 0` (else a `failed`-only event's "Retry failed" would 400). `EventStatusResponse` gains `failed` (+ `total`); the FE event detail shows `· N failed`, a warning note (retry-first copy, since a transient fetch blip is also marked failed), and a **"Retry failed"** button. `EventOutcome` splits `photos_skipped` (now only already-completed) from `photos_failed`. Cross-service (BE + ML + FE), **no ML-contract break**. Migration verified up→down→up on a throwaway Postgres (`bp8a_test`, dropped; dev `app` DB untouched) + a gated real-Postgres `mark_media_failed` round-trip; **BE 346 / ML 128 passed**; 2× review loop (**R1 caught a blocker** — the `process_event` guard hadn't been widened, so "Retry failed" 400'd — fixed + tested; R2 → softer transient-failure copy + a version-mismatch-marks-nothing-failed test + a generic-exception-path test). **BP8b (access/download audit) has landed** ([decisions/0050](decisions/0050-product-build-BP8b-download-audit.md)): every entitled photo download is now recorded in an append-only **`download_audit`** table (**migration `0010`**) — surfaced to school-admins two ways: a per-photo **download-history** panel (staff photo detail + lightbox) and a paginated school-wide **"Access log"** page. **Trust, not compliance.** Recording is a **separate action from the signed-URL mint** — the `GET /media/{id}/download` mint is used for **both viewing and downloading** (`SignedImage` renders every tile/lightbox/detail off that URL), so it records nothing; a dedicated **`POST /media/{id}/download`** → `GalleryService.record_download` (same entitlement gate as the mint) is fired by the FE **only on the actual save** (`recordDownload` in `useDownloadToDisk` + `useDownloadAll`, fire-and-forget so the audit never blocks a download) — so a mere **view is never logged as a download**. `actor_role`/`event_id` are denormalized and the row outlives a deleted actor/subject via FK **SET NULL** (`subject_student_id` set only for a student self-download; a blocked/404 download records nothing). A new **`audit:view`** perm is granted to **school_admin only** — the one deliberate one-line difference from the otherwise-identical teacher set, so adding teachers later is a single `ROLE_PERMISSIONS` edit. Reads go through a read-only **`AuditService`** (`GET /v1/media/{id}/download-log` + `GET /v1/audit/downloads`, tenant strictly from the token) composing actor email + event/student names in-Python; mirrors the BP5 `match_corrections` structure (`DownloadAuditEntry` VO + `DownloadAuditRepository` port + postgres adapter + registry/container + gated real-PG test). FE: a self-gated **`DownloadHistory`** panel (renders nothing + fires no `audit:view` fetch for a non-admin) + a `(school)/audit` **server-paginated** log page (nav + `RoleGate` school-admin-only) + a `formatDateTime` util. **No new env var, no ML change.** Migration `0010` verified up→down→up on a **throwaway** Postgres (`bp8b_migtest`, dropped; dev `app` DB untouched) + a gated real-Postgres round-trip (record → list/count → filters → pagination → **newest-first ordering** → tenant-safe); **BE 368 passed**; FE gate (tsc + lint + build) green; 2× review loop (**R1 no security/tenant bug** — added a `created_at DESC, id DESC` stable sort tiebreaker; **R2 no blocker** — **removed the misleading client-side filter chips** in favour of clean server pagination + `role="alert"` error note + `Query()` annotations); **plus an owner-caught fix**: the audit was initially hooked on the shared signed-URL mint, so a mere *view* logged a download — split into a separate `POST` download action (view mint records nothing). Honest limits (documented): `_compose` lists the full school roster per call (bounded, admin-only); the server-side event/student filters are wired end-to-end but reserved for a future drill-in (no filter UI in v1); only download *intent* (the signed-URL mint) is recorded. **BP8c (rate limiting + security headers) has landed** ([decisions/0051](decisions/0051-product-build-BP8c-rate-limiting-security-headers.md)) — pure hardening (**no migration, no ML change, on by default**) redeeming what 0029 deferred. A **`RateLimiter`** port + FastAPI middleware throttles per a **global** tier + a stricter **`/v1/auth/*`** tier + a per-**`school_id`** tier (verified from the JWT best-effort — a forged/unverified claim is never trusted; the backend can't see the client IP behind the Next BFF, so there is **no per-IP tier** — that belongs at the ingress). Pluggable store via `RATE_LIMITER_REGISTRY` — **`InMemoryRateLimiter`** (per-replica) default, **`RedisRateLimiter`** (`INCR`+`EXPIRE`, reuses `BE_REDIS_URL`) config-gated — both **fail-open** (a store outage returns `allowed=True`; a limiter fault must never take the API down). First tier exceeded → **`429` + `Retry-After`** + a cardinality-safe `backend_rate_limit_rejections_total{scope}` metric; `/healthz`/`/readyz`/`/metrics` are **exempt** (a throttled probe would flap the deploy); the redis client is closed on `lifespan` shutdown. The limiter is built **once per app in `create_app`** (not the process-global `lru_cache` container) so an in-memory limiter can't accumulate counts across the test suite. **Middleware order** (`create_app`, outer→inner): **security-headers → CORS → rate-limit → metrics → routes** — so the headers land on the 429 + every error, the 429 short-circuits **inside** metrics (its own rejection metric, no `__unmatched__` pollution), and CORS preflight `OPTIONS` is handled before the limiter. **Security headers on BOTH** the backend (`_install_security_headers`, `setdefault` so it never clobbers `WWW-Authenticate`/`Retry-After` — nosniff / `X-Frame-Options: DENY` / `Referrer-Policy: no-referrer` / a maximal `default-src 'none'` CSP / HSTS gated on `hsts_enabled`, default off) and the **frontend** (`next.config.ts` `headers()` on `/:path*` — the browser-facing set, since only the Next BFF talks to the browser: nosniff, frame-DENY, referrer, `Permissions-Policy`, `Cross-Origin-Opener-Policy`, HSTS, and a CSP with `frame-ancestors 'none'` + a Supabase-scoped `img`/`media`/`connect-src` + `'unsafe-inline'` script/style with `'unsafe-eval'` **dev-only**). New `BE_RATE_LIMIT_*` / `BE_SECURITY_HEADERS_ENABLED` / `BE_HSTS_*` env vars (+ a test-only `BE_TEST_REDIS_URL`) → all in `.env.example`. **Honest limits (documented):** fixed-window (2× burst at a window boundary; sliding-window is the scale-up); the **auth tier is a single global bucket** across all schools' `/v1/auth/*` incl. refresh (default **300/min** so real concurrent logins aren't locked out — per-IP belongs at the ingress); the FE CSP keeps `'unsafe-inline'` (a **nonce-strict CSP via `proxy.ts`** is the follow-up); in-memory = per-replica (redis is the cross-replica option). **BE 385 passed / 26 skipped** (+ a gated real-Redis round-trip + connect-failure fail-open + an always-on mid-op-`RedisError` fail-open; middleware global/auth/school tiers, probe exemption, malformed-`Authorization` no-500, fail-open-on-raise, rejection-metric; security headers on 200/error/429 + HSTS gate) + layering; FE gate (tsc + lint + build) green, CSP checked against the **installed** Next 16 docs; 2× review loop (**R1 no blocker** — ordering/verified-decode/off-by-one/fail-open/test-isolation all sound; **R2 no code blocker**) → applied the redis `aclose`-on-shutdown, the probe/metrics exemption, the auth default 60→300, the fixed-window-burst doc, `Cross-Origin-Opener-Policy`, and +4 tests. **BP8d (multi-replica enrollment — FAISS write lock, Option B / Redis) has landed** ([decisions/0052](decisions/0052-product-build-BP8d-multi-replica-enrollment.md)) — ML-service only (**no migration, no backend/FE change, no ML-contract change; config-gated, Option A default**), delivering the "Option B" [0011](decisions/0011-faiss-adapter-lifecycle.md)/architecture §7.4 promised. Enrollment serialized each school's FAISS index writes with an in-process `asyncio.Lock` — the fleet lock **only** under single-replica enrollment (a SPOF/bottleneck). Now the per-school write lock is a **pluggable `WriteLockProvider`** — an **adapter seam, not a domain port** (the FAISS index adapter is its only user, so `domain`/`orchestration` stay import-pure and the layering invariant holds): `InProcLockProvider` (Option A, default — the same asyncio.Lock, moved out of `IndexCache` into a container-memoized singleton so the registry stays process-wide/outlives LRU eviction) or `RedisLockProvider` (Option B, config-gated **`ML_FAISS_LOCK_IMPL=redis`**) — a per-school `redis.asyncio` token-checked `Lock` (`SET NX PX` + Lua compare-and-del release, key `faiss:lock:{school_id}`) reusing the **shared** Redis client the container already builds for the queue — so **enrollment can run multi-replica** without two replicas clobbering a school's index (reads were already cross-replica-safe via the `meta.version` reload). **Fail-LOUD** (the deliberate opposite of BP8c's fail-open rate limiter): a lock-backend outage (`RedisError`/`OSError`) or a `wait_s`-timeout (`acquire()` → `False`) **raises `LockAcquisitionError`** (→ ML 500 → the backend records `enrollment_status=failed`, retryable, never blocks account creation per [0026](decisions/0026-students-and-ml-enrollment.md)) — **never** a silent unlocked write, which would risk a lost enrollment (correctness > availability here). `_RedisLockCtx` wraps the redis lock so the error mapping + a best-effort release stay in the adapter; the FAISS critical section (`load → rebuild-in-thread → store.save[meta.json last] → cache.invalidate`, for both `upsert` + `delete`) is **unchanged** — the lock just wraps it. New `faiss_lock_impl`/`faiss_lock_lease_s`(60s)/`faiss_lock_wait_s`(30s) settings + a `FAISS_LOCK_REGISTRY` + a memoized `container.write_lock_provider()`; `ML_FAISS_LOCK_IMPL`/`_LEASE_S`/`_WAIT_S` in `.env.example` + a `docker-compose.yml` comment on scaling (`ML_FAISS_LOCK_IMPL=redis` + raise the `ml-service` replica count). **Honest limits (documented):** a write slower than `lease_s` auto-expires the lock under a live holder (the classic TTL-lock hazard — loudly logged `faiss_write_lock_lease_lost` with a "raise `ML_FAISS_LOCK_LEASE_S`" hint; `lease_s` must exceed the slowest index rebuild+upload — a monitorable operational invariant; lease auto-extension is a future refinement); `wait_s` + the enroll must stay under `BE_ML_HTTP_TIMEOUT_S` (60s); `redis.asyncio.Lock` keeps its release token in thread-local storage, so acquire+release must run on one thread (they do — both awaited on the event-loop thread, only the pure rebuild offloaded). **ML 137 passed / 10 skipped** (+ gated real-Redis mutual-exclusion / cross-school concurrency / wait-timeout fail-loud on a real Redis; + an **always-on** dead-port fail-loud + a `__aexit__` release-swallow [`LockNotOwnedError` + generic `RedisError`] + an **adapter-uses-the-injected-provider** spy + the container inproc/redis/`ConfigurationError` selectors) + layering + mypy; 2× review loop (**R1 no blocker, ship-ready** — the cardinal invariant [no `store.save` without the lock], fail-loud on both paths, singleton wiring, no re-entrancy, shared-redis lifecycle all verified; **R2 no code blocker**) → applied the loud lease-loss logging + thread-local comment + the container/adapter/release tests + pinned `faiss_lock_impl` in the container-test settings (pydantic-settings was reading `.env` for the unset field). **BP8e (complete student erasure) has landed** ([decisions/0053](decisions/0053-product-build-BP8e-student-erasure.md)) — the **final** slice of BP8 and the whole BP1–BP8 roadmap. "Delete a student" was incomplete: three things orphaned — the reference-photo **object in storage** (no `delete` on either service's storage port), the ML-owned **`matches`**, and the ML **detection-audit** candidate rows naming the student. BP8e closes all three so **delete means gone** (trust, not legal/consent — out of scope). **Cross-service (BE + ML); no migration, no new perm (`student:manage`), no new env var; no BE↔ML HTTP-contract change** (the existing ML DELETE just does more). Ownership-split: the **backend** adds `ObjectStore.delete` (supabase `remove([path])` idempotent / local_fs no-op) and `StudentService.delete_student` removes the reference-photo object **best-effort with a bounded 3× retry** (0.2s backoff) — a leaked object is logged `orphaned_reference_photo` and never blocks the erasure (owner's chosen behaviour), skipped for a photoless student; order is `get_student → ML delete → object delete → login delete (cascade)`. The **ML** adds `MatchRepository.delete_by_student` (`DELETE … WHERE school_id AND student_id`) + `DetectionRepository.delete_candidates_by_student` (`DELETE FROM face_detection_candidates WHERE student_id` — only the student-naming rows; the media-centric parents `media_detections`/`media_frames`/`face_detections` stay, shared across students), and `EnrollmentService.delete` purges them (constructor gains the two repos, injected in `container.enrollment_service()` — already built for inference). **`match_corrections` + `notification_reads` need NO code** — the two-level FK cascade (`users`→`students` CASCADE → `match_corrections.student_id`/`notification_reads.student_id` CASCADE) erases the student's rows automatically, and `download_audit` rows are anonymized (`subject_student_id`/`actor_user_id` SET NULL) — both **proven by a gated real-Postgres cascade test** (`test_student_erasure_cascades_and_anonymizes`; the in-memory fakes can't model a two-level cascade; a second student's data stays untouched). Verified: BE + ML ruff/mypy/unit (object-erase + 3×-retry-then-best-effort + photoless-skip; `EnrollmentService.delete` purges matches+detections via stub repos) + **gated real-Postgres** round-trips on **throwaway** DBs (the cascade/anonymize proof + `delete_by_student`/`delete_candidates_by_student` tenant-scoped, others survive) + layering; 2× review loop. Honest limit (documented): `download_audit` is anonymized not deleted (the audit outlives its subject, BP8b). Deferred (documented, `product/03`): **event hard-delete** (a real "delete event" purging its media rows + storage objects + matches/detections — same machinery) + **time-based retention** (an admin-triggered/scheduled purge). **BP8 is complete (a, b, c, d, e) → the entire BP1–BP8 product-improvement roadmap is complete.** **A Round-2 product-improvement track has since opened** ([decisions/0054](decisions/0054-product-review-round-2-and-BP9-roadmap.md)): the per-role, Greenfield-scale review lives in `product/02-product-review.md` and the **BP9–BP17** roadmap in `product/04-improvement-roadmap-round-2.md`. **BP9 (scale-ready lists & galleries) has landed** ([decisions/0055](decisions/0055-product-build-BP9-scale-ready-lists-galleries.md)) — the app now stays bounded at real-school scale (~800 students / ~120 events / ~900-photo student), **one phase**, **no ML change**. **Backend:** server pagination on the **6 unbounded list endpoints** (students/events/staff/schools/admins/event-media) replicating the BP8b audit template — a generic `services/pagination.py::Page[T]` + `{items,total,limit,offset}` `*PageResponse` envelopes, a shared `api/pagination.py` (`limit`/`offset`/`q`/`sort`/`dir` + a per-endpoint status filter; out-of-range or unknown-sort → **422** via the endpoint's `*Sort` enum). **Global count-sort via a bounded id-scan** in `ListingService` (fetch all matching ids `list_ids` → sort in-Python off the school-wide grouped aggregate BP2 already runs → hydrate one page `list_by_ids`) so sorting by count columns spans the whole list **without ever SQL-joining the isolated ML `matches` seam**. **De-rostering:** `GalleryService.{event_students,student_events,media_appearances}` + `AuditService._compose` fetch **only matched ids** (new `Student`/`Event`.`list_by_ids`, Media's pre-existed) instead of `list_by_school`+in-Python filter. Sort enums (`StudentSort`/`EventSort`/`UserSort`/`SchoolSort` + `*_COUNT_SORTS`) are pure `domain/models.py` value types (layering-safe). **Migration `0011`** adds the composite indexes (each default sort + name search); new `BE_DEFAULT_PAGE_SIZE`/`BE_MAX_PAGE_SIZE`. **The list response-model change (`list[...]`→envelope) is breaking — it ships WITH the FE (one phase).** **Frontend:** a generic `useInfiniteList` (`useSWRInfinite`, `keepPreviousData`, resets to page 1 on filter change) + a shared `<LoadMore>` (IntersectionObserver sentinel + always-present "Load more" fallback + "Showing N of M") back **uniform infinite-scroll** on all five list pages (server `q`/`sort`/`dir`/`status`, search debounced 300 ms; `SearchInput`/`SortableHead`/`FilterChips` kept as controls; chip counts from the dashboard rollup — `FilterChips.count` now optional). `PhotoGrid` **windows** its tiles (adjust-state-during-render reset; never mounts 900) and page-loads the event gallery via `onLoadMore`/`hasMore`; **`useDownloadAll` streams to disk** via the File System Access API (bounded memory, survives 900 photos) with a capped (500) buffered fallback. **Honest limits (documented):** offset paging (keyset = scale-up); the student `/me` gallery still fetches its media-id list whole (windowed render — appearance/`/me` pagination out of scope); the download fallback caps non-streaming browsers at 500; FE `MediaProcessingStatus` stays `pending|completed` (no status-filter UI on the media page). Verified: BE ruff+mypy+**403 unit** (+ the new BP9 pagination/de-rostering suite: envelope/slice/search/global-count-sort/422/tenant + a de-rostering spy) + **22 gated real-Postgres** repo tests (ILIKE-escape + tenant scope on a **throwaway `bp9_migtest`** DB, dropped; dev `app` untouched) + layering; migration `0011` up→down→up on the throwaway DB; FE tsc+lint+`next build` green; 2× review loop. **BP17 (image thumbnails) has landed** ([decisions/0056](decisions/0056-product-build-BP17-image-thumbnails.md)) — the fast-UI companion to BP9, **no ML change, no migration** (columns from `0012`): gallery/masonry tiles + the student-list/detail **avatar** load a small low-res image, the lightbox/download stay full-res, via **backend-generated thumbnails**. *(Two drafts superseded: draft 1 = on-the-fly Supabase transforms [Pro-plan only]; draft 2 = browser "save-twice" to a `{uuid}.thumb` key — the `.thumb`-suffixed upload **fails** at Supabase, so no thumbnail was ever stored and the list fell back to full-res. Both reverted; `BE_IMAGE_TRANSFORMS_*` gone.)* The FE uploads only the original; on register/create the backend **downloads it, compresses it, uploads a `thumb-{name}.jpg` sibling**, and stores the path — deterministic thumbnails on any plan, browser-independent. **Pillow behind a `Thumbnailer` port** (`adapters/imaging/pillow_thumbnailer.py`; `domain`/`services` stay image-lib-free — the layering test now bans `PIL`); `ObjectStore` grows `download_bytes`/`upload_bytes` (supabase real / local_fs stub). A pure `services/thumbnails.py` (`thumb_key` → `{dir}/thumb-{name}.jpg` under the same prefix + best-effort `generate_thumbnail`) is called by `MediaService.register_media` + `StudentService.create_student`/`set_reference_photo`: image → generate + store; **video / a failed compress / a store outage → `thumbnail_path=null`** (best-effort — a thumbnail failure never fails the upload); the photo-replace still best-effort-deletes the old original+thumbnail (BP8e). The mint reverts to a **single** `SignedUpload` target. Serve: `GalleryService.download_url(thumbnail=)` behind `GET /media/{id}/download?size=thumb|full` (default full) + a new `StudentService.reference_photo_url` behind `GET /v1/students/{id}/reference-photo?size=` (`student:manage`, tenant from token; 404 photoless/foreign) select `thumbnail_path if (thumb AND present) else storage_path` — **falls back to full-res for pre-BP17 rows + video**; `MediaVariant` StrEnum → free 422 on a bad `size`; the entitlement gate runs before the size selection (thumb never widens entitlement). FE: single mint→PUT→register (`image-compress.ts` removed); read models expose the thumb's presence (`StudentResponse.reference_photo_thumbnail_path`, `MediaResponse.thumbnail_path`, `GalleryMediaResponse.has_thumbnail`) and the FE requests `?size=thumb` **only when one exists** (avatar `useStudentReferencePhoto` size; gallery tiles via `hasThumbnail` on the shared `GalleryItem`/`PhotoTile`), else full; lightbox/download stay full; the create/replace dialogs memoize the uploaded path. New dep `pillow`; env `BE_THUMBNAILER_IMPL`/`BE_IMAGE_THUMBNAIL_MAX_EDGE`/`_QUALITY` (in `.env.example`). Honest limits (documented): a backend download→compress→upload per image (throttled by the FE pool; sync so the thumb is immediate — async is the scale-up); no backfill; video keeps its browser poster; local-fs dev gets no thumbnails. Verified: BE ruff+mypy+**435 passed / 30 skipped** + layering (`PIL` banned in domain/services): generate a `thumb-*.jpg` under the prefix on image register/create, video/fail/outage→null, the stored-path serve + full-res fallback on both surfaces (service + route), gate fires regardless of size, replace regenerates + deletes the old objects; FE tsc+lint+`next build` green; **no migration, no ML change**; 2× review loop. **BP10 (bulk photo enrollment) has landed** ([decisions/0057](decisions/0057-product-build-BP10-bulk-enrollment.md)) — the Round-2 "switch-on" unblocker (theme A): a class of faces enrolls from **one upload** instead of 800 one-at-a-time. **One read endpoint + one cleanup endpoint; no migration / no ML change / no new dep / no new perm** (reuses `student:manage`). `POST /v1/students/match-photos` maps photo **filenames** → students (auto-fills an editable FE table; the FE sends only filenames, so the ~800-email roster never hits the browser), matched by **email (case-insensitive)** or a **UUID stem**, tenant-scoped (`StudentRepository.resolve_by_emails` + the existing `list_by_ids`; a foreign email → unmatched, no cross-tenant enumeration). The upload-and-enroll loop runs on the **frontend** — a bounded-concurrency pool over the already-tested per-student `PUT …/reference-photo` (set + BP17 thumbnail + old-object cleanup + ML enroll), the same pattern as the event multi-uploader — so there is **no new enroll/upload backend surface**. **Upload-after-confirm + only-assigned = no orphans by construction** (unmatched/skipped photos are never uploaded); the rare uploaded-but-attach-failed object is best-effort reaped via a tenant-prefix-guarded `DELETE /v1/students/reference-photo-upload` (a foreign path → 400; the delete swallows a store blip). Batch capped at a configurable **`BE_BULK_PHOTO_MAX_FILES`** (default 50, schema-enforced → 422; FE mirror `NEXT_PUBLIC_BULK_PHOTO_MAX_FILES`). **"Retry failed (N)"** loops the existing `POST …/enroll` over the `failed` set (a photoless `pending` student uses bulk photo upload instead) — no new endpoint. FE: a 3-step **`BulkPhotoDialog`** (pick → editable **map & fix** → results) that holds the `File` objects until confirm, a single-select searchable **`StudentPicker`** (reuses BP5's `useStudents({q})` popover), and the pooled **`useBulkPhotoEnroll`** (orphan cleanup on attach-failure). Honest limits (documented): 50/batch (a 500-class ≈ 10 batches; a background job is the scale-up), client-side loop, `no_face` won't self-heal on retry, the picker is Tab-navigable without arrow-roving (mirrors BP5), local-fs dev has no real bytes. **BE ruff+mypy+457 passed / 31 skipped** + layering + a gated real-Postgres `resolve_by_emails` round-trip (throwaway `bp10_migtest`, dropped; dev `app` untouched); FE tsc+lint+`next build` green; 2× review loop (R1 no blocker — tenant/route-ordering/matching/async all sound → fixed the stale retry-count refresh; R2 → best-effort cleanup + per-row aria-labels + one-shot live regions + UUID-via-route/same-student-twice/store-failure coverage). **No migration, no ML change, no new dependency, no new permission.** **BP11 (Organizing structure — classes/sections + event terms/categories) is underway**, sliced into three approve-before-commit sub-phases per the owner-approved plan (an HTML explainer + a decisions Q&A): **BP11a** student classes · **BP11b** event term/category + calendar · **BP11c** teacher delegation (staged — a "focus" filter by default, an opt-in "restrict" switch); cohort-scoped *matching* stays deferred to BP15 (no ML change). **BP11a (student classes) has landed** ([decisions/0058](decisions/0058-product-build-BP11a-student-classes.md)): a tenant-owned **class** concept (`student_groups`) a student belongs to at most one of (a nullable `students.student_group_id` FK, **ON DELETE SET NULL** so deleting a class un-assigns its students, never deletes them — **migration `0013`**), with a **`StudentGroupRepository`** + a pure **`ClassService`** (class CRUD + single/bulk student assignment, all tenant-scoped from the token, a foreign class/student → 404), the students slice gaining a `student_group_id` **list filter** + a class-name **LEFT JOIN** on its read model (denormalized like `email`), and a new **`class:manage`** permission granted to **school_admin only** (the one-line admin/teacher difference, like `audit:view`) for class **lifecycle** — reads + student assignment ride on the existing `student:manage` (both roles). Routes: `GET/POST /v1/classes`, `GET/PATCH/DELETE /v1/classes/{id}`, `POST /v1/classes/{id}/members` (bulk assign), `PATCH /v1/students/{id}` (set/clear one student's class; `student_group_id` required-but-nullable so an empty body is a 422, never a silent un-assign) + the `?student_group_id=` students filter. FE: a **Classes** nav item + a `(school)/classes` page (`RoleGate` school_admin — list + create/rename/delete) + a `classes/[classId]` detail (roster + an **inline** searchable "Add students" dialog that bulk-assigns — inline, not a portaled popover, so the modal scroll-lock never blocks it, the BP10 trap), plus a class `<select>` filter + badge on the students list (the active filter is **derived** so a deleted class can't strand the list) + a class selector on the student detail; a `useClasses` SWR hook keyed `"classes"`. **No ML change, no new backend dependency, no new env var.** Migration `0013` verified up→down→up on a **throwaway** Postgres (`bp11a_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**484 passed / 33 skipped** + layering (+ `test_bp11a_classes.py` 27 tests + 2 gated real-Postgres round-trips covering the class CRUD/counts/LEFT-JOIN/filter/**SET NULL cascade** + the bulk tenant-scope); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers**: tenant isolation, the `false()` filter branch, the 3-tuple LEFT-JOIN mapping, the permission split, SET NULL cascade + async all verified; **R2** → fixed a stuck-class-filter dead-end [derived, not effect-reconciled], a stale edit-dialog prefill [remount `key`], simplified the name guard, +4 tests [admin-only lifecycle guards / blank-name / name-only / class-filter×count-sort], a11y [roster `role="status"`, result-count live region]). **BP11b (event term/category + calendar) has landed** ([decisions/0059](decisions/0059-product-build-BP11b-event-categories-calendar.md)): events gain a free-text **term** + a **category**, plus a read-only **month calendar** (List⇄Calendar tabs) — **no ML change, no new permission, no new dep, no new env var**. Per an owner change during the HTML review, **categories are per-school configurable** (a tenant-owned `event_categories` table like BP11a's `student_groups`) — seeded with 6 defaults (`Sports·Academic·Arts·Trip·Ceremony·Other`) **on school-create** (`OnboardingService.create_school` → `EventCategoryRepository.seed_defaults`) **AND** into every existing school in **migration `0014`**'s `INSERT…SELECT` data step; a pure **`EventCategoryService`** does list / add [case-insensitive dedupe → 409] / remove, all on **`event:manage`** (admins + staff). Events reference a category via nullable `events.category_id` (**ON DELETE SET NULL** — removing a category un-tags its events, never deletes them) + a nullable `events.term`; the category name is **denormalized via a LEFT JOIN** on the object reads (like `student_group_name`). The events list gains **category / term / date-range** filters (threaded through both the row-native and count-sort id-scan paths; a malformed category_id → `false()`) + `GET /v1/events/terms` (registered **before** `/{event_id}`); create/update thread category_id + term (a foreign category → 404; term/category follow the `None`=unchanged convention — **can't be cleared** via PATCH, per 0027). FE: a from-scratch read-only month calendar whose one real risk — the **timezone off-by-one** (`new Date("2026-07-04")` = UTC-midnight → the wrong day west of UTC) — is contained in one pure `lib/events/calendar.ts` (`parseLocalDate`/`toISODate`/`buildMonthGrid`; never `new Date(iso)`/`toISOString`); a `categoryColor` hash-palette (red excluded), a **Manage categories** dialog (confirm-then-remove), shared category/term `<select>` filters (BP11a "derived, not effect-reconciled" stale guard) + a category badge, and the create/edit dialogs' category + term fields. Migration `0014` verified up→down→up **+ seed** on a throwaway Postgres (`bp11b_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**510 passed / 34 skipped** + layering (+ `test_bp11b_calendar.py` 26 + a gated real-Postgres round-trip covering CRUD/seed/LEFT-JOIN-name/filters/**SET NULL cascade**); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers**: the timezone module / tenant isolation / the seed / the LEFT-JOIN·filter·count-sort paths / SET NULL / route ordering / can't-clear / async all verified; **R2** → confirm-before-category-delete, a month-truncation note, calendar gridcell aria-labels, grid-built-once, the edit can't-clear UX, dropped the red tint, +4 tests). **BP11c (teacher delegation) has landed** ([decisions/0060](decisions/0060-product-build-BP11c-teacher-delegation.md)): a school admin **assigns teachers to classes** and a teacher's students/events lists **focus** on those classes — the owner's call was **focus-only** (a soft "My classes / All" default; **no** hard "restrict" switch), so delegation is convenience-only and adds **no new security boundary** (a teacher could already see the whole school). **Migration `0015`** adds a **`teacher_classes`** N:M join (both FKs **ON DELETE CASCADE**, `UNIQUE(teacher_user_id, student_group_id)`) + **`events.student_group_id`** (nullable FK **ON DELETE SET NULL** — the event↔class link BP11b deferred here, mirroring `category_id`; the class name denormalized via a LEFT JOIN). A **`TeacherClassRepository`** (`add` idempotent `ON CONFLICT DO NOTHING` / `remove`→bool / `replace_for_teacher` atomic / both-direction id lists) + a pure **`DelegationService`** (class-side assign/remove/list, staff-side list/set, + the caller's own `my_classes`/`my_group_ids`) — every write validates the class **and** the target-is-an-in-school-teacher first (foreign → **404**, never leaks; `assign_teachers` silently skips a foreign/non-teacher id). The **focus scope** is one `scope_group_ids` clause threaded through both the row-native **and** count-sort id-scan paths (BP9): students `student_group_id IN scope` (an un-classed student is no teacher's; empty → `false()`), events `IN scope **OR IS NULL**` (their classes' events **plus** untagged/school-wide; empty → only untagged) — the empty case handled explicitly (never `.in_([])`). `resolve_focus_group_ids` returns a scope **only** for a teacher who sent `mine=true`; an **admin's `mine` is ignored** (no escalation). Reuses **`class:manage`** (admin-only) — **no new permission**. Routes: `GET/POST /v1/classes/{id}/teachers`, `DELETE …/{tid}`, `GET/PUT /v1/staff/{id}/classes`, `GET /v1/classes/mine` (before `/{group_id}`), + `mine`/`student_group_id` on `GET /v1/{students,events}`; create/update thread `student_group_id` (foreign → 404, can't be cleared via PATCH per 0027). FE: a shared **`FocusToggle`** ("My classes / All", `role=group`+`aria-pressed`; shown only for a teacher with ≥1 class; default on; `mine` **truthy-only** so "All" sends nothing), a class-detail **Teachers** section (inline assign/remove), a staff **Classes** column + **Edit classes** checkbox dialog (cross-invalidates the class-detail roster + vice-versa), and an optional event class `<select>` + filter + badge (the month calendar threads it too). **No ML change, no new backend dependency, no new env var, no new permission.** Migration `0015` verified up→down→up on a **throwaway** Postgres (`bp11c_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**535 passed / 35 skipped** + layering (+ `test_bp11c_delegation.py` 25 + a gated real-Postgres round-trip: the link both-directions/idempotent/remove/replace + the event LEFT-JOIN name + the class filter + the focus scope [`IN` OR untagged + empty] + **both cascades** [class-delete → events SET NULL + links CASCADE; teacher-delete → links CASCADE]); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers** both BE + FE: tenant isolation, the focus scope [no escalation], route ordering, the migration, `.in_([])`-free, async/atomicity all verified; **R2** → cross-invalidated the `class-teachers`↔`staff-classes` SWR caches + a count-sort-path focus test). **BP11 (organizing structure) is now complete (a, b, c).** **BP13 (bulk actions & batch review) has landed** ([decisions/0061](decisions/0061-product-build-BP13-bulk-actions-batch-review.md)) — the Round-2 "do it to many at once" phase (theme E), **no migration, no ML change, no new permission** (reuses BP5's `match_corrections` overlay + BP9's `useDownloadAll` + `events.status`). Owner calls: **all three** surfaces; **no auto-confirm** (manual multi-select, the lane confidence-sorted); a one-click **"Reject all remaining"** guarded by a confirm dialog. **Batch review:** `POST /v1/events/{id}/review/batch` → `ReviewService.set_verdicts_batch` applies many confirm/reject verdicts at once — each is exactly the single BP5 `set_verdict` write (same hide-from-student gate, undo, `resolves_review`/dashboard-count), **tenant-safe by construction** (pairs validated against the event's ML appearances fetched once into a `(media,student)→appearance` map; a pair outside that event+school map is silently skipped — a crafted `media_id` can never write cross-event/tenant, proven since a `matches` row carries the media's real event_id; `_require_event` 404s first). **Bulk event status:** `POST /v1/events/bulk-status` → `EventService.set_status_bulk` → `EventRepository.set_status_bulk` (one tenant-scoped `UPDATE … WHERE school_id AND id IN`, a foreign id skipped) archives/restores many; registered **before** `/{event_id}`. FE: the event gallery **Needs-review** tab → a confidence-sorted **checklist** (Confirm/Reject selected + guarded Reject-all-remaining; `globalMutate("dashboard")` so the "N to review" badge drops; a per-tile "Open photo →" still deep-links the single-photo `AppearanceEditor`); the **events list** gains a checkbox column + a bulk **Archive/Restore/Clear** bar (selection **derived** from the loaded rows so a filter change can't act on a hidden id — the BP11a stale-safe pattern); **`PhotoGrid` gains a staff-grid `selectionMode`** (a tile click selects instead of opening the lightbox, `aria-pressed` + a checkmark overlay; the student masonry surface is untouched) + a **Select → Download N** bar reusing BP9's streaming `useDownloadAll` (a cancelled save dialog = no false success toast). New `batchReview`/`bulkEventStatus` endpoints; caps (2000 verdicts / 500 events → 422); an `added` verdict → 422. **No new backend dependency, no new env var.** BE ruff+mypy+**544 passed / 36 skipped** + layering (+ `test_bp13_bulk.py`: batch applies-many / skips-a-non-event-pair / stamps-`resolves_review` / `count_resolved`-feeds-the-dashboard / foreign-event-404 / **reject-hides-from-student** / staff-only / 422s; bulk-status tenant-scoped / archive+restore / `event:manage` / 422 + a gated real-Postgres `set_status_bulk` tenant round-trip on a throwaway `bp13_migtest`, dropped; dev `app` untouched); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers** both BE + FE: the crafted-`media_id` cross-event write is provably impossible, the batch reject reuses the exact BP5 overlay [no bypass], route ordering / perms / caps / async + the derived stale-safe selection all verified; **R2** → a `count_resolved`→dashboard coverage test). **Honest limits (documented):** batch review is **per-event** (no global queue); **no auto-confirm** (confidence-sorted); the batch loops per-pair upserts (bounded by the cap — a set-based upsert is the scale-up); the events bulk bar acts on **loaded** rows (no select-across-all-pages). **BP14 (program analytics & trends) has landed** ([decisions/0062](decisions/0062-product-build-BP14-program-analytics.md)) — the Round-2 "program view" phase (theme G): dashboards were **point-in-time counts** — no rate, trend, or funnel. Owner calls: **query-only trends** (derived from timestamps — no snapshot table/job), **add `users.last_login_at`** (the one small migration), **both tiers in one phase**. A pure **`AnalyticsService`** composes existing + a handful of new grouped aggregates (the BP1/BP2 pattern, no N+1) into two reads. **School program view** (`GET /v1/analytics/school`, `dashboard:view`, tenant from token): delivery (announced ÷ total events), sign-in (signed-in students ÷ total), engagement (distinct-seen ÷ total) rates + **per-term rollups** + a monthly upload/event **trend** — events+photos+per-term all derive from **one `list_by_school` pass** + the grouped `counts_by_event` scan (so the "announced" predicate stays identical to BP4's `count_distributed`, no extra round-trips). **Estate adoption view** (`GET /v1/analytics/estate`, `school:manage`, platform-only, cross-tenant): a per-school **funnel** (staff→students→enrolled→events→distributed) via new cross-tenant grouped aggregates (the BP2 `list_schools` pattern, zipped by `school_id`) + a **transparent heuristic** — `stalled` = students>0 && enrolled==0 (the enrollment wall), `idle` = enrolled but no event created in 30 days. **Migration `0016`** adds nullable `users.last_login_at`, stamped `now()` on a successful login (`AuthService.login` → new `UserRepository.touch_last_login`, a direct Core UPDATE), **never on refresh** (not interactive); **no backfill** (forward-looking from launch). New grouped aggregates on users/students/events/media/notification_reads (the monthly trend buckets photos by upload `created_at` but **events by their `event_date`** — when the event happened, undated excluded — each keyed `'YYYY-MM'`, Postgres); the analytics VOs (`SchoolAnalytics`/`EstateAnalytics`/`SchoolFunnel`/`TermRollup`/`MonthPoint`) live in `analytics_service.py` (import-pure — only `dataclasses`+`datetime` — so the layering invariant holds). FE: a **Program analytics** section folded **into the school Dashboard** (a `ProgramAnalytics` component — **no separate nav item/page**, owner's call to keep it in the dashboard they already land on — `RateCard`s with a `—` zero-denominator + slim bars, a dependency-free `TrendChart` [CSS bars + a visually-hidden SR data table], a per-term table; it fetches its own analytics so a load/error never blocks the dashboard, and renders only once the school has data [the existing `!isEmpty` gate], so a fresh school sees the setup checklist not a wall of "—") + a platform **Estate health** page (`(platform)/estate`, its own nav item — the platform admin has no dashboard to fold into — stalled/idle alert cards + an adoption-funnel `Table` with a healthy/idle/stalled `StatusPill`, rows linking to `/schools/{id}`). **No ML change, no new permission, no new dependency, no new env var.** Migration `0016` verified up→down→up on a **throwaway** Postgres (`bp14_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**556 passed / 38 skipped** + layering (+ `test_bp14_analytics.py`: school rates/per-term/trend composition + tenant isolation + missing-school 404; estate funnel + stalled/idle + totals; login-stamps-not-refresh; the two routes' shape + 403/401 permission gates + a login→analytics sign-in-count e2e; + 2 gated real-Postgres aggregate round-trips); FE tsc+lint+`next build` green; 2× review loop. **Honest limits (documented):** query-only trends (a snapshot table + rollup job is the scale-up — "enrolled over time" is a current funnel, not a historical line); no login backfill; per-term photos from an events-pass join in-Python (bounded ~120 events/yr); engagement = distinct openers ÷ all students (coarse); the stalled/idle rule is a heuristic, not a model; in-app delivery only (outbound email is BP12). **The recommended Round-2 track (BP9–BP14 + BP17) is now complete; the deprioritised BP12 (distribution reach) / BP15 (accuracy at scale) / BP16 (lifecycle & retention) remain at the back of the queue.** **The product build track is now PAUSED** (owner call, 2026-07-27, [decisions/0063](decisions/0063-park-remaining-backlog.md)): everything still open is parked — the deprioritised **BP12/BP15/BP16** *and* the **BP6 video timeline** (who appears when, per-timestamp; deferred from [0043](decisions/0043-product-build-BP6-video-end-to-end.md)) — and tracked in one place, **[`product/05-parked-backlog.md`](product/05-parked-backlog.md)** (which also lists the documented scale-up/polish refinements + the pending FE live-smoke / Docker-`buffalo_l` verification). The next build starts only when the owner picks a parked/new item and re-confirms scope. **A Round-3 product review has since been written** ([decisions/0064](decisions/0064-product-review-round-3-ux.md), docs-only, build still paused): a static, code-grounded, multi-agent UX review of the post-BP9–17 product — 4 persona walks (first-run + Greenfield scale, incl. the parent lens) + a leads verifier (11 of 25 inherited rough-edge leads **refuted**, recorded in `06` §7b so they're never re-chased) + an R2 re-tester (every `product/02` claim: **32 RESOLVED / 5 PARTIAL / 2 UNRESOLVED / 4 PARKED / 0 REGRESSED**; design bar: Linear delivered · Stripe substantially closed · Pinterest half-closed — grid yes, chrome no) + 6 senior-PM sweeps, then a 2× doc loop (3 citation verifiers re-opened ~230 citations, zero findings killed + a cold-read coherence pass). Results in **[`product/06-product-review-round-3-ux.md`](product/06-product-review-round-3-ux.md)** (themes **J–Q**; **two Criticals**: **J** — a student who forgets their password has **no recovery path** and the only remedy, delete/recreate, permanently destroys their photo history [BP8e purges matches + the worker skips completed media]; **K** — a dead-lettered/lost event job strands the event in "Distribution is running" forever [no DLQ consumer, re-enqueue refused, zero failure metrics]; 18 Highs incl. the inverted student arrival moment, the 7-words-for-one-pipeline vocabulary, "Only you can see these" being false vs `gallery:view_all`, the review lane offering no reference face, and delivery/accuracy/attribution running unmeasured), a **BP18–BP25 roadmap proposal** in **[`product/07-improvement-roadmap-round-3.md`](product/07-improvement-roadmap-round-3.md)** (recommended order **BP18** account recovery → **BP19** pipeline resilience → **BP21** strings/honesty → **BP20** arrival moment → **BP22** review-loop → **BP25** floor sweep → **BP23** instrumentation → **BP24** two-way doors; optional companions BP18b session-revocation + BP20b student chrome), and an HTML exec summary `product-review-r3.html`. **Owner scope:** parked items (BP12/15/16, video timeline) excluded from the review + roadmap — `product/05` untouched. Nothing is scheduled; a phase starts only on owner pick + scope re-confirm. **The owner picked BP18 (account recovery & credential safety, the fix for Critical #1), sliced a–d** (a: student recovery · b: credential-safety guards + self-service + the student name · c: school-record lifecycle · d: session revocation + student disable — the one migration); explainer `bp18-plan.html`, verified before code. **BP18a (student credential recovery) has landed** ([decisions/0065](decisions/0065-product-build-BP18a-student-credential-recovery.md)): a **1:1 port** of the BP7c staff/admin resend giving a locked-out student a fresh one-time temp password **without** the destructive delete that erases their photo history — `StudentService.resend_invite` (tenant-scoped 404 before any write; touches only the password, so matches/photos survive) behind `POST /v1/students/{id}/resend-invite` (`student:manage`, `ProvisionedStudentResponse`), a FE "Send new password" action (reuses `InviteResultDialog`), a login "Forgot your password? Ask your school" line, and a `?reason=expired` "you were signed out" cue (read via `window.location.search`, not `useSearchParams`, so `/login` stays statically prerendered). **No migration, no ML change, no new dep/permission.** Honest limit until BP18d: a resend kills the old password at login but an already-issued refresh token survives ≤14 days. BE ruff+mypy+**561 passed / 38 skipped** (+5, incl. an auth-layer old-pw-dies/new-pw-works round-trip) + layering; FE lint+tsc+`next build` green; 2× review loop. **BP18b (credential-safety guards, self-service & the student name) has landed** ([decisions/0066](decisions/0066-product-build-BP18b-credential-safety.md)), **no migration**: an additive `name` on `/auth/me` (resolved for a student via `student_repo().get_by_user_id`, tenant-safe, null for staff/platform; shown in the shell footer) + a **last-active-admin guard** (`OnboardingService.set_staff_status` refuses disabling a school's only active admin via a new `count_active_by_school_and_role`; `ValidationError`→400 surfaced as a toast, the FE deliberately doesn't pre-disable the button — an a11y call) + **shown-once close-guards** on the invite + bulk-import dialogs (copied?/downloaded? → a `ConfirmDialog` so a stray dismissal can't lose the one-time credential) + a **resend-invite confirm** for an already-signed-in staff/admin (resend replaces a working password) + a reachable **"Change password"** link in the shell footer. BE ruff+mypy+**568 passed / 38 skipped** (+7: 5 last-admin service, a route sole-admin→400, a `/me`-name e2e) + layering; FE lint+tsc+`next build` green; 2× review loop (R1 SHIP no blockers; R2 SHIP → the route 400 test + the a11y BE-guard-+-toast simplification). One behavior change: disabling a sole active admin now 400s. **BP18c (school-record lifecycle) has landed** ([decisions/0067](decisions/0067-product-build-BP18c-school-record-lifecycle.md)), **no migration** (the `schools` columns were already mutable): `PATCH /v1/schools/{id}` (name / max_teachers / status), platform-only — the write-once record finally gets a lifecycle (R3-A1-01). BE `SchoolRepository.update` (partial: only provided non-None fields change) + `OnboardingService.update_school` (reuses create's validation for provided fields; missing school → 404) + `PatchSchoolRequest` (all-optional; schema → 422 for bad status/cap). **No guard on suspend** (reversible, removes no access, gates new creates only — already enforced); lowering the cap below the current count is allowed (checked at create only). FE: an **Edit-school** dialog + **Suspend/Reactivate** on the platform school detail (both revalidate; the status pill now reads "Active"/"Suspended"). BE ruff+mypy+**576 passed / 38 skipped** (+8: 5 service, 3 route incl. a schema 422) + layering; FE lint+tsc+`next build` green; 2× review loop (R1 clean; R2 SHIP → a route 422 test + a cap-lowering docstring clause). **BP18d (session revocation + student disable) has landed** ([decisions/0068](decisions/0068-product-build-BP18d-session-revocation-student-disable.md)) — the **fourth & final** slice, **completing BP18 (a,b,c,d)** and closing Round-3 **Critical #1**; **the one BP18 migration (`0017`)**. **Part A — "log out everywhere":** a per-account **`users.token_version`** counter (**migration `0017`**, additive + reversible, existing rows adopt 0) + a **`tv`** claim on **both** JWTs; **`UserRepository.set_password(revoke_sessions=True)` bumps it in the same UPDATE** as the hash, so BP18a's student resend, BP7c's staff/admin resend, and `change_password` **all** revoke previously-issued tokens automatically; **`get_current_user` AND `refresh` reject a stale `tv` → 401** (closing BP18a's honest gap — a pre-reset refresh token could mint access tokens for ≤14 days). Two traps handled: **`AuthService._maybe_rehash` passes `revoke_sessions=False`** (the one caller that must NOT bump — else a login-time argon2 rehash would invalidate the token it just issued), and **`change_password` re-issues a fresh pair** (reload → issue with the new `tv`) so a self-change isn't a self-logout — the route goes **204 → 200 `TokenResponse`** and the BFF (`/api/auth/change-password`) swaps the cookies. **Part B — student disable (no migration):** **`StudentService.set_status`** — a 1:1 shape of `set_staff_status` (tenant-scoped `get_student` → **404 before any write**, flips the linked login via the existing `users.set_status`, returns a re-read `Student`, idempotent, **no last-admin guard** since a student locks out only themselves; auth already honours a disabled account at login/refresh/the per-request reload, so the student is locked out immediately yet keeps every profile/photo/`match` row, unlike delete) behind **`PATCH /v1/students/{id}/status`** (`student:manage`, tenant from token, reuses `UpdateUserStatusRequest`); the **`Student` read model gains `status: UserStatus`** off the **existing** users JOIN (`_select_with_email_and_class` selects `UserRow.status`; `_to_student` defaults `active` for a fresh `create()`) → `StudentResponse.status` (inherited by `StudentListItem`). FE: a **Disable login** (behind a `ConfirmDialog`) / **Enable login** (direct) action + a **"Login: Active/Disabled"** `StatusPill` on the student detail (staff-page tone convention) + a **"Login disabled"** list pill (labelled so it never reads as an enrollment state). **Honest limits (documented):** `token_version` revocation is keyed on a password *change/reset* — **disabling a login is enforced by the per-request status reload + refresh's status check (which runs before `tv`), NOT by `tv`** — so a disabled student's live **access token dies on the next request and their refresh token on the next refresh** (no survival gap); the two checks are **independent** (`get_current_user` checks status first, then `tv` — no interaction bug). A standalone admin "sign this user out everywhere" without a password change/disable is a future one-line `token_version` bump. **BE + FE only — no ML change, no new dependency, no new permission** (disable reuses `student:manage`). One behaviour change: **`POST /v1/auth/change-password` now returns 200 `TokenResponse`** (was 204; the BFF sets the re-issued cookies — no other caller depended on 204). Migration `0017` verified **up→down→up on a throwaway Postgres** (`bp18d_migtest`, dropped; dev `app` DB untouched — the column appears `integer`, drops, re-adds); BE ruff+mypy+**583 passed / 38 skipped** (+6: disable↔enable/idempotent/tenant-404 service tests, a disable→login-401→re-enable e2e, a **disabled-student's-live-access-token→401** route test [the mid-session kill, not just login-time], an unknown-404) + a **gated real-Postgres** `token_version` bump-on-change / **no-bump-on-rehash** round-trip **and** a student-read-model-reflects-login-status JOIN round-trip (32 passed on the throwaway DB) + layering; FE lint+tsc+`next build` green (`/login` stays statically prerendered); 2× review loop (**R1 SHIP — 0 blockers**: revocation complete on both authenticated paths, the rehash + self-reissue traps both handled, student-disable 404s before any write, `Student.status` populated on every read path with a faithful fake [the `FakeStudentRepo` resolves status on every read via a `_status_of` link, mirroring the JOIN], the 204→200 consistent through router/BFF/tests; **R2 SHIP** → the "Login disabled" relabel, the live-session-kill route test, and the two honest-limit notes). **BP18 (Account recovery & credential safety) is now complete (a, b, c, d) — Round-3 Critical #1 is fully closed.** **The owner picked BP19 (Pipeline resilience & stall visibility — the fix for Round-3 Critical #2), sliced a–d** (a: unstick + a visible "Failed" event [the one migration] · b: failure metrics · c: stall/second-batch/failed-in-dashboard visibility · d: upload survival); explainer `bp19-plan.html`, verified before code. **BP19a (unstick + visible "Failed" event) has landed** ([decisions/0069](decisions/0069-product-build-BP19a-unstick-visible-failed-event.md)) — Round-3 **Critical #2**: a processing job that permanently fails (an `EmbeddingVersionMismatch` nacks every delivery, or the job is exhausted) is retried 5× then **dead-lettered to a Redis stream with NO consumer**, stranding the event at `processing_status='processing'` **forever** (the UI spins "Distribution is running", the Process button is hidden, the API refuses re-enqueue — **only out-of-band SQL** could rescue it; students never get their photos). Mirrors **BP8a** ([decisions/0049], which gave *media* a `failed` state + retry), applied to *events* — **cross-service (BE + ML worker); migration `0018`; NO BE↔ML HTTP-contract change** (the worker already writes the backend status columns directly, [0027]). **Migration `0018`** widens `ck_events_processing_status` to add `'failed'` (`EventProcessingStatus.FAILED`; must apply **before** the worker deploys since the worker writes it; reversible only while no `failed` row exists — like `0009`). **The DLQ consumer that was missing:** `BackendEventStore.mark_event_failed` (writes `failed`, tenant-scoped, **state-guarded** — `processing_status IN (queued,processing,failed)` so a stale re-drain can never clobber a `completed` event back to `failed`); the `JobQueue` port gains **`drain_dead_letters()`** (reads the DLQ via `XRANGE` capped at 256/sweep, drops a malformed entry in place, returns actionable `DeadLetter{job,reason,receipt}` **without** removing) + **`remove_dead_letter(receipt)`**; `WorkerRunner.run()` now runs the consume loop AND a **`_dlq_loop`** concurrently (`asyncio.TaskGroup`, `ML_DLQ_POLL_S`=30s) that marks each dead-lettered event `failed` (`InferenceService.mark_event_failed`) **then** removes the entry — **mark-before-remove**, so a crash between the two just re-marks idempotently next drain (the failure is never lost). **The unstick guard:** `EventService.process_event` is widened — Process is refused only when the event is `queued`/`processing` **and** enqueued within `BE_EVENT_INFLIGHT_STALE_S` (default 30 min, vs `enqueued_at`); a **`failed`** event re-enqueues (Retry — the primary unstick, and a `failed` event **always** retries even with 0 outstanding photos → the worker skips the completed roster + self-heals it to `completed`) **and** an event stuck in-flight past the threshold re-enqueues (the fallback for a job that dead-lettered without a consumer, or was lost with the stream) — so an event can never strand, no SQL ever needed. **FE:** the event detail renders a `Failed` pill (tone `error`, never recomputed away by the counts logic) + a **Retry** button (shown for any `failed` event) + a clear failure note (`aria-live`); `use-event-status` treats `failed` as terminal (poll stops); the events list inherits the `failed` pill. **Honest limits (documented):** migration precedes the worker deploy; the DLQ drain is periodic (~30s to `failed`); a malformed DLQ entry is dropped in place (names no event); multi-replica workers each drain (redundant but idempotent); the state-guard leaves a **rare transient flap** possible (a crash in the mark→remove micro-window + a retry before restart → the event momentarily shows `failed`, self-correcting); the DLQ read is bounded 256/sweep; **orphaned-object reaping for event media is deferred** (a known small leak, per `product/07`). The failure *metrics* are **BP19b**; the age cue + second-batch + failed-in-dashboard are **BP19c**. Migration `0018` verified **up→down→up on a throwaway Postgres** (`bp19_migtest`, dropped; dev `app` untouched — CHECK accepts `failed`, rejects a bogus value, down refuses while a `failed` row exists); BE ruff+mypy+**586 passed / 39 skipped** + layering (guard: fresh-refused / stale-retryable / `failed`-retryable incl. the all-completed self-heal) + ML ruff+mypy+**139 passed / 13 skipped** + layering (DLQ: marks-then-removes strict-order, empty-drain no-op) + a **gated real-Postgres** `mark_event_failed` (idempotent + tenant-scoped + **no-clobber-of-`completed`**) + a **gated real-Redis** `drain_dead_letters`/`remove_dead_letter`/malformed-drop round-trip; FE lint+tsc+`next build` green; 2× review loop (**R1 caught a real crash-window bug** — a stale re-drain could clobber a `completed` event back to `failed` and re-strand it → fixed with the state-guarded write + a gated no-clobber test; **R2** → closed the `failed`-with-no-outstanding **Retry dead-end** [self-heal + FE button], added the gated Redis test [the only untested new logic], promoted `ML_DLQ_POLL_S`, bounded the DLQ read, fixed the "contact support" copy). **No new dependency, no new permission. BP19b (failure metrics) has landed** ([decisions/0070](decisions/0070-product-build-BP19b-failure-metrics.md)) — the second slice of BP19, redeeming Round-3 **R3-S1-02** (the failure half of the pipeline emitted **zero metrics**) — **ML-service only, no migration, no backend/FE change, no ML-contract change**. Exploration found **the inference worker had NO `/metrics` server at all** — only the ML *API* (a separate process) served `/metrics`, so even the worker's existing job-outcome counters (recorded in the worker process) went unscraped. **The worker now serves `/metrics`** via `observability/metrics.py::start_metrics_server` → `prometheus_client.start_http_server(ML_WORKER_METRICS_PORT`, default 9100`)` in `workers/inference_worker.py::main`, **fail-safe** (a port-in-use `OSError` logs + continues — metrics are not core); it exposes the **default registry**, so its `/metrics` now serves BOTH the pre-existing outcome counters (finally scraped) AND the new failure metrics. New metrics (`observability/metrics.py`, same cardinality discipline — bounded labels only): **`photos_failed_total`** (folded into `record_job_outcome` from the already-computed `EventOutcome.photos_failed`), **`jobs_failed_total{reason}`** (a terminally-failed/dead-lettered job — incremented once per drained dead-letter in `runner._drain_dead_letters_once`, `reason` = the queue's closed cause), **`embedding_version_mismatch_total{school_id}`** (the stale-index "ALERT", now countable — incremented in the runner's version-mismatch handler; fires per delivery attempt [~5× per stuck event], a mismatch-**rate** signal not a distinct-job count), + **`dlq_depth`** & **`inflight_oldest_age_ms`** gauges refreshed each DLQ sweep (`runner._refresh_queue_gauges` → `set_queue_gauges`). Queue stats via the port: **`JobQueue.dead_letter_depth()`** (redis `XLEN` of the DLQ) + **`oldest_pending_age_ms()`** (redis `XPENDING` summary → oldest pending id → the `<ms>-<seq>` id's ms prefix → `now − ms`, clamped ≥ 0, `None` when nothing is pending); inproc stubs (0 / `None`). New `ML_WORKER_METRICS_PORT` setting + `.env.example` + compose `ml-worker` port. **Honest limits (documented):** worker + API expose **separate `/metrics`** (two scrape targets, shared definitions / own values); multi-replica workers each report the **same shared `dlq_depth`** → aggregate across instances with **`max()` not `sum()`** (+ a distinct port per worker-on-one-host, like `ML_QUEUE_CONSUMER`); a bind failure degrades silently to no-metrics (logged WARNING); `inflight_oldest_age_ms` assumes reasonable worker↔Redis clock sync (a skew only nudges the gauge, clamped ≥ 0); the mismatch counter counts attempts not distinct jobs. ML ruff+mypy+**145 passed / 14 skipped** + layering (observability: the failure counters + the `photos_failed` fold + the gauges + `render_latest` exposes the new names; **runner-wiring**: a version-mismatch increments the counter / a dead-letter increments `jobs_failed_total{reason}` / a sweep pushes the gauges) + a **gated real-Redis** `dead_letter_depth`/`oldest_pending_age_ms` round-trip (idle→`None`, a pending job has a measurable age); **no backend/FE change**; 2× review loop (**both SHIP, no blockers**: **R1** confirmed the cardinal metrics rule holds [every label bounded], `oldest_pending_age_ms` sound [`None`-safe, `_ensure_group` before `XPENDING`, clamped], the fail-safe server + default-registry exposure correct → applied its one robustness NIT: **the DLQ drain now runs before the gauge refresh in its OWN `try`**, so a persistent gauge-read failure can never starve the drain [BP19a's core recovery]; **R2** → added the **runner-wiring tests** [the load-bearing "does the runner actually increment these" gap] + the help-string honesty [the mismatch counter says "attempts"; both gauges note "worker-observed; use `max()`"]). **No new dependency, no new permission. BP19c (stall + second-batch + failed-in-dashboard visibility) has landed** ([decisions/0071](decisions/0071-product-build-BP19c-stall-second-batch-failed-visibility.md)) — the third slice of BP19, the **display half** of theme K: redeems **R3-S1-03** (a stuck event looked healthy), **R3-S1-04** (a "second batch" of new photos on a completed event was invisible), **R3-S1-10** ("All processed" over failed photos). **BE + FE; no migration, no ML change, no new dependency, no new permission.** **(1) Staleness cue (FE):** the event detail shows **"Processing since {enqueued_at}"** (the age was delivered but rendered nowhere) + escalates past a threshold ("This is taking longer than usual; you can retry below.") + surfaces the **stale-in-flight Retry** (19a's widened guard allows it) — the threshold a FE mirror **`NEXT_PUBLIC_EVENT_INFLIGHT_STALE_S`** (default 1800, mirrors `BE_EVENT_INFLIGHT_STALE_S`); "now" is tracked in a lazy-`useState(() => Date.now())` + a 30s-interval effect (never read impurely in render — the React-compiler purity rule). **(2) Second batch (BE + FE):** a new pure **`derivePillStatus(processing_status, {total, pending})`** in `lib/events/status.ts` — shared by the event **detail** (refactored to it) and the events **list** — so a `completed` event with new `pending` photos reads *unfinished* (not "Completed"); the list row carries `pending` via new **`MediaRepository.pending_counts_by_event`** → `EventListing.pending` → `EventListItem.pending` (threaded through **both** the row-native and count-sort id-scan paths of `list_events_page`); the dashboard alert **`count_not_started_with_media` → `count_active_with_pending_media`** (active, **not-in-flight**, `EXISTS` ≥1 pending media — so a second batch on a completed/failed event now fires, in-flight + archived stay excluded). **(3) Failed in the dashboard (BE + FE):** `school_status_counts` already had `failed` — expose it: `SchoolDashboard.photos_failed` → `MediaSummary.failed` + `NeedsAttention.photos_failed`; the FE `photosHint` stops saying "All processed" when pending/failed > 0 + a new **"N photos failed processing"** needs-attention alert. `derivePillStatus` keys on **`pending`** (not `pending+failed`), so a completed-with-failed-photos event still reads "Completed" (BP8a's deliberate event-level semantics survive) while a completed-with-new-uploads event reads unfinished. **Honest limits (documented):** the FE stale threshold is a `NEXT_PUBLIC_*` mirror — keep it `>=` the backend guard so the FE never offers a retry the backend would 400 (a drift only shifts the escalation timing; graceful toast on a stray 400); the "distribute" vocabulary stays BP4-era (R3's one-grammar cleanup is **BP21** — 19c widened the *predicate* without renaming the field, and fixed the one alert description the widening made false); the list pill is derived, but the BP2 list-rollup counts stay raw ML (galleries/detail remain the effective source of truth). One additive read-shape change (`EventListItem.pending`, `DashboardResponse.media.failed`, `needs_attention.photos_failed`) ships WITH the FE. BE ruff+mypy+**590 passed / 39 skipped** + layering (dashboard: second-batch widening + `photos_failed` + not-in-flight/archived exclusions incl. an explicit in-flight-not-flagged guard; listing: `pending` carried + a second-batch case) + a **gated real-Postgres** round-trip for `count_active_with_pending_media` (the completed-with-second-batch e6 case) + `pending_counts_by_event` (throwaway `bp19c_test`, dropped; dev `app` untouched); FE lint+tsc+`next build` green; 2× review loop (**both SHIP, no blockers**: **R1** traced all seven areas correct — the tenant-scoped-both-sides + NULL-safe SQL, `pending` threaded through both list paths, `derivePillStatus` a faithful extraction, the impure-render fix respecting the Rules of Hooks, and FE/BE stale thresholds contract-consistent; **R2** → fixed the widened alert's now-false copy + added an in-flight-not-flagged service test). **BP19d (upload survival) has landed** ([decisions/0072](decisions/0072-product-build-BP19d-upload-survival.md)) — the fourth & **final** slice of BP19, **completing it (a,b,c,d) → Round-3 Critical #2 is fully closed.** Redeems R3-A2-09/A3-06/S3-05: the event uploader could lose an in-progress upload with **no warning** (the FE had **zero** `beforeunload` handlers anywhere) and had **no retry** for a file that failed mid-batch except re-picking everything. **FE-only — no backend/ML change, no migration, no new dependency, no new permission.** `useMediaUpload` now **retains each file's `File` handle** by item id (`useRef<Map<id,File>>`, append-only — `File`s are cheap references, not the bytes, and the map dies with the page) so a new **`retryFailed()`** re-runs the bounded-concurrency pool over **only** the `error` items (the pool extracted to a shared `runPool(entries)`); a **"Retry failed (N)"** button shows when `failed>0 && !isUploading` (so no overlapping pools race the same item). A reusable new hook **`useBeforeUnload(active)`** (`lib/hooks/use-before-unload.ts`) guards a **tab-close/reload/external-nav** while uploading; and the "Back to event" button (was `disabled` during an upload — a trap) now routes through `onBack()` → a **`ConfirmDialog`** ("Leave while uploading?") so the user can leave deliberately but not by accident. **Honest limits (documented):** **in-app breadcrumb / browser-Back navigation is NOT guarded** — `beforeunload` doesn't fire for client-side router nav and Next 16 App Router exposes no stable nav-blocking API, so a mid-upload breadcrumb/Back click still leaves silently (a documented App-Router constraint, not an oversight); leaving via **in-app** nav doesn't lose photos — the pool is a **detached** `Promise.all`, so its workers keep pulling from the batch after unmount and **all** files (in-flight *and* queued) finish uploading in the background (an in-app "Back" costs *progress visibility + retry*, not photos), whereas a **tab-close/reload** destroys the JS context and DOES kill pending uploads (exactly what `useBeforeUnload` guards); the `useBeforeUnload` rollout to the other long-ops the plan named (`useDownloadAll`/`useBulkPhotoEnroll`, both confirmed present + unguarded) is a deferred follow-on. FE **lint+tsc+`next build` green** (no BE/ML suite delta); 2× review loop (**both SHIP, no code blockers**: **R1** verified all six areas — the single-pool invariant [the "Retry failed" button gates on `!isUploading`, each `runPool` has its own `idx`], unmount-safety via the `mounted` ref, the `beforeunload` hook textbook-correct [deps + cleanup], clean types — 0 fixes; **R2** → wrote the honest-limit doc + **corrected the confirm copy** [investigating it revealed the detached-pool background-continue behavior, so the copy now says "they'll keep uploading in the background… closing the tab would stop them" instead of implying loss] + the append-only-map invariant comment; a11y confirmed sound). **BP19 (Pipeline resilience & stall visibility) is now complete (a, b, c, d) — Round-3 Critical #2 is fully closed** (the whole BP18 + BP19 = both Round-3 Criticals resolved). **The owner picked BP21 (Say what it does — Round-3 theme M, the strings/honesty cleanup), sliced a–b** (a: all copy/UX + the "How matching works" explainer; b: error truthfulness); explainer `bp21-plan.html`, verified before code. **BP21a (say what it does — the copy + explainer pass) has landed** ([decisions/0073](decisions/0073-product-build-BP21a-say-what-it-does.md)) — **FE-only, no backend/ML change, no migration, no new dependency, no new permission.** The product named its two pipelines in **seven words** (matching = "Process"/"Redistribute"/"Distribution"/"Processing"; announcing = "Notify"/"Distribute"/"Delivered") and the **overloaded "Distribution" meant *both*** — so "click Process → read 'Distribution started.'" read as "students were told" (they weren't). Swept **one grammar = Match / Announce** across every surface (the `PROCESSING_LABEL` pills [`lib/events/status.ts`], the event detail + the DistributionCard [component name kept], the events list, the dashboard alerts + checklist, analytics "**Announce rate**"/"**Announced**", estate, the browser tab title→"Photos"); **internal identifiers deliberately untouched** (`processing_status`/`EventProcessingStatus` values + the `DistributionCard`/`processEvent`/`notifyStudents` names — no backend-contract touch). Plus the honesty fixes: a **true privacy scope** on the student page/explainer (not "only you can see these" — staff have `gallery:view_all`); a new **`app/(help)/how-matching-works`** static explainer (a `(help)` route group with an all-roles `AuthGuard`) — plain-language face-matching / confidence-% / "This isn't me" — + an inline **confidence legend** (`hasConfidence`-gated so a null-confidence photo shows just the link) in both `appearance-editor.tsx` (staff) and `appearance-list.tsx` (read-only), linked from the student hero/empty-state + the staff review lane; the **erasure dialog** now tells both sides (destroys login/profile/enrollment/**matched-photo history** · keeps the event photos in every gallery · anonymizes past download records, accurate to [0053](decisions/0053-product-build-BP8e-student-erasure.md)); the **audit** caption truthful ("records in-app saves, not views or a right-click save") + a **teacher download disclosure** in `DownloadHistory` (accurate to [0050](decisions/0050-product-build-BP8b-download-audit.md): the `POST` records, the view mint records nothing, `audit:view` is admin-only but teachers' downloads *are* recorded); a **photoless student → "No photo yet"** (a shared `enrollDisplay` in `lib/students/enrollment.ts`) not the "Pending" pill; the schools-**list** status pill "Active"/"Suspended" (matching the detail), a dropped stale class-dialog **"(soon)"** (delegation shipped BP11c), and the event uploader's **30 MB** hint (previously silent). **Announce stays honestly in-app** ("in-app only for now"; no new string implies an outbound email/text — outbound is parked BP12). FE **lint+tsc+`next build` green** (`/how-matching-works` prerenders static); `git status` shows only `frontend/` changed; no BE/ML suite delta; 2× review loop (**R1** — rename-completeness [no user-facing "Distribution" leftover] / route+link+guard / the `enrollDisplay` conditional / every honesty claim vs 0050/0053 all verified clean → **1 fix** [an audit column header that no longer matched its cell]; **R2** — the explainer / a11y of the new links+legend+teacher disclosure / the null-confidence edge / cross-surface vocabulary consistency all clean → **3 small fixes** [pluralized the "Announced to N students · X opened" roster; the audit header "Self-download by"→**"Student (self)"**; tighter audit description + a warmer re-enroll-failure toast pointing at the failure note]; the verbose erasure dialog kept as-is — accuracy over brevity for a destructive action). **BP21b (error truthfulness) has landed** ([decisions/0074](decisions/0074-product-build-BP21b-error-truthfulness.md)) — the second & **final** slice of BP21 (**completing it, a+b**), **FE + the BFF proxy only, no backend/ML change, no migration, no new dependency, no new permission.** Four misdirected error states, fixed in one place: **(1)** a **shared 401 interceptor** (`components/swr-provider.tsx` SWR `onError`) — a mid-session 401 on **any data key** (session dead, the BFF already cleared the cookies) revalidates `"auth/me"` so the `AuthGuard` bounces to `/login?reason=expired` **instead of a "Something went wrong" over a Retry that can never succeed** (the `"auth/me"` key is skipped so it can't loop); **(2)** **422 field parse** (`lib/api/client.ts` `errorMessage`/`parseValidationDetail`/`fieldFromLoc`) turns FastAPI's `detail:[{loc,msg}]` into "Email: value is not a valid email address" (field humanized from `loc`, capped at 3, throw-safe) instead of "Unprocessable Entity"; **(3)** **429 forward + humanize** — the BFF proxy (`app/api/v1/[...path]/route.ts`) now forwards **`Retry-After`** (was content-type only), `bffFetch` reads it onto a new optional `ApiError.retryAfter` and `formatRetryAfter` renders "try again in N seconds/minutes" (BP8c sends integer seconds; a large value formats to minutes); **(4)** **generic 5xx** — `status >= 500` shows "Something went wrong on our end — please try again in a moment." **never the raw `str(exc)`**, while 4xx keep their actionable `detail` (login's "Invalid credentials" etc. unchanged). Folded into `bffFetch` so all ~40 `toast(isApiError(err) ? err.message : …)` sites get the truthful message with **zero call-site changes**. Honest limits (documented): `Retry-After`-as-HTTP-date degrades to "in a moment" (BP8c never sends one); internal field names *could* surface in a 422 but only **email** is a reachable user-typed 422 (id/path fields are app-controlled, `max_teachers` is client-guarded); the 5xx path drops the backend detail by design; and three co-existing backend-fault phrasings (AuthGuard "Couldn't reach the server." / the ~15 `EmptyState`s / this toast) are a candidate for a future string-consolidation. FE **lint+tsc+`next build` green**; `git status` shows only `frontend/` changed; no BE/ML suite delta; 2× review loop (**R1** — loop-safety [traced the SWR 2.4.2 runtime + the BFF refresh boundary + all ~40 `ApiError` consumers], the redirect incl. the stale-`data` case, login/change-password-aren't-SWR, 422 throw-safety, no regressions → **zero findings**, one harmless nit; **R2** — copy/a11y/consistency/edges clean [the reachable email 422 reads best; error toasts ride an accessible `role="alert"`; the 401 is a silent redirect, no error flash] → **1 fix** [minute-format a large `Retry-After`] + the honest-limits list). **BP21 (Say what it does) is now complete (a, b).** **The owner picked BP20 (The arrival moment — Round-3 theme L) + BP20b (student chrome), one phase.** **BP20 has landed** ([decisions/0075](decisions/0075-product-build-BP20-arrival-moment.md)) — **FE-only, no backend/ML change, no migration, no new dependency, no new permission** (two Explore passes confirmed the backend already serves everything). The student receive surface had mechanics (BP3/9/17) but not the *moment*: a "3 new" badge opened on **3-year-old** photos (oldest-first stream, the new event the *last* chip), the banner pointed nowhere, unseen flags **burned on page load**, the badge **froze** once-per-session, ~60 events were a **flat pill wall**, photos had **no story**, and "Download all" was an unlabeled pile that silently kept the **oldest 500** on non-Chromium browsers. Seven pieces: **(1)** newest-first — FE-`reverse()` the whole-list events + media (backend serves asc; media has no timestamp so reverse = newest-uploaded first; the Lightbox index + download `entryBase` index the same reversed array); **(2)** an **actionable banner** — a one-visit `newEvents` snapshot of the unseen events rendered as **filter buttons** (`setSelected(event_id)`, capped 6 + "+N more"), with **arrive-to-clear** mark-seen moved **off mount** to fire once after BOTH notifications loaded AND the first photo load **succeeded** (`PhotoArea.onLoaded`→`photosLoaded`) — never dismiss unseen photos before they render; **(3)** **un-freeze the signals** — `useMyNotifications`+`useDashboard` opt into per-hook `revalidateOnFocus`+`refreshInterval:60_000` (SWR dedupes the shared badge keys; the rest of the app keeps the no-poll default); **(4)** a **year-grouped native `<select>`** (`EventFilter`, `groupByYear` newest-first, undated→"Other") beyond 8 events, chips below; **(5)** **photo story** — an optional `caption` "{event} · {date}" (built via an `eventMeta` map + `formatEventDate`) on `GalleryItem`, threaded `PhotoGrid`→`Lightbox` (a story line + folded into `alt`) + `PhotoTile` (the tile's accessible name + a masonry hover scrim), back-compat so staff surfaces pass none and are unchanged; **(6)** **named saves** — `useDownloadAll(mediaIds, {entryBase, zipName})` → zip entries `{event}/{date}-{nnn}` in `my-photos-{date}.zip` + single saves by event (`useDownloadToDisk(…, label?)`), `onDownloadAll`→`{saved, capped}`, an **honest sticky** capped toast (leads with filter-by-event, names **desktop Chrome/Edge** since iOS Chrome is WebKit/same cap), `toast(…, {sticky})` + `sanitizeFilename` added; **(7)** **BP20b** — `AppShell` branches on `role==="student"` to a **warm slim** top-bar+drawer (reusing `NavList`/`UserFooter`) over a new **`bg-canvas-warm`** `@theme` token + a **`display-xl`** hero on My Photos (staff/platform chrome untouched; the resize-close effect guarded off — the student drawer is used at all sizes). Honest limits (documented): mark-seen on **successful** render only; the banner is a one-visit snapshot (a mid-session announcement lights the badge, not the banner, till reload); newest-first is a fetch-order reverse (no true `created_at`); the non-streaming **500-cap** + no failed-subset retry; per-event zip folders; the warm wash is student-only/light-only. FE **lint+tsc+`next build` green**; `git status` shows only `frontend/` (14 files + the new `event-filter.tsx`); no BE/ML suite delta; 2× review loop (**R1** — reverse/index alignment [incl. the buffered `slice`], arrive-to-clear one-shots [no double-mark/loop], the poll×BP21b-401-interceptor, staff back-compat on every touched caller, `sanitizeFilename` [no accidental char-range], the lazy-`Date` initializer, the BP20b branch → **0 blockers**, 1 documented should-fix [the mark-seen-on-error asymmetry, a deliberate commented choice]; **R2** — a11y/copy/consistency/AA-on-warm/edges → **0 blockers**, **3 fixes** [the banner is a labelled `role="group"` not an aria-live region wrapping buttons, with per-button names + a 6-cap; the capped-toast guidance leads with the universal remedy + names desktop Chrome/Edge; the masonry scrim caption is `aria-hidden`] + softer partial-download copy; AA confirmed on the warm wash [ink 16.98:1 / ink-secondary 5.58:1 / accent-dark 10.82:1]). **BP20 (a + BP20b) is complete.** **The owner picked BP22 (Review loop, armed — Round-3 theme N), slices 1–3** (slice 4, the student "This isn't me" safety, **BLOCKED** → a separate future decision); explainer `bp22-plan.html`, verified before code. **BP22 (slices 1–3) has landed** ([decisions/0076](decisions/0076-product-build-BP22-review-loop-armed.md)) — **one phase, FE-only, no backend/ML change, no migration, no new dependency, no new permission.** "Put the evidence where the decision is": the teacher's review lane showed **name + % but no picture of the student** (guessing → wrong confirms leak wrong photos, the exact thing BP5 prevents), review debt was **invisible at the Announce moment** (auto-announce on by default, so uncertain matches went student-visible instantly), and the triage round-trip **reset itself** (uncontrolled gallery Tabs, an unlinked events-list pill, "Open photo →" losing your place). Three fixes: **(1)** a new **`StudentRefAvatar`** (reuses the teacher-permitted `useStudentReferencePhoto` + `StudentAvatar` already on the students list; SWR-deduped per student, 404→initials, decorative `alt=""`) rendered **before the name** in the **review tile** (`gallery/page.tsx` `NeedsReview`) + the staff **`AppearanceRow`** (`appearance-editor.tsx`) + read-only **`AppearanceList`** (`appearance-list.tsx`) — every "is this the right person?" now shows the person; **staff-only** (the `(school)` group is `AuthGuard`-walled admin/teacher, the student `/me` lightbox passes `showAppearances={false}`, and the endpoint is `student:manage`-gated → a student token 403s to initials — verified defense-in-depth, a student never sees a peer's enrolled face); **(2)** the **DistributionCard** (`events/[eventId]/page.tsx`) fetches `useEventReview` for an **overlay-correct** "N matches to review" link (→ `/events/{id}/gallery?tab=review`) + an announce-time **`ConfirmDialog`** ("N still need review — announce anyway?"; Cancel=go review, Confirm=announce — **no hard block**, the no-auto-confirm stance stands) when debt > 0; **(3)** the gallery **Tabs go URL-addressable** (`?tab=` derived via `useSearchParams` on the already-dynamic route + `router.replace(…,{scroll:false})` — deep-linkable + browser-Back-safe, garbage `?tab=`→`all`), the events-list **"N to review" pill becomes a `<Link>`** to the review lane, and the tab badge is **fixed to count match pairs** (Σ candidates) not photos. Honest limits (documented): the **events-list pill stays a RAW-ML count** (BP5 divergence — `event_match_counts` doesn't subtract resolved) while the DistributionCard + tab badge use the overlay-correct count, so after corrections the same event can read a different number on adjacent screens (a per-event resolved overlay is a backend follow-up); the dashboard "N to review" is itself a school-wide approximation; a per-student ref-photo mint per distinct face in the lane (bounded, same as the students list); the announce-confirm is a soft advisory (a stale count if a colleague clears the lane mid-flight). FE **lint+tsc+`next build` green** (the gallery route stays `ƒ` dynamic → `useSearchParams` needs no Suspense boundary); `git status` shows only `frontend/` (5 files + the new `student-ref-avatar.tsx`); no BE/ML suite delta; 2× review loop (**R1** — the **staff-only privacy boundary** [defense-in-depth to the backend permission], the per-student dedupe/404 fallback, the URL-tab control [no render loop / Back / garbage→all], the single-`onNotify` announce-confirm, the overlay-correct count, no regression on the other `AppearanceEditor`/`AppearanceList` callers → **0 blockers**, 1 comment-precision nit [softened]; **R2** — decorative-avatar a11y [no double-announce] / pluralized non-alarmist confirm copy / truncate-safe review-tile layout / edges → **0 blockers**, 2 touch-ups [dropped a now-redundant `justify-between`; a decorative-`alt` note] + the honest-limits list). **BP22 (slices 1–3) is complete.** **The owner picked BP25 (Floor sweep — Round-3 theme Q), all four groups, one phase.** **BP25 has landed** ([decisions/0077](decisions/0077-product-build-BP25-floor-sweep.md)) — **FE-only, no backend/ML change, no migration, no new permission, no new dependency** (~41 files touched + 4 new). Theme Q was "the floor's thin patches": sub-AA data text everywhere (the one High), an unresponsive calendar, sub-24px targets, naked links, an sr-only lightbox hint (+ the 0043 video-arrow conflict), no URL state on the lists, no per-page titles, a brand-less auth, a frozen spinner, category-vs-status color collision. **A. AA + a11y floor:** a filtered script swapped **data-bearing** `text-ink-muted` (3.2:1) → `text-ink-secondary` (5.9:1) [69 spots/29 files, skipping lines with `size-`/`placeholder:`/`aria-hidden`/`hover:text-` so icons/placeholders/hover-buttons stay muted] · calendar `overflow-x-auto`+`min-w-[600px]`+ a darker AA today badge (`bg-accent-hover`) · focus rings on the 3 naked links · ≥24px tap targets (events checkboxes + toast dismiss, padded-label) · a visible lightbox "← → · Esc" hint + **suppress the window arrow-nav when a `<video>` is focused** (`document.activeElement.tagName==="VIDEO"`; revises the 0043 tradeoff, owner sign-off) · per-page browser titles via a `useDocumentTitle` folded into `PageHeader` (+ 4 direct callers). **B. Design residue:** an auth brand moment (Images + "Photos" wordmark) + a `<main>` · **non-semantic** `--color-cat-{1..6}-{soft,ink}` category tokens (all AA 6.4:1+, distinct from success/warning/error/info) replacing the status-tone reuse in `categories.ts` · `animate-spin` on the dashboard "matching now" Loader2 · `font-mono` on the shown-once temp password. **C. URL state:** a shared **`useUrlParams`** (get/set over `useSearchParams` + `router.replace(scroll:false)`, atomic multi-param set) + **`useUrlListSort`** put q/sort/dir/status/category/term/class/mine/tab into the URL on the **students/events/staff/schools** lists (shareable + Back-safe, mirroring BP22's `?tab=`); each page split into a `*Content` inner wrapped in `<Suspense>` by the default export (so the lists stay `○` static-prerenderable); the debounced search reads the URL for the fetch + an **adjust-during-render** Back-sync + writes only once the debounce **settles to the current input** (so Back never re-adds a dropped `q`). **D. Lows:** a skip-link (sr-only→`#main-content` on `<main tabIndex={-1}>`, both shell branches) + the auth `<main>` · students bulk-select parity (a checkbox column + a bulk **"Assign to class"** bar reusing the BP11a `assignStudentsToClass` endpoint, stale-safe selection, the column gated on the school having ≥1 class) · an offline hint (`useOnlineStatus` via `useSyncExternalStore` → a shell offline bar) · search-match **`<Highlight>`** (`<mark>`, regex-escaped) on the list name/email cells fed the applied query · a foot "Download all" on the student gallery · scroll-restore is App-Router-native (no config change). Honest limits (documented): scroll-restore doesn't survive the infinite-list reset-to-page-1; the URL search lags by the 300ms debounce; two custom categories can hash-collide; highlight is name/email-cell only (a student email-match doesn't highlight); bulk-select is class-assign on the loaded page; the quiet input border is left as-is; per-page titles are client-side (no SEO). FE **lint+tsc+`next build` green** (the lists stay `○` static via the Suspense boundary); `git status` shows only `frontend/`; no BE/ML suite delta; 2× review loop (**R1** — URL-state loop-safety across all 4 pages / bulk-select stale-safe / token-swap correctness [no wrongly-swapped icon] / Suspense / smaller pieces → **1 should-fix**: the debounced-search-vs-URL **Back-bounce** [the lagging debounce re-added a just-cleared `q` for ~300ms] → fixed to write only once the debounce settles to the current input; **R2** — AA math [the 6 category pairs / today badge / mark highlight / offline bar all pass] / a11y / consistency / edges → **no blockers, 3 should-fix** [gated the students checkbox column on classes existing (was dead UI); pointed the search highlight at the applied query not the leading input; lifted the skip-link above the offline bar] + the honest-limits list). **BP25 (floor sweep) is complete.** **The owner picked BP23 (Run it on numbers — Round-3 theme O, "the owner runs blind"), all four owner-chosen groups, one phase;** explainer `bp23-plan.html`, verified before code. **BP23 has landed** ([decisions/0078](decisions/0078-product-build-BP23-instrumentation.md)) — the instrumentation phase turning on the numbers the product collected but never read. **Backend + FE; one migration (`0019`, `media.uploaded_by`); no ML change, no new permission, no new dependency.** **A · attribution:** `media.uploaded_by` (nullable FK→users **SET NULL**, no backfill) stamped at register from the route `actor.id` (a real plumb — `register_media` had no actor param — route→service→repo→domain VO); `users.last_login_at` exposed on `UserResponse` + a row-native `UserSort.LAST_LOGIN_AT` (`nulls_last()` both directions) → a staff "Last sign-in" column+sort; `events.created_by`→`created_by_email` resolved **in-Python** in a new `EventService.get_event_detail` (`EventDetail` VO — NOT a JOIN on the shared events select), detail-route-only. **B · flagship metrics (query-only, seam-free):** **event reach** = (announced ∩ opened) ÷ announced — `NotificationReadRepository.distinct_opened_event_ids` **intersected in-Python** with the announced ids (an honest floor, never over-reports an opened-then-un-announced event; the audience-weighted opens÷roster is scoped out — needs a roster-snapshot write or an N+1 over the ML seam); distinct **savers** (`count_distinct_saver_students` over `subject_student_id`, student self-saves only — staff excluded); a **first-opens trend** (`monthly_first_open_counts` over the immutable `notification_reads.created_at`, a decline-capable engagement line); a **"Quality"** section (`monthly_verdict_counts` → confirm-rate `confirmed÷(confirmed+rejected)` + a separate wrong-person rate; **`added` excluded from the precision denominator** — report-a-miss/recall, shown on its own). `AnalyticsService` gained `corrections`+`audit` ports. **C · answers:** never-signed-in (`last_login_at IS NULL`) + never-opened (a **same-schema `NOT EXISTS`** anti-join over `notification_reads`, never the ML seam) student filters (`ActivityFilter` `login`/`opened` params → `_filtered`→list/count/list_ids→`ListingService`; FE one "activity" `<select>`) + a dashboard failed-enrollments deep-link; a **separate `GET /v1/students/{id}/engagement`** (`EngagementService` — NOT fields on the write-path `StudentResponse`) → a student-detail Engagement card; the notify **roster** gains `first_seen_at` (persistent ever-opened) + `download_count` (`NotificationService` gained an `audit` port). **D · estate:** `SchoolFunnel` gains `created_at`/`not_started`/`days_to_first_delivery` (`first_distributed_at_by_school` − school age)/`stalled_since` (`last_event_created_at_by_school` = "no event since …", not `updated_at`); the FE funnel **sorts client-side** (aria-sort'd headers). **Migration `0019` verified up→down→up** on a throwaway `bp23_migtest` (dropped; dev `app` untouched); BE ruff+mypy+**604 passed / 46 skipped** + layering (+ `test_bp23_instrumentation.py` + **7 gated real-Postgres round-trips** on a throwaway `bp23_test`, dropped: uploaded_by round-trip + **SET NULL on uploader delete**, last_login sort, reach/first-open/first-seen, distinct-savers [staff excluded]+per-event downloads, monthly verdicts, estate first-distributed/last-event, activity filters — each tenant-scoped); FE tsc+lint+`next build` green (the students/staff/estate/dashboard/events lists stay `○` static via Suspense); 2× review loop (**R1** correctness/tenant/cross-seam/async — SHIP, 0 blockers: all 8 aggregates tenant-scoped + no ML-seam SQL-join + migration/plumb/read-model-additivity/async verified → 1 should-fix [Open-rate could read >100% if opened-then-un-announced]; **R2** numbers-honest/a11y/copy/edges — 0 blockers → the >100% edge **superseded by the honest announced∩opened intersection**, estate `aria-sort`, engagement labels "Events they're in"/"Photos they're in", quality cards gated on `adjudicated>0`, staff sort `nulls_last`, +2 tests). **Honest limits:** audience-weighted rate scoped out (reach = events-with-≥1-opener); saved/sign-in/engagement share the total-student denominator (a floor); trends query-derived (no snapshot table); `stalled_since`=last-event; roster `seen` still resets on re-announce (`first_seen_at` persists beside it); the matching **model** work stays parked **BP15** (BP23 ships the *metric* only). **BP23 (Run it on numbers) is complete.** **The owner picked BP24 (Two-way doors — Round-3 theme P, "mistakes become recoverable"), all five slices, sliced into two sub-phases** (a: the backend "doors" — clearable tags + classes-at-scale; b: the FE error-survival sweep); explainer `bp24-plan.html`, verified before code. **BP24a has landed** ([decisions/0079](decisions/0079-product-build-BP24a-clearable-tags-classes-at-scale.md)) — the backend half, **no migration, no ML change, no new permission, no new dependency.** **Slice 1 · clearable event tags — a documented revision of the LOCKED decision 0027** (owner-approved): an event's three **tag** fields (`category_id`/`term`/`student_group_id`) become **tri-state** on PATCH — OMITTED → unchanged, explicit `null` → **cleared**, a value → set (name/description/date/status/auto_notify keep 0027's "None = unchanged"). A tiny **`UnsetType`/`UNSET`** enum sentinel (`domain/models.py`, distinct from `None`, mypy narrows `is UNSET`/`isinstance` cleanly); the route reads Pydantic v2 **`body.model_fields_set`** (no `exclude_unset` precedent — introduced, scoped to this one route) to pass `field if provided else UNSET`; `EventService.update_event` + `EventRepository.update` (+ port + fake) type those three `str | None | UnsetType = UNSET`, the repo does `if x is not UNSET: row.x = req_uuid(x) if x else None`, and `_validate_*` validate **only a real id** so **a foreign id still 404s** while clear/UNSET skip. FE: the edit dialog always offers the empty option + sends `value || null` on change (`updateEvent` patch type `string | null`); create-event **stops silently preselecting "Other"** (L18). **Slice 3 · classes at CSV scale — BOTH shapes**: **(a)** an optional `class` column on the student CSV (`lib/csv.ts` header-detects it; absent → today's name+email) — `StudentService.bulk_create_students` gained a `groups` port + **resolves-or-creates the class by name once per distinct name** (a `class_cache` seeded from existing classes, case-insensitive) then `set_group`, **best-effort** (a class blip logs + the student stays `created`); **(b)** a **`POST /v1/classes/{id}/members/by-email`** (`ClassService.assign_students_by_email` validates the class → 404, trims/dedupes/lowercases, `resolve_by_emails` → `set_group_bulk`) returning `(assigned, unmatched)` — FE `PasteEmailsDialog` (textarea → split; a 1000 FE cap mirror; the unmatched named for fix-and-retry). Honest limits: 0027 revised **not** repealed (only the 3 tag fields — a BP11b test updated); `model_fields_set` at exactly one route; class names aren't unique-by-name (dedupes within a batch; a rare concurrent import could duplicate — documented). BE ruff+mypy+**617 passed / 47 skipped** + layering (+ `test_bp24_two_way_doors.py`: the clear/leave/set × 3-field truth table + a **route round-trip proving `model_fields_set`**; the CSV auto-create/dedupe/reuse/back-compat + **class-assign-failure-keeps-student**; the by-email matched/deduped/unmatched/foreign-404 + a **foreign-school-email tenant guard**) + a **gated real-Postgres** clearable `postgres_events.update` tri-state round-trip (throwaway `bp24_test`, dropped; dev `app` untouched); FE tsc+lint+`next build` green; 2× review loop (**R1** correctness/tenant/tri-state — SHIP, 0 blockers/0 should-fix, the truth table correct in every cell → 1 NIT [double-strip hoist]; **R2** edge/back-compat/a11y/copy — 0 blockers → 3 should-fix [FE 1000-cap mirror, the foreign-school tenant test, the class-fail-keeps-student test] + 2 nits). **BP24a is complete.** **BP24a is complete.** **BP24b (the FE error-survival sweep) has landed** ([decisions/0080](decisions/0080-product-build-BP24b-fe-error-survival-sweep.md)) — the **FE half** of BP24 (theme **P**'s "batch work survives errors"), **FE-only: no backend/ML change, no migration, no new dependency, no new permission.** Three FE gaps where a batch op failed silently or hid the actionable state. **Slice 2 · the CSV error loop** (`bulk-import-dialog.tsx`): a pure `flagRows()` pre-flags each parsed row **Ready / Duplicate / Invalid** (a light email regex + **in-file case-insensitive** first-occurrence dedupe) as a status pill + a live "N of M ready … K flagged will be skipped" `role="status"` summary — the **server still validates authoritatively** (a heads-up, not the source of truth) — plus a **"Download skipped rows"** CSV on results (the non-`created` set via a shared `saveCsv`) so fixing 30 typo'd emails is fix-and-reimport, not hand-transcription. **Slice 4 · honest partial downloads** (`use-download-all.ts` + both call sites): the result gains a **`cancelled`** flag (`streamToDisk` → `{saved, cancelled}`; a picker-dismiss is `cancelled`, distinct from all-fetches-failed `saved:0`; the buffered all-failed path still **throws**), and **both** the staff gallery + student `/me` unify on honest branches — `cancelled` → silent, `saved===0 && !cancelled` → an **error** toast, `capped` → "the first N of M", `0<saved<total` → "saved N of M", full → success (staff) / silent (student) — so a cancel never reads as success and an all-failed run is flagged, not mistaken for a cancel. **Slice 5 · the residue**: create-teacher gains an `onCreated` → `useStaff().mutate()` (a new teacher appears without a reload, R3-S3-01), and the notify roster gets a reused **`FilterChips`** (All / Not opened, counts) + a `ROSTER_PREVIEW=12` collapse ("Show all N / Show fewer", derived each render, empty → "Everyone matched has opened") so "who needs a nudge?" is one click, not an eye-scan. Honest limits (documented): the CSV pre-flag is a **client heads-up** (the server's per-row verdict is authoritative); the roster is still **fetched whole** (collapse/filter is FE-only over a bounded roster — server pagination is the scale-up); the download honesty covers cancel/partial/capped/all-failed, and the non-streaming **cap** (500) is the pre-existing BP9 limit — a **capped+partial** run still reads "first 500" (`saved===0` is checked before `capped`, so all-failed correctly errors; only the mixed some-saved case slightly over-claims, matching the pre-BP24 student path). FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**both SHIP — 0 blockers, 0 should-fix**: **R1** the shared-hook blast radius contained to exactly its 2 call sites, all 3 `DownloadAllResult` return paths correct, buffered all-failed still throws, `exitSelect()` only on a real save, the CSV pre-flag pure + index-aligned, the roster filter/collapse derived → 2 optional NITs not applied; **R2** scoped `<th>`, text-labelled pills, `role="status"`/`alert` live regions, no focus loss on collapse, the all-invalid + 12-vs-13 + `saved===0`-before-`capped` edges, staff/student toast + flag-palette consistency → 3 NITs, 1 doc-only applied [the capped+partial copy edge]). **BP24 (Two-way doors) is now complete (a, b) — the recommended Round-3 tail (BP18 → … → BP24) is done.** **Next:** the owner picks the next item — the remaining open work is the parked **BP12/15/16** + the BP6 video timeline ([product/05](product/05-parked-backlog.md)) and the **blocked** BP22 slice 4 (student "This isn't me" safety); a phase starts only on owner pick + scope re-confirm. **Post-BP24, a Round-4 (pre-release) product review scoped to the staff + school-admin interface was written** ([`product/08`](product/08-product-review-round-4-staff-admin.md) + roadmap [`product/09`](product/09-improvement-roadmap-round-4.md) + an HTML exec summary `product-review-r4.html`): a static, code-grounded walk of the `(school)` surface (4 exploration passes — admin surface / teacher surface / the 5 operational flows / prior-review context) concluding the **design bar is met** (Linear chrome + Stripe density) and the open work is **operability at scale + one distribution blocker**, tiered **BP26–BP31** (bulk-ops parity · governance/audit · teacher coherence · review tools · onboarding/copy). **The owner then set the v1 scope (2026-08-29): NO student login in v1** — students don't use the app; the `(student)/me` surface + student credentials + the in-app **announce/"opened"** loop go **dormant** (shipped, unused), and **distribution is staff-mediated** (a teacher/admin **downloads a student's photos and shares them via WhatsApp**). So the review's one Blocker (**R4-D00**) is no longer outbound email (that original BP26 scope is **parked as BP12** post-v1) but that staff lacked a **per-student "download all"**. **BP26 (v1) has landed** ([decisions/0081](decisions/0081-product-build-BP26-v1-staff-download.md)) — a **FE-only** staff **"Download all N photos"** on the student detail's "Appears in" section (**both teacher + school-admin**), reusing **`GET /v1/students/{id}/media`** (no `event_id` = the full **effective** set per BP5; SWR key distinct from the per-event read) + the streaming **`useDownloadAll`** (client-zip → File System Access API, buffered fallback), foldered by event/date (`{Event}/{date}-nnn.jpg` in `{Student-Name}-photos-{date}.zip`). Reuses the download entitlement — **both staff roles already hold `gallery:view_all`** and the per-photo mint is the same path staff use in the event gallery (**no widening, no bypass**); the button only shows once the student is matched into ≥1 photo. Honest states reused (cancelled→silent / all-failed→error / capped→"first N of M" / partial→"saved N of M" / full→success), copy harmonized with the sibling staff event-gallery, and the a11y mirrors the self-view (button-label flip + `sr-only` live region, not a visible per-tick one). **2 files** (`use-galleries.ts` + `students/[studentId]/page.tsx`); **no backend/ML change, no migration, no new dependency, no new permission, no new env var.** FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**R1** correctness — 0 blockers: unconditional hooks / index-aligned `entryBase`↔`mediaIds` / all download branches + throw-catch / entitlement reused-not-widened / no SWR-key collision / no `AppearsInSection` regression; **R2** edge/a11y/copy — 0 blockers → **2 should-fix applied** [the sr-only-vs-visible live region + the staff-copy harmonization]; placement confirmed [keep it in the section header, not the crowded page-header row]). Honest limits (documented): the non-streaming **500-cap** (BP9) on Firefox/Safari; "all photos" = the **effective** set (a `needs_review` photo included, a rejected one excluded — can differ from the EngagementCard's raw `photos_appearing`); one extra lightweight list read per detail visit. **Out of scope (documented):** outbound email/SMS (**parked BP12**), WhatsApp *integration* (v1 share is manual), the dormant student surface/credentials (a "v1 hygiene" cleanup noted in `product/09`), a per-student download on the gallery "By student" tab (an easy follow-on). **Next:** the owner picks the next Round-4 phase (**BP27** bulk-ops · **BP28** audit/governance · **BP29** teacher coherence · **BP30** review tools · **BP31** onboarding/copy) — a phase starts only on owner pick + scope re-confirm. **The owner then began the Round-4 roadmap phase-by-phase via an owner-directed multi-agent pipeline** (planning agent → plan-review agent → implementation agent → 2× review→fix loop). **BP27a (bulk student lifecycle + select-all-matching) has landed** ([decisions/0082](decisions/0082-product-build-BP27a-bulk-student-lifecycle.md)) — the first slice of **BP27 (Bulk operations parity)** (Round-4 **R4-A04**/**R4-A06**): at ~800-student scale, disable/delete were one-at-a-time and multi-select acted on the loaded page only. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** Backend = pure best-effort loops over the tested single-writes — `StudentService.bulk_set_status`/`bulk_delete_students` (per-row try/except, a foreign-id `NotFoundError` or ML-down `UpstreamError` → per-row `error`, **the batch never aborts**) + `ListingService.list_student_ids` (select-all-matching — one call to the **existing** `StudentRepository.list_ids` with the identical page filters, **no new repo method**) behind **`GET /v1/students/ids`** (param-mirror of `list_students` minus sort/dir/limit/offset, `mine`-scope via `resolve_focus_group_ids` — an admin's `mine` ignored, no escalation) + **`POST /v1/students/bulk-status`/`bulk-delete`** (`student:manage`, `tenant_of`, in the literal block **before** `/{student_id}`; `_MAX_BULK_IDS=1000` caps the body). **Bulk delete is best-effort — a deliberate contract difference from single-delete's 502**: the row records `error` and the batch continues; `delete_student` is ML-first (no orphaned ML data) and the ML `DELETE` is **idempotent** (verified — 204 on an absent student), so an errored row **self-heals on retry**. **Tenant isolation is inherited, not re-implemented** — each looped single-write resolves the student via a `school_id`-scoped `get_student` **before any write** (a foreign id → per-row `error`, never a cross-tenant write); `/ids` reuses the identical tenant-scoped WHERE; the **`matches` seam is never SQL-joined** (delete loops the per-student ML RPC, select-all is an id-scan). **Frontend** extends the BP25 multi-select bar (not rebuild) with a discriminated **`Selection`** (`{mode:"ids"}` stale-safe [acted set ∩ loaded rows] | `{mode:"all"}` whole-server snapshot spanning pages), the checkbox column widened to `total>0` (Assign stays class-gated), **Disable/Enable/Delete** (delete behind a count-echoing BP8e-accurate `ConfirmDialog`) + a **"Select all N matching"** control (→ `getStudentIds` → `mode:"all"`); the selection **resets on filter/search change** (adjust-state-during-render) but **survives a re-sort**; every action runs `mutate()`+`mutateDashboard()`, clears the selection, and toasts honestly (partial "Disabled X of N — Y couldn't be updated/deleted"); a single **`bulkAction`** state guards double-submit **and** spins only the *clicked* button + an in-flight `role="status"` "· working…"; `getStudentIds` **reuses `listQuery`** (drift-proof "identical id set" invariant). **7 files + a new `test_bp27_bulk.py` (18 tests).** BE ruff+mypy+**635 passed / 47 skipped** + layering (foreign-id-untouched at the service **and** end-to-end route level, ML-failure isolation, retry-self-heal, `/ids`=the page id set for a status filter+`mine`-scope+foreign-school exclusion, 401/403/422, the cap, the route-ordering regression guard); FE lint+tsc+`next build` green (`/students` stays `○` static); 2× review loop (**R1 SHIP** — tenant/no-abort/route-ordering/the FE `Selection` machine all verified → 3 fixes [an end-to-end cross-tenant route test, the `getStudentIds`↔`listQuery` drift, a `>_MAX_BULK_IDS` docstring]; **R2 no blockers** — delete copy BP8e-accurate, honest partial toasts → 4 fixes [the in-flight `role="status"` announcement, a spinner on the *clicked* Disable/Enable button, sort-no-longer-wipes-selection, "deleted" not "updated"]). Honest limits: bulk delete is best-effort + irreversible (no undo, BP16 parked); select-all is a click-time snapshot; `_MAX_BULK_IDS=1000` (a >1000-student school would 422 the follow-up — narrow the filter); a set-based bulk write is the scale-up. **Not committed (awaiting owner review). Next: BP27b** (student bulk-resend + staff CSV invite, sharing a shown-once credentials dialog — R4-A05/A13), then **27c** (bulk-photo retry/overwrite + remove-from-class — R4-A07/A10), then the remaining Round-4 tiers (**BP28** audit/governance · **BP29** teacher coherence · **BP30** review tools · **BP31** onboarding/copy) — a phase/slice starts only on owner pick + scope re-confirm. **BP27b (shown-once bulk credentials — student bulk-resend-invite [R4-A05] + staff CSV bulk invite [R4-A13]) has landed** ([decisions/0083](decisions/0083-product-build-BP27b-bulk-credentials.md)) — the second BP27 slice, the plaintext-secret surface pulled out of 27a so it's designed + reviewed on its own; two features share one FE **`BulkCredentialsDialog`**. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** Both **return N one-time temp passwords at once** → a **shown-once secret contract** (plaintext in the response body only, on success rows only, generated per-row + hashed immediately by the reused single-writers, never persisted plaintext, never re-fetchable, **never logged**). Backend = two best-effort loops over the tested single-writes: **`StudentService.bulk_resend_invite`** (loops `resend_invite`; an error row carries **`email=""` because `get_student` 404s a foreign id BEFORE any student is resolved** — the loop never reads an email on the failure path; not active-school-gated so recovery works on a suspended school; reuses 27a's `BulkIdsRequest` cap 1000) behind `POST /v1/students/bulk-resend-invite` (`student:manage`, before `/{student_id}`); **`OnboardingService.bulk_create_staff`** (teacher-only, no `role`; pre-loop suspension → 400; per email **two separate try/excepts** so the two `ValidationError` sources don't collapse — `validate_email`→`invalid`, `create_teacher`→`limit_reached`/`duplicate` [**message discarded, no cross-tenant leak despite global email-uniqueness**]/`error`; the cap **re-counts per call** so the loop fills to `max_teachers` then `limit_reached`; `_MAX_BULK_STAFF=100` const; `emails: list[str]` not `EmailStr` so one bad address is a per-row `invalid` not a 422) behind `POST /v1/staff/bulk` (`staff:manage` **admin-only**, 201, before `/{user_id}`). FE: the shared **`BulkCredentialsDialog`** (results table + emphasized **Download-credentials CSV** [`saveCsv` promoted to `lib/csv.ts`] + a **per-row Copy button** + a close-guard keyed on `results.some(r=>r.tempPassword) && !downloaded` so an all-error batch closes freely + `reset()` **drops the passwords from state**; an `invalid`/`error` row shows the **server reason**, a `duplicate` discloses "may exist at another school"), a **"Resend credentials"** students-bar action (clears selection, **no** list revalidate — resend changes no list field), and a staff **CSV importer** (`bulk-invite-dialog.tsx`: pick→preview[advisory flags]→results→shared dialog) that **`mutate()`s the roster**. **12 modified + 3 new** (incl. `test_bp27b_credentials.py`, 21 tests). BE ruff+mypy+**656 passed / 47 skipped** + layering (password-only-on-success both endpoints service+route, the resend foreign-id `email=""`+no-password+batch-continues+foreign-untouched, staff duplicate[same+cross-school]/invalid/**cap-partial [max=2,1 existing,invite 3→1 created+2 limit_reached]**/suspended-pre-raise, **teacher-403-on-`/staff/bulk`**, student-403/401/over-cap-422/empty-422, route-ordering guard); FE lint+tsc+`next build` green (`/staff`+`/students` stay `○` static); 2× review loop (**R1 correctness+security SHIP — 0 blockers/0 should-fix** [shown-once VO defaults + every append path + no-secret-in-logs grep + `ConflictError`-discard + the `email=""` invariant + tenant scoping + cap re-count + admin-only gating all verified]; **R2 edge/a11y/copy — 0 blockers → 3 fixes** [per-row Copy button, surface the server reason, global-`duplicate` disclosure]). Honest limits: shown-once no bulk re-fetch (single resend recovers); the global-uniqueness `duplicate` footgun (disclosed, not a leak); cap partial-success ("At capacity"); preview flags advisory (server authoritative); a large `all`-mode resend renders many passwords in one modal (Download-CSV the save path). **27a committed (`1c8f1d1`); 27b not yet committed (awaiting owner review). Next: BP27c** (bulk-photo retry/overwrite + remove-from-class — R4-A07/A10), the last BP27 slice, then the remaining Round-4 tiers (**BP28** audit/governance · **BP29** teacher coherence · **BP30** review tools · **BP31** onboarding/copy) — a phase/slice starts only on owner pick + scope re-confirm. **BP27c (bulk-photo retry/overwrite + bulk-remove-from-class — R4-A07/A10) has landed** ([decisions/0084](decisions/0084-product-build-BP27c-bulk-photo-retry-remove-class.md)) — the third & **final** BP27 slice, mostly FE. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** **Item 1 (bulk-photo retry-failed, FE, mirrors BP19d):** a `useRef<Map<id,BulkEnrollInput>>` (rewritten per-batch in `run()`) + an extracted **`runPool(entries)`** (reads its arg, not `items` state) + **`retryFailed()`** re-running BOTH `error` AND enroll-failed `done` rows (= `summary.failed`), behind a **"Retry failed (N)"** button gated `!isRunning && summary.failed>0` (**single-pool invariant** — mounts only after the prior pool's `Promise.all` drains; a parent `handleRetry` re-arms the dialog's `firedRef` list-refresh); re-running an enroll-failed row is orphan-safe (`setStudentReferencePhoto` overwrites+reaps the prior object); honest `no_face`-won't-self-heal copy. **Item 2 (overwrite-confirm / keep-existing, FE-only, no backend change):** `Row.keepExisting` (default **Replace** — flipping it would silently drop intentional uploads), a per-row **Replace|Keep existing** radiogroup only for `enrolled` rows, **`start()` excludes kept rows** (so `setStudentReferencePhoto` is never called for them — the whole mechanism), an **`effectiveCount`** (matched−kept) for the "Upload N" label/disabled/"K kept" summary/empty-toast + a batch **overwrite `ConfirmDialog`** (names `effectiveReplacing`) firing only when ≥1 enrolled would be replaced. **Item 3 (bulk-remove-from-class, BE+FE):** **`ClassService.remove_students_bulk`** (best-effort loop over the tested single-writer `set_student_group(None)` — **Option B**, since `set_group_bulk` can't clear; a foreign id 404s **before any write** → per-row `error`, batch never aborts; PII-safe logging; reuses `BulkActionResult` via a verified layering-clean `services→services` import) behind **`POST /v1/students/bulk-remove-class`** (`student:manage`, `tenant_of`, `BulkIdsRequest`→`BulkActionResponse`, **before** `/{student_id}`); FE `bulkRemoveStudentsFromClass` + a **"Remove from class"** bar action (gated on `classes.length>0`, acts on `targetIds` [spans `all`-mode], `afterBulkMutation` since the class name is list-visible). **7 files** (incl. +8 bulk-remove tests in `test_bp27_bulk.py`). BE ruff+mypy+**664 passed / 47 skipped** + layering (removes-many/only-selected, foreign-id→`error`+batch-continues+**foreign-still-classed** [service + e2e route], round-trip-clears-class, 401/403/422, route-ordering guard); FE lint+tsc+`next build` green (`/students` stays `○` static; retry/overwrite carried by tsc + the mirrored BP19d invariant — no FE test harness); 2× review loop (**R1 correctness SHIP — 0 blockers/0 should-fix** [single-pool invariant / `firedRef` re-arm / retry set / keep-existing-no-upload / bulk-remove 404-before-write + route ordering / the nested modal-over-non-modal all verified] → 2 comment-accuracy fixes; **R2 edge/a11y/copy — 0 blockers → 2 fixes** [the "Remove from class"→**"Cleared class for"** toast honesty since the backend returns `ok` for a not-in-a-class no-op, and a more-visible radio focus ring]). Honest limits: retry re-uploads a fresh object (no self-heal for `no_face`); keep-existing default is Replace (opt-out per-row + the batch confirm); bulk-remove is a per-id loop (~800, capped 1000; a set-based UPDATE is the scale-up; removing a not-in-a-class student is a harmless no-op `ok`). **27a/27b committed (`1c8f1d1`/`9aaba83`); 27c not yet committed (awaiting owner review). BP27 (Bulk operations parity) is now complete (a, b, c).** **Next:** the remaining Round-4 tiers — **BP28** governance/audit · **BP29** teacher coherence · **BP30** review tools · **BP31** onboarding/copy; a phase starts only on owner pick + scope re-confirm. **The owner then set the pipeline to autonomous** (commit+push each slice on pipeline completion, then continue) and picked **BP28 (Governance & audit completeness)**, sliced **28a** (surface the wired audit filters + date-range + CSV export, no migration) · **28b** (an admin-action audit table, migration `0020`) · optional **A15** (teacher-cap visibility). **BP28a (access-log filters + date-range + CSV export) has landed** ([decisions/0085](decisions/0085-product-build-BP28a-audit-filters-export.md)) — R4-A24/A26. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** The access log (BP8b) had event/student filters wired but **no UI** + no export. Backend (the only real gap): `DownloadAuditRepository.list_recent`/`count_recent` gain **date-range** (`created_from`/`created_to`) + **`actor_role`**, threaded to `GET /v1/audit/downloads`; two hardenings — the date/role params are **typed** (`datetime`/`Role`) so a bad value **422s at the boundary**, and `actor_role` filters the **denormalized column, NOT a users JOIN** (so a deleted-actor row still matches its role — the audit outlives the account); `count` mirrors `list` (honest total); no new index (`(school_id, created_at)` serves the range). FE: `audit/page.tsx` reworked to the students-page **URL-backed** filter pattern (a `<Suspense>`'d `AuditContent` so `/audit` stays `○` static; `RoleGate` school_admin), Event+Student+**Role** selects + a from/to **UTC** date range (hinted) + a **client-side CSV export** (bounded 200-row page walk, capped 10k with a "first 10,000" toast, current filters, reuses `toCsv`/`saveCsv`). **11 files, no new files.** BE ruff+mypy+**670 passed / 47 skipped** + layering (a gated round-trip extended with a real-SQL date-range + `actor_role` incl. **delete-the-actor-then-re-filter** denormalized-survival proof; the 2×422; a filters-reach-the-service spy; count==filtered-total; the audit:view matrix; on a throwaway `bp28a_migtest`, dropped, dev `app` untouched); FE lint+tsc+`next build` green (`/audit` stays `○` static); 2× review loop (**R1 correctness SHIP — 0 blockers/0 should-fix**; 2 NITs out-of-scope [picker 200-cap; CSV **formula-injection** — a pre-existing app-wide `toCsv` limit NOT fixed since the shared guard would corrupt `token_urlsafe` credential passwords]; **R2 edge/a11y/copy — 0 blockers → 4 fixes** [capped-export toast, picker-truncation hint, unified "Role" naming, "Date filters use UTC" hint]). Honest limits: UTC day-boundaries (local-day is the refinement); pickers show first-200 (hinted; typeahead is the scale-up); export bounded 10k (hinted); the app-wide CSV formula-injection limit; offset pagination. **BP28a (0085) committed+pushed (`9f4e840`); BP28b (admin-action audit) has landed** ([decisions/0086](decisions/0086-product-build-BP28b-admin-action-audit.md)) — the second & **final** slice of BP28 (Round-4 **R4-A25**: governance-lifecycle actions — who disabled/deleted/re-enrolled/re-invited a student or teacher, who edited a school — left **no record**). **BE + FE — migration `0020`** (a new backend-owned table); no ML change, no new dependency, no new permission (reuses `audit:view`), no new env var. An append-only **`admin_action_audit`** (mirrors BP8b's `download_audit`): `id`·`school_id`(FK CASCADE)·`actor_user_id`(FK **SET NULL** — the trail outlives the account)·`actor_role`(denormalized, CHECK ∈ 4 roles)·`action`(CHECK ∈ 11: `student_{created,disabled,enabled,deleted,reenrolled,invite_resent}`·`staff_{created,disabled,enabled,invite_resent}`·`school_updated`)·`target_type`(CHECK ∈ student/staff/school)·`target_id`(nullable, **NO FK** — heterogeneous, like `match_corrections`↛`matches`)·`target_label`(nullable, captured at write time)·`created_at` + 4 composite indexes (school + target/actor/action drill-downs). **The load-bearing design:** a private `_record_action` on **both** `StudentService` + `OnboardingService` writes one row **best-effort** (`try/except → _log.warning("admin_action_audit_record_failed", school_id=…)` — never blocking/rolling back the mutation; safe because each repo method owns its own transaction, so the mutation already committed) inside the single-writers — so **BP27's bulk loops are audited row-by-row for free** (they thread the actor down + call those same single-writers). Hooks record the *effective* action only (a no-op status set / a refused last-admin disable / an un-threaded `actor_role=None` caller record nothing; `update_school` keys the row to the TARGET school since a platform admin's `school_id` is None). Reads via `AdminActionAuditService.school_action_log` (adapter `postgres_admin_action_audit.py` + registry/container) join the actor's **current** email from the backend's OWN `users` rows — batched in-Python, no N+1, **never a cross-service SQL join** (a deleted actor → `actor_email=None`, the denormalized role + label still show what happened); `GET /v1/audit/admin-actions` (`audit:view`, tenant from token; date params typed `datetime`, action/target typed enums → **422 at the boundary**). FE: a new `lib/audit/actions.ts` (`ACTION_LABELS` humanizer + filter options), a Radix **`components/ui/tabs.tsx`** wrapper (roving tabindex + arrow-keys + tab/tabpanel aria), and `audit/page.tsx` reworked to **two tabs** (Downloads / Admin actions, `?tab=` URL-backed inside the existing `<Suspense>` so `/audit` stays `○` static) each with URL-backed filters + a client-side CSV export; the Target cell falls back to **"Deleted student"** for a null-label `student_deleted` row. **Owner decision (flagged by plan-review, resolved by default):** a `student_deleted` row stores **id-only (`target_label=None`)**, preserving BP8e "delete means gone" (full-identity retention is a one-line override). BE ruff+mypy+**705 passed / 48 skipped** + layering (record + filtered reads + actor-email compose [deleted→None] + the effective-action hook placements; a **gated real-Postgres** round-trip: record→list/count→filters→newest-first→tenant scope + **actor SET NULL survival**, on a throwaway `bp28b_migtest`, dropped, dev `app` untouched); migration `0020` up→down→up on the throwaway; FE lint+tsc+`next build` green (`/audit` stays `○` static); 2× review loop (**R1 correctness SHIP — 0 blockers**: best-effort own-transaction safety / tenant scoping on reads + every write-hook / effective-action placements / no cross-seam join / actor-threading mypy cleanliness all verified → 1 NIT [`school_id` into both warning logs]; **R2 edge/a11y/copy — 0 blockers → 2 should-fix, both applied**: replace the hand-rolled `role="tablist"` `TabButton` with the app's Radix `Tabs` [arrow-key nav + tabpanel wiring], render a muted **"Deleted student"** for a null-label `student_deleted` row [cell + CSV]). Honest limits: `student_deleted` id-only (name deliberately not retained); actor email is the CURRENT email (a convenience join, the denormalized role is the point-in-time fact); best-effort write (a failed audit is logged not retried — a transactional-outbox trail is the scale-up); the app-wide `toCsv` formula-injection + 10k client-side-export limits (BP28a); offset pagination. **BP28 (Governance & audit completeness) is complete (28a + 28b).** **A15 (teacher-cap visibility — the BP28 closer) has landed** ([decisions/0087](decisions/0087-product-build-A15-teacher-cap-visibility.md)) — R4-A15/R3-A2-04, **BE + FE, no migration/ML/dep/perm/env-var**: a school-admin creating a teacher at the `max_teachers` cap learned only via a post-submit **409**; A15 surfaces the cap on the staff page **before** submit. The planning agent's key find — the dashboard read **already computes both numbers**: `DashboardService.school_summary` already loads the school (`max_teachers`) + already computes `teacher_count = count_by_school_and_role(school_id, Role.TEACHER)` (collapsed into a `has_staff` bool), so `SchoolDashboard`/`DashboardResponse` gain a nested **`StaffSummary {teacher_count, max_teachers}`** (no new query, no new endpoint, no router change — `GET /v1/dashboard` is already `tenant_of(actor)` + `dashboard:view`; the numbers are the **same source the 409 enforces**, status-agnostic = active+disabled, so display can't drift). FE: `staff/page.tsx` reads it via the shared `useDashboard()` SWR key + renders a seat-usage line **as its own element** (NOT the search-filtered `PageHeader` `${total} teachers`) — "At capacity —"/"Over capacity —"/never-negative — and **soft-gates** the Add-teacher/empty-state/BulkInvite triggers at capacity with a visible note (**advisory-only** — a loading/errored read disables nothing, the 409 stays the true backstop). **7 files, no new files.** BE ruff+mypy+**707 passed / 48 skipped** + layering (seat usage reflects cap+count incl. a **disabled teacher still consuming a seat** + admins excluded; tenant-scoped; the route returns the block + is tenant-isolated); FE lint+tsc+`next build` green (`/staff`+`/dashboard` stay `○` static); 2× review loop (**R1 correctness SHIP — 0 blockers** [display==enforcement/no-second-query, proven tenant isolation, fail-safe advisory guard, seat-line/search-total independence, the soft-gate a real functional block] → 2 NITs to R2; **R2 edge/a11y/copy SHIP — 0 blockers → 2 fixes + 1 test**: dropped `role="status"` [re-announced on every ~60s revalidate], removed the **dead `title`** on the disabled triggers [`pointer-events-none` so it never showed] + the redundant `aria-disabled`, added the disabled-teacher-counts test). Honest limits: the gate is advisory (the 409 is the true gate); lowering the cap below the count shows "Over capacity" but force-removes no one (BP18c). **BP28 is now fully closed (28a + 28b + A15).** **BP29 (Teacher-role coherence) has landed** ([decisions/0088](decisions/0088-product-build-BP29-teacher-role-coherence.md)) — the second Tier-1 Round-4 phase, **the teacher persona finished** (closes **R4-T01–T08**). **FE-only — no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** **Security invariant (unchanged):** delegation stays **convenience-scope, NOT a boundary** (BP11c's owner call — a teacher could already see the whole school); BP29 is **legibility only**, no `class:manage` grant. Six FE items: **(1)** role-aware onboarding — a teacher no longer sees the admin `SetupChecklistCard` (its "Add a teacher" step dead-ends at the `school_admin`-only `/staff`); a one-line "your role" note instead (admin path + `DashboardContent` byte-for-byte unchanged); **(2)** a **delegation-clarity banner** (new `components/delegation/delegation-banner.tsx`) shown when `role==="teacher" && !useMyClasses().isLoading && myClasses.length===0`, mounted on students + events, dismissal in `localStorage` via **`useSyncExternalStore`** (server snapshot `false`=shown → SSR-safe, the repo's `use-online-status.ts` pattern; the `role==="teacher"` guard is **load-bearing** since an admin's `useMyClasses` is `enabled`-gated off → also `[]`); **(3)** mark **"(my class)"** on the students/events class **filter** `<select>` options in the teacher's set (text suffix); **(4)** live my-classes refresh — `use-my-classes.ts` adds `{revalidateOnFocus:true, refreshInterval:60_000}` (the BP20 pattern, teacher-`enabled`-gated → no admin/student polling); **(5)** read-only roster **baseline** — the marked class filter IS the teacher's read-only roster (`GET /v1/students?student_group_id=`), no new route/nav (avoids drift toward the mutation-dense admin class page); **(6)** graceful **RoleGate** denial — `role-gate.tsx` renders an `EmptyState` ("Not available for your role" + a "Go to dashboard" link to `homePathForRole(user.role)`) instead of the silent `router.replace` bounce, still `return`ing **before** `children` so a guarded page's fetch never fires. **Deferred (documented): the "My work" lens (R4-T04)** — needs **new backend reads** (no `created_by`/actor filter on events/media; the dashboard `needs_review` is school-wide) and `mine=true` "my events" is empty for exactly the un-delegated teacher, so a half-lens would break the FE-only shape. **6 files (1 new), all under `frontend/`.** FE lint+tsc+`next build` green (`/dashboard`/`/students`/`/events` stay `○` static); no backend suite delta; no FE test harness (repo norm — gate + manual walk). 2× review loop (**R1 correctness/entitlement SHIP — 0 blockers** [100%-`frontend/` diff, no new permission/endpoint/grant, the load-bearing role guard, `useSyncExternalStore` fidelity, Rules-of-Hooks, the SWR override teacher-gated, RoleGate fetch-never-fires all verified] → 2 NITs not worth acting on; **R2 edge/a11y/copy SHIP — 0 required fixes → 1 NIT applied**: the RoleGate `EmptyState` `role="alert"`→`role="status"` [informational access-denied, not an error — the 0037 convention]; 3 NITs left as-is [permanent-per-browser dismissal acceptable; inline marker `Set`s low-ceremony; the "Go to dashboard" copy holds since RoleGate only wraps `(school)` pages]). Honest limits: legibility-not-enforcement; banner dismissal permanent-per-browser; roster = the marked filter (a dedicated page is a future add); the "My work" lens deferred; no FE automated tests. **BP29 is complete.** **BP30 (Review-loop power tools at scale) has landed** ([decisions/0089](decisions/0089-product-build-BP30-review-loop-power-tools.md)) — a Tier-2 Round-4 phase, **the review lane's power tools at 200-ambiguous-match volume** (closes **R4-A20/A21/A22/A23** + **R4-F04**). **FE-only — composes shipped BP13/BP22/BP5 primitives; no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** **Two load-bearing invariants (verified in code):** **no auto-confirm** (BP22 stance — the threshold multi-select STAGES a selection, the human commits) + **staff-only/student-safety** (the show-removed disclosure lives inside `AppearanceEditor`, which the student lightbox never renders [`showAppearances={false}`], reads only the staff-gated appearances endpoint, and leaves the BP5 `effective_*` gate untouched). Five items: **(1) threshold multi-select** (R4-A21) — a "Select below" row (chips `<60/70/80%` + a clamped free `%` field) → `setSelected(pairs where round(confidence*100)<pct)`, calling only `setSelected` never `batchReview`; **(2) batch-undo** (R4-A20) — `apply()` captures the resolved `ReviewPair[]` into `lastRejected` **before** `mutate()` (no stale closure) on a reject + an inline `role="status"` banner "Rejected N · Undo" fanning out `Promise.allSettled(undoCorrection…)` (reverts each pair to raw-ML `needs_review` pending — no batch-delete endpoint); **(3) "Show removed (N)" disclosure** (R4-A20, Option A FE-only) — `AppearanceEditor` re-lists the `verdict==="rejected"` rows it already receives (client-filtered today) with per-row Re-add, `N` from the same prop (no new fetch), safe for both callers; **(4) table-view toggle** (R4-A22) — a `view:"grid"|"table"` local state over the same `pairs`/`selected` (presentational); **(5) add-students cue** (R4-A23) — "Showing first X — type to refine" from the already-returned `useStudents` `total`; **(6) lightbox auto-paging** (R4-F04) — `PhotoGrid` threads `hasMore`/`loadingMore`/`onLoadMore` into `Lightbox`, a `goNext` `useCallback` calls `onLoadMore()` only if `!loadingMore` + a `wantNext` ref + a `[mediaIds.length]`-keyed effect advances `index` once the page lands (race-safe: no double-fetch/no out-of-range); non-paged callers behave as before; the review-lane hand-rolled `<ul>` is untouched. **Option B (event-wide rejected grid) deferred.** **4 files, no new files, all FE.** FE lint (0/0)+tsc+`next build` green (gallery route stays dynamic `ƒ`); no backend suite delta; no FE test harness (repo norm). 2× review loop (**R1 correctness/entitlement SHIP — 0 blockers** [both invariants, undo async, lightbox race-safety, Rules-of-Hooks all verified] → 4 optional NITs; **R2 edge/a11y/copy SHIP-READY — 0 blockers → 4 polish fixes applied**: removed-row % `text-ink-muted`→`text-ink-secondary` [AA, BP25 convention], free-% field clamped `[0,100]`, `AppearanceEditor` docstring updated, cue trailing period dropped; a11y confirmed [named chips/toggle, `role="status"` undo banner, `aria-expanded` disclosure + count, per-row Re-add `aria-label`]). Honest limits: still no auto-confirm; per-event lane; the undo window is session-transient; "Show removed" is per-photo not an event-wide grid (Option B deferred); the popover cue counts the first page; the review-lane table isn't windowed (fine at ~200/event). **BP30 is complete.** **BP31 (Onboarding feedback loop & copy/discoverability polish) has landed** ([decisions/0090](decisions/0090-product-build-BP31-onboarding-copy-polish.md)) — the **final** Round-4 phase, a "floor-sweep 2" (closes **R4-A01/A02/A08/A09/A14/A18** + **F01/F02**). **FE-only — no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** **Three items CUT** (plan-review verified already-shipped, not near-no-ops): **A19** (BP24a clearable-tag explainer), **A11** (BP11a grade/section display), **F05** (partial-download messaging already honest) — keeping the sweep at **8 real items**. The one backend question — A09's per-row reject `reason` — is **already returned** (`BulkStudentResultResponse.error` + the FE type exist; the students `BulkImportDialog` just didn't render it) → wholly FE-only. Items: **A01** checklist momentum (`void mutateDashboard()`/`globalMutate("dashboard")` added **alongside** the existing mutates at the 4 completing flows so the `SetupChecklistCard` advances at once, fire-and-forget + an "Almost there — announce to reach your students." cue when only announce remains — `nextKey==="has_distributed"`, since "all core done" retires the card); **A02** dashboard warmth ("Welcome — let's get your school set up"); **A08** both reference-photo dropzones hint "This photo enrolls the student's face for matching…"; **A09** `bulk-import-dialog.tsx` renders the per-row reason (server `error` for `invalid`; static fallback for `duplicate`/null-error; nothing for `created`); **A14** a `SearchInput` filters the `EditClassesDialog` list (selection preserved on save); **A18** a decorative "Matching…" pulse badge next to the Photos-card `StatusPill` gated on `inFlight` (`aria-hidden` — the existing "Matching since…" `aria-live` region is authoritative; respects reduced-motion); **F01** an inline action beside the failure note (branch on `enrollment_failure_reason` — `ml_unavailable`→Re-enroll, else the existing `ReferencePhotoDialog`); **F02** the events-list "Matching" pill wrapped in a `Link` to `/events/{id}` (Process) when derived `pill ∈ {not_started, failed}` (dashboard alerts can't deep-link to one event — `needs_attention` has no per-event IDs, a documented backend follow-up). **8 files, no new files, all FE.** FE lint+tsc+`next build` green (touched pages keep their markers — `/dashboard`/`/students`/`/staff`/`/events` stay `○`); no backend suite delta; no FE test harness (repo norm). 2× review loop (**R1 correctness SHIP → 1 should-fix applied**: the A01 completion cue was a **dead branch** [`allCoreDone` requires `has_distributed`, which retires the card] → re-keyed to the reachable one-step-left state; else [fire-and-forget, A09 field names, A14 selection-safe, A18 `inFlight` gate, F01/F02 branches] verified clean; **R2 edge/a11y/copy SHIP → 1 should-fix + 2 NITs applied**: the A18 badge dropped `role="status"` [double-announced with the live region → decorative `aria-hidden`], "Syncing…"→**"Matching…"** [BP21 one-grammar], `students/page.tsx` unified to the typed `mutateDashboard()` [+ removed the unused `swr` import]; kept the A01 cue `role="status"` [one-time, no loop] + both F01 placements). Honest limits: F02 is FE-only-bounded (the dashboard per-event deep-link needs a `needs_attention` ID — a follow-up); a photoless bulk student stays `pending` so it won't tick the enroll step; A19/A11/F05 dropped; no FE automated tests. **BP31 is complete — and with it the ENTIRE Round-4 roadmap (BP26 → BP27 → BP28 → A15 → BP29 → BP30 → BP31) is done** (the v1-scoped pre-release staff/admin improvement track, `product/08`+`09`, fully delivered). **Remaining open work is the parked backlog** — **BP12** (outbound email/SMS distribution), **BP15** (accuracy at scale), **BP16** (lifecycle & retention), the **BP6 video timeline**, and the **blocked** BP22-slice-4 (student "This isn't me" safety) — tracked in [`product/05-parked-backlog.md`](product/05-parked-backlog.md). A phase starts only on owner pick + scope re-confirm.

> The TEMP wiring demo (decisions/0006) has been **removed** in Phase 4
> ([decisions/0017](decisions/0017-docker-observability-ci.md)): the `demo.py`
> modules, `/temp/*` routes, `demo_events` table, FE `app/temp/` + `app/api/temp/`,
> and the TEMP deps/env are gone. FE/BE are clean shells again until real features
> land; the ML service now runs its real enrollment API + inference worker.

## Commands

Run Python commands from the repo root (uv workspace). `uv` fetches Python 3.12 itself.

```bash
uv sync --all-packages              # install all workspace deps (creates .venv)
uv run pytest                       # all Python tests
uv run pytest services/ml_service   # a single service's tests
uv run ruff check . && uv run mypy .
./scripts/check.ps1                 # local gate: ruff + mypy + layering grep + pytest
uv run uvicorn ml_service.api.main:app --reload          # ML API       :8000 (/metrics too)
uv run python -m ml_service.workers.inference_worker      # ML inference worker
uv run uvicorn backend.main:app --reload --port 8001     # backend     :8001
# Apply ML DB migrations (URL from env, never committed):
ML_DATABASE_URL=postgresql+asyncpg://... uv run alembic -c services/ml_service/alembic.ini upgrade head
# Apply backend DB migrations (separate chain, version table alembic_version_backend):
BE_DATABASE_URL=postgresql+asyncpg://... uv run alembic -c services/backend/alembic.ini upgrade head
# Bootstrap the first platform admin (args/getpass, never .env; needs a migrated DB):
BE_DATABASE_URL=postgresql+asyncpg://... uv run python -m backend.cli.bootstrap_admin --email ops@example.com
# Frontend needs Node >= 20.9 (Next 16); the shell defaults to too-old Node 18 — `nvm use 22` first.
cd frontend && npm install && npm run dev                # frontend    :3000
cd frontend && npm run lint && npx tsc --noEmit && npm run build   # FE gate (lint + types + build)
docker compose up --build           # all 3 images + Postgres + Redis (needs Docker running)
./scripts/up.ps1                    # helper: Postgres+Redis detached (stay up), apps in foreground; Ctrl+C stops only the apps
./scripts/sync-env.ps1              # add keys present in .env.example but missing from .env (prints what it added); -Check = dry run (up.ps1 runs this advisory-check)
```

Helper scripts live in `scripts/` (see `scripts/README.md`).

Notes for future instances:
- The Python Dockerfiles build from the **repo root** context (they need root `pyproject.toml` + `uv.lock`); only the frontend builds from `./frontend`.
- pytest uses `--import-mode=importlib` so same-named test files coexist across services — don't add `__init__.py` to `tests/` dirs.
- New Python deps go in the **service's** `pyproject.toml`; shared dev tools in the root `[dependency-groups] dev`. Re-run `uv sync --all-packages` after.

## Status: Phase 4 done (Docker + observability + CI + TEMP removal) — v1 feature-complete

The repo is git-initialized (`main`) with a secrets-safe `.gitignore`. The ML service is being built in reviewed phases. **Phase 1** delivered the pure `domain/` (models, the 9 ports, `apply_threshold_and_gap`, errors) + `orchestration/` (`EnrollmentService`, `InferenceService`). **Phase 2** delivered a real adapter for every port under `adapters/` — `SCRFDDetector` + `ArcFaceEmbedder` (InsightFace `buffalo_l`, separate modules), `FaissPerSchoolVectorIndex` (+ pluggable index store, LRU cache), `SupabaseMediaStore`/`LocalFsMediaStore`, `DecordFrameExtractor`/`OpenCvFrameExtractor`, `PostgresMatchRepository`/`PostgresThresholdProvider`/`PostgresReferencePhotoRepository`, and `RedisStreamsJobQueue`/`InProcJobQueue` — plus the ML metadata schema in `db/` with Alembic (`0001_initial`). **Phase 3 is complete** (see [decisions/0016](decisions/0016-wiring-api-worker.md)): the wiring composition root (`wiring/settings.py` full req §12 surface, `wiring/registry.py` name→class per port, `wiring/container.py` lazy-memoized adapter build + inject), the enrollment API (`api/routes/enrollment.py` `POST …/enroll` + `DELETE …`, `api/deps.py` container-backed, `api/routes/health.py` with dep-probing `/readyz`, central error mapping in `api/main.py`), and the inference worker (`workers/runner.py` consume/ack/nack + retry/DLQ, `workers/inference_worker.py` entrypoint). **Phase 4 is complete** (see [decisions/0017](decisions/0017-docker-observability-ci.md)): the Dockerfile builds one image runnable as API/worker/migrate with `buffalo_l` baked in at build time; `docker-compose.yml` adds an `ml-worker` service + a shared `faiss` volume (API writes / worker reads); `observability/{metrics,logging,tracing}.py` provide the req §13 Prometheus metrics (served at `GET /metrics`), structlog JSON logging, and opt-in OTel tracing (via `ML_OTEL_EXPORTER_OTLP_ENDPOINT`); CI (`.github/workflows/ci.yml`) + `scripts/check.ps1` run ruff/mypy/pytest + the layering grep, plus a `docker-build` job; and the TEMP wiring demo is fully removed. 106 tests pass (+5 gated on real Postgres/Redis/models); ruff/mypy/layering clean. Design docs with diagrams live in `services/ml_service/docs/` (`00`–`09`). **Not yet run on this host:** the Docker build + `buffalo_l` bake and the InsightFace runtime path (they execute in CI's `docker-build` job / on a Docker host). **Post-v1:** the inference worker now persists the full per-face **detection audit** — media-centric `media_detections`/`media_frames`/`face_detections`/`face_detection_candidates` (replace-by-media) + `matches.frames_matched` + the `student_media_appearances` view — via a 10th `DetectionRepository` port and Alembic `0002`; see [decisions/0021](decisions/0021-persist-per-frame-detections.md). The two ML-service specs remain the binding source of truth:

- `ml-service-requirements.md` — locked v1 requirements (the "what"). Source of truth for functional/non-functional requirements, locked decisions (§8), interface contracts (§9), and data contracts (§10).
- `ml-service-architecture.md` — v1 architecture (the "how"). Source of truth for module layout (§5), adapter choices + versions (§6), and the FAISS index lifecycle (§7).

When implementing, treat both docs as binding. The architecture doc's §5 module tree is the intended layout (relocated under `services/ml_service/src/ml_service/`); §6's adapter table fixes the initial library choices and versions. If a code change contradicts either doc, surface the conflict rather than silently diverging.

## What this service is

A multi-tenant face-recognition service for distributing event photos/videos to the students who appear in them. Two pipelines that share one embedding model version but are otherwise independent:

- **Enrollment** (synchronous HTTP, student-id-triggered): resolve a student's reference-photo URIs → fetch → detect → embed → upsert into a per-school vector index (see [decisions/0009](decisions/0009-enrollment-contract.md)).
- **Inference** (async, queue-driven workers): fetch media → (video) extract frames at fixed FPS → detect → embed → search the school's index → apply threshold/gap decision → dedupe → persist match records.

## Architecture: hexagonal (ports and adapters)

The design exists to satisfy NFR-1/NFR-2 (swap ML stack or storage by config alone). The whole structure depends on strict layering:

- `domain/` — pure, imports no third-party libs. Models, the 10 `Protocol` ports (req §9 + `ReferencePhotoRepository` (see [decisions/0009](decisions/0009-enrollment-contract.md)) + `DetectionRepository` (see [decisions/0021](decisions/0021-persist-per-frame-detections.md))), and the pure `apply_threshold_and_gap()` decision function.
- `orchestration/` — `EnrollmentService` / `InferenceService`, plus the shared `identify_in_frames` kernel (`identify.py` — the per-frame `face → person` loop used by both the worker and the dev test UI, [decisions/0020](decisions/0020-identify-all-faces-and-per-frame.md)); `InferenceService` also maps that timeline into the detection audit written via `DetectionRepository` ([decisions/0021](decisions/0021-persist-per-frame-detections.md)). Imports only `domain`.
- `adapters/` — one subpackage per port; the only place concrete libs (faiss, insightface, azure, redis, sqlalchemy) are imported.
- `api/`, `workers/`, `wiring/` — the only modules allowed to import adapters. `wiring/container.py` builds concrete adapters from config via a name→class registry and injects them into the services.

**Layering invariant (wire into CI):** no concrete ML/IO library may be imported in `domain/` or `orchestration/`. The doc's acceptance test:
```
grep -r "import faiss\|import cv2\|import insightface\|import boto3" ml_service/domain ml_service/orchestration
```
must return nothing. The API and worker are thin shells — both build a job context and call the same service code paths.

## Correctness invariants (do not break these)

These come straight from the specs and are the easy things to get subtly wrong:

- **Tenant isolation (NFR-3, FR-I4):** all matching is strictly within one `school_id`. Enforced at the `VectorIndex` interface. There is no cross-school search in v1.
- **Threshold resolution is once per job, not per face.** Resolve `Thresholds` into the job context, pass it as a value into the pure decision function. Per-school value with global-default fallback when null (req §6.1).
- **Reproducibility (NFR-4):** each match record persists `embedding_model_version`, `detector_model_version`, `threshold_used`, `gap_threshold_used` — the values actually used at decision time, not re-read at write time.
- **Two-layer idempotency (NFR-5):** in-memory worker dedupe keyed on `(student_id, media_id)` first; DB unique constraint on `(media_id, student_id)` as the second line of defence. `save_batch` is the only **`matches`** write path and uses `INSERT ... ON CONFLICT` where higher confidence wins. (The detection audit is a separate, independently-idempotent write path — replace-by-media — [decisions/0021](decisions/0021-persist-per-frame-detections.md).)
- **Decision logic (req §6.2):** top-K=2. ≥threshold filter; 0 → unknown (log only, no record, FR-I8); 1 → emit; 2 → emit top-1 alone if `(top1-top2) > gap`, else emit both with `needs_review=true`.
- **Identification is per-face (`face → person`), across every frame.** `identify_in_frames` (`orchestration/identify.py`) detects **every** face in **every** frame and decides each independently — a group photo names everyone; one frame can match several students. The worker persists the deduped best per `(student_id, media_id)` as `matches` (the two-layer idempotency above), but the kernel *also* returns the full per-frame/per-face timeline (`frames[]`, incl. each face's raw top-k candidates) — the dev test UI renders it for video (per timestamp, **not** globally deduped), and the worker **persists** it as a media-centric detection audit (`media_detections`/`media_frames`/`face_detections`/`face_detection_candidates`, replace-by-media) plus `matches.frames_matched` and the `student_media_appearances` view — see [decisions/0021](decisions/0021-persist-per-frame-detections.md).
- **Embedding convention:** 512-dim ArcFace, L2-normalized; cosine similarity via FAISS `IndexFlatIP`. Lock `EMBEDDING_DIM=512` / `SIMILARITY_METRIC="cosine"` in `domain/models.py`; every adapter must emit normalized vectors.
- **Detector and embedder stay in separate adapter modules** even though both ship in the `buffalo_l` bundle — sharing the import breaks NFR-1.
- **Enrollment is replace-not-append (FR-E3);** per-photo failures don't abort the request (FR-E4).

## FAISS lifecycle (the trickiest part — see architecture §7)

File-backed index per school in blob storage (`index.faiss` + `id_map.json` + `meta.json`). `meta.json.version` is the cache-invalidation key, bumped on every successful write and written **last** as the commit point. Each worker keeps an LRU cache of loaded indexes; the read path re-checks `meta.version` (cheap HEAD) and reloads on staleness. On read, validate `meta.embedding_model_version` matches the configured embedder and **fail loud** on mismatch — never search a stale-model index. v1 serializes writes via a single-replica enrollment deployment (Option A); per-school Redis lock (Option B) is the documented scale-up.

## Planned stack (architecture §6)

Python, hexagonal. FastAPI (CPU API pods) + GPU inference workers consuming Redis Streams. InsightFace (SCRFD detector + ArcFace embedder), faiss-cpu, **Supabase Storage** media store (default; `local_fs` for dev — diverges from architecture §6's Azure default, see [decisions/0010](decisions/0010-supabase-media-store.md)), decord for video frames (OpenCV fallback), Postgres via SQLAlchemy 2.x async (asyncpg), pydantic-settings for config. Observability: prometheus_client (`/metrics`, req §13 counters + latency histogram) + structlog (JSON) + opt-in OTel tracing at the service-call boundary — labels are `school_id` + model versions only, never `student_id`/`media_id` (cardinality bomb). See [decisions/0017](decisions/0017-docker-observability-ci.md) + docs/09.

**Platform note:** `insightface` and `decord` have no Windows/py312 wheels, so they are declared Linux-only (`; sys_platform == 'linux'`) — they run in the Docker image; local Windows dev uses the OpenCV extractor and import-gated tests. All other heavy deps install cross-platform. See [decisions/0014](decisions/0014-queue-and-platform-adapters.md).
