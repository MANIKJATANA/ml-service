# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Working rules (always follow)

- **Record every decision.** Any change or non-trivial decision gets a dated entry in `decisions/` (see `decisions/README.md` for the format). Update the index there.
- **Keep this file current.** When architecture, commands, or conventions change, update CLAUDE.md in the same change.
- **Never commit or push on your own.** Make and verify changes, but do not run `git commit`, `git push`, or open PRs until the user explicitly asks. Leave changes staged/unstaged for them to review.
- **Self-review.** After making changes, review your own work and fix the issues you introduced before reporting done.
- **Review→fix loop (2×) after each phase.** After implementing a phase, run a *review agent → apply fixes* cycle **twice** — distinct focus per round (round 1: correctness / bugs / async / error-handling; round 2: edge cases / quality / simplification / test-coverage gaps) — verifying the gate (ruff + mypy + pytest + layering) is green after each round, **before** presenting the phase. Then stop for the user's review/approval before starting the next phase.
- **All DB schema changes go through migrations.** Any change to the database schema (tables, columns, indexes, constraints, types) must be a versioned migration file in the migrations folder — never an ad-hoc schema change made directly in application code. Application code may only assume the schema a migration has already established.
- **Every new env var goes in `.env.example`.** Whenever code introduces a new environment variable — a new `Settings`/pydantic-settings field, an `os.environ`/`os.getenv` read, or a `${VAR}` in `docker-compose.yml` — add it to `.env.example` in the **same** change, documented with its default or a placeholder (**never a real secret**). `.env.example` must always list the full env surface the code supports. Do **not** touch `.env` for this (it's gitignored and holds real values).
- **Never read `.env` files** (or any secrets files), and never store secrets in memory or in code.

## Repo shape: monorepo, 3 Docker images

This is **one repo** that builds **three images** (see [decisions/0003](decisions/0003-monorepo-structure.md)):

| Image | Path | Stack | Role |
|---|---|---|---|
| Frontend | `frontend/` | Next.js (Node) | UI |
| Backend (BE) | `services/backend/` | Python + FastAPI | The "core system": uploads, storage, notifications, distribution; calls the ML service's enrollment API and enqueues inference jobs |
| ML service | `services/ml_service/` | Python + FastAPI + workers | Face enrollment/inference, as specced below |

Python is managed with **`uv` in workspace mode** — one root `pyproject.toml` + one `uv.lock` shared by the Python members (`backend`, `ml_service`); shared Python code goes in `packages/`. The Next.js frontend is a separate Node package. BE is the only caller of the ML service; the ML service never calls BE.

The structure above is **scaffolded** (see [decisions/0004](decisions/0004-scaffold-monorepo.md)): each service is a runnable shell with `/healthz` + `/readyz` and a passing health test. The ML service additionally has its implemented `domain/` + `orchestration/` core (Phase 1, [decisions/0008](decisions/0008-domain-core-design.md)); the **backend build-out is underway** — architecture + scope locked in [decisions/0022](decisions/0022-backend-architecture-and-scope.md) (ports + adapters like the ML service, roll-our-own JWT, reads ML results from the shared DB; docs-first phases, reference in `services/backend/docs/`). **Phase 1 (foundations) has landed** ([decisions/0023](decisions/0023-backend-db-schema.md)): settings, the backend DB + its **own** Alembic chain (`schools`/`users`, migration `0001`, distinct version table `alembic_version_backend`), the ports/registry/container skeleton, structlog, and dep-probing `/readyz`. **Phase 2 (auth + RBAC) has landed** ([decisions/0024](decisions/0024-auth-jwt-and-rbac.md)): roll-our-own JWT (argon2 `PasswordHasher` + PyJWT `TokenService` ports, services stay crypto-free), the `Permission`/`ROLE_PERMISSIONS` RBAC behind one `PermissionResolver` seam, `get_current_user`/`require_permissions` deps, `/v1/auth/{login,refresh,change-password,me}`, migration `0002` (`users.must_change_password`) + case-insensitive email, and the platform-admin bootstrap CLI. **Phase 3 (onboarding + teacher limits) has landed** ([decisions/0025](decisions/0025-onboarding-and-teacher-limits.md)): the first protected routes — platform `POST/GET /v1/schools` + `POST /v1/schools/{id}/admins`, school-admin `POST/GET /v1/staff` — with tenant isolation deriving `school_id` from the token (never the URL), the `max_teachers` teacher cap in `OnboardingService`, `LimitExceededError`→409, `UserRepository.count/list_by_school_and_role`, a shared `UserResponse` (also for `/me`), and shared test doubles in `tests/backend_fakes.py` (no migration). **Phase 4 (students + ML enrollment) has landed** ([decisions/0026](decisions/0026-students-and-ml-enrollment.md)): staff-managed students where creating a student provisions a `students` profile (migration `0003`) **and** a linked `role=student` login (temp password); the reference photo is uploaded FE→Supabase via a backend-minted **signed upload URL** (30 MB FE-enforced; no bytes through the backend), then the backend fires **synchronous ML enrollment** and records `enrollment_status` (an ML outage never blocks account creation). Adds the `StudentRepository`/`ObjectStore` (supabase/local_fs)/`MlEnrollmentClient` (http/fake) ports + `UserRepository.delete`, `UpstreamError`→502, `POST /v1/students` (+ `/upload-url`, `/{id}/enroll`) + `GET`/`DELETE` behind `student:manage` (tenant from the token; two-write create uses compensating-delete, delete uses the `students.user_id` cascade), a shared `tenant_of`, and deps `httpx`+`supabase`. **Phase 5 (events + media + event-level processing) has landed** ([decisions/0027](decisions/0027-events-media-enqueue-status.md)) — and, per owner review, **spans both services with ML-written job status**: uploading a photo only **records** a backend `media` row (`pending`, migration `0004` adds `events`+`media`); a single **`POST /v1/events/{id}/process`** ("redistribute") enqueues **one event job `{school_id, event_id}`** (Redis `XADD`; the queue no longer carries per-photo jobs) and sets the event `processing_status=queued`. The **ML worker** consumes the event job and, via a new **`BackendEventStore`** (read-only-ish mirror `ml_service/db/backend_tables.py` over the shared DB), **reads the event's `media` roster AND writes the backend status columns directly** — event `processing` on pickup / `completed` when done, each photo `completed` as it finishes (skipping photos already `completed`), running the existing per-photo pipeline (which still writes `matches`+`media_detections`) on the rest. **The backend just reads its own `events`/`media` rows for job status — there is NO backend poller and no `media_detections`-presence signal for it** (Phase 6 adds `MlResultsReader`/`db/ml_read.py`, but for gallery *contents*, not job status). Backend ports `EventRepository`/`MediaRepository`/`EventJobProducer` (redis/inproc); ML gains `process_event`; routes behind `event:manage`/`media:upload`/`job:status:view`, tenant from the token; dep `redis>=5`. **This deliberately overrides earlier invariants with owner sign-off** — "no ML changes", "ML never reads the backend" (ML now reads *and writes* backend status columns), and the per-photo job contract ([decisions/0022](decisions/0022-backend-architecture-and-scope.md)) — and the two ML specs were updated to match. Deletes deferred (archive instead); result *contents* (galleries) are Phase 6 (below). **Phase 6 (galleries + download) has landed** ([decisions/0028](decisions/0028-galleries-and-download.md)): the distribution UX reads — the backend's first read of ML result *contents*, via one isolated coupling: `db/ml_read.py` (read-only `matches` Core table on its own `MetaData`, kept out of `Base.metadata` so backend Alembic never manages it) + a 3-method `MlResultsReader` port (`list_{event,student,media}_appearances`) returning pure `Appearance`s (join keys + `confidence`/`needs_review`). A `GalleryService` joins those "who-is-where" facts to the backend's OWN students/events/media rows (grouping in-Python) for the two views — event→students→photos + student→events→photos — plus media→appearances and the student `/me` self-scope; browse-all is the existing `GET /events/{id}/media`. Download mints a short-lived signed URL (`ObjectStore.create_signed_download_url`, `BE_DOWNLOAD_URL_TTL_S`) behind a `GalleryScope` dep — staff (`gallery:view_all`) any media in-school, a student (`gallery:view_own`) only media they appear in, else 404. Adds `StudentRepository.get_by_user_id` + `MediaRepository.list_by_ids`; routes `GET /v1/events/{id}/students`, `/v1/events/{id}/students/{sid}/media`, `/v1/students/{id}/events`, `/v1/students/{id}/media`, `/v1/media/{id}/appearances`, `/v1/media/{id}/download`, `/v1/me/{events,media}`; the perms were pre-seeded in Phase 2. **No migration, no ML change** (pure reads); the consumed `matches` columns get a Phase-7 `information_schema` contract test. **Phase 7 (hardening) has landed** ([decisions/0029](decisions/0029-hardening.md)) — pure hardening, still **no migration, no ML change**: backend Prometheus metrics at `GET /metrics` (`observability/metrics.py` + an `@app.middleware("http")` in `main.py`; labels are method / **route template** / status only — never a concrete `student_id`/`media_id` or the raw path, and unmatched requests collapse to one `__unmatched__` label; dep `prometheus-client`); conditional CORS via `BE_CORS_ORIGINS` (empty → no middleware); the Phase-6 promise redeemed — a `matches` contract test (`tests/adapters/test_ml_read_contract.py`): an always-on model-drift guard comparing `db/ml_read.py`'s `matches` to the ML service's authoritative `Match` ORM model, plus a gated `information_schema` live-schema check; and CI/compose parity — the backend layering grep added to CI (mirroring `scripts/check.ps1`), the backend image added to the `docker-build` job, a Postgres+Redis **`integration` job** that applies both Alembic chains and runs the gated DB/Redis tests, and the compose `backend` service gaining a `redis` dep + `BE_REDIS_URL`/`BE_QUEUE_STREAM`/`BE_CORS_ORIGINS`. Deferred (documented): OTel tracing, rate limiting, security headers. The backend is now v1 feature-complete + hardened; the **frontend build-out is underway** — Next.js 16 + React 19, architecture + design system locked in [decisions/0030](decisions/0030-frontend-architecture-and-design-system.md) (BFF + HttpOnly-cookie auth, Tailwind v4 `@theme` tokens synthesized from `frontend/design/*.DESIGN.md`, per-persona route groups, docs-first phases F0–F7). **F0 (foundations)** + **F1 (auth + app shell + primitive seed, [decisions/0031](decisions/0031-frontend-auth-and-shell.md))** have landed: the BFF auth handlers + `/api/v1` refresh-and-retry proxy + `proxy.ts` presence gate, login/change-password, the role-aware shell + `components/ui` seed; **F2 (platform admin — schools + admins, [decisions/0032](decisions/0032-frontend-platform-admin.md))** adds the `(platform)/schools` list/create + `[schoolId]` detail + add-admin, the first data `Table` + Radix `Dialog` + `Breadcrumb`, and an app-wide `SWRConfig` (its live smoke is pending — Docker was down); **F3 (staff + students + enrollment, [decisions/0033](decisions/0033-frontend-staff-and-students.md))** adds the `(school)` group's **staff** (Table + create-teacher `Dialog`, surfacing the **409** duplicate/**teacher-cap**) and **students** (Table w/ `StudentAvatar` + enrollment pill; create runs the **direct-to-Supabase** reference-photo upload → **synchronous ML enrollment**; `[studentId]` detail with re-enroll + delete), cashing in the one reserved **backend additive** — read-only `email` on the students read model (inner JOIN on `users`, **no migration**) — plus a `RoleGate` (staff is school-admin-only; teachers lack `staff:manage`), the `avatar`/`file-dropzone`/`progress-bar`/`confirm-dialog` primitives, and `lib/api/upload.ts` (mint-then-**XHR-PUT straight to Supabase**, memoized so a rejected-field resubmit doesn't re-upload; its live smoke — the Supabase PUT contract — is likewise pending Docker); **F4 (events + media upload + processing, [decisions/0034](decisions/0034-frontend-events-and-processing.md))** adds `(school)/events` list/create + `[eventId]` detail (edit `PATCH` / archive-restore) + `[eventId]/upload` **multi-file** uploader (a bounded-concurrency `useMediaUpload` pool; photos go **straight to Supabase** via mint→XHR-PUT→register) plus the **Process** action with **live status polling** (`useEventStatus` auto-stop `refreshInterval`; optimistic queued flip so a failed refetch can't strand the UI; the processing pill derived from counts; 400/502 as toasts) — **no backend change** (the Phase-5 contract was complete); **F5 (galleries + download, [decisions/0035](decisions/0035-frontend-galleries-and-download.md))** adds the distribution UX — `(school)/events/[eventId]/gallery` (Radix **Tabs**: All / By-student), a student **"Appears in"** section, and a deep-linkable `photos/[mediaId]` — via a lazy **masonry `PhotoGrid`** (`useInView` IntersectionObserver → per-tile signed-URL fetch through a shared **`SignedImage`** with 403 re-mint + fallback), a full-screen **`Lightbox`** (←/→/Esc, `aria-live` counter, deterministic focus, appearances), and per-tile **download** (`GET /media/{id}/download` → blob save), again **no backend change** (Phase-6 reads complete; new dep `@radix-ui/react-tabs`); **F6 (student self-view, [decisions/0036](decisions/0036-frontend-student-self-view.md))** adds the recipient's side — one consolidated **`(student)/me/events`** ("My Photos") page with an event `FilterChips` filter driving the reused **`PhotoGrid`** — with **appearances hidden for students** via a `showAppearances` prop (`PhotoGrid`→`Lightbox`) that gates the staff-only `/appearances` fetch *and* the panel (403 + privacy; defense-in-depth), download still entitlement-scoped, and `/me/*` reads reusing the F5 gallery schemas (no new types, **no backend change, no new dep**); **F7 (polish + hardening, [decisions/0037](decisions/0037-frontend-polish-and-hardening.md))** closes out the FE build-out — **`app/{error,not-found,global-error}.tsx`** (unified with `FullPageError` into one `FullPageMessage` with a real `<h1>`; the boundary uses `reset`, not the `unstable_`-prefixed `unstable_retry`), a **mobile nav drawer** (Radix Dialog; shared `NavList`/`UserFooter`; close-on-navigate + `matchMedia` resize-close), an **a11y sweep** (`FilterChips` → a roving-tabindex **radiogroup**; `role="alert"` on the 13 error `EmptyState`s + `role="status"` on `GridSkeleton`; full-page `<h1>`s), a **consistent-error-UX** audit (401→login, 403/409/502→toast, 404→in-page/router, render fault→`error.tsx`), the **Lightbox** mobile-panel scroll, the dead `ComingSoon` removed, and a **frontend README** — all **no backend change**. **The FE is now v1 feature-complete + hardened (F0–F7); live smoke across F2–F6 remains pending a running stack.** **The FE needs Node ≥ 20.9 (Next 16) — the shell defaults to too-old Node 18; use `nvm use 22`** (pinned in `frontend/.nvmrc`); its gate is `npm run lint` + `tsc --noEmit` + `next build`. **Post-v1, a product-improvement build track has begun** — the whole-product analysis lives in `product/00`–`03` (a durable knowledge base + the D/P/X lenses/targets + the phased **BP1–BP8** roadmap; owner-delegated prioritization, consent out of scope). **BP1 (Admin Command Center) has landed** ([decisions/0038](decisions/0038-product-build-BP1-admin-command-center.md)): a read-only **`GET /v1/dashboard`** (student/event/photo rollups + needs-attention signals) via grouped/EXISTS/filtered queries over the backend's own rows + the isolated `matches` seam (`MlResultsReader.count_needs_review`, only already-declared columns), composed by a pure `DashboardService`; new aggregate methods on the existing repos + a frozen `EventRollup`; a new **`dashboard:view`** perm (school_admin + teacher, tenant from the token); the "undistributed" alert counts only **active** events. FE: the placeholder `/dashboard` → a real command center (`StatCard` row + needs-attention alerts + first-run invitation) + **nav information-scent badges** via a shared `useDashboard` (SWR key `"dashboard"`, `enabled` gates off non-staff). **No migration, no ML change**; gated real-Postgres tests cover every new aggregate's SQL + tenant scoping. **BP2 (list data-richness & scale) has landed** ([decisions/0039](decisions/0039-product-build-BP2-list-richness.md)): the four admin lists gain **per-row counts + search/filter/sort** (still query-only, no migration/ML). A new isolated **`ListingService`** zips per-row counts to rows in-Python (no N+1) via new batched aggregates on the existing repos + the `matches` seam (`event_match_counts`/`student_appearance_counts`, only declared columns) + `EventMatchCounts`/`StudentAppearanceCounts`/`SchoolRollup`; list-variant schemas (`EventListItem`/`StudentListItem`/`SchoolWithRollupResponse`) keep write DTOs frozen; new **`GET /v1/schools/{id}/admins`** roster + additive `UserResponse.created_at`. FE adds a shared `SearchInput` + `useSort`/`SortableHead` (direction-aware, tie-stable) reusing the F5 `FilterChips`; students/events/schools/staff each get counts + search + (where apt) filter + the schools detail gains `StatCard` rollups + the admin roster. **BP3 (student receive experience) has landed** ([decisions/0040](decisions/0040-product-build-BP3-student-receive-experience.md)): the student `/me/events` gallery goes **Pinterest-grade**, **FE-only** (no backend/ML; one tiny new dep `client-zip`) — real **natural-aspect masonry** via a `variant` on the shared `PhotoGrid`/`PhotoTile` (`MasonryTile` for students with a restrained hover zoom + hover-download; `GridTile` unchanged for staff), a warm hero + a "new since last visit" cue, **download-all** as one client-zip (`useDownloadAll` — bounded concurrency, skip-failures, partial-result toast + `aria-live` progress), and a **global `prefers-reduced-motion` guard** in `globals.css` (closing a gap F7 claimed but never enforced). *(BP3's client-side `useNewSince` "new since" was **superseded by BP4's** authoritative server signal and removed.)* **BP4 (Distribution — the flagship) has landed** ([decisions/0041](decisions/0041-product-build-BP4-distribution.md)): real *delivery* — an authoritative, cross-device in-app **"new photos"** signal for students (**derived**: announced = `notified_at` set OR (`auto_notify` AND `completed_at`); unseen = no read OR `seen_at` < effective; composed in-Python, **never** a SQL join to the `matches` seam — no per-student rows, no backend worker, no ML change), plus staff **"Notify students"** + an **auto-announce** toggle + a **Notified·Seen roster**, and a **multi-channel notifier seam** (`NotificationChannel` port + `CompositeNotifier` over a `BE_NOTIFICATION_CHANNELS` comma-list — `log` now, email/whatsapp future drop-ins that compose together-or-one-at-a-time; best-effort, PII-free). **Migration `0005`** adds `events.auto_notify`/`notified_at` + `notification_reads`, and `set_processing` now keeps `completed_at` **set-forward** on redistribute (a deliberate 0027 tweak so an auto event doesn't un-announce mid-run). Honest limit (documented): **auto drives only the in-app signal** — outbound-on-auto needs a future worker. New `notification:send` perm; staff routes on `events.py`, student on `me.py`; FE student nav badge + `/me` banner (mark-seen on unmount) + staff `DistributionCard`. Migration verified up+down on real Postgres; **no new backend dep**. **BP5 (Trust & accuracy loop) has landed** ([decisions/0042](decisions/0042-product-build-BP5-trust-accuracy.md)): matching becomes a **correctable** loop via a **backend-owned `match_corrections` overlay** (verdict `confirmed`/`rejected`/`added`) keyed on the stable **`(media_id, student_id)`** — **no ML change, no cross-seam SQL join** (composed in-Python off the isolated `matches` seam). **Reject → hide + block download** (the security-sensitive *effective-appearance* gate: plain/confirmed/added → allow, rejected/never-present → 404, unit-tested truth table); students **self-serve "this isn't me"** (`POST /v1/me/media/{id}/not-me`, membership-checked); staff **confirm/reject/undo** + **report-a-miss** (add a missed student → they see + can download) + a **"Needs review (N)"** lane. Three pure overlay helpers (`effective_media_student_ids`/`effective_event_pairs`/`effective_student_pairs`, in `gallery_service.py`) are shared by `GalleryService` (all 6 reads + the download gate, batched — no N+1), the new `ReviewService`, **and** (revising BP4) `NotificationService` — so a rejected student is **never** notified/rostered and an `added` one **is**. Migration **`0006`** (`match_corrections`: `verdict` CHECK enum + `resolves_review` + `UNIQUE(media_id,student_id)` upsert, `corrected_by`→users SET NULL, no FK to ML `matches`); the BP1 dashboard "N to review" becomes **`count_needs_review(raw) − count_resolved()`** (clamped, per-pair so 1:1); the **BP2 list rollups stay raw ML** (documented divergence — galleries are the effective source of truth). New `match:review` perm (admin+teacher); staff `review.py` routes + student `me.py`; `MediaAppearanceResponse` gains `verdict` + nullable `confidence`. FE: a shared **`AppearanceEditor`** (one compact row per student — name + confidence + an inline **X** to remove; a dismissible **"Add students"** dropdown [Radix Popover, new dep `@radix-ui/react-popover`] with a searchable multi-select for report-a-miss) powers both the staff **photo detail** *and* the staff **gallery lightbox** (via `canManageAppearances` on `PhotoGrid`/`Lightbox` — so staff correct **any** photo's mapping inline, needs-review or not), the **event gallery** gains the review tab, and the **student `/me` lightbox** gets "This isn't me". **No ML change.** Migration verified up+down on a **throwaway** DB (never the dev DB); gated real-Postgres coverage for the corrections adapter; 2× review loop (R1 verified the entitlement gate has **no bypass**; R2 fixed coverage/a11y + the **F8 notification-overlay** gap with owner sign-off). **BP6 (Video end-to-end) has landed** ([decisions/0043](decisions/0043-product-build-BP6-video-end-to-end.md)): surfaces the already-built ML video pipeline in the UI, **FE-only** (**no backend/ML change, no migration, no new dep**). `SignedImage` gains a `kind`/`asPlayer` **video branch** — a full `<video controls>` in the lightbox + photo-detail, a muted `#t=0.1` first-frame **poster** + play badge in grid tiles — rendered off the **same signed URL** the `<img>` used (confirmed inline-served: `create_signed_url` sets **no `download` disposition/transform**, so Supabase serves it inline with range requests → it streams + seeks). `PhotoGrid` moves from `mediaIds: string[]` to **`items: GalleryItem[]` (`{id, mediaType}`)**, deriving index-aligned `mediaIds`/`mediaTypes` for the Lightbox; the **event uploader accepts video** (`mediaTypeOf` by MIME → registers the real `media_type`; `registerMedia`/`RegisterMediaRequest` already accepted `"video"`), while `uploadReferencePhoto` stays image-only. A video's appearances reuse the **BP5 overlay** unchanged. **Honest limits** (documented): video size bounded by the existing `BE_MAX_UPLOAD_MB` (30 MB) + Supabase bucket limit; no stored poster thumbnail (browser first-frame); lightbox arrows navigate over video seek. FE gate green (tsc + lint + build); 2× review loop (R1 traced inline-serve + `mediaTypes`/`mediaIds` alignment, reverted a layout-changing tile class; R2 media-aware a11y/copy). Deferred: the "who appears when" **timeline** (a new isolated `student_media_appearances` read + `GET /media/{id}/timeline`, corrections-overlaid). **BP7 (Onboarding & bulk) is underway, sliced into four approve-before-commit sub-phases** — **BP7a** setup checklist · **BP7b** reference-photo quality feedback (surface the already-returned-but-discarded ML enroll `detail`; 1-col migration, no ML change) · **BP7c** staff lifecycle + invite model (server-generated temp passwords shown-once + disable/enable [`users.status` already exists, no migration] + resend-invite) · **BP7d** CSV bulk student import (nullable `reference_photo_path` migration → name+email now / photo later, server-gen temp passwords, a bulk endpoint looping the reusable `create_student`). **BP7a (setup checklist) has landed** ([decisions/0044](decisions/0044-product-build-BP7a-setup-checklist.md)): a server-composed **`setup_checklist`** block (5 booleans) on `GET /v1/dashboard` + a guided **`SetupChecklistCard`** on the FE dashboard that supersedes the old `FirstRun` and retires once the school has distributed. **Query-only — no migration, no ML.** Only 2 signals are net-new: `has_staff` (the existing `UserRepository.count_by_school_and_role(TEACHER)`, wired via a new `users` dep on `DashboardService`) and `has_distributed` (a new `EventRepository.count_distributed` aggregate whose predicate mirrors BP4's event-level "announced" — `notified_at` set OR `auto_notify`+`completed_at` — status-agnostic, tenant-scoped); the other three (`has_enrolled_student`/`has_event`/`has_media`) compose off existing counts (`enrolled > 0`, not merely-added). FE: four **core** steps (enroll→event→upload→distribute) drive the "N of 4" progress + the primary CTA; "add a teacher" is **optional/last** (a solo school-admin never needs one). Gated real-Postgres coverage for `count_distributed`; 2× review loop (R1 no bugs; R2 fixed a D6 CTA-wording inconsistency, a D8 sub-AA contrast token on completed labels, and the primary CTA landing on the optional teacher step). **BP7b (reference-photo enrollment quality feedback) has landed** ([decisions/0045](decisions/0045-product-build-BP7b-enrollment-quality-feedback.md)): a failed enrollment now explains **why** instead of a bare "Failed". The ML enroll response already carried a per-photo `detail`/status that the backend parsed then **discarded**; `StudentService._run_enroll` now maps it to a closed **`EnrollmentFailureReason`** `{no_face, ml_unavailable, error}` (`multiple_faces` is **not** a failure — the ML enrolls the largest face), persists it (**migration `0007`**: nullable `students.enrollment_failure_reason` + CHECK, lockstep with the enum), **clears it on a successful (re-)enroll**, and exposes it on `StudentResponse` (inherited by `StudentListItem`). FE: a reason-specific title+fix **`EnrollmentFailureNote`** (`role="alert"`) on the student detail + a compact reason under the "Failed" pill on the list; graceful generic fallback when a `failed` row's reason is null. **No ML change, no new dep.** The cross-service `"no_face"` wire value is a pinned `_ML_STATUS_NO_FACE` constant + mapping tests. **Honest scope:** the fix action — replacing the reference photo **in place** — is **BP7d**; until then `no_face`/`error` copy steers to delete-and-re-add (Re-enroll retries the same photo, fixing only `ml_unavailable`). Migration verified up→down→up on a **throwaway** Postgres (`bp7b_migtest`, dropped; dev `app` DB untouched) + gated real-Postgres adapter round-trip; full backend suite **315 passed**; 2× review loop (R1 no bugs; R2 → the named constant, a `multiple_faces`→error contract pin, a route-level serialization assertion, sharper copy). **Also fixed (separate commit):** the backend ML-enroll HTTP timeout default 30s→60s (`BE_ML_HTTP_TIMEOUT_S`) — the ML service loads `buffalo_l` lazily on its first request, so a cold container's first synchronous enroll could exceed 30s and `ReadTimeout`. **BP7c (staff lifecycle + invite model) has landed** ([decisions/0046](decisions/0046-product-build-BP7c-staff-lifecycle-invite.md)): staff/admin provisioning becomes a real **invite model** + lifecycle — **NO migration** (`users.status` active/disabled already existed and auth already rejects a disabled account). Create-teacher/add-admin now **server-generate** the temp password (`secrets.token_urlsafe(12)`, CSPRNG) returned **exactly once** as **`ProvisionedUserResponse {user, temp_password}`** (`password` dropped from `CreateUserRequest`; the plaintext is never in list/get/me, never logged, only its hash persisted); **disable/enable** via `PATCH /v1/staff/{id}` + `PATCH /v1/schools/{sid}/admins/{id}` backed by a new `UserRepository.set_status` (real lockout — auth rejects at login, refresh, **and** the per-request `get_current_user` reload); **resend-invite** via `POST …/{id}/resend-invite` (regenerate + `must_change_password`, works on a disabled account too). One entitlement guard **`OnboardingService._require_managed_user`** — target must match the caller's school **and** the route's expected role, else **404 (not 403)** — structurally blocks cross-tenant, wrong-role, and self-action (the manager is always a different role than the managed). FE: a shared shown-once **`InviteResultDialog`** (Copy button + `copyToClipboard` fallback; plaintext only in component state) replaces the typed-password field, and each teacher/admin row gains **Resend invite** + **Enable/Disable** actions (per-row `aria-label`). **Scope calls (documented):** students stay caller-supplied → **BP7d** moves them to server-gen; the "N of M" teacher capacity is platform-side (BP2 school-detail rollup), the admin's own `/staff` shows a **count**; the last-active-admin lockout is **allowed** (platform-recoverable). Full backend suite **327 passed** + a gated real-Postgres `set_status` round-trip + a **disabled→login-401→re-enable→200** e2e; 2× review loop (R1 no bugs, security airtight; R2 → per-row aria-labels + resend-revalidate + 2 regression-guard tests). **BP7d (CSV bulk student import — the flagship) has landed** ([decisions/0047](decisions/0047-product-build-BP7d-csv-bulk-import.md)): a class imports from a **name+email CSV**. **Migration `0008`** makes `students.reference_photo_path` **nullable** so bulk students are created **photoless → `pending`** (no ML call); students join BP7c's **invite model** — `create_student` **server-generates** the temp password (a shared `services/credentials.generate_temp_password` now used by onboarding *and* students) with the photo **optional** → **`ProvisionedStudentResponse {student, temp_password}`** (shown once, hash-only). **`POST /v1/students/bulk`** (`student:manage`, tenant from token) loops the reusable two-write (`_provision_student`, compensating-delete) over **≤500** `{name,email}` rows **best-effort** — per-row `created`/`duplicate`/`invalid`(`domain.emails.validate_email`)/`error`, the batch never aborts, and **only `created` rows carry a `temp_password`+`student_id`**; `enroll_student` now **guards a photoless student** (400). FE: a dependency-free CSV parser (`lib/csv.ts` — quoted fields, `""` escapes, CRLF/LF, **BOM strip**, header detection), a 3-step **`BulkImportDialog`** (pick→preview→results) with a **"Download credentials"** CSV, and the single-create dialog drops the typed password (server-gen, shown once via the shared `InviteResultDialog`) while **keeping the reference photo required** (a photoless single student would be stuck until BP7d-2). **Owner scope: bulk-first** — the reference-photo **set/replace** (enrolls photoless/bulk students + closes BP7b's replace-a-bad-photo loop) is deferred to **BP7d-2**; there's also **no student password-resend** yet (lost credentials → delete/recreate), so the results dialog makes Download the emphasized action. **Migration `0008` verified up→down→up on a throwaway Postgres** (`bp7d_migtest`, dropped; dev `app` DB untouched) + a gated photoless round-trip; full backend suite **337 passed**; 2× review loop (R1 no blockers — best-effort loop/tenant/secret/CSV/migration all airtight; R2 → BOM strip, re-pick-same-file, SR phase announcements, Download-primary hierarchy, +2 regression tests). **BP7d-2 (reference-photo set/replace) has landed** ([decisions/0048](decisions/0048-product-build-BP7d2-reference-photo-replace.md)) — the deferred BP7d follow-up, **no migration** (column already nullable from `0008`), no ML change: a **`PUT /v1/students/{id}/reference-photo`** that sets/replaces a student's reference photo then (re-)enrolls — so a **photoless (bulk-imported) student can enroll** ("Add photo") and staff can **swap a bad photo to fix a failed enrollment** ("Replace photo", closing BP7b's loop). `StudentService.set_reference_photo` = tenant-scoped `get_student` (foreign→404 **before** the path guard) → the same `_require_tenant_photo_path` prefix guard → a new `StudentRepository.set_reference_photo` → `_run_enroll` (clears the failure reason on success) → `_reload` with a path-carrying fallback. FE: a `ReferencePhotoDialog` on the student detail (Add/Replace, upload-path memoized so a failed PUT doesn't re-upload), **Re-enroll hidden for a photoless student** (would 400), and the BP7b failure-note fix-copy now points at "Replace photo". Full backend suite **345 passed** + a gated real-Postgres `set_reference_photo` round-trip; 2× review loop (R1 no bugs — guard order/reason-semantics/adapter/FE-flow verified; R2 → stale-comment + fallback copy + the upload-path memo + a replace-on-enrolled test + a route-404 test). **BP7 (Onboarding & bulk) is now complete (a, b, c, d, d-2).** **BP8 (Ops & reliability) is underway, sliced into five approve-before-commit sub-phases** — **BP8a** failed-photo state + retry · **BP8b** access/download audit · **BP8c** rate limiting (+ security headers) · **BP8d** multi-replica enrollment (Redis-lock Option B, config-gated) · **BP8e** retention/erasure. **BP8a (failed-photo state + retry) has landed** ([decisions/0049](decisions/0049-product-build-BP8a-failed-photo-retry.md)): a photo the ML worker can't process used to look `pending` **forever** (silently skipped); now it's marked **`failed`** — visible + retryable. **Migration `0009`** widens `ck_media_processing_status` to add `'failed'` (the ML worker writes it, so it must apply **before** the worker deploys — documented); `InferenceService.process_event` marks a photo `failed` on a fetch/decode/other per-photo error via a new **`BackendEventStore.mark_media_failed`** (writes `failed`, no `completed_at`), while the systemic **`EmbeddingVersionMismatch` still aborts the whole event** (nack + alert) and marks **nothing** failed (the index is wrong, not the photo). Retry = **redistribute** — the worker's roster-skip stays `== completed` only (so a `failed` photo is re-attempted), and the backend's **`process_event` enqueue guard was widened** to refuse only when `pending + failed == 0` (else a `failed`-only event's "Retry failed" would 400). `EventStatusResponse` gains `failed` (+ `total`); the FE event detail shows `· N failed`, a warning note (retry-first copy, since a transient fetch blip is also marked failed), and a **"Retry failed"** button. `EventOutcome` splits `photos_skipped` (now only already-completed) from `photos_failed`. Cross-service (BE + ML + FE), **no ML-contract break**. Migration verified up→down→up on a throwaway Postgres (`bp8a_test`, dropped; dev `app` DB untouched) + a gated real-Postgres `mark_media_failed` round-trip; **BE 346 / ML 128 passed**; 2× review loop (**R1 caught a blocker** — the `process_event` guard hadn't been widened, so "Retry failed" 400'd — fixed + tested; R2 → softer transient-failure copy + a version-mismatch-marks-nothing-failed test + a generic-exception-path test). **BP8b (access/download audit) has landed** ([decisions/0050](decisions/0050-product-build-BP8b-download-audit.md)): every entitled photo download is now recorded in an append-only **`download_audit`** table (**migration `0010`**) — surfaced to school-admins two ways: a per-photo **download-history** panel (staff photo detail + lightbox) and a paginated school-wide **"Access log"** page. **Trust, not compliance.** Recording is a **separate action from the signed-URL mint** — the `GET /media/{id}/download` mint is used for **both viewing and downloading** (`SignedImage` renders every tile/lightbox/detail off that URL), so it records nothing; a dedicated **`POST /media/{id}/download`** → `GalleryService.record_download` (same entitlement gate as the mint) is fired by the FE **only on the actual save** (`recordDownload` in `useDownloadToDisk` + `useDownloadAll`, fire-and-forget so the audit never blocks a download) — so a mere **view is never logged as a download**. `actor_role`/`event_id` are denormalized and the row outlives a deleted actor/subject via FK **SET NULL** (`subject_student_id` set only for a student self-download; a blocked/404 download records nothing). A new **`audit:view`** perm is granted to **school_admin only** — the one deliberate one-line difference from the otherwise-identical teacher set, so adding teachers later is a single `ROLE_PERMISSIONS` edit. Reads go through a read-only **`AuditService`** (`GET /v1/media/{id}/download-log` + `GET /v1/audit/downloads`, tenant strictly from the token) composing actor email + event/student names in-Python; mirrors the BP5 `match_corrections` structure (`DownloadAuditEntry` VO + `DownloadAuditRepository` port + postgres adapter + registry/container + gated real-PG test). FE: a self-gated **`DownloadHistory`** panel (renders nothing + fires no `audit:view` fetch for a non-admin) + a `(school)/audit` **server-paginated** log page (nav + `RoleGate` school-admin-only) + a `formatDateTime` util. **No new env var, no ML change.** Migration `0010` verified up→down→up on a **throwaway** Postgres (`bp8b_migtest`, dropped; dev `app` DB untouched) + a gated real-Postgres round-trip (record → list/count → filters → pagination → **newest-first ordering** → tenant-safe); **BE 368 passed**; FE gate (tsc + lint + build) green; 2× review loop (**R1 no security/tenant bug** — added a `created_at DESC, id DESC` stable sort tiebreaker; **R2 no blocker** — **removed the misleading client-side filter chips** in favour of clean server pagination + `role="alert"` error note + `Query()` annotations); **plus an owner-caught fix**: the audit was initially hooked on the shared signed-URL mint, so a mere *view* logged a download — split into a separate `POST` download action (view mint records nothing). Honest limits (documented): `_compose` lists the full school roster per call (bounded, admin-only); the server-side event/student filters are wired end-to-end but reserved for a future drill-in (no filter UI in v1); only download *intent* (the signed-URL mint) is recorded. **BP8c (rate limiting + security headers) has landed** ([decisions/0051](decisions/0051-product-build-BP8c-rate-limiting-security-headers.md)) — pure hardening (**no migration, no ML change, on by default**) redeeming what 0029 deferred. A **`RateLimiter`** port + FastAPI middleware throttles per a **global** tier + a stricter **`/v1/auth/*`** tier + a per-**`school_id`** tier (verified from the JWT best-effort — a forged/unverified claim is never trusted; the backend can't see the client IP behind the Next BFF, so there is **no per-IP tier** — that belongs at the ingress). Pluggable store via `RATE_LIMITER_REGISTRY` — **`InMemoryRateLimiter`** (per-replica) default, **`RedisRateLimiter`** (`INCR`+`EXPIRE`, reuses `BE_REDIS_URL`) config-gated — both **fail-open** (a store outage returns `allowed=True`; a limiter fault must never take the API down). First tier exceeded → **`429` + `Retry-After`** + a cardinality-safe `backend_rate_limit_rejections_total{scope}` metric; `/healthz`/`/readyz`/`/metrics` are **exempt** (a throttled probe would flap the deploy); the redis client is closed on `lifespan` shutdown. The limiter is built **once per app in `create_app`** (not the process-global `lru_cache` container) so an in-memory limiter can't accumulate counts across the test suite. **Middleware order** (`create_app`, outer→inner): **security-headers → CORS → rate-limit → metrics → routes** — so the headers land on the 429 + every error, the 429 short-circuits **inside** metrics (its own rejection metric, no `__unmatched__` pollution), and CORS preflight `OPTIONS` is handled before the limiter. **Security headers on BOTH** the backend (`_install_security_headers`, `setdefault` so it never clobbers `WWW-Authenticate`/`Retry-After` — nosniff / `X-Frame-Options: DENY` / `Referrer-Policy: no-referrer` / a maximal `default-src 'none'` CSP / HSTS gated on `hsts_enabled`, default off) and the **frontend** (`next.config.ts` `headers()` on `/:path*` — the browser-facing set, since only the Next BFF talks to the browser: nosniff, frame-DENY, referrer, `Permissions-Policy`, `Cross-Origin-Opener-Policy`, HSTS, and a CSP with `frame-ancestors 'none'` + a Supabase-scoped `img`/`media`/`connect-src` + `'unsafe-inline'` script/style with `'unsafe-eval'` **dev-only**). New `BE_RATE_LIMIT_*` / `BE_SECURITY_HEADERS_ENABLED` / `BE_HSTS_*` env vars (+ a test-only `BE_TEST_REDIS_URL`) → all in `.env.example`. **Honest limits (documented):** fixed-window (2× burst at a window boundary; sliding-window is the scale-up); the **auth tier is a single global bucket** across all schools' `/v1/auth/*` incl. refresh (default **300/min** so real concurrent logins aren't locked out — per-IP belongs at the ingress); the FE CSP keeps `'unsafe-inline'` (a **nonce-strict CSP via `proxy.ts`** is the follow-up); in-memory = per-replica (redis is the cross-replica option). **BE 385 passed / 26 skipped** (+ a gated real-Redis round-trip + connect-failure fail-open + an always-on mid-op-`RedisError` fail-open; middleware global/auth/school tiers, probe exemption, malformed-`Authorization` no-500, fail-open-on-raise, rejection-metric; security headers on 200/error/429 + HSTS gate) + layering; FE gate (tsc + lint + build) green, CSP checked against the **installed** Next 16 docs; 2× review loop (**R1 no blocker** — ordering/verified-decode/off-by-one/fail-open/test-isolation all sound; **R2 no code blocker**) → applied the redis `aclose`-on-shutdown, the probe/metrics exemption, the auth default 60→300, the fixed-window-burst doc, `Cross-Origin-Opener-Policy`, and +4 tests. **BP8d (multi-replica enrollment — FAISS write lock, Option B / Redis) has landed** ([decisions/0052](decisions/0052-product-build-BP8d-multi-replica-enrollment.md)) — ML-service only (**no migration, no backend/FE change, no ML-contract change; config-gated, Option A default**), delivering the "Option B" [0011](decisions/0011-faiss-adapter-lifecycle.md)/architecture §7.4 promised. Enrollment serialized each school's FAISS index writes with an in-process `asyncio.Lock` — the fleet lock **only** under single-replica enrollment (a SPOF/bottleneck). Now the per-school write lock is a **pluggable `WriteLockProvider`** — an **adapter seam, not a domain port** (the FAISS index adapter is its only user, so `domain`/`orchestration` stay import-pure and the layering invariant holds): `InProcLockProvider` (Option A, default — the same asyncio.Lock, moved out of `IndexCache` into a container-memoized singleton so the registry stays process-wide/outlives LRU eviction) or `RedisLockProvider` (Option B, config-gated **`ML_FAISS_LOCK_IMPL=redis`**) — a per-school `redis.asyncio` token-checked `Lock` (`SET NX PX` + Lua compare-and-del release, key `faiss:lock:{school_id}`) reusing the **shared** Redis client the container already builds for the queue — so **enrollment can run multi-replica** without two replicas clobbering a school's index (reads were already cross-replica-safe via the `meta.version` reload). **Fail-LOUD** (the deliberate opposite of BP8c's fail-open rate limiter): a lock-backend outage (`RedisError`/`OSError`) or a `wait_s`-timeout (`acquire()` → `False`) **raises `LockAcquisitionError`** (→ ML 500 → the backend records `enrollment_status=failed`, retryable, never blocks account creation per [0026](decisions/0026-students-and-ml-enrollment.md)) — **never** a silent unlocked write, which would risk a lost enrollment (correctness > availability here). `_RedisLockCtx` wraps the redis lock so the error mapping + a best-effort release stay in the adapter; the FAISS critical section (`load → rebuild-in-thread → store.save[meta.json last] → cache.invalidate`, for both `upsert` + `delete`) is **unchanged** — the lock just wraps it. New `faiss_lock_impl`/`faiss_lock_lease_s`(60s)/`faiss_lock_wait_s`(30s) settings + a `FAISS_LOCK_REGISTRY` + a memoized `container.write_lock_provider()`; `ML_FAISS_LOCK_IMPL`/`_LEASE_S`/`_WAIT_S` in `.env.example` + a `docker-compose.yml` comment on scaling (`ML_FAISS_LOCK_IMPL=redis` + raise the `ml-service` replica count). **Honest limits (documented):** a write slower than `lease_s` auto-expires the lock under a live holder (the classic TTL-lock hazard — loudly logged `faiss_write_lock_lease_lost` with a "raise `ML_FAISS_LOCK_LEASE_S`" hint; `lease_s` must exceed the slowest index rebuild+upload — a monitorable operational invariant; lease auto-extension is a future refinement); `wait_s` + the enroll must stay under `BE_ML_HTTP_TIMEOUT_S` (60s); `redis.asyncio.Lock` keeps its release token in thread-local storage, so acquire+release must run on one thread (they do — both awaited on the event-loop thread, only the pure rebuild offloaded). **ML 137 passed / 10 skipped** (+ gated real-Redis mutual-exclusion / cross-school concurrency / wait-timeout fail-loud on a real Redis; + an **always-on** dead-port fail-loud + a `__aexit__` release-swallow [`LockNotOwnedError` + generic `RedisError`] + an **adapter-uses-the-injected-provider** spy + the container inproc/redis/`ConfigurationError` selectors) + layering + mypy; 2× review loop (**R1 no blocker, ship-ready** — the cardinal invariant [no `store.save` without the lock], fail-loud on both paths, singleton wiring, no re-entrancy, shared-redis lifecycle all verified; **R2 no code blocker**) → applied the loud lease-loss logging + thread-local comment + the container/adapter/release tests + pinned `faiss_lock_impl` in the container-test settings (pydantic-settings was reading `.env` for the unset field). **BP8e (complete student erasure) has landed** ([decisions/0053](decisions/0053-product-build-BP8e-student-erasure.md)) — the **final** slice of BP8 and the whole BP1–BP8 roadmap. "Delete a student" was incomplete: three things orphaned — the reference-photo **object in storage** (no `delete` on either service's storage port), the ML-owned **`matches`**, and the ML **detection-audit** candidate rows naming the student. BP8e closes all three so **delete means gone** (trust, not legal/consent — out of scope). **Cross-service (BE + ML); no migration, no new perm (`student:manage`), no new env var; no BE↔ML HTTP-contract change** (the existing ML DELETE just does more). Ownership-split: the **backend** adds `ObjectStore.delete` (supabase `remove([path])` idempotent / local_fs no-op) and `StudentService.delete_student` removes the reference-photo object **best-effort with a bounded 3× retry** (0.2s backoff) — a leaked object is logged `orphaned_reference_photo` and never blocks the erasure (owner's chosen behaviour), skipped for a photoless student; order is `get_student → ML delete → object delete → login delete (cascade)`. The **ML** adds `MatchRepository.delete_by_student` (`DELETE … WHERE school_id AND student_id`) + `DetectionRepository.delete_candidates_by_student` (`DELETE FROM face_detection_candidates WHERE student_id` — only the student-naming rows; the media-centric parents `media_detections`/`media_frames`/`face_detections` stay, shared across students), and `EnrollmentService.delete` purges them (constructor gains the two repos, injected in `container.enrollment_service()` — already built for inference). **`match_corrections` + `notification_reads` need NO code** — the two-level FK cascade (`users`→`students` CASCADE → `match_corrections.student_id`/`notification_reads.student_id` CASCADE) erases the student's rows automatically, and `download_audit` rows are anonymized (`subject_student_id`/`actor_user_id` SET NULL) — both **proven by a gated real-Postgres cascade test** (`test_student_erasure_cascades_and_anonymizes`; the in-memory fakes can't model a two-level cascade; a second student's data stays untouched). Verified: BE + ML ruff/mypy/unit (object-erase + 3×-retry-then-best-effort + photoless-skip; `EnrollmentService.delete` purges matches+detections via stub repos) + **gated real-Postgres** round-trips on **throwaway** DBs (the cascade/anonymize proof + `delete_by_student`/`delete_candidates_by_student` tenant-scoped, others survive) + layering; 2× review loop. Honest limit (documented): `download_audit` is anonymized not deleted (the audit outlives its subject, BP8b). Deferred (documented, `product/03`): **event hard-delete** (a real "delete event" purging its media rows + storage objects + matches/detections — same machinery) + **time-based retention** (an admin-triggered/scheduled purge). **BP8 is complete (a, b, c, d, e) → the entire BP1–BP8 product-improvement roadmap is complete.**

> The TEMP wiring demo (decisions/0006) has been **removed** in Phase 4
> ([decisions/0017](decisions/0017-docker-observability-ci.md)): the `demo.py`
> modules, `/temp/*` routes, `demo_events` table, FE `app/temp/` + `app/api/temp/`,
> and the TEMP deps/env are gone. FE/BE are clean shells again until real features
> land; the ML service now runs its real enrollment API + inference worker.

## Commands

Run Python commands from the repo root (uv workspace). `uv` fetches Python 3.12 itself.

```bash
uv sync --all-packages              # install all workspace deps (creates .venv)
uv run pytest                       # all Python tests
uv run pytest services/ml_service   # a single service's tests
uv run ruff check . && uv run mypy .
./scripts/check.ps1                 # local gate: ruff + mypy + layering grep + pytest
uv run uvicorn ml_service.api.main:app --reload          # ML API       :8000 (/metrics too)
uv run python -m ml_service.workers.inference_worker      # ML inference worker
uv run uvicorn backend.main:app --reload --port 8001     # backend     :8001
# Apply ML DB migrations (URL from env, never committed):
ML_DATABASE_URL=postgresql+asyncpg://... uv run alembic -c services/ml_service/alembic.ini upgrade head
# Apply backend DB migrations (separate chain, version table alembic_version_backend):
BE_DATABASE_URL=postgresql+asyncpg://... uv run alembic -c services/backend/alembic.ini upgrade head
# Bootstrap the first platform admin (args/getpass, never .env; needs a migrated DB):
BE_DATABASE_URL=postgresql+asyncpg://... uv run python -m backend.cli.bootstrap_admin --email ops@example.com
# Frontend needs Node >= 20.9 (Next 16); the shell defaults to too-old Node 18 — `nvm use 22` first.
cd frontend && npm install && npm run dev                # frontend    :3000
cd frontend && npm run lint && npx tsc --noEmit && npm run build   # FE gate (lint + types + build)
docker compose up --build           # all 3 images + Postgres + Redis (needs Docker running)
./scripts/up.ps1                    # helper: Postgres+Redis detached (stay up), apps in foreground; Ctrl+C stops only the apps
./scripts/sync-env.ps1              # add keys present in .env.example but missing from .env (prints what it added); -Check = dry run (up.ps1 runs this advisory-check)
```

Helper scripts live in `scripts/` (see `scripts/README.md`).

Notes for future instances:
- The Python Dockerfiles build from the **repo root** context (they need root `pyproject.toml` + `uv.lock`); only the frontend builds from `./frontend`.
- pytest uses `--import-mode=importlib` so same-named test files coexist across services — don't add `__init__.py` to `tests/` dirs.
- New Python deps go in the **service's** `pyproject.toml`; shared dev tools in the root `[dependency-groups] dev`. Re-run `uv sync --all-packages` after.

## Status: Phase 4 done (Docker + observability + CI + TEMP removal) — v1 feature-complete

The repo is git-initialized (`main`) with a secrets-safe `.gitignore`. The ML service is being built in reviewed phases. **Phase 1** delivered the pure `domain/` (models, the 9 ports, `apply_threshold_and_gap`, errors) + `orchestration/` (`EnrollmentService`, `InferenceService`). **Phase 2** delivered a real adapter for every port under `adapters/` — `SCRFDDetector` + `ArcFaceEmbedder` (InsightFace `buffalo_l`, separate modules), `FaissPerSchoolVectorIndex` (+ pluggable index store, LRU cache), `SupabaseMediaStore`/`LocalFsMediaStore`, `DecordFrameExtractor`/`OpenCvFrameExtractor`, `PostgresMatchRepository`/`PostgresThresholdProvider`/`PostgresReferencePhotoRepository`, and `RedisStreamsJobQueue`/`InProcJobQueue` — plus the ML metadata schema in `db/` with Alembic (`0001_initial`). **Phase 3 is complete** (see [decisions/0016](decisions/0016-wiring-api-worker.md)): the wiring composition root (`wiring/settings.py` full req §12 surface, `wiring/registry.py` name→class per port, `wiring/container.py` lazy-memoized adapter build + inject), the enrollment API (`api/routes/enrollment.py` `POST …/enroll` + `DELETE …`, `api/deps.py` container-backed, `api/routes/health.py` with dep-probing `/readyz`, central error mapping in `api/main.py`), and the inference worker (`workers/runner.py` consume/ack/nack + retry/DLQ, `workers/inference_worker.py` entrypoint). **Phase 4 is complete** (see [decisions/0017](decisions/0017-docker-observability-ci.md)): the Dockerfile builds one image runnable as API/worker/migrate with `buffalo_l` baked in at build time; `docker-compose.yml` adds an `ml-worker` service + a shared `faiss` volume (API writes / worker reads); `observability/{metrics,logging,tracing}.py` provide the req §13 Prometheus metrics (served at `GET /metrics`), structlog JSON logging, and opt-in OTel tracing (via `ML_OTEL_EXPORTER_OTLP_ENDPOINT`); CI (`.github/workflows/ci.yml`) + `scripts/check.ps1` run ruff/mypy/pytest + the layering grep, plus a `docker-build` job; and the TEMP wiring demo is fully removed. 106 tests pass (+5 gated on real Postgres/Redis/models); ruff/mypy/layering clean. Design docs with diagrams live in `services/ml_service/docs/` (`00`–`09`). **Not yet run on this host:** the Docker build + `buffalo_l` bake and the InsightFace runtime path (they execute in CI's `docker-build` job / on a Docker host). **Post-v1:** the inference worker now persists the full per-face **detection audit** — media-centric `media_detections`/`media_frames`/`face_detections`/`face_detection_candidates` (replace-by-media) + `matches.frames_matched` + the `student_media_appearances` view — via a 10th `DetectionRepository` port and Alembic `0002`; see [decisions/0021](decisions/0021-persist-per-frame-detections.md). The two ML-service specs remain the binding source of truth:

- `ml-service-requirements.md` — locked v1 requirements (the "what"). Source of truth for functional/non-functional requirements, locked decisions (§8), interface contracts (§9), and data contracts (§10).
- `ml-service-architecture.md` — v1 architecture (the "how"). Source of truth for module layout (§5), adapter choices + versions (§6), and the FAISS index lifecycle (§7).

When implementing, treat both docs as binding. The architecture doc's §5 module tree is the intended layout (relocated under `services/ml_service/src/ml_service/`); §6's adapter table fixes the initial library choices and versions. If a code change contradicts either doc, surface the conflict rather than silently diverging.

## What this service is

A multi-tenant face-recognition service for distributing event photos/videos to the students who appear in them. Two pipelines that share one embedding model version but are otherwise independent:

- **Enrollment** (synchronous HTTP, student-id-triggered): resolve a student's reference-photo URIs → fetch → detect → embed → upsert into a per-school vector index (see [decisions/0009](decisions/0009-enrollment-contract.md)).
- **Inference** (async, queue-driven workers): fetch media → (video) extract frames at fixed FPS → detect → embed → search the school's index → apply threshold/gap decision → dedupe → persist match records.

## Architecture: hexagonal (ports and adapters)

The design exists to satisfy NFR-1/NFR-2 (swap ML stack or storage by config alone). The whole structure depends on strict layering:

- `domain/` — pure, imports no third-party libs. Models, the 10 `Protocol` ports (req §9 + `ReferencePhotoRepository` (see [decisions/0009](decisions/0009-enrollment-contract.md)) + `DetectionRepository` (see [decisions/0021](decisions/0021-persist-per-frame-detections.md))), and the pure `apply_threshold_and_gap()` decision function.
- `orchestration/` — `EnrollmentService` / `InferenceService`, plus the shared `identify_in_frames` kernel (`identify.py` — the per-frame `face → person` loop used by both the worker and the dev test UI, [decisions/0020](decisions/0020-identify-all-faces-and-per-frame.md)); `InferenceService` also maps that timeline into the detection audit written via `DetectionRepository` ([decisions/0021](decisions/0021-persist-per-frame-detections.md)). Imports only `domain`.
- `adapters/` — one subpackage per port; the only place concrete libs (faiss, insightface, azure, redis, sqlalchemy) are imported.
- `api/`, `workers/`, `wiring/` — the only modules allowed to import adapters. `wiring/container.py` builds concrete adapters from config via a name→class registry and injects them into the services.

**Layering invariant (wire into CI):** no concrete ML/IO library may be imported in `domain/` or `orchestration/`. The doc's acceptance test:
```
grep -r "import faiss\|import cv2\|import insightface\|import boto3" ml_service/domain ml_service/orchestration
```
must return nothing. The API and worker are thin shells — both build a job context and call the same service code paths.

## Correctness invariants (do not break these)

These come straight from the specs and are the easy things to get subtly wrong:

- **Tenant isolation (NFR-3, FR-I4):** all matching is strictly within one `school_id`. Enforced at the `VectorIndex` interface. There is no cross-school search in v1.
- **Threshold resolution is once per job, not per face.** Resolve `Thresholds` into the job context, pass it as a value into the pure decision function. Per-school value with global-default fallback when null (req §6.1).
- **Reproducibility (NFR-4):** each match record persists `embedding_model_version`, `detector_model_version`, `threshold_used`, `gap_threshold_used` — the values actually used at decision time, not re-read at write time.
- **Two-layer idempotency (NFR-5):** in-memory worker dedupe keyed on `(student_id, media_id)` first; DB unique constraint on `(media_id, student_id)` as the second line of defence. `save_batch` is the only **`matches`** write path and uses `INSERT ... ON CONFLICT` where higher confidence wins. (The detection audit is a separate, independently-idempotent write path — replace-by-media — [decisions/0021](decisions/0021-persist-per-frame-detections.md).)
- **Decision logic (req §6.2):** top-K=2. ≥threshold filter; 0 → unknown (log only, no record, FR-I8); 1 → emit; 2 → emit top-1 alone if `(top1-top2) > gap`, else emit both with `needs_review=true`.
- **Identification is per-face (`face → person`), across every frame.** `identify_in_frames` (`orchestration/identify.py`) detects **every** face in **every** frame and decides each independently — a group photo names everyone; one frame can match several students. The worker persists the deduped best per `(student_id, media_id)` as `matches` (the two-layer idempotency above), but the kernel *also* returns the full per-frame/per-face timeline (`frames[]`, incl. each face's raw top-k candidates) — the dev test UI renders it for video (per timestamp, **not** globally deduped), and the worker **persists** it as a media-centric detection audit (`media_detections`/`media_frames`/`face_detections`/`face_detection_candidates`, replace-by-media) plus `matches.frames_matched` and the `student_media_appearances` view — see [decisions/0021](decisions/0021-persist-per-frame-detections.md).
- **Embedding convention:** 512-dim ArcFace, L2-normalized; cosine similarity via FAISS `IndexFlatIP`. Lock `EMBEDDING_DIM=512` / `SIMILARITY_METRIC="cosine"` in `domain/models.py`; every adapter must emit normalized vectors.
- **Detector and embedder stay in separate adapter modules** even though both ship in the `buffalo_l` bundle — sharing the import breaks NFR-1.
- **Enrollment is replace-not-append (FR-E3);** per-photo failures don't abort the request (FR-E4).

## FAISS lifecycle (the trickiest part — see architecture §7)

File-backed index per school in blob storage (`index.faiss` + `id_map.json` + `meta.json`). `meta.json.version` is the cache-invalidation key, bumped on every successful write and written **last** as the commit point. Each worker keeps an LRU cache of loaded indexes; the read path re-checks `meta.version` (cheap HEAD) and reloads on staleness. On read, validate `meta.embedding_model_version` matches the configured embedder and **fail loud** on mismatch — never search a stale-model index. v1 serializes writes via a single-replica enrollment deployment (Option A); per-school Redis lock (Option B) is the documented scale-up.

## Planned stack (architecture §6)

Python, hexagonal. FastAPI (CPU API pods) + GPU inference workers consuming Redis Streams. InsightFace (SCRFD detector + ArcFace embedder), faiss-cpu, **Supabase Storage** media store (default; `local_fs` for dev — diverges from architecture §6's Azure default, see [decisions/0010](decisions/0010-supabase-media-store.md)), decord for video frames (OpenCV fallback), Postgres via SQLAlchemy 2.x async (asyncpg), pydantic-settings for config. Observability: prometheus_client (`/metrics`, req §13 counters + latency histogram) + structlog (JSON) + opt-in OTel tracing at the service-call boundary — labels are `school_id` + model versions only, never `student_id`/`media_id` (cardinality bomb). See [decisions/0017](decisions/0017-docker-observability-ci.md) + docs/09.

**Platform note:** `insightface` and `decord` have no Windows/py312 wheels, so they are declared Linux-only (`; sys_platform == 'linux'`) — they run in the Docker image; local Windows dev uses the OpenCV extractor and import-gated tests. All other heavy deps install cross-platform. See [decisions/0014](decisions/0014-queue-and-platform-adapters.md).
