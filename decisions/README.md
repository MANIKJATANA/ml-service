# Decision Log

Every change and decision made in this repo gets recorded here as a dated entry, so the reasoning behind the code is never lost.

## How to use

- One file per decision: `NNNN-short-slug.md` (zero-padded sequence, e.g. `0001-use-faiss-per-school.md`).
- Keep entries short: context, the decision, and why. Note alternatives rejected when relevant.
- Add a one-line pointer to the index below.
- When a decision is superseded, don't delete it — add a new entry and mark the old one `Superseded by NNNN`.

## Index

- [0001 — Adopt decision log and working rules](0001-adopt-decision-log-and-working-rules.md) — how decisions are recorded and the repo's working conventions.
- [0002 — Initialize git repository](0002-initialize-git-repo.md) — `git init` on `main`, secrets-safe `.gitignore`, initial commit.
- [0003 — Monorepo structure: FE + BE + ML service, 3 images](0003-monorepo-structure.md) — Next.js + FastAPI BE + ML service; uv workspace; `services/` + `frontend/` layout.
- [0004 — Scaffold the monorepo (uv workspace + 3 images)](0004-scaffold-monorepo.md) — runnable shells, health endpoints, Dockerfiles, compose with Postgres + Redis.
- [0005 — Add scripts/ folder with a stack-up helper](0005-add-scripts-folder.md) — `scripts/up.ps1` wraps `docker compose up --build`.
- [0006 — Temporary end-to-end wiring demo](0006-temporary-wiring-demo.md) — **TEMP** FE→BE→ML (HTTP + Redis) with Postgres writes from both; to be removed.
- [0007 — All DB schema changes go through migrations](0007-db-migrations-in-migration-folder.md) — schema changes live in versioned migration files, never ad-hoc in application code.
- [0008 — Domain core design (Phase 1)](0008-domain-core-design.md) — pure frozen-dataclass models, async Protocol ports, the §9 signature divergences, and the no-fakes testing approach.
- [0009 — Enrollment contract & the ReferencePhotoRepository port](0009-enrollment-contract.md) — student-id-triggered enroll/refresh; ML resolves photo URIs from its own table and fetches via `MediaStore`; adds a 9th port.
- [0010 — Supabase Storage as the default MediaStore](0010-supabase-media-store.md) — Supabase Storage for media (event media + reference photos); diverges from architecture §6 (Azure); `local_fs` for dev.
- [0011 — FAISS per-school adapter lifecycle](0011-faiss-adapter-lifecycle.md) — `IndexFlatIP` per school, Option A in-process lock, pluggable index store (meta.json last), rebuild-on-write, over-fetch+collapse, fail-loud version check.
- [0012 — ML metadata schema + Alembic (async) migrations](0012-db-schema-and-alembic.md) — `matches`/`school_thresholds`/`student_reference_photos`; ML owns its schema; async Alembic reading `ML_DATABASE_URL`.
- [0013 — Carry detector landmarks inside FaceBox](0013-facebox-landmarks.md) — optional `FaceBox.landmarks` enables ArcFace `norm_crop` alignment without changing the `embed` port signature.
- [0014 — Redis Streams queue + in-proc queue; Linux-only heavy deps](0014-queue-and-platform-adapters.md) — default Redis Streams `JobQueue` (+ real in-proc), OpenCV video fallback, `insightface`/`decord` gated to Linux wheels.
- [0015 — Run DB migrations before the apps start](0015-run-migrations-before-apps.md) — one-shot `migrate` compose service (`alembic upgrade head`) gates the apps; `up.ps1` runs it between infra and apps.
- [0016 — Phase 3: wiring + API + worker](0016-wiring-api-worker.md) — settings/registry/container composition root; enrollment routes + central error mapping; the inference worker consume/ack/nack loop.
- [0017 — Phase 4: Docker, observability, CI, TEMP-demo removal](0017-docker-observability-ci.md) — one image/three roles + baked `buffalo_l`; `ml-worker` + shared FAISS volume; §13 metrics/structlog/OTel + `/metrics`; CI layering gate; TEMP demo removed.
- [0018 — Redis socket_timeout must exceed the XREADGROUP BLOCK window](0018-redis-socket-timeout-vs-block.md) — redis-py 8.x's 5s default `socket_timeout` collided with the queue's 5s `block_ms`, crashing the worker on idle polls; set `ML_REDIS_SOCKET_TIMEOUT_S=30`.
- [0019 — Dev-only browser test UI for enroll + identify](0019-dev-test-ui.md) — flag-gated `GET /test` page + `/v1/test/{enroll,check}` reusing the real container; adds a media-store `upload` helper; `ML_ENABLE_TEST_UI`. *(identify amended by 0020.)*
- [0020 — Identify every face (face → person); per-frame detail via a shared kernel](0020-identify-all-faces-and-per-frame.md) — `orchestration/identify.py` returns per-frame/per-face detail + a deduped people map; worker persistence unchanged; test UI names every face and reports video per timestamp; per-frame persistence designed-for (**implemented by [0021]**).
- [0021 — Persist the full per-face detection audit + a student view](0021-persist-per-frame-detections.md) — media-centric `media_detections`→`media_frames`→`face_detections`→`face_detection_candidates` (replace-by-media) + `matches.frames_matched` + the `student_media_appearances` view; adds the 10th `DetectionRepository` port; the kernel retains raw candidates; embeddings declined.
- [0022 — Backend architecture & scope (build-out begins)](0022-backend-architecture-and-scope.md) — ports + adapters like the ML service (registry + `BE_*_IMPL` selectors, `local_fs`/`inproc` dev adapters); roll-our-own JWT; backend reads ML results from the shared DB (job done = `media_detections` present, no ML changes); backend owns identity/PII (UUID-PK→opaque-ID string); two Alembic chains in one DB need distinct version tables; RBAC resolver seam (static now, per-school later); phased `0023`–`0029`.
- [0023 — Backend DB schema (Phase 1: foundations + schools/users)](0023-backend-db-schema.md) — own Alembic chain with a distinct version table (`alembic_version_backend`); `schools` (+ `max_teachers`) and `users` (nullable `school_id` = platform admin; role/tenant CHECKs); UUID PK in the DB, `str` id in the domain; `SchoolRepository`/`UserRepository` ports + Postgres adapters wired via registry/container.
- [0024 — Backend auth (roll-our-own JWT) + RBAC seam (Phase 2)](0024-auth-jwt-and-rbac.md) — argon2 `PasswordHasher` + PyJWT `TokenService` ports (services stay crypto-free); `Permission` enum + `ROLE_PERMISSIONS` behind one `PermissionResolver` seam (`StaticPermissionResolver` now, `DbPermissionResolver` later, zero call-site change); `get_current_user`/`require_permissions` deps; `/v1/auth/{login,refresh,change-password,me}`; migration `0002` adds `users.must_change_password`; `UserRepository` gains `set_password`; platform-admin bootstrap CLI (args/getpass, never `.env`).
- [0025 — Platform + school onboarding, teacher limits (Phase 3)](0025-onboarding-and-teacher-limits.md) — first protected routes: platform `POST/GET /v1/schools` + `POST /v1/schools/{id}/admins`, school-admin `POST/GET /v1/staff`; tenant isolation derives `school_id` from the token (never the URL) for school-scoped ops; `max_teachers` caps teachers only, enforced in `OnboardingService` (count-then-create, single-writer race accepted); provisioned accounts get a caller-set temp password + `must_change_password` (no SMTP); `LimitExceededError`→409; `UserRepository` gains `count/list_by_school_and_role`; shared `UserResponse` (also for `/me`); no migration; shared test doubles in `tests/backend_fakes.py`.
- [0026 — Students + ML enrollment (Phase 4)](0026-students-and-ml-enrollment.md) — staff-managed students: creating a student provisions a `students` profile (migration `0003`) **and** a linked `role=student` login (temp password); the reference photo is uploaded FE→Supabase via a backend-minted **signed upload URL** (30 MB FE-enforced, no bytes through the backend), then the backend triggers **synchronous ML enrollment** ([0009](0009-enrollment-contract.md)) and records `enrollment_status` (an ML outage never blocks account creation). New ports: `StudentRepository`, `ObjectStore` (supabase/local_fs), `MlEnrollmentClient` (http/fake) + `UserRepository.delete`; `UpstreamError`→502; routes `POST /v1/students/{upload-url,,{id}/enroll}` + `GET`/`DELETE` behind `student:manage`, tenant from the token; two-write create uses compensating-delete, delete uses the `students.user_id` cascade; shared `tenant_of`; deps `httpx`+`supabase`.
- [0028 — Galleries (two views + browse-all) + download (Phase 6)](0028-galleries-and-download.md) — the distribution UX reads: the backend's first read of ML result *contents*, isolated to `db/ml_read.py` (read-only `matches` Core table on its own `MetaData`, out of `Base.metadata`) + a 3-method `MlResultsReader` port returning pure `Appearance`s. A `GalleryService` joins those "who-is-where" facts to the backend's own students/events/media rows (grouping in-Python) for the two views (event→students→photos, student→events→photos), media→appearances, and the `/me` student self-scope. Download = short-lived signed URL (`ObjectStore.create_signed_download_url`, `BE_DOWNLOAD_URL_TTL_S`) gated by a `GalleryScope` dep (staff any / student own-only, else 404). Adds `StudentRepository.get_by_user_id` + `MediaRepository.list_by_ids`; reuses the seeded `gallery:view_all`/`gallery:view_own` perms; **no migration, no ML change** (pure reads); the consumed `matches` columns get a Phase-7 `information_schema` contract test.
- [0027 — Events, media upload, event-level processing + status (Phase 5)](0027-events-media-enqueue-status.md) — the async inference half, **event-level, ML-written status** (revised twice after owner review): uploading a photo only **records** a `media` row (`pending`, no enqueue); a single **`POST /v1/events/{id}/process`** ("redistribute") enqueues **one event job `{school_id, event_id}`** (the queue no longer carries per-photo jobs) and sets the **event** `processing_status=queued`. The **ML worker** consumes the event job, **reads the backend `events`/`media` from the shared DB AND writes their status columns** (new `ml_service/db/backend_tables.py` + `BackendEventStore`): event `processing` on pickup / `completed` when done, each photo `completed` as it finishes (skipping photos already `completed`). The backend just **reads its own row** — **no poller, no `MlResultsReader`/`ml_read`, no `media_detections`-presence signal**. Overrides three earlier invariants **with owner sign-off** (no-ML-changes, ML-never-reads-backend, per-photo job contract) plus ML now *writes* two backend status columns — done in **both services this phase**; the ML specs get a matching update. New tables (migration `0004`: `events` incl. `processing_status`, `media`); backend ports `EventRepository`/`MediaRepository`/`EventJobProducer` (redis/inproc); routes behind `event:manage`/`media:upload`/`job:status:view`, tenant from the token; dep `redis>=5`. Deletes deferred (archive); result *contents* (galleries) are Phase 6.
- [0029 — Hardening: HTTP metrics, CORS, the `matches` contract test, CI/compose parity (Phase 7)](0029-hardening.md) — pure hardening, **no migration / no ML change**: backend Prometheus at `GET /metrics` (`observability/metrics.py` + an HTTP middleware; labels are method / **route template** / status — never a concrete `student_id`/`media_id` or raw path, unmatched → one `__unmatched__` label; dep `prometheus-client`); conditional CORS via `BE_CORS_ORIGINS`; the Phase-6 promise — a `matches` contract test (`test_ml_read_contract.py`): an always-on model-drift check (`ml_read.matches` vs ML's `Match` model) + a gated `information_schema` live-schema check; and CI/compose parity (backend layering grep in CI, backend image in `docker-build`, a Postgres+Redis `integration` job that applies both Alembic chains and runs the gated tests, and the compose `backend` service's redis dep + `BE_REDIS_URL`/`BE_QUEUE_STREAM`/`BE_CORS_ORIGINS`). Deferred (documented): OTel tracing, rate limiting, security headers.
- [0030 — Frontend architecture + design system (Phase F0)](0030-frontend-architecture-and-design-system.md) — opens the FE build-out (docs-first): **BFF + HttpOnly-cookie** auth (Next route handlers proxy FastAPI, `proxy.ts` optimistic gate, transparent refresh), **hybrid rendering** (Server Components for first paint, Client + **SWR** for interactive/polled), a **typed client + SWR** data layer, **Tailwind v4 `@theme`** tokens synthesized from `frontend/design/*.DESIGN.md` (light canvas, single indigo `#6366f1` accent, Geist), **per-persona route groups** `(auth)/(platform)/(school)/(student)`, a Radix-based owned component kit, the **F0–F7 phase plan** (per-phase gate = lint+tsc+build + a mandatory **2× review→fix loop** + smoke, no test framework in v1), and one additive read-only field (`email` on `StudentResponse`). F0 ships docs + the Tailwind/PostCSS toolchain only — **no UI, no BFF, no components yet**.
- [0031 — Frontend auth + app shell + primitive seed (Phase F1)](0031-frontend-auth-and-shell.md) — the first real UI: **BFF auth handlers** (`app/api/auth/*` set/clear HttpOnly `access`/`refresh` cookies; login returns only `must_change_password`, never the JWTs) + a **transparent `/api/v1/[...path]` proxy** that attaches the Bearer and **refreshes-and-retries on 401** (clears the session on an unrecoverable 401, never on a 403); `proxy.ts` is an **optimistic presence gate only** (role/must-change routing lives in the client `AuthGuard` shell); **client-side data via SWR** (`useMe`; `server.ts` deferred since a Server Component can't refresh mid-render); the **`components/ui` seed** (Button/Input/Field/Card/Toast/StatusPill/Skeleton/EmptyState/Spinner/PageHeader/AppShell — Radix **Dialog deferred to F2**, custom `aria-live` toast); login + change-password + a role dashboard + navigable **`ComingSoon`** placeholders. Design/a11y calls: primary button + active nav use **`accent-hover` #4f46e5** (AA vs. #6366f1's 4.47:1); `BFF_BACKEND_ORIGIN` defaults to **127.0.0.1** (Node's `fetch` IPv6-`localhost` `ECONNREFUSED`). Deferred: mobile nav drawer (F2/F3). No migration, no backend change.
- [0032 — Frontend platform admin: schools + admins (Phase F2)](0032-frontend-platform-admin.md) — the first feature area: **`(platform)/schools`** (data `Table` + loading/empty/error) with a **Create-school `Dialog`**, and **`/schools/[schoolId]`** detail + **Add-administrator `Dialog`** (visible temp password; provisioned `must_change_password`) with a `Breadcrumb`. Brings the first **Radix `Dialog`** (deferred from F1) + composable **`Table`** + **`Breadcrumb`** primitives, an app-wide **`SWRConfig`** (no-retry / no-revalidate-on-focus — the per-screen Retry button is the single retry path), and a **`/api/v1/[...path]` 502 fix** (backend-unreachable → clean 502, matching the auth handlers). No admin-**list** endpoint exists → the detail page is add-only (toast confirmation). Design/a11y from the 2× review loop: `scope="col"` headers, **sans tabular** numerics (Stripe `tnum`, not mono; list/detail consistency), `accent-hover` links, integer-guarded `max_teachers` (`inputMode="numeric"`). **Live smoke pending** (Docker was down when it landed); no migration, no backend change.
- [0033 — Frontend staff + students + enrollment (Phase F3)](0033-frontend-staff-and-students.md) — the first **`(school)`-scoped** feature area: **staff** (Table + Create-teacher `Dialog`, surfacing the **409** duplicate/**teacher-cap**) and **students** (Table w/ `StudentAvatar` + enrollment pill; Create-student `Dialog` running the **direct-to-Supabase** reference-photo upload → **synchronous ML enrollment**; `[studentId]` detail with **Re-enroll** + **Delete**/`ConfirmDialog`). Cashes in the one reserved **backend additive** — read-only `email` on the students read model (inner JOIN on `users`, purely additive, **no migration**). Upload (`lib/api/upload.ts`) mints a signed URL via the BFF then **XHR PUTs bytes straight to Supabase** (never through Next; `timeout` + progress; **memoized** so a rejected-field resubmit doesn't re-upload). New: **`RoleGate`** (staff is school-admin-only — teachers lack `staff:manage`; guards a direct URL), primitives `avatar`/`file-dropzone`/`progress-bar`/`confirm-dialog` (`StudentAvatar` has a wired-but-unused `photoUrl` for a future thumbnail; the dropzone object-URL uses `useMemo`+cleanup, not setState-in-effect), and `lib/students/enrollment.ts` (shared tone/label). Error UX: re-enroll never 502s (ML-down → 200 `failed`), delete does. **Live smoke pending** (Docker down — the Supabase PUT contract is the one unverified path); the 2× review loop found **no blockers**.
- [0034 — Frontend events + media upload + processing (Phase F4)](0034-frontend-events-and-processing.md) — the async inference half from the UI, **no backend change** (the Phase-5 contract was complete): **`(school)/events`** list/create + **`[eventId]`** detail (Edit `PATCH` / Archive-Restore) + **`[eventId]/upload`** multi-file. Photos upload **straight to Supabase** (`uploadEventMedia` = mint → XHR-PUT → register) through a **bounded-concurrency (3) `useMediaUpload`** pool (per-file status/progress, failure-isolated, unmount-guarded). **Process** enqueues one event job and the detail page **live-polls** `GET /events/{id}/status` (`useEventStatus` function `refreshInterval`: 2.5 s while queued/processing, stops at completed/not_started) with an **optimistic** queued flip so a failed refetch can't strand the UI; the button shows only when active + not-in-flight + `pending>0` ("Redistribute" for leftovers; the three `400`s + `502` surface as toasts). The processing pill is **derived from counts** (never a stale green "Completed" over pending work). Status-pill tones retoned so colour is earned (neutral until `processing`=info / `completed`=success), the low-info lifecycle column dropped (inline "Archived" flag); new `Textarea` + `MultiFileDropzone` primitives, `buttonVariants` exported for link-buttons, an `aria-live` processing region. **Live smoke pending** (Docker down — the Supabase PUT + Process→ML-worker→status round-trip is unverified); 2× review loop, **no blockers**.
- [0035 — Frontend galleries + download (Phase F5)](0035-frontend-galleries-and-download.md) — the distribution UX, **no backend change** (the Phase-6 reads were complete): **`(school)/events/[eventId]/gallery`** (Radix **Tabs**: All photos / By student) + a student **"Appears in"** section + a deep-linkable **`photos/[mediaId]`**. A lazy **masonry `PhotoGrid`** (CSS columns, 8px gutter) whose tiles defer their signed-URL fetch via a fire-once `IntersectionObserver` (`useInView`) and render through a shared **`SignedImage`** (spinner → img → one-shot re-mint on a 403 → fallback; full-res, no thumbnail endpoint). A full-screen **`Lightbox`** (Radix Dialog, ←/→/Esc, `aria-live` counter, deterministic focus, download + appearances panel). Download mints a short-lived signed URL per tile (`GET /media/{id}/download`) → **`useDownloadToDisk`** (blob save + new-tab fallback); entitlement-gated server-side (staff any / student own / else 404). `AppearanceList` (name + confidence% + `needs_review` "Review" pill) + **`FilterChips`** (a **radiogroup** single-select, shared by by-student + appears-in). The `media_id`-vs-`id` split (gallery endpoints vs browse-all) is normalised to `mediaIds` at each call site. New dep `@radix-ui/react-tabs`. **Live smoke pending** (Docker down — the gallery joins + signed download + image render are unverified); 2× review loop, **no blockers**.
- [0036 — Frontend student self-view (Phase F6)](0036-frontend-student-self-view.md) — the recipient's side, **no backend change** (the `/me` reads reuse the F5 gallery schemas): one consolidated **`(student)/me/events`** ("My Photos") page — an event **filter** (`FilterChips`: All events + per-event, shown when >1) driving `useMyMedia` → the reused F5 **`PhotoGrid`** (the plan's separate `me/events` + `me/gallery` merged into this one filtered page). **Appearances are hidden for students** via a `showAppearances` prop threaded `PhotoGrid`→`Lightbox` (default true): it **gates the fetch** (`useMediaAppearances(null)`) *and* hides the panel — `GET /media/{id}/appearances` is `gallery:view_all` (staff-only 403) **and** other students' names must not leak (defense-in-depth over the backend RBAC). Reuse over a separate viewer (one boolean, staff call sites unchanged; the sparser student Lightbox — counter + close + download — is an accepted v1). Download stays entitlement-scoped (`GET /media/{id}/download`, own media only; graceful 404). New endpoints `myEvents`/`myMedia` + `use-my-gallery` hooks; role isolation via the `(student)` `AuthGuard` (`proxy.ts` unchanged). **Live smoke pending** (Docker down); 2× review loop **no blockers** (appearances-isolation + download-entitlement traced clean/defense-in-depth); the systemic loading/error `aria-live` gap + radiogroup arrow-roving deferred to the **F7 a11y sweep**.
- [0054 — Product review Round 2 (per-role, at scale) + the BP9–BP17 roadmap](0054-product-review-round-2-and-BP9-roadmap.md) — writes the **missing `product/02-product-review.md`** (`00`/`01` referenced it; Round 1 was built without it) as a **per-role** (platform admin · school admin · teacher · student · parent/guardian resolved → student account), **Greenfield-scale** (~800 students, ~120 events/yr, a ~900-photo student) UX/feature review — every finding citing a rubric lens + severity + **display-vs-capability** gap type, grounded in real file/lines. Nine cross-cutting **themes A–I**; the three **Critical** capability gaps are **A** the enrollment wall (a big school can't be turned on — CSV makes photoless students, faces are 1-by-1), **C** distribution reach (in-app only; no outbound email — delivery targets the student account), **B** no org structure (flat 800-row world). Opens a **BP9–BP17** track in its own file **`product/04-improvement-roadmap-round-2.md`** (the Round-2 sibling of `03`; owner-delegated build order **BP9** scale-ready lists/galleries + **BP17** thumbnails → **BP10** bulk photo enrollment → **BP11** class/term structure → **BP13** bulk/batch → **BP14** analytics — **then the three deprioritised phases at the back: BP12** distribution reach (email to the student account) + **BP15** accuracy (staleness + reconciliation; cohort-matching skipped) + **BP16** lifecycle), and reframes `00`'s "roadmap complete" as "complete (Round 1)". **Docs-only — no code, no migration, no service change;** each BP9+ phase is a separate approved slice.
- [0053 — Product Build BP8e: Complete student erasure](0053-product-build-BP8e-student-erasure.md) — the **final** slice of **BP8** and the whole **BP1–BP8** roadmap. "Delete a student" was incomplete — three things orphaned: the reference-photo **object in storage** (no `delete` on either storage port), the ML-owned **`matches`**, and the ML **detection-audit** candidate rows naming them. BP8e closes all three so **delete means gone** (trust, not compliance). Ownership-split, **no migration/perm/env-var**: the **backend** adds `ObjectStore.delete` (supabase remove / local_fs no-op) and `delete_student` removes the reference-photo object **best-effort with a bounded 3× retry** (a leaked object is logged `orphaned_reference_photo`, never blocks the erasure; skipped for a photoless student); the **ML** adds `MatchRepository.delete_by_student` + `DetectionRepository.delete_candidates_by_student` and `EnrollmentService.delete` purges them (the media-centric detection parents stay — shared across students). `match_corrections` + `notification_reads` need **no code** — the two-level FK cascade (`users`→`students`→…) erases them, and `download_audit` rows are anonymized (subject/actor `SET NULL`) — both **proven by a gated real-Postgres cascade test** (the fakes can't model a two-level cascade). No BE↔ML HTTP-contract change (the existing ML DELETE just does more). Verified: BE + ML ruff/mypy/unit + gated real-Postgres round-trips (cascade/anonymize + matches/detections delete-by-student, on throwaway DBs) + layering; 2× review loop. Deferred (documented): **event hard-delete** + **time-based retention** (same machinery, a future track). **BP8 complete → the BP1–BP8 product-improvement roadmap is complete.**
- [0055 — Product Build BP9: Scale-ready lists & galleries](0055-product-build-BP9-scale-ready-lists-galleries.md) — the first **Round-2** build ([0054](0054-product-review-round-2-and-BP9-roadmap.md), theme D): the app now stays bounded at real-school scale (~800 students / ~120 events / ~900-photo student). **One phase** (owner call — one doc/gate/review; the breaking response-model change ships **with** the FE). **Backend:** server pagination on the **6 unbounded list endpoints** (students/events/staff/schools/admins/event-media), replicating the BP8b audit template — a generic `Page[T]` + `{items,total,limit,offset}` envelopes, a shared `api/pagination.py` (`limit`/`offset`/`q`/`sort`/`dir` + status filter; out-of-range or unknown-sort → **422** via the endpoint's `*Sort` enum). **Global count-sort via a bounded id-scan** (fetch all matching ids → sort in-Python off the school-wide grouped aggregate BP2 already runs → hydrate one page) so sorting by photo/match counts spans the whole list **without ever SQL-joining the isolated ML `matches` seam** ([0028](0028-galleries-and-download.md)/BP4/BP5 intact) — vs. the rejected seam-relaxation. **De-rostering:** `GalleryService` (`event_students`/`student_events`/`media_appearances`) + `AuditService._compose` fetch **only matched ids** via new `Student`/`Event`.`list_by_ids` instead of `list_by_school`+in-Python filter. **Migration `0011`** adds the composite indexes for each default sort + name search; new `BE_DEFAULT_PAGE_SIZE`/`BE_MAX_PAGE_SIZE`. **Frontend:** a generic `useInfiniteList` (`useSWRInfinite`, resets to page 1 on filter change) + a shared `<LoadMore>` (sentinel + always-present "Load more" fallback + "Showing N of M") back **uniform infinite-scroll** on all five list pages (server `q`/`sort`/`dir`/`status`, debounced search; chip counts from the dashboard rollup); `PhotoGrid` **windows** its tiles (never mounts 900) and page-loads the event gallery; **download-all streams to disk** via the File System Access API (bounded memory, survives 900 photos) with a capped buffered fallback. **Honest limits (documented):** offset paging (keyset = scale-up), the student `/me` gallery still fetches its media-id list whole (windowed render), the fallback caps non-streaming browsers at 500. Verified: BE ruff+mypy+**403 unit**+**22 gated real-Postgres** (ILIKE-escape + tenant scope, throwaway `bp9_migtest` DB dropped; dev `app` untouched)+layering; migration `0011` up→down→up on the throwaway DB; FE tsc+lint+`next build` green; 2× review loop. **No ML change.**
- [0056 — Product Build BP17: Image thumbnails](0056-product-build-BP17-image-thumbnails.md) — the fast-UI companion to BP9 ([0054](0054-product-review-round-2-and-BP9-roadmap.md)): gallery/masonry tiles + the student-list/detail **avatar** now load a **small low-res image**; the lightbox + download stay **full-res**. Mechanism (owner-decided after two superseded drafts): **the backend generates the thumbnail**. *(Draft 1 used on-the-fly Supabase transforms — Pro-plan only, reverted. Draft 2 used browser "save-twice" to a `{uuid}.thumb` key — the `.thumb`-suffixed upload **fails** at Supabase, so no thumbnail was stored and the list fell back to full-res.)* The FE uploads only the original; on register/create the backend **downloads it, compresses it, uploads a `thumb-{name}.jpg` sibling**, and stores the path — deterministic thumbnails on any plan, browser-independent. **No ML change, no migration** (columns exist from `0012`); one new dep (Pillow). **Pillow behind a `Thumbnailer` port** (`adapters/imaging/pillow_thumbnailer.py`; `domain`/`services` stay image-lib-free — the layering test bans `PIL`); `ObjectStore` grows `download_bytes`/`upload_bytes`. A pure `services/thumbnails.py` (`thumb_key` + best-effort `generate_thumbnail`) is called by `MediaService.register_media` + `StudentService.create_student`/`set_reference_photo` (image → generate + store; **video / a failed compress / a store outage → null**, best-effort — never fails the upload); the replace still best-effort-deletes the old original+thumbnail (BP8e). The mint reverts to a **single** target. Serve: `GalleryService.download_url(thumbnail=)` behind `GET /media/{id}/download?size=thumb|full` (default full) + `StudentService.reference_photo_url` behind `GET /v1/students/{id}/reference-photo?size=` (`student:manage`, tenant from token; 404 photoless/foreign) select `thumbnail_path if (thumb AND present) else storage_path` — **falls back to full-res for pre-BP17 rows + video**; `MediaVariant` StrEnum → free 422 on a bad `size`; the entitlement gate runs before the size selection. FE: single mint→PUT→register (`image-compress.ts` removed); read models expose the thumb's presence (`StudentResponse.reference_photo_thumbnail_path`, `MediaResponse.thumbnail_path`, `GalleryMediaResponse.has_thumbnail`) and the FE requests `?size=thumb` **only when one exists** (avatar `useStudentReferencePhoto` size; gallery tiles via `hasThumbnail` on the shared `GalleryItem`/`PhotoTile`), else full; lightbox/download stay full; the create/replace dialogs memoize the uploaded path. New env `BE_THUMBNAILER_IMPL`/`BE_IMAGE_THUMBNAIL_MAX_EDGE`/`_QUALITY`. Honest limits (documented): a backend download→compress→upload per image (throttled by the FE pool; sync so the thumb is immediate — async is the scale-up); no backfill; video keeps its browser poster; local-fs dev gets no thumbnails. Verified: BE ruff+mypy+**435 passed / 30 skipped** + layering (`PIL` banned in domain/services): generate a `thumb-*.jpg` under the prefix on image register/create, video/fail/outage→null, the stored-path serve + full-res fallback on both surfaces (service + route), gate fires regardless of size, replace regenerates + deletes the old objects; FE tsc+lint+`next build` green; **no migration, no ML change**; 2× review loop.
- [0057 — Product Build BP10: Bulk photo enrollment](0057-product-build-BP10-bulk-enrollment.md) — the Round-2 "switch-on" unblocker ([0054](0054-product-review-round-2-and-BP9-roadmap.md), theme A): a class of faces enrolls from **one upload** instead of 800 one-at-a-time. **One read endpoint + one cleanup endpoint; no migration / no ML change / no new dep / no new perm** (reuses `student:manage`). `POST /v1/students/match-photos` maps photo **filenames** → students (auto-fills an editable FE table; the FE sends only filenames, so the ~800-email roster never hits the browser), matched by **email (case-insensitive)** or a **UUID stem**, tenant-scoped (`StudentRepository.resolve_by_emails` + the existing `list_by_ids`; a foreign email → unmatched, no cross-tenant enumeration). The upload-and-enroll loop runs on the **frontend** — a bounded-concurrency pool over the already-tested per-student `PUT …/reference-photo` (set + BP17 thumbnail + old-object cleanup + ML enroll), the same pattern as the event multi-uploader — so there is **no new enroll/upload backend surface**. **Upload-after-confirm + only-assigned = no orphans by construction** (unmatched/skipped photos are never uploaded); the rare uploaded-but-attach-failed object is best-effort reaped via a tenant-prefix-guarded `DELETE /v1/students/reference-photo-upload` (a foreign path → 400; the delete swallows a store blip). Batch capped at a configurable **`BE_BULK_PHOTO_MAX_FILES`** (default 50, schema-enforced → 422; FE mirror `NEXT_PUBLIC_BULK_PHOTO_MAX_FILES`). **"Retry failed (N)"** loops the existing `POST …/enroll` over the `failed` set (a photoless `pending` student uses bulk photo upload instead) — no new endpoint. FE: a 3-step **`BulkPhotoDialog`** (pick → editable **map & fix** → results) that holds the `File` objects until confirm, a single-select searchable **`StudentPicker`** (reuses BP5's `useStudents({q})` popover), and the pooled **`useBulkPhotoEnroll`** (orphan cleanup on attach-failure). Honest limits (documented): 50/batch (a 500-class ≈ 10 batches; a background job is the scale-up), client-side loop, `no_face` won't self-heal on retry, the picker is Tab-navigable without arrow-roving (mirrors BP5), local-fs dev has no real bytes. Verified: BE ruff+mypy+pytest + layering + a gated real-Postgres `resolve_by_emails` round-trip (throwaway `bp10_migtest`, dropped; dev `app` untouched); FE tsc+lint+`next build` green; 2× review loop (R1 no blocker — tenant/route-ordering/matching/async all sound → fixed the stale retry-count refresh; R2 → best-effort cleanup + per-row aria-labels + one-shot live regions + UUID-via-route/same-student-twice/store-failure coverage).
- [0058 — Product Build BP11a: Student classes](0058-product-build-BP11a-student-classes.md) — the first slice of **BP11 (Organizing structure)** ([0054](0054-product-review-round-2-and-BP9-roadmap.md), theme B — the flat 800-row world), sliced a/b/c per the owner-approved plan (an HTML explainer + a decisions Q&A): **a** = student classes (this), **b** = event term/category + calendar, **c** = teacher delegation (staged — a "focus" filter by default, an opt-in "restrict" switch); cohort-scoped *matching* stays deferred to BP15. A tenant-owned **class** (`student_groups`) a student belongs to at most one of (a nullable `students.student_group_id` FK, **ON DELETE SET NULL** — deleting a class un-assigns, never deletes, its students — **migration `0013`**), a **`StudentGroupRepository`** + a pure **`ClassService`** (CRUD + single/bulk assignment, tenant-scoped, a foreign class/student → 404), the students slice gaining a `student_group_id` **list filter** + a class-name **LEFT JOIN** (denormalized like `email`), and a new **`class:manage`** perm (school_admin only, like `audit:view`) for lifecycle — reads + assignment ride on `student:manage` (both roles). Routes `GET/POST /v1/classes`, `GET/PATCH/DELETE /v1/classes/{id}`, `POST …/{id}/members` (bulk), `PATCH /v1/students/{id}` (set/clear class; required-but-nullable → an empty body 422s, never a silent un-assign) + the `?student_group_id=` filter. FE: a **Classes** page (`RoleGate` admin — list + create/rename/delete) + a `[classId]` roster (an **inline** searchable Add-students dialog, not a portaled popover — sidesteps the BP10 modal-scroll trap) + a class `<select>` filter (**derived** so a deleted class can't strand the list) + a per-row badge + a detail selector; `useClasses` keyed `"classes"`. **No ML change, no new backend dep, no new env var.** Migration `0013` up→down→up on a throwaway Postgres (`bp11a_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**484 passed / 33 skipped** + layering (+ `test_bp11a_classes.py` 27 + 2 gated real-Postgres round-trips: the CRUD/counts/LEFT-JOIN/filter/**SET NULL cascade** + the bulk tenant-scope); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers**: tenant isolation / the `false()` filter branch / the 3-tuple LEFT-JOIN mapping / the permission split / SET NULL cascade / async all verified; **R2** → a stuck-class-filter dead-end fixed by deriving the active filter, a stale edit-dialog prefill fixed by a remount `key`, a simplified name guard, +4 tests, a11y). **BP11c remains.**
- [0059 — Product Build BP11b: Event term/category + month calendar](0059-product-build-BP11b-event-categories-calendar.md) — the second slice of **BP11**: events gain a free-text **term** + a **category**, plus a read-only **month calendar** (List⇄Calendar tabs). Per an owner change during the HTML review, **categories are per-school configurable** (a tenant `event_categories` table like BP11a's `student_groups`) — seeded with 6 defaults on school-create AND into every existing school in **migration `0014`**'s `INSERT…SELECT` data step; a pure `EventCategoryService` (list / add [case-insensitive dedupe → 409] / remove) on **`event:manage`** (admins + staff — no new perm). Events reference one via nullable `events.category_id` (**ON DELETE SET NULL** — un-tags its events, never deletes them) + a nullable `events.term`; the category name is denormalized via a LEFT JOIN (like `student_group_name`). The events list gains **category / term / date-range** filters (threaded through both the row-native and count-sort id-scan paths) + `GET /v1/events/terms` (before `/{event_id}`); create/update thread category+term (a foreign category → 404; term/category can't be cleared via PATCH, per 0027). FE: a from-scratch month calendar whose one real risk — the **timezone off-by-one** (`new Date("2026-07-04")` = UTC-midnight → the wrong day west of UTC) — is contained in one pure `lib/events/calendar.ts` (`parseLocalDate`/`toISODate`/`buildMonthGrid`; never `new Date(iso)`/`toISOString`); a `categoryColor` hash-palette (red excluded), a **Manage categories** dialog (confirm-then-remove), shared category/term `<select>` filters (BP11a "derived, not effect-reconciled" stale guard) + a category badge, and the create/edit category+term fields. **No ML change, no new dependency, no new permission, no new env var.** Migration `0014` up→down→up **+ seed** on a throwaway Postgres (`bp11b_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**510 passed / 34 skipped** + layering (+ `test_bp11b_calendar.py` 26 + a gated real-Postgres round-trip: CRUD / seed / LEFT-JOIN name / all filters / **SET NULL cascade**); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers**: the timezone module / tenant isolation / the seed / the LEFT-JOIN·filter·count-sort paths / SET NULL / route ordering / can't-clear / async all verified; **R2** → confirm-before-category-delete, a month-truncation note, calendar gridcell aria-labels, grid-built-once, the edit can't-clear UX, dropped the red tint, +4 tests). **BP11c (teacher delegation) remains.**
- [0060 — Product Build BP11c: Teacher delegation](0060-product-build-BP11c-teacher-delegation.md) — the **last** slice of **BP11**: a school admin **assigns teachers to classes** and a teacher's list views **focus** on those classes. Owner call: **focus-only** (a soft "My classes / All" default) — **no hard restrict**, so delegation is convenience-only and adds **no new security boundary** (a teacher could already see the whole school). **Migration `0015`** adds a `teacher_classes` N:M join (both FKs **CASCADE**, `UNIQUE(teacher, class)`) + `events.student_group_id` (nullable FK **SET NULL** — the event↔class link BP11b deferred here, mirroring `category_id`). A `TeacherClassRepository` + a pure `DelegationService` (assign/remove/list from the class side, list/set from the staff side, + the caller's own `my_group_ids`) — every write validates the class **and** target-is-an-in-school-teacher first (foreign → **404**, `assign` silently skips a foreign/non-teacher id). The **focus scope** is one `scope_group_ids` clause threaded through both the row-native and count-sort paths: students `IN scope` (an un-classed student is no teacher's), events `IN scope **OR IS NULL**` (their classes' events **plus** untagged/school-wide); an admin's `mine=true` is **ignored**. Reuses **`class:manage`** (admin-only) — **no new permission**. Routes: `GET/POST /v1/classes/{id}/teachers`, `DELETE …/{tid}`, `GET/PUT /v1/staff/{id}/classes`, `GET /v1/classes/mine` (before `/{group_id}`), + `mine`/`student_group_id` on the students/events lists; create/update thread `student_group_id` (foreign → 404, can't be cleared via PATCH). FE: a shared **`FocusToggle`** (shown only for a teacher with ≥1 class; default on; `mine` truthy-only so "All" sends nothing), a class-detail **Teachers** section (inline assign/remove), a staff **Classes** column + **Edit classes** checkbox dialog (cross-invalidates the class-detail roster), and an optional event class `<select>` + filter + badge (calendar threads it too). **No ML change, no new backend dependency, no new env var, no new permission.** Migration `0015` up→down→up on a throwaway Postgres (`bp11c_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**535 passed / 35 skipped** + layering (+ `test_bp11c_delegation.py` 25 + a gated real-Postgres round-trip: the link both-directions/idempotent/remove/replace + the event LEFT-JOIN name + the class filter + the focus scope [`IN` OR untagged + empty] + **both cascades**); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers** both BE + FE: tenant isolation, the focus scope [no escalation], route ordering, the migration, `.in_([])`-free, async/atomicity all verified; **R2** → cross-invalidated the `class-teachers`↔`staff-classes` SWR caches + a count-sort-path focus test). **BP11 (organizing structure) is now complete (a, b, c).**
- [0061 — Product Build BP13: Bulk actions & batch review](0061-product-build-BP13-bulk-actions-batch-review.md) — the Round-2 "do it to many at once" phase (theme E): everything was one-at-a-time. **No migration, no ML change, no new permission** — it reuses BP5's `match_corrections` overlay + BP9's `useDownloadAll` + `events.status`. Owner calls: **all three** surfaces; **no auto-confirm** (manual multi-select, the lane confidence-sorted); a one-click **"Reject all remaining"** guarded by a confirm dialog. **Batch review:** `POST /v1/events/{id}/review/batch` → `ReviewService.set_verdicts_batch` applies many confirm/reject verdicts at once — each is exactly the single BP5 `set_verdict` write (same hide-from-student gate, undo, `resolves_review`/dashboard-count), **tenant-safe by construction** (pairs validated against the event's ML appearances fetched once; a pair outside that event+school map is silently skipped — a crafted `media_id` can never write cross-event/tenant; `_require_event` 404s first). **Bulk event status:** `POST /v1/events/bulk-status` → `EventRepository.set_status_bulk` (one tenant-scoped `UPDATE … WHERE school_id AND id IN`, foreign id skipped) archives/restores many. FE: the event gallery Needs-review tab → a confidence-sorted checklist (Confirm/Reject selected + guarded Reject-all-remaining, `globalMutate("dashboard")` so the badge drops); the events list gains a checkbox column + a bulk Archive/Restore/Clear bar (selection **derived** from loaded rows so a filter change can't act on a hidden id); `PhotoGrid` gains a **staff-grid `selectionMode`** (a tile click selects instead of opening the lightbox; masonry/student surface untouched) + a **Select → Download N** bar reusing BP9's streaming `useDownloadAll` (cancel = no false success). New batch caps (2000 verdicts / 500 events → 422); `added` verdict → 422. **No migration, no ML change, no new dependency, no new permission, no new env var.** BE ruff+mypy+**544 passed / 36 skipped** + layering (+ `test_bp13_bulk.py`: batch applies-many/skips-non-event-pair/stamps-resolves/foreign-404/reject-hides-from-student/count_resolved-feeds-dashboard/staff-only/422s; bulk-status tenant-scoped/archive+restore/event:manage/422 + a gated real-Postgres `set_status_bulk` tenant round-trip on a throwaway `bp13_migtest`, dropped; dev `app` untouched); FE tsc+lint+`next build` green; 2× review loop (**R1 SHIP — 0 blockers** both BE + FE: the crafted-media_id cross-event write is provably impossible, the batch reject reuses the exact BP5 overlay [no bypass], route ordering/perms/caps/async + the derived stale-safe selection all verified; **R2** → a count_resolved→dashboard coverage test). **Honest limits:** batch review is per-event (no global queue); no auto-confirm (confidence-sorted); the batch loops per-pair upserts (bounded by the cap); the events bulk bar acts on **loaded** rows (no select-across-all-pages). **The next Round-2 phase off §4 is BP14 (program analytics & trends).**
- [0062 — Product Build BP14: Program analytics & trends](0062-product-build-BP14-program-analytics.md) — the Round-2 "program view" phase (theme G): dashboards were **point-in-time counts** — no rate, trend, or funnel. Owner calls: **query-only trends** (derived from timestamps — no snapshot table/job), **add `users.last_login_at`** (the one small migration), **both tiers**. A pure **`AnalyticsService`** composes existing + a few new grouped aggregates into two reads. **School program view** (`GET /v1/analytics/school`, `dashboard:view`): delivery (announced ÷ total events), sign-in (signed-in ÷ total students), engagement (distinct-seen ÷ total students) + per-term rollups + a monthly upload/event trend — events+photos+per-term all from **one `list_by_school` pass** + `counts_by_event` (announced predicate identical to BP4's), tenant from the token. **Estate adoption view** (`GET /v1/analytics/estate`, `school:manage`, platform-only): per-school funnel (staff→students→enrolled→events→distributed) via **cross-tenant grouped aggregates** (BP2 `list_schools` pattern, zipped by `school_id`, no N+1) + a transparent heuristic — `stalled` = students>0 && enrolled==0 (the enrollment wall), `idle` = enrolled but no event in 30 days. **Migration `0016`** adds nullable `users.last_login_at`, stamped `now()` on a successful login (`AuthService.login`→`UserRepository.touch_last_login`), **never on refresh**; no backfill (forward-looking). New grouped aggregates on users/students/events/media/notification_reads (the monthly trend buckets photos by upload `created_at` but **events by their `event_date`**, undated excluded, keyed `'YYYY-MM'`); the analytics VOs live in `analytics_service.py` (import-pure, layering-safe). FE: a **Program analytics** section folded **into the school Dashboard** (a `ProgramAnalytics` component — no separate nav item, owner's call — RateCards with a `—` zero-denominator + slim bars, a dependency-free `TrendChart` [CSS bars + SR data table], a per-term table; fetches its own analytics so a load/error never blocks the dashboard, renders only once the school has data) + a platform **Estate health** page (at `/estate`, its own nav item — the platform admin has no dashboard to fold into — stalled/idle alerts + an adoption-funnel `Table` with a healthy/idle/stalled pill). **No ML change, no new permission, no new dependency, no new env var.** Migration `0016` up→down→up on a throwaway Postgres (`bp14_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**556 passed / 38 skipped** + layering (+ `test_bp14_analytics.py`: school rates/per-term/trend + tenant isolation + missing-school 404; estate funnel + stalled/idle + totals; login-stamps-not-refresh; the two routes' shape + 403/401 gates + a login→analytics sign-in e2e; + 2 gated real-Postgres aggregate round-trips); FE tsc+lint+`next build` green; 2× review loop. **Honest limits:** query-only trends (a snapshot table is the scale-up); no login backfill; per-term photos from an events-pass join in-Python (bounded ~120 events); engagement = distinct openers ÷ all students (coarse); the stalled/idle rule is a heuristic; in-app delivery only (outbound email is BP12). **The recommended Round-2 track (BP9–BP14 + BP17) is complete; the deprioritised BP12/BP15/BP16 remain at the back of the queue.**
- [0098 — WhatsApp platform config: 3 DB-controlled fields (sender number, token, interim number)](0098-product-build-WhatsApp-platform-config-3-fields.md) — an owner-requested simplification of the [0097](0097-product-build-WhatsApp-live-test.md) platform WhatsApp screen so live-testing needs **no restarts and no `.env` edits**. Keep exactly **three fields**, all DB-controlled and showing their current value: **sender number**, **Meta access token**, **interim test number**. **BE + FE; migration `0025`; no ML change, no new dependency, no new permission** (reuses `SCHOOL_MANAGE`). **New column `platform_config.sender_number`** (migration `0025`, additive/nullable — the Meta sender **phone-number ID**, for the Meta provider it IS the URL's `phone_number_id`, not a `+country` number) threaded ORM→VO→repo/port/fake→service→schemas→FE. **Sender ID resolved fresh per send** via a `phone_number_id_provider` (async callable, mirroring 0097's token provider): `container._meta_phone_number_id()` reads the DB `sender_number` — a UI edit applies on the next send, no restart; the sender stays memoized. **DB-ONLY, no env fallback (owner follow-up):** the Meta token + sender ID now come from the DB ALONE — the env fallback was removed and the settings fields `whatsapp_meta_access_token`/`whatsapp_meta_phone_number_id` + the `.env.example` entries `BE_WHATSAPP_META_ACCESS_TOKEN`/`BE_WHATSAPP_META_PHONE_NUMBER_ID` deleted, so a stale `.env` value can never be silently used (providers return `""` when the DB is unset → a send fails clearly; only the non-secret `api_version`/`base_url`/`template_lang` + `BE_WHATSAPP_SENDER_IMPL` stay in env; `Settings` uses `extra="ignore"` so a leftover `.env` line is harmless). Consequence: after deploy the token + sender MUST be set at Platform → WhatsApp before any send works. **The interim toggle is gone; the interim path is gated purely on the number's presence** (`if platform.interim_test_number:`) — set a number → all sends divert to it, clear it → normal delivery; the `interim_mode` **column is kept but vestigial** (no destructive migration). **Clearable fields (the correctness point):** turning interim OFF now means CLEARING the number, so the partial-update semantics were made explicit + uniform — the service `_clean` no longer collapses `""`→`None`; `None` = omitted → unchanged, a provided `""` = clear; the repo `_merge_str` + fake merge uniformly (`None`→keep, `""`→NULL, value→set); the FE sends the token as `trim() || null` (blank = keep, write-only) and the visible sender/interim as `trim()` (cleared `""` clears server-side). API: `UpdatePlatformConfigRequest`/`PlatformConfigResponse` gain `sender_number` (returned in full — not a secret), drop `interim_mode`; the Meta **token stays masked** (`token_set`+`token_last4`, never returned/logged). FE: the `(platform)/whatsapp` page shows the three fields seeded from current config (sender + interim visible, token write-only), a **live warning banner while an interim number is set**, and an honest "all three apply on the next send, no restart" + closed-24h-window note. Guide (`WHATSAPP-GUPSHUP-SETUP.md`/`.html`) updated (three DB-controlled fields; "setting an interim number turns it ON — no toggle"). Migration `0025` up→down→up on a **throwaway** Postgres (`wa_sender_migtest`, dropped; dev `app` untouched); BE ruff+mypy (133 files)+**853 passed / 52 skipped**+layering (+3: `sender_number` partial-update + clear-interim-turns-off service tests, a `_meta_phone_number_id` DB-vs-env provider test; the route put/get covers `sender_number` + asserts `interim_mode` gone; the gated pg round-trip adds `sender_number` + an explicit `""`-clears step); FE lint+tsc+`next build` green (`/whatsapp` `○` static); self-reviewed. Honest limits: interim is now number-presence (a set number diverts ALL sends — guarded by the banner + "clear to resume" copy); `interim_mode` stays a vestigial column; "sender number" is the Meta phone-number ID; the token stays DB-stored + masked (0097 tradeoff); real non-interim delivery still needs the owner's live Meta setup (verified business + number + approved template + a permanent token). The dev `app` DB is applied by the compose `backend-migrate` on the next `--build`. **Not yet committed (pending the owner's go-ahead).**
- [0097 — WhatsApp "live-test": UI-editable token + interim real-photo send](0097-product-build-WhatsApp-live-test.md) — two owner-requested live-testing conveniences. **BE + FE; migration `0024`; no ML change, no new dependency, no new permission** (reuses `SCHOOL_MANAGE` + `whatsapp:send`). **(1)** A platform-admin, **DB-stored, UI-editable Meta access token** fetched fresh per send — Meta's temp token expires ~daily and lived only in `.env` (baked into the memoized sender → a file edit + restart to refresh); now `MetaWhatsAppSender` takes a **token-provider** (async callable) and the container's `_meta_token()` returns the DB token **or** the `.env` value (DB-first, env-fallback), so a token pasted in the UI takes effect on the next send, no restart. A **1-row `platform_config`** singleton (migration `0024`: `meta_access_token`/`interim_test_number`/`interim_mode`/timestamps) + repo (partial-update upsert) + service + `GET`/`PUT /v1/platform/whatsapp-config` (platform-admin only); the response exposes only **`token_set` + `token_last4`** — the full token is **never** returned/logged. **Security tradeoff, owner-approved + documented** (reverses the env-only-secret design of 0093/0094): a DB-stored secret, but platform-admin-only, masked, env-fallback. **(2)** An **interim free-form send** (config-gated by `interim_mode` + `interim_test_number`): from the existing "Send on WhatsApp" flow, a free-form text intro ("📸 Your school has shared N new photo(s)…") + the N real Supabase photos to a **test number** (not the student) — new `send_text`/`send_image_link` on the `WhatsAppSender` port + all 3 adapters; reuses the **effective media overlay** (BP5, rejected excluded), the budget cap, the send-log; **skips the student consent gate** (recipient is the test number); best-effort. **Interim-off = the template path byte-for-byte unchanged** (regression-tested). FE: a platform page (`(platform)/whatsapp`) — a **write-only** token field (blank = keep; shows "set · ending 1234"), the test number, an interim-mode toggle with a **warning banner while ON** ("diverts ALL sends to the test number, not students") + an honest closed-24h-window note; a nav entry + hook + endpoints/types. Guide (`whatsapp-gupshup-setup.md`/`.html`) updated with the UI-token + interim-mode note. Migration `0024` up→down→up on a **throwaway** Postgres (dev `app` untouched) + a gated partial-update round-trip; BE ruff+mypy (204 files)+**850 passed / 52 skipped**+layering; FE lint+tsc+`next build` green (`/whatsapp` static); 2× review loop (**R1 SHIP** — one should-fix: the interim log's `sender_number` → the platform sender/`"interim"` marker, never the recipient; **R2 caught a BLOCKER** — the interim **sent** path still leaked the recipient into `sender_number` [the R1 fix missed the success path; the PII test only exercised failures] → fixed on all four paths + a sent-path assertion added; + the interim-ON warning banner, the closed-window copy, the guide note). Honest limits: interim free-form delivers only inside the recipient's 24h window (a closed-window send is accepted-but-not-delivered while the app says "Sent"); interim mode diverts all sends while ON (off by default, warned); the Meta token is DB-stored (owner-approved tradeoff); `_meta_token` reads the DB once per send (a short-TTL cache is a future refinement). The **live smoke** stays the owner's step (paste token, set the test number, interim ON, send). **Not yet committed (pending the owner's go-ahead). Next: the live smoke → production (image-header template + real number + permanent token).**
- [0096 — WhatsApp: a Meta Cloud API provider (env-configured, alongside Gupshup)](0096-product-build-WhatsApp-meta-provider.md) — a second real WhatsApp provider — the **direct Meta WhatsApp Cloud API** (Graph API, the platform's own Meta WhatsApp Business account) — added **alongside** Gupshup, selectable by `BE_WHATSAPP_SENDER_IMPL=meta`, with Meta's credentials as env vars. Owner ask: "make the config env vars + integrate the Meta API too." W1's pluggable seam (`WhatsAppSender` port + `WHATSAPP_SENDER_REGISTRY` + a config-selected container branch) meant this was a **new adapter + one registry line + one container branch + env creds** — no change to the W2 send flow, no migration, no ML change, no new dependency, no new permission. `fake` stays default; **fake/gupshup byte-for-byte unchanged** (R1-verified). New `adapters/whatsapp/meta_sender.py::MetaWhatsAppSender` mirrors the Gupshup adapter (httpx, `_redact(to)`, token never logged): `POST {base_url}/{api_version}/{phone_number_id}/messages`, `Authorization: Bearer`, a JSON template message with an **image-header component** (+ a `body` component only when `caption` is set — the share service passes None), `_meta_to_receipt` parses `messages[0].id` (guarded → `""`), a 2xx-error-body defensive check. **Two Meta quirks (documented):** `sender_number` is IGNORED (Meta's sender is the `phone_number_id` in the URL) and a template is matched by NAME (not a UUID); version-sensitive lines flagged `CONFIRM against Meta Cloud API docs`. Registry gains `"meta"`; the container's `whatsapp_sender()` is restructured to explicit `if fake / elif gupshup / elif meta / else ConfigurationError` (the Meta secret read only here via `get_secret_value()`). New `BE_WHATSAPP_META_*` settings + `.env.example` placeholders (SecretStr token, a "use a permanent/system-user token; keep the API version current" note). **Provider-aware settings screen:** `WhatsAppConfigResponse` gains `provider` (via the config service's new `provider` property → both routes); the FE labels the template field by it (Gupshup="Template ID"/UUID; Meta="Template name") + a "Provider:" line, and under Meta the "Sender number" field's hint says it's **not used** (so a Meta admin isn't misled). A Meta appendix added to `whatsapp-gupshup-setup.md`/`.html`. BE ruff+mypy (199 files)+**828 passed / 51 skipped**+layering (+ `_meta_to_receipt` parse [8 cases], the container meta-build/memoize/unknown-impl→ConfigurationError, the `provider` passthrough; all `from_config`/`WhatsAppConfigService` callers updated); FE lint+tsc+`next build` green (`/settings/whatsapp` static); 2× review loop (**R1 SHIP — 0 findings**: secret airtight, no fake/gupshup regression, robust parse, layering clean; **R2 SHIP — should-fix applied**: the Meta-aware sender-number hint, the provider-aware `.md` CTA, swept 2 stale two-provider comments, the non-dict-element parse test). Honest limits: the real Meta send is untested until a live account (behind `fake`); `sender_number` ignored for Meta (multi-number is future); keep the Graph API version current + use a permanent token. **Committed + pushed.** Next: the live smoke (either provider) + the paused W3b/W3c.
- [0095 — WhatsApp W3a: variant-object cleanup (the reaper)](0095-product-build-WhatsApp-W3a-variant-reaper.md) — the first slice of **W3** (the WhatsApp scale/reliability phase): closes the W2 "honest limit" that the ≤5 MB send-variant objects (`{whatsapp_variant_prefix}/{school}/{media}.jpg`) are never reaped. **Backend-only; no migration, no ML change, no new dependency, no new permission, no worker.** W3's architect finding shaped the slicing — **the backend has no worker process** (only the ML inference worker exists), so the flagship send-to-all (W3c) needs new infra; W3a was picked first as the only slice with **zero live-Gupshup dependency, no worker, no PII**. An **operator-run CLI** `python -m backend.cli.reap_whatsapp_variants [--older-than-hours N] [--dry-run] [--school ID]` over a **pure** `services/whatsapp_variant_reaper.py` (imports only the `ObjectStore` port + stdlib + structlog — layering-clean): lists under the prefix (or `{prefix}/{school}`), keeps every object **younger than** `retention`, deletes the rest **best-effort** (a delete failure is counted, run continues), returns a frozen `ReapSummary {scanned, deleted, skipped_recent, errors}`. **Safety by construction:** age-based only — it **never reaps a variant younger than `retention`** (default 24h, **24× the 1h signed-URL TTL**), so it can't race a fresh send (a reaped variant is re-created on the next send, the key being deterministic); a **non-positive retention is refused at two layers** (the CLI argparse validator — before the container builds — AND a defense-in-depth `ValueError` in the service). One port addition: **`ObjectStore.list_prefix(prefix) -> list[StoredObject]`** (new frozen `StoredObject {key, last_modified}`, tz-aware UTC) in the local_fs adapter (recursive `rglob`+mtime — the fully-testable path; its byte methods promoted from no-op stubs to real filesystem ops so the round-trip is genuine — dev-only, prod uses supabase, and `download_bytes` still raises `UpstreamError` on a missing object, preserving the BP17/W2 contract), the supabase adapter (two-level `.list()` walk, unreadable timestamp → treated as recent/kept), and the fake. Scheduling is operator-driven + documented (a `docker-compose.yml` comment near `backend` gives the one-shot; the CLI/module docstrings + `.env.example` note it); new setting `whatsapp_variant_retention_hours: int = 24` (+ `BE_WHATSAPP_VARIANT_RETENTION_HOURS`). BE ruff+mypy (198 files)+**817 passed / 51 skipped**+layering (+ the reaper decision path against the fake [reap-old/keep-recent, dry-run, `--school` narrowing, best-effort + the `deleted+skipped_recent+errors==scanned` invariant, empty-prefix zero, exact-cutoff boundary reaped, default-`now` keeps a fresh upload, **`<=0` refused**]; the CLI guard [`_positive_hours` rejects `<=0`; `main(["--older-than-hours","0"])` exits 2 **before** building the container — proven with a monkeypatched `Container`]; the local_fs `list_prefix`/byte round-trip); 2× review loop (**R1 SHIP — 0 blockers**: the retention rule proven, the local_fs deviation proven backward-compatible [dev-only, prod=supabase, in-suite callers use the fake, the missing-object `UpstreamError` contract preserved+tested]; **R2 SHIP — 4 should-fix + 2 nits applied**: the `<=0` guard [both layers], a CLI guard smoke test, refreshing the stale W2 "not reaped" comment, this doc + the scheduling runbook, the summary-consistency assertion, the supabase scale note). Honest limits: operator-driven not scheduled (the backend has no scheduler); the supabase `list_prefix` is unit-untested + loads the full listing into memory (bounded at v1 scale; a paginated walk is the scale-up); age-based not send-log-aware (simpler + safe past the 1h URL TTL). **Committed + pushed.** Next: W3b (delivery receipts webhook) / W3c (send-to-all — needs a new backend worker + dedupe, best after the live Gupshup smoke).
- [0094 — WhatsApp W2: the send flow](0094-product-build-WhatsApp-W2-send-flow.md) — the third slice of the owner-locked **WhatsApp auto-send** track ([0092](0092-product-build-WhatsApp-Phase0-student-mobile-optin.md) Phase 0, [0093](0093-product-build-WhatsApp-W1-provider-foundation.md) W1): the **actual send** — staff send a student their photos inline on WhatsApp. **BE + FE; migration `0023`; no ML change, no new dependency.** The whole flow is built + tested against the **fake sender**; the real Gupshup delivery stays a deferred live smoke. Owner decisions: **student-centric** (send ONE student ALL/a subset of their **effective** photos — one recipient, honest cost; photo-fanout rejected as too risky), **12,000/school/month** budget cap, a **server-side loop** (one budget check — not an in-browser pool), a new **`whatsapp:send`** perm (school_admin + teacher, the `notification:send` precedent), and the **≤5 MB enforcement** now in `make_whatsapp_variant` (quality-then-edge step-down; un-shrinkable → skip, never sent over-cap). `WhatsAppShareService.send_student_photos` gates in exact order: tenant `get` (**404 before anything**) → config enabled → sender/template resolve → **consent (opt-in AND non-null number, server-authoritative)** → **effective set via `GalleryService.student_media` (BP5 overlay REUSED, not re-derived)** → budget → best-effort per-media loop (BP27 pattern). **The security spine, R1-proven unbypassable:** a client `media_ids` is INTERSECTED with the school-scoped effective set, so a crafted/foreign/**rejected** id can never reach `send_image` (recorded `skipped "not entitled"`); tenant is double-enforced (ML seam + media re-filter); the recipient number is never a column / never in an `error` / never in the response / **redacted** in the Gupshup `UpstreamError` (last-4). Each media: produce the ≤5 MB variant → upload to a deterministic per-media key (overwritten on re-send) + a short-lived signed URL → `send_image` → a `whatsapp_send_log` row. **Migration `0023`** (`whatsapp_send_log`: `id` PK, `school_id` FK CASCADE, `student_id`/`media_id`/`actor_user_id` FK **SET NULL** [the spend/audit row outlives an erased student/media], denormalized `actor_role`, `sender_number` [the platform SENDER, never the recipient], `status` sent/failed/skipped CHECK, PII-free `provider_message_id`/`error`, indexes `(school_id, created_at)` [budget count] + `(school_id, student_id, created_at)`) + a `WhatsAppSendLogRepository` (postgres `record`/`count_sent_since` + fake) + ORM. Route `POST /v1/students/{id}/whatsapp-send` (`whatsapp:send`, tenant from token, body capped 1000 → 422); container memoizes `whatsapp_share_service()` — the **first wiring of `whatsapp_sender()` into a service**. FE: a **"Send N on WhatsApp"** button (disabled + reason when not opted-in/no number) → a `ConfirmDialog` stating the true cost → `use-whatsapp-send` (server loops, not the browser) → an honest toast (sent=0→error / partial→sticky / full→success) that **surfaces an over-budget skip distinctly** ("monthly WhatsApp limit reached"); wired into the student detail "Appears in" (beside BP26 Download-all) + the event gallery By-student tab. Migration `0023` up→down→up on a **throwaway** Postgres (`wa_w2_migtest`, dropped; dev `app` untouched); BE ruff+mypy (194 files)+**802 passed / 51 skipped**+layering (+ 18 service + 12 route tests: the gate truth table [each sends nothing], **rejected-by-id→skipped**/`added`→sent, happy/subset, partial-failure isolation, **budget cap stops sends**+zero-remaining, ≤5 MB skip, **PII-free**, **tenant-404-before-any-send**, teacher-allowed/student·platform-403, over-cap-422; + a gated send-log round-trip); FE lint+tsc+`next build` green (`/settings/whatsapp` static, send surfaces dynamic); 2× review loop (**R1 SHIP — 0 blockers**: crafted-`media_id` provably unreachable, consent server-authoritative, budget race bounded, PII airtight, tenant double-enforced; **R2 SHIP — 2 should-fix + 3 nits applied**: the distinct over-budget toast, the variant-object-non-cleanup doc + corrected "short-lived" wording, the dead `loading` prop removed, the "Send 0" flash gated, the SR summary aligned). Honest limits: the real Gupshup send is untested (behind `fake`; a W2 smoke once the owner sets up account/number/template); **no server-side send dedupe** (a retry re-sends — double cost; the button is in-flight-disabled); a bounded **concurrent-request budget race** (a single request can never exceed the cap); **variant objects aren't reaped** (deterministic key, overwritten on re-send — a cleanup job is a follow-up); the confirm count is an honest upper bound (server may send fewer, never more). **Not yet committed (awaiting owner review). Next: the W2 live smoke (owner sets up Gupshup) → W3 (send-to-all-in-an-event via a queue, per-school numbers, delivery webhooks, a variant reaper).**
- [0093 — WhatsApp W1: provider foundation + per-school settings](0093-product-build-WhatsApp-W1-provider-foundation.md) — the second slice of the owner-locked **WhatsApp auto-send** track ([0092](0092-product-build-WhatsApp-Phase0-student-mobile-optin.md) shipped Phase 0): the **provider-agnostic foundation**. **W1 sends NOTHING to students** (the send flow is W2). **BE + FE; migration `0022`; no ML change, no new dependency, no new permission-beyond-one, no real secret in code.** **Provider = Gupshup** (over Wati — its one-key/multi-source-number JSON send API fits "platform owns ONE account, schools share a number now / own it in W3"; a **registry** makes Wati a one-line swap; the real HTTP contract is provider-doc-dependent and every such adapter line is flagged `CONFIRM against Gupshup live docs`, shipped behind a `fake`-default flag). Pieces: a **`WhatsAppSender` port** + `WhatsAppReceipt` VO (DISTINCT from the BP4 PII-free `NotificationChannel` — this carries the recipient number + image + template/sender and RETURNS a receipt or RAISES; `send_image(*, to, image_url, template_name, sender_number, caption=None)`, a signed link not bytes, reuses `UpstreamError`/`ValidationError`); a **`FakeWhatsAppSender`** (real deterministic default) + **`GupshupWhatsAppSender`** (httpx, behind `BE_WHATSAPP_SENDER_IMPL=gupshup`) via `WHATSAPP_SENDER_REGISTRY` + memoized container builders — **the sender is wired into NO service, there is NO send endpoint**; the **ONE platform secret** `whatsapp_api_key: SecretStr` (`.get_secret_value()` only in the container, never logged; `.env.example` **placeholder only**; the per-school table has **no secret column**); a **`school_whatsapp_config`** table (migration `0022`: `school_id` PK + CASCADE FK, `enabled` bool default false, nullable `sender_number`/`template_name`/`business_name`, timestamps — one row per school, read by PK so tenant isolation is inherent, **lazy not seeded** → a missing row synthesizes a `disabled` default, no onboarding change) with a `WhatsAppConfigRepository` (postgres **upsert** via `on_conflict_do_update` + a fake) + a pure `WhatsAppConfigService` (blank→None, loose-validate `sender_number` via the Phase-0 `validate_mobile`, tenant from the route); a new **`whatsapp:manage`** permission (**school_admin only**, the `audit:view` precedent); routes `GET`/`PUT /v1/schools/whatsapp-config` **registered BEFORE the `schools` router** so `/whatsapp-config` isn't shadowed by `/v1/schools/{school_id}` (no collision — verified); and the **≤5 MB image-variant helper** (`Thumbnailer` gains optional per-call `max_edge`/`quality` overrides — BP17 callers unchanged — + `services/whatsapp_image.py::make_whatsapp_variant`, built now/unused, **NOT a hard byte cap** — the loop-down-quality enforcement is deferred to W2). FE: a school-admin **WhatsApp settings** page (`RoleGate` — Enable toggle + Sender number [truthful hint across shared-with-default / shared-without-default / own-number] + Template name [provider-approval context] + Business name + an honest "saving does not send anything yet" note), a `useWhatsAppConfig` hook + endpoints/types (`effective_sender_number`/`using_shared_number` computed server-side). Migration `0022` up→down→up on a **throwaway** Postgres (`wa_w1_migtest`, dropped; dev `app` untouched) + a gated upsert round-trip; BE ruff+mypy (190 files)+**756 passed / 50 skipped**+layering (httpx confined to the Gupshup adapter, PIL to the thumbnailer) (+ fake-sender / **Gupshup `_to_receipt` 5-case parser** / config-service default+blank+effective-number-edge+malformed-400 / route GET·PUT·**teacher-403**·**platform-403**·**two-tenant isolation** / image-helper / registry·container·permissions·layering); FE lint+tsc+`next build` green (`/settings/whatsapp` `○` static); 2× review loop (**R1 SHIP — 0 findings**: secret handling + the router-ordering deviation both airtight/test-proven; **R2 SHIP — 4 should-fix + 2 nits applied**: the "≤5 MB cap" over-claim corrected to "not a hard cap; W2 enforces", the FE shared-number hint made truthful with no configured default, provider-approval context on the template hint, the `_to_receipt` test, tighter toggle copy, the migration `onupdate` note). Honest limits: the real Gupshup adapter is untested end-to-end (needs a live account/number/approved template — behind `fake`, a W2 smoke), the helper isn't a hard byte cap (W2 enforces), the future send-time `UpstreamError` surfaces the recipient number (PII, redact in W2), W1 sends nothing. **Not yet committed (awaiting owner review). Next: W2 (the send flow) — after the owner creates a Gupshup account + WhatsApp Business number + one approved Utility image-header template.**
- [0092 — WhatsApp Phase 0: student mobile number + WhatsApp opt-in](0092-product-build-WhatsApp-Phase0-student-mobile-optin.md) — the first slice of the owner-locked **WhatsApp auto-send** track (direction in `whatsapp-build-plan.html`): stores **where to send** before any sending exists. **Phase 0 sends nothing** — no provider, no queue. **BE + FE; migration `0021`; no ML change, no new dependency, no new permission, no new env var.** Two additive `students` columns — `mobile_number` (`String`, nullable, NOT unique/CHECK) + `whatsapp_opt_in` (`Boolean`, NOT NULL default false — **consent never assumed**) — threaded domain→adapter→service→API→FE. A pure **`domain/phones.py`** loose validator (`^\+?[0-9]{7,15}$`; None/blank → NULL since mobile is optional; the provider validates authoritatively at send time) applied at every write boundary; the single read chokepoint `_to_student` populates both on every read. Write paths: `CreateStudentRequest` (+ optional mobile/opt-in), the bulk CSV import (a `mobile`/`phone` column — **no opt-in for bulk**, consent isn't defaulted for a roster), and a **dedicated `PATCH /v1/students/{id}/mobile`** (mirrors `/status`, not the class-only `/{id}`; `student:manage`, tenant from the token, **404-before-write**; **no admin-action audit** — a contact edit isn't governance). FE: a detail-page **WhatsApp** cell (number or `—` + "Opted in"/"Not opted in" as text) + a `MobileEditor` dialog, a create-dialog field + opt-in, a bulk-import Mobile column; both opt-in checkboxes carry a clarifying `aria-describedby` hint ("…once delivery is turned on") — no list column (tasteful surface). Migration `0021` up→down→up on a throwaway Postgres (`wa_phase0_migtest`, dropped; dev `app` untouched) + a gated adapter round-trip; BE ruff+mypy+**730 passed / 49 skipped** + layering (+ `test_phones.py` truth table, service create/set/**opt-in-existing**/**tenant-404-before-write**/bulk-best-effort, route round-trips); FE lint+tsc+`next build` green (`/students` stays `○`, detail `ƒ`); 2× review loop (**R1 SHIP — 0 findings**; **R2 SHIP → 3 should-fix + 2 nits applied**: the `+`+15-digit accept test, an opt-in-only toggle test, checkbox `id`/`aria-describedby`+hint on both forms, disable-input-while-saving). Honest limits: loose validation (provider is authoritative), two independent states storable (a future send MUST gate on opt-in **AND** a non-null number), no CSV opt-in, no uniqueness. **Not yet committed (awaiting owner review). Next: W1 (provider foundation + per-school settings).**
- [0091 — Per-student "Download all" on the event gallery's By-student tab](0091-product-build-per-student-event-gallery-download.md) — the small BP26 follow-on flagged in [0081](0081-product-build-BP26-v1-staff-download.md) ("an easy follow-on — a per-student download on the gallery 'By student' tab"). **FE-only — no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** On the event gallery's **By student** tab (the `FilterChips` students filter), `EventStudentPhotos` gains a **"Download all N"** button above the `PhotoGrid` that zips the picked student's photos **from this event** (the event-scoped sibling of BP26's all-events student-detail download): reuses `useEventStudentMedia` (the effective set the tab already renders, BP5-corrected) + the streaming `useDownloadAll` + the **entitlement-gated `GET /media/{id}/download` mint staff already use** (both roles hold `gallery:view_all` — no widening, no new endpoint); entries `{event_date}-nnn`, zip `{Student}-{Event}-photos-{date}.zip`; `ByStudent` threads the active student's name, `useEvent(eventId)` (SWR-deduped) supplies the event name/date. Honest download states mirror the sibling staff surfaces (cancelled→silent / all-failed→error / capped→"first N of M" sticky / partial→"saved N of M" sticky / full→success) + BP26 a11y (label flip + `sr-only` `aria-live`). Hooks run before the early returns (Rules of Hooks); switching the chip re-keys `useDownloadAll`. **1 file.** FE lint+tsc+`next build` green (gallery route stays `ƒ`); no backend suite delta; self-reviewed. Honest limits: the BP9 500-cap on Firefox/Safari (capped toast); "all" = the effective set (can differ from the chip's raw `media_count`).
- [0090 — Product Build BP31: Onboarding feedback loop & copy/discoverability polish](0090-product-build-BP31-onboarding-copy-polish.md) — the **final** phase of the Round-4 roadmap, a "floor-sweep 2" of small momentum + truthfulness fixes (closes **R4-A01/A02/A08/A09/A14/A18** + **R4-F01/F02**). **FE-only — no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** **Three items CUT** (plan-review verified they're already shipped, not near-no-ops): **A19** (clearable-tag explainer shipped BP24a), **A11** (grade/section already displayed by BP11a), **F05** (partial-download messaging already distinguishes cancelled/capped/partial) — kept the sweep crisp at **8 real items**. The one backend question — A09's per-row reject `reason` — is **already returned** (`BulkStudentResultResponse.error` + the FE type exist; the students `BulkImportDialog` just didn't render it), so the whole phase is FE-only. Eight items: **A01** checklist momentum (`void mutateDashboard()`/`globalMutate("dashboard")` added **alongside** the existing mutates at the 4 completing flows so the `SetupChecklistCard` advances immediately, fire-and-forget + an "Almost there — announce to reach your students." cue when the next incomplete core step is announce — `nextKey==="has_distributed"`, the reachable near-complete moment since "all core done" retires the card); **A02** dashboard warmth ("Welcome — let's get your school set up" heading/subhead); **A08** both reference-photo dropzones hint "This photo enrolls the student's face for matching…"; **A09** `bulk-import-dialog.tsx` renders the per-row reason (server `error` for `invalid`; static fallback for `duplicate`/null-error; nothing for `created`); **A14** a `SearchInput` filters the `EditClassesDialog` list (selection preserved — a checked-but-filtered-out class still saves); **A18** a decorative "Matching…" pulse badge next to the Photos-card `StatusPill` gated on `inFlight` (`aria-hidden` — the existing "Matching since…" `aria-live` region is authoritative, no double SR chatter; respects the reduced-motion guard); **F01** an inline action beside the failure note (branch on `enrollment_failure_reason` — `ml_unavailable`→Re-enroll, else the existing `ReferencePhotoDialog`); **F02** the events-list "Matching" pill wrapped in a `Link` to `/events/{id}` (Process) when the derived `pill ∈ {not_started, failed}` (dashboard alerts can't deep-link to one event — `needs_attention` has no per-event IDs, a documented backend follow-up). **8 files, no new files, all FE.** FE lint+tsc+`next build` green (touched pages keep their markers — `/dashboard`/`/students`/`/staff`/`/events` stay `○`); no backend suite delta; no FE test harness (repo norm — gate + manual walk). 2× review loop (**R1 correctness SHIP → 1 should-fix applied**: the A01 completion cue was a **dead branch** [`allCoreDone` requires `has_distributed`, which retires the card] → re-keyed to the reachable one-step-left state; everything else [fire-and-forget, A09 field names, A14 selection-safe, A18 `inFlight` gate, F01/F02 branches] verified clean; **R2 edge/a11y/copy SHIP → 1 should-fix + 2 NITs applied**: the A18 badge dropped `role="status"` [double-announced with the live region → decorative `aria-hidden`], "Syncing…"→**"Matching…"** [BP21 one-grammar], `students/page.tsx` unified to the typed `mutateDashboard()` [+ removed the unused `swr` import]; kept the A01 cue `role="status"` [one-time progress, doesn't loop] + both F01 placements [contextual fix beside the note + general header controls]). Honest limits: F02 is FE-only-bounded (the dashboard per-event deep-link needs a backend `needs_attention` ID — a follow-up); a photoless bulk student stays `pending` so it won't tick the enroll step; A19/A11/F05 dropped as already-shipped; no FE automated tests. **BP31 is complete — and with it the ENTIRE Round-4 roadmap (BP26 → BP27 → BP28 → A15 → BP29 → BP30 → BP31) is done.** Remaining open work is the parked backlog (BP12 outbound / BP15 accuracy / BP16 lifecycle / the BP6 video timeline / the blocked BP22-slice-4) in [`product/05`](../product/05-parked-backlog.md); a phase starts only on owner pick + scope re-confirm.
- [0089 — Product Build BP30: Review-loop power tools at scale](0089-product-build-BP30-review-loop-power-tools.md) — a Tier-2 Round-4 phase, **the review lane's power tools at 200-ambiguous-match volume** (closes **R4-A20/A21/A22/A23** + **R4-F04**). **FE-only — composes shipped BP13/BP22/BP5 primitives; no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** **Two load-bearing invariants (verified in code):** **no auto-confirm** (BP22 stance — the threshold multi-select STAGES a selection, the human commits) + **staff-only/student-safety** (the show-removed disclosure lives inside `AppearanceEditor`, which the student lightbox never renders [`showAppearances={false}`], reads only the staff-gated appearances endpoint, and leaves the BP5 `effective_*` gate untouched). Five items: **(1) threshold multi-select** (R4-A21) — a "Select below" row (chips `<60/70/80%` + a clamped free `%` field) computes `setSelected(pairs where round(confidence*100)<pct)`, calling only `setSelected` never `batchReview`; **(2) discoverable batch-undo** (R4-A20) — `apply()` captures the resolved `ReviewPair[]` into `lastRejected` **before** `mutate()` (no stale closure) on a reject + an inline `role="status"` banner "Rejected N · Undo" that fans out `Promise.allSettled(undoCorrection…)` (reverts each pair to raw-ML `needs_review` pending — no batch-delete endpoint); **(3) "Show removed (N)" disclosure** (R4-A20, Option A FE-only) — `AppearanceEditor` re-lists the `verdict==="rejected"` rows it already receives (client-filtered today) with a per-row Re-add, `N` from the same prop (no new fetch), hidden when 0, safe for both callers; **(4) table-view toggle** (R4-A22) — a `view:"grid"|"table"` local state rendering the same `pairs`/`selected` as denser rows (purely presentational); **(5) add-students cue** (R4-A23) — `AddStudents` shows "Showing first X — type to refine" from the already-returned `useStudents` `total` (only when `total>items.length`, no extra fetch); **(6) lightbox auto-paging** (R4-F04) — `PhotoGrid` threads `hasMore`/`loadingMore`/`onLoadMore` into `Lightbox`; `canNext` extended; a `goNext` `useCallback` calls `onLoadMore()` only if `!loadingMore` + a `wantNext` ref + a `[mediaIds.length]`-keyed effect advances `index` once the page lands (race-safe: no double-fetch, no out-of-range index); callers passing no paging props behave as before; the review-lane hand-rolled `<ul>` (not `PhotoGrid`) is untouched. **Option B (an event-wide rejected grid) deferred** (needs a new staff-gated read). **4 files, no new files, all FE.** FE lint (0/0) + tsc + `next build` green (gallery route stays dynamic `ƒ`); no backend suite delta; no FE test harness (repo norm — gate + manual walk). 2× review loop (**R1 correctness/entitlement SHIP — 0 blockers**: both invariants, the undo async [pre-`mutate` capture, `allSettled` idempotency], the lightbox race-safety [double-fetch gate + out-of-range guard + self-terminating effect], Rules-of-Hooks all verified → 4 optional NITs; **R2 edge/a11y/copy SHIP-READY — 0 blockers → 4 polish fixes applied**: the removed-row confidence % `text-ink-muted`→`text-ink-secondary` [data-bearing → AA, the BP25 convention], the free-% field clamped `[0,100]`, the `AppearanceEditor` docstring updated for the direct Re-add, the popover cue's trailing period dropped; a11y confirmed [named chips/toggle/Apply, `role="status"` undo banner, `aria-expanded` disclosure + count, per-row Re-add `aria-label`]; the dense row-list-vs-`Table`-primitive left as an intentional divergence). Honest limits: still no auto-confirm; per-event lane; the undo window is session-transient; "Show removed" is per-photo not an event-wide grid (Option B deferred); the popover cue counts the first page ("type to refine"); the review-lane table isn't windowed (fine at ~200/event). **BP30 is complete. Next: BP31 (the final Round-4 phase — onboarding feedback loop & copy polish).**
- [0088 — Product Build BP29: Teacher-role coherence](0088-product-build-BP29-teacher-role-coherence.md) — the second Tier-1 phase of the Round-4 roadmap ([`product/09`](../product/09-improvement-roadmap-round-4.md)), **the teacher persona finished** — closes **R4-T01–T08**. **FE-only — no backend change, no migration, no ML change, no new dependency, no new permission, no new env var.** **Security invariant (unchanged):** delegation stays **convenience-scope, NOT a boundary** (BP11c's owner call — a teacher could already see the whole school); BP29 is **legibility only**, no `class:manage` grant. Six FE items: **(1)** role-aware onboarding (R4-T02/T08) — a teacher no longer sees the admin `SetupChecklistCard` (whose "Add a teacher" step dead-ends at the `school_admin`-only `/staff`); a one-line muted "your role" note instead (admin path + `DashboardContent` byte-for-byte unchanged); **(2)** a **delegation-clarity banner** (R4-T01) — new `components/delegation/delegation-banner.tsx`, shown when `role==="teacher" && !useMyClasses().isLoading && myClasses.length===0` ("You're seeing all classes. Ask your admin to assign you classes to focus your lists."), mounted below the `PageHeader` on students + events (renders in every list state), dismissal in `localStorage` via **`useSyncExternalStore`** (server snapshot `false`=shown → SSR-safe, the repo's `use-online-status.ts` pattern); the `role==="teacher"` guard is **load-bearing** (an admin's `useMyClasses` is `enabled`-gated off → also `[]`); **(3)** mark **"(my class)"** on the students/events class **filter** `<select>` options in the teacher's set (a text suffix); **(4)** live my-classes refresh (R4-T06) — `use-my-classes.ts` adds `{revalidateOnFocus:true, refreshInterval:60_000}` (the BP20 pattern, teacher-`enabled`-gated → no admin/student polling); **(5)** read-only roster (R4-T03) **baseline, no new surface** — the marked class filter IS the teacher's read-only roster (`GET /v1/students?student_group_id=`, a read a teacher already can do), no dedicated route / no teacher Classes nav (would risk drift toward the mutation-dense admin class page); **(6)** graceful **RoleGate** denial (R4-T07) — `role-gate.tsx` renders an `EmptyState` ("Not available for your role" + a "Go to dashboard" link to `homePathForRole(user.role)`) instead of the silent `router.replace` bounce, still `return`ing **before** `children` so a guarded page's fetch never fires. **Deferred (documented): item 7 "My work" lens (R4-T04)** — "my uploads" + per-teacher "pending reviews" need **new backend reads** (no `created_by`/actor filter on events/media; the dashboard `needs_review` is school-wide) and "my events" (`mine=true`) is empty for exactly the un-delegated teacher — shipping a half-lens would break the FE-only shape. **6 files (1 new), all under `frontend/`.** FE lint+tsc+`next build` green (`/dashboard`/`/students`/`/events` stay `○` static); no backend suite delta; no FE test harness (repo norm — gate + manual walk). 2× review loop (**R1 correctness/entitlement SHIP — 0 blockers**: 100%-`frontend/` diff, no new permission/endpoint/grant, the load-bearing role guard [admin/platform/student never see a teacher surface], `useSyncExternalStore` fidelity [no hydration mismatch/re-subscribe storm], Rules-of-Hooks, the SWR override teacher-gated, RoleGate fetch-never-fires all verified → 2 NITs [cosmetic import order; a one-frame checklist flash while `useMe` resolves] not worth acting on; **R2 edge/a11y/copy SHIP — 0 required fixes → 1 NIT applied**: the RoleGate `EmptyState` `role="alert"`→`role="status"` [informational access-denied, not an error — the 0037 convention]; 3 NITs left as-is [permanent-per-browser dismissal acceptable for a low-urgency nudge; inline marker `Set`s the low-ceremony choice; the "Go to dashboard" copy holds since RoleGate only wraps `(school)` pages]; confirmed the banner is `role="status"` w/ a real `aria-label="Dismiss"` focus-ringed button + no existing `Callout` primitive to reuse). Honest limits: legibility-not-enforcement (a teacher still sees the whole school); banner dismissal permanent-per-browser (no reset on re-delegation); roster = the marked filter (a dedicated page is a future add); the "My work" lens deferred; no FE automated tests. **BP29 is complete. Next: BP30 (Review-loop power tools).**
- [0087 — Product Build A15: Teacher-cap visibility (the BP28 closer)](0087-product-build-A15-teacher-cap-visibility.md) — the optional, cheap closer of **BP28 (Governance & audit completeness)** (Round-4 **R4-A15** / Round-3 **R3-A2-04**): a school-admin creating a teacher at the `max_teachers` cap learned only via a **post-submit 409** (`LimitExceededError`); A15 surfaces the cap on the staff page **before** submit. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** The planning agent's key find: the dashboard read **already computes both numbers**. **Backend** (no new query, no new endpoint): `DashboardService.school_summary` already loads the school (carrying `max_teachers`) + already computes `teacher_count = count_by_school_and_role(school_id, Role.TEACHER)` (it just collapsed the count into a `has_staff` bool) — so `SchoolDashboard` gains `teacher_count`/`max_teachers` (reusing the computed value; a comment pins the count as **status-agnostic** = active + disabled, matching the 409's enforcement so display can't drift), and a nested `StaffSummary` block on `DashboardResponse` (mirrors `students`/`events`/`media`, always populated). `GET /v1/dashboard` is already tenant-scoped (`tenant_of(actor)`) + `dashboard:view` (school_admin holds it) — no router change; the surfaced numbers are the **same source the 409 enforces**. **Frontend:** `staff/page.tsx` calls `useDashboard()` (shared `"dashboard"` SWR key, no extra request), renders a seat-usage line **as its own element** (NOT the `PageHeader` `${total} teachers`, which is the search-filtered `count_page_by_role` — a search would corrupt it): "{n} of {m} teacher seats used" → "At capacity — " (remaining==0) / "Over capacity — " (count>max, since a cap can be lowered below the count), never a negative number; **soft-gates** the Add-teacher + empty-state + BulkInvite triggers at capacity with a visible note ("Ask your platform administrator to raise the cap." — accurate, cap is platform-only per BP18c); **advisory-only** — a loading/errored dashboard read disables nothing (409 backstop), so a failed advisory read can never block a create. **7 files, no new files.** BE ruff+mypy+**707 passed / 48 skipped** + layering (seat usage reflects cap+count incl. a **disabled teacher still consuming a seat** + admins excluded; count tenant-scoped; the route returns the `staff` block + is tenant-isolated; no new gated Postgres test — `count_by_school_and_role` scoping already covered); FE lint+tsc+`next build` green (`/staff`+`/dashboard` stay `○` static); 2× review loop (**R1 correctness SHIP — 0 blockers**: display==enforcement [no second query], proven tenant isolation, the fail-safe advisory guard, seat-line/search-total independence, the soft-gate is a real functional block → 2 NITs deferred to R2; **R2 edge/a11y/copy SHIP — 0 blockers → 2 fixes + 1 test, all applied**: dropped `role="status"` on the seat line [was re-announcing on every ~60s SWR revalidate], removed the **dead `title` tooltip** on the disabled triggers [a `disabled` button is `pointer-events-none` so it never showed — the visible note is the explanation; also dropped the redundant `aria-disabled`], added the disabled-teacher-counts test). Honest limits: the seat count is authoritative but the at-capacity gate is **advisory** (the 409 remains the true gate — defense-in-depth); lowering the cap below the count shows "Over capacity" but force-removes no one (BP18c behavior); no FE test harness (gate + manual walk). **BP28 is now fully closed (28a + 28b + A15).** **Next: BP29 (Teacher-role coherence).**
- [0086 — Product Build BP28b: Admin-action audit (the governance actor trail)](0086-product-build-BP28b-admin-action-audit.md) — the second & **final** slice of **BP28 (Governance & audit completeness)** (Round-4 **R4-A25** — governance-lifecycle actions [who disabled/deleted/re-enrolled/re-invited a student or teacher, who edited a school] left **no record**). **BE + FE — migration `0020`** (a new backend-owned table); **no ML change, no new dependency, no new permission** (reuses `audit:view`), **no new env var.** An append-only **`admin_action_audit`** (mirrors BP8b's `download_audit`): `id`·`school_id`(FK **CASCADE**)·`actor_user_id`(FK **SET NULL** — the trail outlives the account)·`actor_role`(denormalized, CHECK ∈ 4 roles)·`action`(CHECK ∈ 11: `student_{created,disabled,enabled,deleted,reenrolled,invite_resent}`·`staff_{created,disabled,enabled,invite_resent}`·`school_updated`)·`target_type`(CHECK ∈ student/staff/school)·`target_id`(nullable, **NO FK** — heterogeneous, like `match_corrections`↛`matches`)·`target_label`(nullable, captured at write time)·`created_at` + 4 composite indexes (school + target/actor/action drill-downs). **The load-bearing design:** a private `_record_action` on **both** `StudentService` + `OnboardingService` writes one row **best-effort** (`try/except → _log.warning("admin_action_audit_record_failed", school_id=…)` — a failed audit must NEVER block/roll back the mutation; safe because each repo method owns its own transaction, so the mutation already committed) inside the single-writers — so **BP27's bulk loops are audited row-by-row for free** (they thread the actor down + call those same single-writers). Hooks record the *effective* action only (a no-op status set / a refused last-admin disable / an un-threaded `actor_role=None` caller record nothing; `update_school` keys the row to the **TARGET** school since a platform admin's `school_id` is None). Reads: `AdminActionAuditService.school_action_log` (adapter `postgres_admin_action_audit.py` + registry/container) joins the actor's **current** email from the backend's OWN `users` rows — **batched in-Python, no N+1, never a cross-service SQL join** (a deleted actor → `actor_email=None`, the denormalized role + label still show what happened); `GET /v1/audit/admin-actions` (`audit:view`, tenant from token; date params typed `datetime`, action/target typed enums → **422 at the boundary**). FE: a new `lib/audit/actions.ts` (`ACTION_LABELS` humanizer + filter options), a Radix **`components/ui/tabs.tsx`** wrapper (roving tabindex + arrow-keys + `tab`/`tabpanel` aria), and `audit/page.tsx` reworked to **two tabs** (Downloads / Admin actions, `?tab=` URL-backed inside the existing `<Suspense>` so `/audit` stays `○` static) each with URL-backed filters + a client-side CSV export; the Target cell falls back to **"Deleted student"** for a null-label `student_deleted` row. **Owner decision (flagged by plan-review, resolved by default):** a `student_deleted` row stores **id-only (`target_label=None`)**, preserving BP8e "delete means gone" (full-identity retention is a one-line override). **5 new files** (adapter · service · migration `0020` · `test_bp28b_admin_action_audit.py` · `lib/audit/actions.ts` + `components/ui/tabs.tsx`) + `domain/{models,ports}.py`·`db/models.py`·both single-writer services·`api/routers/{audit,students,staff,schools}.py`·`api/schemas/audit.py`·`wiring/{registry,container}.py` + 8 test-builder files (the actor-threading blast radius — mypy 176 files confirms no signature mismatch); FE `audit/page.tsx`·`lib/api/{types,endpoints}.ts`·`lib/hooks/use-audit.ts`·`app-shell.tsx`. BE ruff+mypy+**705 passed / 48 skipped** + layering (record + filtered reads + actor-email compose [deleted→None] + the effective-action hook placements; a **gated real-Postgres** round-trip: record→list/count→filters→newest-first→tenant scope + **actor SET NULL survival**, on a throwaway `bp28b_migtest`, dropped, dev `app` untouched); migration `0020` up→down→up on the throwaway; FE lint+tsc+`next build` green (`/audit` stays `○` static); 2× review loop (**R1 correctness SHIP — 0 blockers**: best-effort own-transaction safety / tenant scoping on reads + every write-hook / effective-action placements / no cross-seam join / actor-threading mypy cleanliness all verified → 1 NIT [`school_id` into both warning logs]; **R2 edge/a11y/copy — 0 blockers → 2 should-fix, both applied**: replace the hand-rolled `role="tablist"` `TabButton` with the app's Radix `Tabs` [arrow-key nav + tabpanel wiring], and render a muted **"Deleted student"** for a null-label `student_deleted` row [cell + CSV]). Honest limits: `student_deleted` id-only (name deliberately not retained); actor email is the CURRENT email (a convenience join, the denormalized role is the point-in-time fact); best-effort write (a failed audit is logged not retried — a transactional-outbox trail is the scale-up); the app-wide `toCsv` formula-injection + 10k client-side-export limits (BP28a); offset pagination. **BP28 is complete (28a + 28b).** **Next:** optional **A15** (teacher-cap visibility), then **BP29** teacher coherence · **BP30** review tools · **BP31** onboarding/copy — each through the full pipeline, committed+pushed on completion (autonomous).
- [0085 — Product Build BP28a: Access-log filters + date-range + CSV export](0085-product-build-BP28a-audit-filters-export.md) — the first slice of **BP28 (Governance & audit completeness)** (Round-4 **R4-A24** the access log had no filter UI [though event/student were wired] + **R4-A26** no CSV export). **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** **Backend** (the only genuine gap): `DownloadAuditRepository.list_recent`/`count_recent` (port + postgres adapter + fake) gain **date-range** (`created_from`/`created_to`) + **`actor_role`**, threaded via `AuditService.school_download_log` to `GET /v1/audit/downloads`. Two hardenings from the review: the date params are **typed `datetime`** + `actor_role` is **typed `Role`** so a bad value **422s at the boundary** (route passes `actor_role.value`); and the `actor_role` filter is a compare on the **denormalized `download_audit.actor_role` column, NOT a users JOIN**, so a row whose actor was later deleted (FK SET NULL) **still matches its role** (the audit outlives the account); `count_recent` mirrors `list_recent`'s predicates (honest total); no new index (existing `(school_id, created_at)` serves the range). **Frontend:** `audit/page.tsx` reworked to the students-page **URL-backed** pattern (an `AuditContent` inner wrapped in `<Suspense>` so `/audit` stays `○` static; `RoleGate` school_admin preserved), filters (event/student/**Role**/from/to/offset) in the URL with **offset reset in the same `set()`** on any change; Event+Student `<select>`s bounded to 200 (with an in-dropdown truncation hint), a Role select, a from/to date range (`<input type=date>` → inclusive **UTC** day-boundary ISO, hinted), and a **client-side CSV export** (`getDownloadLog` walked in bounded 200-row pages, capped 10,000 with a "first 10,000" toast, using the current filters; reuses `toCsv`/`saveCsv`). **11 files, no new files.** BE ruff+mypy+**670 passed / 47 skipped** + layering (a gated download-audit round-trip extended with a real-SQL date-range window + `actor_role` incl. **deleting the actor then re-filtering** to prove denormalized-column survival; the 2×422; filters-reach-the-service spy; count==filtered-total; the audit:view matrix) — verified on a throwaway `bp28a_migtest` (dropped; dev `app` untouched); FE lint+tsc+`next build` green (`/audit` stays `○` static); 2× review loop (**R1 correctness SHIP — 0 blockers/0 should-fix** [tenant safety / denormalized-`actor_role` deleted-actor survival / 2×422 / list-count parity / static prerender / bounded-tenant-scoped export all verified]; 2 NITs correctly out-of-scope [the picker 200-cap; CSV **formula-injection** — a pre-existing app-wide `toCsv` property NOT fixed here since the shared guard would corrupt `token_urlsafe` credential passwords]; **R2 edge/a11y/copy — 0 blockers → 4 fixes** [capped-export toast, picker-truncation hint, unified "Role" naming, "Date filters use UTC" hint]). Honest limits: UTC day-boundaries (local-day is the refinement); pickers show first 200 (typeahead is the scale-up); export bounded 10k (server streaming is the scale-up); CSV formula-injection is an app-wide shared-`toCsv` limit (low-risk, staff-set columns); offset pagination. **Next: 28b** — the **admin-action audit** (a new `admin_action_audit` table, **migration `0020`** mirroring BP8b, + write-hooks inside the single-writers so BP27's bulk loops are audited for free, + a second FE audit tab, reusing `audit:view`); **one owner decision flagged** — whether a `student_deleted` audit row may retain the deleted student's name/email (accountability) or store **id-only** (default, preserving BP8e "delete means gone"). Then optional **A15** (teacher-cap visibility).
- [0084 — Product Build BP27c: Bulk-photo retry/overwrite + bulk-remove-from-class](0084-product-build-BP27c-bulk-photo-retry-remove-class.md) — the third & **final** slice of **BP27 (Bulk operations parity)** (Round-4 **R4-A07** bulk-photo had no retry + silently overwrote enrolled students · **R4-A10** no bulk-remove-from-class), mostly FE. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** **Item 1 (bulk-photo retry-failed, FE, mirrors BP19d):** `use-bulk-photo-enroll.ts` gains a `useRef<Map<id,BulkEnrollInput>>` (rewritten per-batch in `run()`; `retryFailed` only looks up the current items so it always resolves a live handle), an extracted **`runPool(entries)`** (reads its **arg**, not `items` state — one shared pool impl), and **`retryFailed()`** re-running BOTH `error` AND enroll-failed `done` rows (`done && enrollmentStatus!=="enrolled"`, = `summary.failed`); a **"Retry failed (N)"** button gated `!isRunning && summary.failed>0` (**single-pool invariant** — mounts only after the prior pool's `Promise.all` drains), a parent `handleRetry` re-arming the dialog's `firedRef` list-refresh; re-running an enroll-failed row is orphan-safe (`setStudentReferencePhoto` overwrites + reaps the prior object); honest `no_face`-won't-self-heal copy. **Item 2 (overwrite-confirm / keep-existing, FE-only, no backend change):** `Row.keepExisting` (default **Replace** — flipping it would silently drop intentional uploads), a per-row **Replace|Keep existing** radiogroup only for `studentId && enrollmentStatus==="enrolled"` (native radios), **`start()` excludes kept rows** (so `setStudentReferencePhoto` is never called for them — the whole mechanism), an **`effectiveCount`** driving the "Upload N" label/disabled/"K kept" summary/empty-toast + a batch **overwrite `ConfirmDialog`** (naming `effectiveReplacing`) firing only when ≥1 enrolled would be replaced. **Item 3 (bulk-remove-from-class, BE+FE):** **`ClassService.remove_students_bulk`** (best-effort loop over the tested `set_student_group(None)` — **Option B**, since `set_group_bulk` can't clear; a foreign id 404s **before any write** → per-row `error`, batch never aborts; PII-safe logging; reuses `BulkActionResult` via a verified-layering-clean `services→services` import) behind **`POST /v1/students/bulk-remove-class`** (`student:manage`, `tenant_of`, `BulkIdsRequest`→`BulkActionResponse`, **before** `/{student_id}`); FE `bulkRemoveStudentsFromClass` + a **"Remove from class"** bar action (gated on `classes.length>0`, acts on `targetIds` [spans `all`-mode], `afterBulkMutation` since the class name is list-visible). **7 files** (incl. +8 bulk-remove tests in `test_bp27_bulk.py`). BE ruff+mypy+**664 passed / 47 skipped** + layering (removes-many/only-selected, foreign-id→`error`+batch-continues+**foreign-still-classed** [service + e2e route], round-trip-clears-class, 401/403/422, route-ordering guard); FE lint+tsc+`next build` green (`/students` stays `○` static; retry/overwrite carried by tsc + the mirrored BP19d invariant — no FE test harness); 2× review loop (**R1 correctness SHIP — 0 blockers/0 should-fix** [single-pool invariant, `firedRef` re-arm, retry set, keep-existing-no-upload, bulk-remove 404-before-write + route ordering, the nested modal-over-non-modal all verified] → 2 comment-accuracy fixes; **R2 edge/a11y/copy — 0 blockers → 2 fixes** [the "Remove from class"→**"Cleared class for"** toast honesty since the backend returns `ok` for a not-in-a-class no-op, and a more-visible radio focus ring]). Honest limits: retry re-uploads a fresh object (no self-heal for `no_face`); keep-existing default is Replace (opt-out per-row + the batch confirm); bulk-remove is a per-id loop (~800, capped 1000; set-based UPDATE is the scale-up; removing a not-in-a-class student is a harmless no-op `ok`). **27a/27b committed (`1c8f1d1`/`9aaba83`); 27c not yet committed (awaiting owner review). BP27 (Bulk operations parity) is now complete (a, b, c).** **Next:** the remaining Round-4 tiers — **BP28** governance/audit · **BP29** teacher coherence · **BP30** review tools · **BP31** onboarding/copy; a phase starts only on owner pick + scope re-confirm.
- [0083 — Product Build BP27b: Shown-once bulk credentials (student resend + staff CSV invite)](0083-product-build-BP27b-bulk-credentials.md) — the second slice of **BP27 (Bulk operations parity)** (Round-4 **R4-A05** no bulk credential resend + **R4-A13** no bulk staff invite), the **shown-once bulk-credentials** surface pulled out of 27a so the plaintext-secret path is designed + reviewed on its own. Two features sharing one FE `BulkCredentialsDialog`: **student bulk-resend-invite** (bulk BP18a recovery) + **staff CSV bulk invite** (bulk BP7d). **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** Both **return N plaintext temp passwords at once** → governed by a **shown-once secret contract** (plaintext in the response body only, on success rows only, generated per-row + hashed immediately by the reused single-writers, never persisted plaintext, never re-fetchable, **never logged**). Backend = two best-effort loops: **`StudentService.bulk_resend_invite`** (loops `resend_invite`; per row `{student_id, email, status: sent|error, temp_password?}`; **an error row carries `email=""` because `get_student` 404s a foreign id BEFORE any student is resolved — the loop never reads an email on the failure path**; not active-school-gated so recovery works on a suspended school; reuses the 27a `BulkIdsRequest` cap 1000) behind `POST /v1/students/bulk-resend-invite` (`student:manage`, before `/{student_id}`); **`OnboardingService.bulk_create_staff`** (teacher-only, no `role`; pre-loop suspension → 400; per email **two separate try/excepts** so the two `ValidationError` sources don't collapse — `validate_email`→`invalid`, `create_teacher`→`limit_reached`/`duplicate`[**message discarded, no cross-tenant leak despite global email-uniqueness**]/`error`; `temp_password` on `created` only; the **cap re-counts per call** so the loop fills to `max_teachers` then `limit_reached`; `_MAX_BULK_STAFF=100` const; `emails: list[str]` not `EmailStr` so one bad address is a per-row `invalid` not a 422) behind `POST /v1/staff/bulk` (`staff:manage` **admin-only**, 201, before `/{user_id}`). Frontend: a shared **`BulkCredentialsDialog`** (results table + emphasized **Download-credentials CSV** [`saveCsv` promoted to `lib/csv.ts`] + a **per-row Copy button** [R2] + a close-guard keyed on `results.some(r=>r.tempPassword) && !downloaded` so an all-error batch closes freely + `reset()` drops the passwords from state; `invalid`/`error` rows show the **server reason** [R2], a `duplicate` discloses "may exist at another school" [R2]); a **"Resend credentials"** students-bar action (clears selection, **no** list revalidate — resend changes no list field); a staff **CSV importer** (`bulk-invite-dialog.tsx`: pick→preview[advisory flags]→results→shared dialog) that **`mutate()`s the roster**. **12 modified + 3 new** (incl. `test_bp27b_credentials.py`, 21 tests). BE ruff+mypy+**656 passed / 47 skipped** + layering (password-only-on-success both endpoints service+route, the resend foreign-id `email=""`+no-password+batch-continues+foreign-untouched, staff duplicate[same+cross-school]/invalid/**cap-partial [max=2,1 existing,invite 3→1 created+2 limit_reached]**/suspended-pre-raise, **teacher-403-on-`/staff/bulk`**, student-403/401/over-cap-422/empty-422, route-ordering guard); FE lint+tsc+`next build` green (`/staff`+`/students` stay `○` static); 2× review loop (**R1 correctness+security SHIP** — 0 blockers/0 should-fix: shown-once VO defaults + every append path + no-secret-in-logs grep + `ConflictError`-discard + the `email=""` invariant + tenant scoping + cap re-count + admin-only gating all verified; **R2 edge/a11y/copy** — 0 blockers → 3 fixes [per-row Copy button, surface the server reason, global-`duplicate` disclosure]). Honest limits: shown-once no bulk re-fetch (single resend recovers); the global-uniqueness `duplicate` footgun (disclosed, not a leak); cap partial-success ("At capacity"); preview flags advisory (server authoritative); a large `all`-mode resend renders many passwords in one modal (Download-CSV is the save path). **27a committed (`1c8f1d1`); 27b not yet committed (awaiting owner review). Next: 27c** (bulk-photo retry/overwrite + remove-from-class — R4-A07/A10), the last BP27 slice.
- [0082 — Product Build BP27a: Bulk student lifecycle + select-all-matching](0082-product-build-BP27a-bulk-student-lifecycle.md) — the first slice of **BP27 (Bulk operations parity)**, the first Tier-1 phase off the Round-4 staff/admin review ([`product/08`](../product/08-product-review-round-4-staff-admin.md) **R4-A04**/**R4-A06**, roadmap [`product/09`](../product/09-improvement-roadmap-round-4.md)). At ~800-student scale, disable/delete were **one-at-a-time** and multi-select acted on the **loaded page only**. **BE + FE — no migration, no ML change, no new dependency, no new permission, no new env var.** Built by the owner-directed **multi-agent pipeline** (planning agent → plan-review agent [caught 2 pre-code blockers: the `delete_student` partial-failure window + `/ids` param-parity] → implementation agent → 2× review loop). **Backend** = pure best-effort loops over the tested single-writes: `StudentService.bulk_set_status`/`bulk_delete_students` (per-row try/except — a foreign id's `NotFoundError` or an ML-down `UpstreamError` → per-row `error`, **the batch never aborts**; PII-safe id-only logging) + `ListingService.list_student_ids` (the select-all-matching primitive — one call to the **existing** `StudentRepository.list_ids` with the identical filter args the page uses, so the id set is exactly what the list shows; no new repo method) behind `GET /v1/students/ids` (param signature = `list_students` minus sort/dir/limit/offset, incl. `resolve_focus_group_ids` for `mine` — an admin's `mine` ignored, no escalation), `POST /v1/students/bulk-status`, `POST /v1/students/bulk-delete` (all `student:manage`, `tenant_of(actor)`, in the literal block **before** `/{student_id}`); `_MAX_BULK_IDS=1000` caps the body (422 over / empty). **Bulk delete is best-effort — a deliberate contract difference from single-delete** (which surfaces an ML-down as 502): the batch records the row `error` and continues; `delete_student` is ML-first (no orphaned ML data) and the ML `DELETE` is **idempotent** (verified — 204 on an absent student), so an errored row **self-heals on retry**. **Tenant isolation is inherited, not re-implemented** — each looped single-write resolves the student via a `school_id`-scoped `get_student` **before any write**, so a foreign id is a per-row `error`, never a cross-tenant write; `/ids` reuses the identical tenant-scoped WHERE; **the `matches` seam is never SQL-joined** (delete loops the per-student ML RPC, select-all is an id-scan). **Frontend** extends the BP25 multi-select bar (not rebuild) with a discriminated **`Selection`** (`{mode:"ids"}` stale-safe [acted-on set ∩ loaded rows] | `{mode:"all"}` whole-server-snapshot spanning pages), the checkbox column widened to `total>0` (Assign stays class-gated), **Disable/Enable/Delete** (delete behind a count-echoing BP8e-accurate `ConfirmDialog`) + a **"Select all N matching"** control (→ `getStudentIds` → `mode:"all"`); selection resets on filter/search change (adjust-state-during-render) but **survives a re-sort**; every action runs `mutate()`+`mutateDashboard()`, clears the selection, and toasts honestly (partial "Disabled X of N — Y couldn't be updated/deleted"); a single **`bulkAction`** state guards double-submit **and** spins only the clicked button + an in-flight `role="status"` "· working…"; `getStudentIds` **reuses `listQuery`** (drift-proof "identical id set" invariant). **7 files + a new `test_bp27_bulk.py` (18 tests).** BE ruff+mypy+**635 passed / 47 skipped** + layering (foreign-id-untouched at the service **and** end-to-end route level, ML-failure isolation, retry-self-heal, `/ids`=the page's id set for a status filter+`mine`-scope+foreign-school exclusion, 401/403/422, the cap, the route-ordering regression guard); FE lint+tsc+`next build` green (`/students` stays `○` static); 2× review loop (**R1 SHIP** — tenant/no-abort/route-ordering/the FE `Selection` machine all verified → 3 fixes [an end-to-end cross-tenant route test, the `getStudentIds`↔`listQuery` drift, the `>_MAX_BULK_IDS` docstring]; **R2 no blockers** — delete copy BP8e-accurate, honest partial toasts → 4 fixes [the in-flight `role="status"` announcement, a spinner on the *clicked* Disable/Enable button, sort-no-longer-wipes-selection, "deleted" not "updated"]). Honest limits: bulk delete is best-effort + irreversible (no undo, BP16 parked); select-all is a click-time snapshot; `_MAX_BULK_IDS=1000` (a >1000-student school would 422 the follow-up — narrow the filter); a set-based bulk write is the scale-up. **Not committed (awaiting owner review). Next: 27b** (bulk-resend + staff CSV invite, shared shown-once credentials dialog — R4-A05/A13), then 27c (bulk-photo retry/overwrite + remove-from-class — R4-A07/A10).
- [0081 — Product Build BP26 (v1): Staff per-student "Download all" (the v1 distribution model)](0081-product-build-BP26-v1-staff-download.md) — the first phase off the **Round-4 (pre-release, staff/admin) review** ([`product/08`](../product/08-product-review-round-4-staff-admin.md) / roadmap [`product/09`](../product/09-improvement-roadmap-round-4.md)), closing its one Blocker **R4-D00** for v1. **Owner v1 pivot (2026-08-29):** **no student login in v1** — students don't use the app; the `(student)/me` surface + credentials + the in-app announce/"opened" loop go **dormant**, and **distribution is staff-mediated** (a teacher/admin **downloads a student's photos and shares them via WhatsApp**). So the review's blocker is no longer "no outbound email" (that original BP26 scope is **parked as BP12** post-v1) but that **staff had no per-student "download all"** — a single photo or an event's whole gallery, yes, but not *every photo of student X across all events* in one zip. **FE-only — no backend/ML change, no migration, no new dependency, no new permission, no new env var.** A new **`useAllStudentMedia(studentId)`** (`GET /v1/students/{id}/media` with **no `event_id`** = the full **effective** set, BP5 overlay; SWR key `students/{id}/media`, distinct from the per-event key) feeds a **`StudentDownloadAll`** button (in the **"Appears in"** section header of the student detail — so it only shows once the student is matched into ≥1 photo) into the existing streaming **`useDownloadAll`** (client-zip → File System Access API, buffered fallback), foldered by event/date (`{Event}/{date}-nnn.jpg` in `{Student-Name}-photos-{date}.zip`). Reuses the download entitlement — **both staff roles already hold `gallery:view_all`** and the per-photo mint is the same path staff use in the event gallery (**no widening, no bypass**). Honest states reused (cancelled→silent / all-failed→error / capped→"first N" / partial→"saved N of M" / full→success), copy harmonized with the sibling staff event-gallery surface; a11y mirrors the self-view (button-label flip + `sr-only` live region, not a visible per-tick one). **2 files** (`use-galleries.ts` + `students/[studentId]/page.tsx`). Honest limits: the non-streaming **500-cap** (BP9) on Firefox/Safari; "all photos" = the **effective** set (a `needs_review` photo is included; a rejected one excluded — can differ from the EngagementCard's raw `photos_appearing`); one extra lightweight list read per detail visit. FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**R1** correctness — 0 blockers: unconditional hooks, `entryBase`/`mediaIds` index-aligned, all download branches + throw-catch handled, entitlement reused-not-widened, no SWR-key collision, no `AppearsInSection` regression; **R2** edge/a11y/copy — 0 blockers → **2 should-fix applied**: the sr-only-vs-visible live region + the staff-copy harmonization; placement confirmed [keep it in the section header, not the crowded page-header row]). **Out of scope (documented):** outbound email/SMS (**parked BP12**), WhatsApp *integration* (v1 share is manual), the dormant student surface/credentials (untouched; a "v1 hygiene" cleanup noted in `product/09`), a per-student download on the gallery "By student" tab (easy follow-on). **Next:** the owner picks the next Round-4 phase (BP27 bulk-ops · BP28 audit/governance · BP29 teacher coherence · BP30 review tools · BP31 onboarding/copy) — a phase starts only on owner pick + scope re-confirm.
- [0080 — Product Build BP24b: Two-way doors (the FE error-survival sweep)](0080-product-build-BP24b-fe-error-survival-sweep.md) — the **FE half** of **BP24 (Two-way doors)** — Round-3 theme **P** ("batch work survives errors", [0064](0064-product-review-round-3-ux.md)), redeeming R3-A2-11 + R3-S3-06 + R3-S3-01 + R3-A2-10/A3-10; the backend "doors" (clearable tags + classes-at-scale) were **BP24a** ([0079](0079-product-build-BP24a-clearable-tags-classes-at-scale.md)). **FE-only — no backend/ML change, no migration, no new dependency, no new permission.** Three FE gaps where a batch op failed silently or hid the actionable state: **Slice 2 · the CSV error loop** (`bulk-import-dialog.tsx`) — a pure `flagRows()` pre-flags each parsed row **Ready / Duplicate / Invalid** (a light email regex + **in-file case-insensitive** first-occurrence dedupe) as a status pill + a live "N of M ready … K flagged will be skipped" `role="status"` summary (the **server still validates authoritatively** — this is a heads-up), and a **"Download skipped rows"** CSV on results (the non-`created` set via a shared `saveCsv`) so fixing 30 typo'd emails is fix-and-reimport, not hand-transcription. **Slice 4 · honest partial downloads** (`use-download-all.ts` + both call sites) — the result gains a **`cancelled`** flag (`streamToDisk` → `{saved, cancelled}`; a picker-dismiss is `cancelled`, distinct from all-fetches-failed `saved:0`; the buffered all-failed path still **throws**), and **both** the staff gallery + student `/me` unify on the honest branches: `cancelled` → silent, `saved===0 && !cancelled` → an **error** toast, `capped` → "the first N of M", `0<saved<total` → "saved N of M", full → success (staff) / silent (student) — so a cancel never reads as success and an all-failed run is flagged, not mistaken for a cancel. **Slice 5 · the residue** — create-teacher gains an `onCreated` → `useStaff().mutate()` (a new teacher appears without a reload, R3-S3-01), and the notify roster gets a reused **`FilterChips`** (All / Not opened, counts) + a `ROSTER_PREVIEW=12` collapse ("Show all N / Show fewer", derived each render, empty → "Everyone matched has opened") so "who needs a nudge?" is one click. Honest limits (documented): the CSV pre-flag is a **client heads-up**, the server's per-row verdict is authoritative; the roster is still **fetched whole** (collapse/filter is FE-only over a bounded roster — server pagination is the scale-up); the download honesty covers cancel/partial/capped/all-failed, and the non-streaming **cap** (500) is the pre-existing BP9 limit — a **capped+partial** run still reads "first 500" (`saved===0` is checked before `capped`, so all-failed correctly errors; only the mixed some-saved case slightly over-claims, matching the pre-BP24 student path). FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**both SHIP — 0 blockers, 0 should-fix**: **R1** — the shared-hook blast radius contained to exactly its 2 call sites, all 3 `DownloadAllResult` return paths correct, buffered all-failed still throws, `exitSelect()` only on a real save, the CSV pre-flag pure + index-aligned, the roster filter/collapse derived → 2 optional NITs not applied; **R2** — scoped `<th>`, text-labelled pills, `role="status"`/`alert` live regions, no focus loss on collapse, the all-invalid + 12-vs-13 + `saved===0`-before-`capped` edges, staff/student toast + flag-palette consistency → 3 NITs, 1 doc-only applied [the capped+partial copy edge]). **BP24 (Two-way doors) is now complete (a + b)** — the recommended Round-3 tail (BP18 → … → BP24) is done. Remaining open: the parked **BP12/15/16** + the BP6 video timeline ([`product/05`](../product/05-parked-backlog.md)) + the **blocked** BP22 slice 4 (student "This isn't me" safety); a phase starts only on owner pick + scope re-confirm.
- [0079 — Product Build BP24a: Two-way doors (clearable tags + classes at CSV scale)](0079-product-build-BP24a-clearable-tags-classes-at-scale.md) — Round-3 theme **P** ("mistakes become recoverable", [0064](0064-product-review-round-3-ux.md)), the **backend half** of BP24 (owner: two sub-phases; the FE error-survival sweep is **BP24b**). **No migration, no ML change, no new permission, no new dependency.** **Slice 1 · clearable event tags — a documented revision of the LOCKED decision 0027** (owner-approved): an event's three **tag** fields (`category_id`/`term`/`student_group_id`) become **tri-state** on PATCH — OMITTED → unchanged, explicit `null` → **cleared**, a value → set (the rest keep 0027's "None = unchanged"). A tiny **`UnsetType`/`UNSET`** enum sentinel (`domain/models.py`, mypy narrows `is UNSET`/`isinstance` cleanly) distinct from `None`; the route reads Pydantic v2 **`body.model_fields_set`** (no `exclude_unset` precedent — introduced, scoped to one route) to pass `field if provided else UNSET`; `EventService.update_event` + `EventRepository.update` (+ port + fake) type those three `str | None | UnsetType = UNSET`, the repo does `if x is not UNSET: row.x = req_uuid(x) if x else None`, and `_validate_*` validate **only a real id** so **a foreign id still 404s** while clear/UNSET skip. FE: the edit dialog always offers "No category"/"School-wide"/empty-term + sends `value || null` on change (`updateEvent` patch type `string | null`); create-event **stops silently preselecting "Other"** (L18). **Slice 3 · classes at CSV scale — BOTH shapes** (owner call): **(a)** an optional `class` column on the student CSV (`lib/csv.ts` header-detects it, absent → today's name+email) — `StudentService.bulk_create_students` gained a `groups` port + **resolves-or-creates the class by name once per distinct name** (a `class_cache` seeded from existing classes, case-insensitive) then `set_group`, **best-effort** (a class blip logs + the student stays `created`); **(b)** a **`POST /v1/classes/{id}/members/by-email`** (`ClassService.assign_students_by_email` validates the class → 404, trims/dedupes/lowercases, `resolve_by_emails`[BP10] → `set_group_bulk`[BP11a]) returning `(assigned, unmatched)` — FE `PasteEmailsDialog` (textarea → split; a 1000 FE cap mirror; the unmatched named for fix-and-retry). Honest limits: 0027 revised **not** repealed (only the 3 tag fields; description/date keep the old rule — a BP11b test updated); `model_fields_set` at exactly one route; class names aren't unique-by-name (the CSV auto-create dedupes **within a batch**; a rare concurrent second import could duplicate — documented). BE ruff+mypy+**617 passed / 47 skipped** + layering (+ `test_bp24_two_way_doors.py`: the clear/leave/set × 3-field truth table + a **route round-trip proving `model_fields_set`**; the CSV auto-create/dedupe/reuse/back-compat + **class-assign-failure-keeps-student**; the by-email matched/deduped/unmatched/foreign-404 + **foreign-school-email-unmatched tenant guard** + a route shape) + a **gated real-Postgres** clearable `postgres_events.update` tri-state round-trip (throwaway `bp24_test`, dropped; dev `app` untouched); FE tsc+lint+`next build` green; 2× review loop (**R1** correctness/tenant/tri-state — SHIP, 0 blockers/0 should-fix, the truth table correct in every cell → 1 NIT [double-strip hoist]; **R2** edge/back-compat/a11y/copy — 0 blockers → 3 should-fix [FE 1000-cap mirror, the foreign-school tenant test, the class-fail-keeps-student test] + 2 nits [unmatched box `role=status`, all-unmatched copy]). **Next: BP24b** (the FE sweep) → [0080].
- [0078 — Product Build BP23: Run it on numbers (instrumentation)](0078-product-build-BP23-instrumentation.md) — Round-3 theme **O** ("the owner runs blind", [0064](0064-product-review-round-3-ux.md)): the product collected the data to answer its own hardest questions and read almost none of it. **One phase, all four owner-chosen groups. Backend + FE. One migration (`0019`, `media.uploaded_by`); no ML change, no new permission, no new dependency.** **A · Attribution (R3-S6-04):** **`media.uploaded_by`** (nullable UUID FK→users **SET NULL**, no backfill) stamped at register from the route `actor.id` (a real plumb — `register_media` had no actor param — route→service→repo→domain VO, additive); **`users.last_login_at` exposed** on `UserResponse` + a new row-native **`UserSort.LAST_LOGIN_AT`** (`nulls_last()` both directions) → a staff "Last sign-in" column+sort; **`events.created_by` → `created_by_email`** resolved **in-Python** in a new `EventService.get_event_detail` (`EventDetail` VO — NOT a JOIN on the shared events select) → detail-route-only. **B · Flagship metrics (query-only, seam-free):** **Event reach** = **(announced ∩ opened) ÷ announced** — `NotificationReadRepository.distinct_opened_event_ids` intersected in-Python with the announced ids (an honest floor, never over-reports an opened-then-un-announced event; the true opens-÷-roster is scoped out — it'd need a roster-snapshot write or an N+1 over the ML seam); **distinct savers** (`count_distinct_saver_students` over `subject_student_id` — student self-saves only, staff excluded); a **first-opens trend** (`monthly_first_open_counts` over the immutable `notification_reads.created_at`, a decline-capable engagement line); a **"Quality" section** (`monthly_verdict_counts` → confirm-rate `confirmed÷(confirmed+rejected)` + a separate wrong-person rate; **`added` excluded from the precision denominator** — it's report-a-miss/recall, shown on its own). `AnalyticsService` gained `corrections`+`audit` ports; `SchoolAnalytics` gained `students_saved`/`events_opened`/`MonthPoint.first_opens`/`quality`. **C · Answers behind the numbers:** never-signed-in (`last_login_at IS NULL`) + never-opened (`NOT EXISTS` over `notification_reads` — a **same-schema anti-join, never the ML seam**) student filters (`ActivityFilter` enum `login`/`opened` params, threaded through `_filtered`→list/count/list_ids→`ListingService`; FE: one "activity" `<select>`) + a dashboard failed-enrollments deep-link; a **separate `GET /v1/students/{id}/engagement`** (`EngagementService` — NOT fields on the write-path `StudentResponse`) → a student-detail Engagement card; the notify **roster** gains `first_seen_at` (persistent ever-opened) + `download_count` (`NotificationService` gained an `audit` port). **D · Estate:** `SchoolFunnel` gains `created_at`/`not_started`/`days_to_first_delivery` (`first_distributed_at_by_school` − school age)/`stalled_since` (`last_event_created_at_by_school` = "no event since …", not `updated_at`); the FE funnel **sorts client-side** (aria-sort'd headers). **Migration `0019` verified up→down→up** on a throwaway `bp23_migtest` (dropped; dev `app` untouched); BE ruff+mypy+**604 passed / 46 skipped** + layering (+ `test_bp23_instrumentation.py` + **7 gated real-Postgres round-trips** on a throwaway `bp23_test`: uploaded_by round-trip + **SET NULL on uploader delete**, last_login sort, reach/first-open/first-seen, distinct-savers [staff excluded] + per-event downloads, monthly verdicts, estate first-distributed/last-event, activity filters — each tenant-scoped); FE tsc+lint+`next build` green (lists stay `○` static via Suspense); 2× review loop (**R1** correctness/tenant/cross-seam/async — SHIP, 0 blockers, all 8 aggregates tenant-scoped + no seam join + migration/plumb/additivity/async verified → 1 should-fix [Open-rate could read >100% if opened-then-un-announced]; **R2** numbers-honest/a11y/copy/edges — 0 blockers → the >100% edge **superseded by the honest announced∩opened intersection**, estate `aria-sort`, engagement labels "Events they're in"/"Photos they're in", quality cards gated on `adjudicated>0`, staff sort `nulls_last`, +2 tests). **Honest limits:** audience-weighted rate scoped out (reach = events-with-≥1-opener); saved/sign-in/engagement share the total-student denominator (a floor); trends are query-derived (no snapshot table); `stalled_since`=last-event; roster `seen` still resets on re-announce (`first_seen_at` persists beside it); the matching **model** work stays parked BP15 (BP23 ships the *metric* only). **Next: BP24** (two-way doors) — plus the blocked BP22 slice 4 whenever re-opened.
- [0077 — Product Build BP25: Floor sweep (all four groups)](0077-product-build-BP25-floor-sweep.md) — Round-3 theme **Q** ("the floor's thin patches", [0064](0064-product-review-round-3-ux.md)): a batch of small display/a11y/navigation gaps. **One phase, all four owner-chosen groups, FE-only** — no backend/ML change, no migration, no new permission, no new dependency (~41 files + 4 new). **A. AA + a11y floor:** the one High — a filtered script swapped **data-bearing** `text-ink-muted` (3.2:1, sub-AA) → `text-ink-secondary` (5.9:1) [69 spots/29 files, skipping any line with `size-`/`placeholder:`/`aria-hidden`/`hover:text-` so icons/placeholders/hover-buttons stay muted] · calendar `overflow-x-auto`+`min-w`+ a darker AA today badge · focus rings on 3 naked links · ≥24px tap targets (events checkboxes + toast dismiss) · lightbox visible key-hint + **suppress arrow-nav when a `<video>` is focused** (revises 0043) · per-page browser titles via a `useDocumentTitle` folded into `PageHeader` (+ 4 direct). **B. Design residue:** an auth brand moment (+ `<main>`) · **non-semantic** `--color-cat-*` category tokens (AA, distinct from status tones) · the frozen "matching now" spinner gets `animate-spin` · a mono temp-password. **C. URL state:** a shared `useUrlParams`/`useUrlListSort` puts q/sort/dir/status/category/term/class/mine/tab into the URL on the students/events/staff/schools lists (shareable + Back-safe), each split into a `*Content` inner wrapped in `<Suspense>` (stays static-prerenderable); the debounced search reads the URL for the fetch + an adjust-during-render Back-sync + writes only once the debounce settles. **D. Lows:** a skip-link + auth `<main>` · students bulk-select parity (checkbox column + "Assign to class" bar, reuses BP11a, gated on classes existing) · an offline hint (`useSyncExternalStore`) · search-match `<Highlight>` (`<mark>`, regex-escaped) on name/email cells · a foot "Download all" · scroll-restore is App-Router-native (documented). Honest limits: scroll-restore doesn't survive the infinite-list reset; URL search lags by the debounce; two custom categories can hash-collide; highlight is name/email-cell only; bulk-select is class-assign on the loaded page; the quiet input border is left as-is; titles are client-side. FE lint+tsc+`next build` green (lists stay `○` static via the Suspense boundary); no BE/ML delta; 2× review loop (**R1** — URL-state loop-safety [4 pages] / bulk-select stale-safe / token-swap correctness / Suspense / smaller pieces → 1 should-fix [the debounced-search **Back-bounce** re-adding a cleared `q` ~300ms → fixed to write only once the debounce settles]; **R2** — AA math [6 category pairs / today badge / mark / offline bar all pass] / a11y / edges → **3 should-fix** [gate the students checkbox column on classes existing; highlight the applied query not the leading input; lift the skip-link above the offline bar] + the honest-limits list). **Next: BP23** (instrumentation) → BP24, plus the blocked BP22 slice 4.
- [0076 — Product Build BP22: Review loop, armed (slices 1–3)](0076-product-build-BP22-review-loop-armed.md) — Round-3 theme **N** ("put the evidence where the decision is", [0064](0064-product-review-round-3-ux.md)), slices **1–3** (R3-A3-02/08 + R3-S2-02/A3-09); **slice 4 — the student "This isn't me" safety — BLOCKED by the owner (a separate future decision)**. **One phase, FE-only** — no backend/ML change, no migration, no new dependency, no new permission. The teacher's review loop was under-armed: the lane showed **name + % but no picture of the student** (guessing → wrong confirms leak wrong photos), review debt was invisible at the Announce moment (auto-announce on by default), and the triage round-trip reset itself (uncontrolled Tabs, an unlinked list pill, "Open photo →" losing your place). Three fixes: **(1)** a new **`StudentRefAvatar`** (reuses the teacher-permitted `useStudentReferencePhoto` + `StudentAvatar` already on the students list; SWR-deduped per student, 404→initials, decorative `alt=""`) rendered before the name in the **review tile** + the staff **`AppearanceRow`** + read-only **`AppearanceList`** — every "is this the right person?" now shows the person (**staff-only** — students pass `showAppearances={false}` + the endpoint is `student:manage`-gated, verified defense-in-depth); **(2)** the **DistributionCard** fetches `useEventReview` for an **overlay-correct** "N matches to review" link (→ `?tab=review`) + an announce-time **`ConfirmDialog`** ("N still need review — announce anyway?", no hard block) when debt > 0; **(3)** the gallery **Tabs go URL-addressable** (`?tab=` via `useSearchParams` on the dynamic route — deep-linkable + Back-safe), the events-list "N to review" **pill becomes a `<Link>`** to the review lane, and the tab badge is **fixed to count pairs** (Σ candidates) not photos. Honest limits (documented): the events-list pill stays a **raw-ML** count (BP5 divergence) while the DistributionCard + tab badge use the overlay-correct count — the same event can read a different number on adjacent screens; the dashboard "N to review" is a school-wide approximation; a per-student ref-photo mint per distinct face in the lane (bounded, like the students list); the announce-confirm is a soft advisory (stale count if a colleague clears the lane mid-flight). FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**R1** — the staff-only privacy boundary [to the backend perm], the per-student dedupe/404, the URL-tab [no loop/Back/garbage→all], single-`onNotify` announce-confirm, overlay-correct count, no other-caller regression → **0 blockers**, 1 comment nit; **R2** — decorative-avatar a11y / pluralized non-alarmist confirm copy / truncate-safe tile / edges → **0 blockers**, 2 touch-ups + the honest-limits list). **Next: BP25** (floor sweep) — plus the blocked slice 4 whenever re-opened.
- [0075 — Product Build BP20: The arrival moment (+ BP20b student chrome)](0075-product-build-BP20-arrival-moment.md) — Round-3 theme **L** ([0064](0064-product-review-round-3-ux.md)): the student receive surface got mechanics (BP3/9/17) but not the *moment* — a "3 new" badge opened on **3-year-old** photos, the banner pointed nowhere, unseen flags **burned on load**, the badge **froze** once-per-session, ~60 events were a **flat pill wall**, photos had **no story**, and "Download all" was an unlabeled pile that silently kept the **oldest 500**. **One phase, FE-only** (two Explore passes confirmed the backend already serves everything — no backend/ML change, no migration, no new dependency, no new permission). Owner calls: **full scope** (moment-fix **+ BP20b** the warm student reskin), **arrive-to-clear** mark-seen, **one phase**. Seven pieces: **(1)** newest-first — FE-`reverse()` the whole-list events + media (backend serves asc, media has no timestamp so reverse = newest-uploaded first); **(2)** an **actionable banner** (a one-visit `newEvents` snapshot of unseen events as filter buttons, capped 6 + "+N more") with mark-seen moved **off mount** to fire once after the photos **successfully render** (not before they're seen); **(3)** **un-freeze** — `useMyNotifications`+`useDashboard` opt into per-hook `revalidateOnFocus`+60 s poll (SWR dedupes the shared keys); **(4)** a **year-grouped `<select>`** (`EventFilter`) beyond 8 events (undated → "Other"), chips below; **(5)** **photo story** — an optional `caption` "{event} · {date}" on `GalleryItem` threaded to the lightbox line + `alt` + the tile accessible name + a masonry hover scrim (back-compat: staff pass none); **(6)** **named saves** — `useDownloadAll(mediaIds, {entryBase, zipName})` → `{event}/{date}-{nnn}` in `my-photos-{date}.zip`, single saves by event, an **honest sticky** capped toast (leads with filter-by-event; names desktop Chrome/Edge since iOS Chrome is WebKit), `toast({sticky})` + `sanitizeFilename` added; **(7)** **BP20b** — `AppShell` branches for `student` to a warm slim top-bar+drawer over a new `bg-canvas-warm` token + a `display-xl` hero (staff/platform chrome untouched; resize-close guarded off). Honest limits (documented): mark-seen on successful render only; the banner is a one-visit snapshot (a mid-session announcement lights the badge, not the banner, till reload); reverse-order = no true `created_at`; the 500-cap fallback + no failed-subset retry; per-event zip folders; warm wash is student-only/light-only. FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**R1** — reverse/index alignment [incl. buffered slice], arrive-to-clear one-shots [no double-mark/loop], poll×401-interceptor, staff back-compat, `sanitizeFilename` [no char-range bug], lazy-`Date`, BP20b branch all verified → **0 blockers**, 1 documented should-fix [mark-seen-on-error asymmetry]; **R2** — a11y/copy/consistency/AA-on-warm/edges → **0 blockers**, **3 fixes** [banner `role="group"` not a live-region-over-buttons + per-button names + 6-cap; capped-toast guidance universal-remedy-first + desktop Chrome/Edge; masonry scrim `aria-hidden`] + softer partial copy). **Next: BP22** (review loop, armed).
- [0074 — Product Build BP21b: Error truthfulness](0074-product-build-BP21b-error-truthfulness.md) — the second & **final** slice of **BP21 (Say what it does)** — Round-3 theme **M** ([0064](0064-product-review-round-3-ux.md)), redeeming R3-S3-06/08/09/10; **FE + the BFF proxy only, no backend/ML change, no migration, no new dependency, no new permission.** Four misdirected error states fixed in one place: **(1)** a **shared 401 interceptor** (`components/swr-provider.tsx` SWR `onError`) — a mid-session 401 on **any data key** (session dead, BFF already cleared cookies) revalidates `"auth/me"` so the `AuthGuard` bounces to `/login?reason=expired` **instead of a dead Retry** (the `"auth/me"` key is skipped so it can't loop); **(2)** **422 field parse** (`lib/api/client.ts` `errorMessage`/`parseValidationDetail`/`fieldFromLoc`) turns FastAPI's `detail:[{loc,msg}]` into "Email: value is not a valid email address" (capped at 3, throw-safe) instead of "Unprocessable Entity"; **(3)** **429 forward + humanize** — the BFF proxy now forwards **`Retry-After`** (was content-type only), `bffFetch` reads it onto a new optional `ApiError.retryAfter` and `formatRetryAfter` renders "try again in N seconds/minutes" (BP8c sends integer seconds); **(4)** **generic 5xx** — `status >= 500` shows "Something went wrong on our end — please try again in a moment." never the raw `str(exc)`, while 4xx keep their actionable `detail`. Folded into `bffFetch` so all ~40 `toast(err.message)` sites benefit with **zero call-site changes**. Honest limits (documented): `Retry-After`-as-HTTP-date degrades to "in a moment" (BP8c never sends one); internal field names *could* surface in a 422 but only email is a reachable user-typed 422; the 5xx path drops the backend detail by design; three co-existing backend-fault phrasings (AuthGuard / EmptyState / toast) a future consolidation. FE lint+tsc+`next build` green; no BE/ML suite delta; 2× review loop (**R1** — loop-safety [traced the SWR 2.4.2 runtime + the refresh boundary + all ~40 consumers], redirect incl. stale-`data`, login/change-password-not-SWR, 422 throw-safety, no regressions — **zero findings**; **R2** — copy/a11y/consistency/edges clean → **1 fix** [minute-format a large `Retry-After`] + the honest-limits list). **BP21 (Say what it does) is now complete (a, b).** **Next: BP20** (the arrival moment).
- [0073 — Product Build BP21a: Say what it does (copy + explainer pass)](0073-product-build-BP21a-say-what-it-does.md) — the first slice of **BP21 (Say what it does)** — Round-3 review theme **M** ([0064](0064-product-review-round-3-ux.md)), sliced **a** (all copy/UX, this) + **b** (error truthfulness, next). **FE-only — no backend/ML change, no migration, no new dependency, no new permission.** The product named its two pipelines in **seven words** (matching = "Process"/"Redistribute"/"Distribution"/"Processing"; announcing = "Notify"/"Distribute"/"Delivered") — the **overloaded "Distribution"** meant *both*, so "click Process → read 'Distribution started.'" read as "students were told" (they weren't). Swept **one grammar = Match / Announce** across every surface (the `PROCESSING_LABEL` pills [`lib/events/status.ts`], the event detail + DistributionCard, the events list, dashboard alerts + checklist, analytics "**Announce rate**"/"**Announced**", estate, the tab title→"Photos"); internal identifiers (`processing_status`/`DistributionCard`/`notifyStudents`) deliberately untouched. Plus the honesty fixes: a **true privacy scope** (not "only you" — staff have `gallery:view_all`); a new **`(help)/how-matching-works`** explainer (all-roles guard) + an inline **confidence legend** (`hasConfidence`-gated) in both appearance components, linked from the student hero/empty-state + the staff review lane; the **erasure dialog** now tells both sides (destroys match history · keeps event photos · anonymizes download records, per [0053](0053-product-build-BP8e-student-erasure.md)); the **audit** caption truthful ("saves in the app, not views") + a teacher download disclosure (per [0050](0050-product-build-BP8b-download-audit.md)); a photoless student → **"No photo yet"** (shared `enrollDisplay`) not "Pending"; the schools-list "Active"/"Suspended" pill, a dropped stale "(soon)", and the 30 MB upload hint. **Announce stays honestly in-app** ("in-app only for now"; outbound is parked BP12). FE lint+tsc+`next build` green (`/how-matching-works` prerenders static); no BE/ML suite delta; 2× review loop (**R1** — rename-completeness / route+link+guard / `enrollDisplay` / honesty-vs-0050/0053 all clean → 1 fix [an audit header↔cell mismatch]; **R2** — explainer / a11y / null-confidence edge / cross-surface consistency clean → 3 small fixes [roster pluralization, "Student (self)" header, tighter audit copy + a warmer re-enroll toast]). **Next: BP21b** (error truthfulness).
- [0072 — Product Build BP19d: Upload survival](0072-product-build-BP19d-upload-survival.md) — the fourth & **final** slice of **BP19 (Pipeline resilience)**, **completing it (a,b,c,d) → Round-3 Critical #2 fully closed.** Redeems R3-A2-09/A3-06/S3-05: the event uploader could lose an in-progress upload with **no warning** (the FE had **zero** `beforeunload` handlers) and had **no retry** for a file that failed mid-batch except re-picking everything. **FE-only — no backend/ML change, no migration, no new dependency, no new permission.** `useMediaUpload` now **retains each `File` handle** (`useRef<Map<id,File>>`, append-only — `File`s are cheap references) so a new **`retryFailed()`** re-runs the bounded pool over **only** the `error` items (extracted `runPool` shared with `add`); a **"Retry failed (N)"** button shows when `failed>0 && !isUploading` (no overlapping pools). A reusable **`useBeforeUnload(active)`** hook guards tab-close/reload/external-nav while uploading; the "Back to event" button (was `disabled` during upload — a trap) now routes through a **`ConfirmDialog`** ("Leave while uploading?") so the user can leave deliberately, not by accident. Honest limits (documented): **in-app breadcrumb / browser-Back nav is NOT guarded** (Next 16 App Router has no stable nav-blocking API — `beforeunload` doesn't fire for client-side nav); leaving via **in-app** nav doesn't lose photos (the detached `Promise.all` pool finishes all uploads in the background — only a tab-close kills them, which `beforeunload` guards); the `useBeforeUnload` rollout to the other long-ops (`useDownloadAll`/`useBulkPhotoEnroll`) is a deferred follow-on. FE lint+tsc+`next build` green; 2× review loop (**both SHIP, no code blockers**: R1 verified the single-pool invariant / unmount-safety / textbook `beforeunload` / clean types — 0 fixes; R2 → corrected the confirm copy [the pool keeps uploading in the background on in-app nav, so it doesn't imply loss] + the honest-limit doc). **BP19 (Pipeline resilience) is now complete (a, b, c, d).** **Next:** the owner picks the next Round-3 phase (recommended: BP21 → BP20 → …).
- [0071 — Product Build BP19c: Stall + second-batch + failed-in-dashboard visibility](0071-product-build-BP19c-stall-second-batch-failed-visibility.md) — the third slice of **BP19 (Pipeline resilience)**, the **display half** of theme K: redeems **R3-S1-03** (a stuck event looked healthy), **R3-S1-04** (a "second batch" of new photos on a completed event was invisible), **R3-S1-10** ("All processed" over failed photos). **BE + FE; no migration, no ML change.** **(1)** The event detail shows **"Processing since {enqueued_at}"** (the age was delivered but never rendered) + escalates past a threshold ("taking longer than usual") + surfaces the stale-in-flight **Retry** (19a's guard) — the threshold a `NEXT_PUBLIC_EVENT_INFLIGHT_STALE_S` mirror; "now" tracked in a lazy-`useState`+30s-interval (never read impurely in render). **(2)** A shared pure **`derivePillStatus(processing_status, {total, pending})`** (`lib/events/status.ts`) drives BOTH the event **detail** (refactored to it) and the events **list**, so a `completed` event with new `pending` photos reads *unfinished* not "Completed"; the row carries `pending` via new `MediaRepository.pending_counts_by_event` → `EventListing.pending` → `EventListItem.pending` (threaded through both list_events_page paths); the dashboard alert `count_not_started_with_media` → **`count_active_with_pending_media`** (active, **not-in-flight**, EXISTS ≥1 pending media — catching a second batch). **(3)** `SchoolDashboard.photos_failed` → `MediaSummary.failed` + `NeedsAttention.photos_failed`; the FE `photosHint` stops saying "All processed" over failures + a **"N photos failed processing"** alert. Honest limits (documented): the FE stale threshold is a `NEXT_PUBLIC_*` mirror (keep `>=` the backend guard); the "distribute" vocabulary stays BP4-era (R3's one-grammar cleanup is BP21 — 19c widened the *predicate*, and fixed the one now-false alert description). BE ruff+mypy+**590 passed / 39 skipped** + layering + a gated real-Postgres `count_active_with_pending_media`/`pending_counts_by_event` round-trip (throwaway `bp19c_test`, dropped); FE lint+tsc+`next build` green; 2× review loop (**both SHIP, no blockers**: R1 traced the tenant-scoped SQL / both-path threading / faithful `derivePillStatus` / hooks-safe impure-render fix / contract-consistent thresholds; R2 → the now-false-copy fix + an in-flight-not-flagged service test). **Next: BP19d** (upload survival — FE-only).
- [0070 — Product Build BP19b: Failure metrics](0070-product-build-BP19b-failure-metrics.md) — the second slice of **BP19 (Pipeline resilience)**, redeeming Round-3 **R3-S1-02** (the failure half of the pipeline emitted **zero metrics**) — **ML-service only, no migration, no backend/FE change, no ML-contract change**. Exploration found **the inference worker had no `/metrics` server at all** (only the ML *API*, a separate process, served `/metrics` — so even the worker's existing job-outcome counters went unscraped). Adds **`start_metrics_server(ML_WORKER_METRICS_PORT)`** (default 9100, `prometheus_client.start_http_server` in the worker `main()`, **fail-safe** — a port clash logs + continues) exposing the default registry (existing counters **and** the new failure metrics); the API keeps its own `/metrics`. New metrics: **`photos_failed_total`** (folded into `record_job_outcome`), **`jobs_failed_total{reason}`** (a dead-lettered job, from the DLQ consumer), **`embedding_version_mismatch_total{school_id}`** (the stale-index "ALERT", now countable — fires per delivery attempt, a rate signal), + **`dlq_depth`**/**`inflight_oldest_age_ms`** gauges refreshed each DLQ sweep. Queue stats via new `JobQueue.dead_letter_depth()` (redis `XLEN`) + `oldest_pending_age_ms()` (redis `XPENDING` → oldest pending id's `<ms>` → `now−ms`, clamped, `None` idle); inproc stubs. New `ML_WORKER_METRICS_PORT` env + compose port. Honest limits (documented): worker+API are **separate scrape targets**; multi-replica workers each report the same shared `dlq_depth` → aggregate with **`max()` not `sum()`**; a bind failure degrades silently to no-metrics; the mismatch counter counts attempts (~5×/stuck job). ML ruff+mypy+**145 passed / 14 skipped** + layering + a gated real-Redis queue-stats round-trip; 2× review loop (**both SHIP, no blockers**: R1 confirmed cardinality/async/fail-safe sound → applied a drain-before-gauge isolation [a gauge-read failure must never starve the DLQ drain]; R2 → the runner-wiring tests + the help-string honesty). **Next: BP19c** (stall + second-batch + failed-in-dashboard visibility — BE + FE).
- [0069 — Product Build BP19a: Unstick + visible "Failed" event](0069-product-build-BP19a-unstick-visible-failed-event.md) — the first slice of **BP19 (Pipeline resilience)**, the fix for Round-3 **Critical #2** ([0064](0064-product-review-round-3-ux.md), theme K): a processing job that permanently fails is retried 5× then **dead-lettered to a Redis stream with no consumer**, stranding the event at `processing_status='processing'` forever (UI spins "Distribution is running", API refuses re-enqueue — only out-of-band SQL could rescue it). Mirrors **BP8a** ([0049], which gave *media* a `failed` state): **migration `0018`** widens `ck_events_processing_status` to add `'failed'` (`EventProcessingStatus.FAILED`); the ML worker gains the **DLQ consumer that was missing** — `BackendEventStore.mark_event_failed` (tenant-scoped, state-guarded so a stale re-drain never clobbers a `completed` event) + `JobQueue.drain_dead_letters`/`remove_dead_letter` (`DeadLetter{job,reason,receipt}`) + a `WorkerRunner._dlq_loop` running alongside the consume loop (`asyncio.TaskGroup`) that marks each dead-lettered event `failed` **then** removes the entry (mark-before-remove, crash-safe). Backend `process_event`'s guard is widened so a **`failed`** event (Retry) OR one **stuck in-flight past `BE_EVENT_INFLIGHT_STALE_S`** (default 30 min, the fallback for a lost job) re-enqueues — no SQL ever needed; a `failed` event always retries + self-heals to `completed`. FE: a `Failed` pill + Retry button + failure note (event detail + list). **Cross-service (BE + ML worker); no ML-contract break, no new dep, no new permission.** Migration `0018` up→down→up on a throwaway Postgres (dev `app` untouched); BE ruff+mypy+**586 passed / 39 skipped** + ML **139 / 13** + layering + gated real-Postgres (`mark_event_failed` + no-clobber) + gated real-Redis (DLQ drain/remove/malformed); FE lint+tsc+`next build` green; 2× review loop (**R1 caught a real crash-window clobber bug** → the state-guarded `mark_event_failed`; **R2** → closed the Retry dead-end + the gated Redis test + `ML_DLQ_POLL_S` + bounded DLQ read). Honest limits documented (migration-precedes-worker; ~30s drain latency; a rare transient flap; multi-replica redundancy; orphaned-object reaping deferred). **Next: BP19b** (failure metrics — the worker `/metrics` endpoint).
- [0068 — Product Build BP18d: Session revocation + student disable](0068-product-build-BP18d-session-revocation-student-disable.md) — the fourth & final slice of **BP18**, **completing it (a,b,c,d)** and closing Round-3 **Critical #1**. **The one BP18 migration (`0017`).** **Part A — "log out everywhere":** a per-account `users.token_version` counter (migration `0017`, additive+reversible) + a `tv` claim on **both** JWTs; `UserRepository.set_password(revoke_sessions=True)` bumps it **in the same UPDATE** as the hash, so BP18a's student resend, BP7c's staff resend, and `change_password` all revoke old tokens; `get_current_user` **and** `refresh` reject a stale `tv` → 401 (closing BP18a's ≤14-day refresh-token gap). Two traps handled: `_maybe_rehash` passes `revoke_sessions=False` (a login-time rehash must not invalidate the token it just issued), and `change_password` **re-issues a fresh pair** (reload → new `tv`) so a self-change isn't a self-logout — the route goes **204→200 `TokenResponse`** and the BFF swaps cookies. **Part B — student disable (no migration):** `StudentService.set_status` (1:1 of `set_staff_status` — `get_student` tenant-404 **before any write**, flips the linked login via `users.set_status`, idempotent, no last-admin guard since a student locks out only themselves) behind `PATCH /v1/students/{id}/status` (`student:manage`); `Student.status` rides the **existing** users JOIN → `StudentResponse.status`. FE: a **Disable/Enable login** action (disable behind a `ConfirmDialog`) + a **"Login: Active/Disabled"** pill on the detail + a **"Login disabled"** list pill (labelled so it never reads as an enrollment state). **Honest limit:** disable is enforced by the per-request status reload + refresh's status check (not `tv`) — so a disabled student's live access token dies next request and their refresh token next refresh (no gap); the two checks are independent (status first, then `tv`). **Migration `0017` up→down→up on a throwaway Postgres** (`bp18d_migtest`, dropped; dev `app` untouched); BE ruff+mypy+**583 passed / 38 skipped** (+6: disable↔enable/idempotent/tenant-404 + a disable→login-401→re-enable e2e + a disabled-student's-live-token→401 + unknown-404) + a gated real-Postgres `token_version` bump/no-bump-on-rehash + student-status-JOIN round-trip + layering; FE lint+tsc+`next build` green; 2× review loop (**R1 SHIP no blockers** — revocation complete on both paths, rehash+self-reissue+tenant-404 all verified; **R2 SHIP** → the "Login disabled" relabel + the live-session-kill test + the honest-limit notes). **No ML change, no new dep, no new permission. BP18 is complete.**
- [0067 — Product Build BP18c: School-record lifecycle](0067-product-build-BP18c-school-record-lifecycle.md) — the third slice of **BP18**, closing the Round-3 finding that the **school record was write-once** (R3-A1-01): no rename / teacher-cap change / suspend in the API or UI, yet `suspended` was a rendered status **and** enforced downstream. Adds `PATCH /v1/schools/{id}` (name / max_teachers / status), platform-only, **no migration** (the columns were already mutable). BE: `SchoolRepository.update` (port + adapter + fake — a partial update where only the provided non-None fields change, `status.value` written, `updated_at` trips) + `OnboardingService.update_school` (reuses create's validation only for provided fields; a missing school → 404 since `school_id` is a client path param on this cross-tenant platform surface) + `PatchSchoolRequest` (all-optional; `Field(ge=1)`/`min_length` + the `SchoolStatus` enum → 422 before the service). **No guard on suspend** (unlike BP18b's last-admin disable): it's reversible + removes no one's access, just gates new creates (already enforced); lowering the cap below the current teacher count is likewise allowed (checked only at create). FE: an **Edit-school** dialog (name + cap, re-prefilled on open) + **Suspend/Reactivate** (suspend confirms first, reactivate immediate) on the platform school detail — both revalidate `useSchool` via `mutate()` (the rollup-less PATCH response → revalidate, not cache-set); the status pill now shows "Active"/"Suspended" not the raw enum. **No migration, no ML change, no new dep/permission.** BE ruff+mypy+**576 passed / 38 skipped** (+8: 5 service, 3 route incl. a schema 422) + layering; FE lint+tsc+`next build` green; 2× review loop (R1 clean; R2 SHIP → the 422 test + a cap-lowering docstring clause). **Next: BP18d.**
- [0066 — Product Build BP18b: Credential-safety guards, self-service & the student name](0066-product-build-BP18b-credential-safety.md) — the second slice of **BP18**, the safety net around BP18a's student recovery ([0065](0065-product-build-BP18a-student-credential-recovery.md)); closes the surrounding Round-3 theme-J gaps, **no migration**. Four pieces: **(1)** an additive `name: str | None` on `UserResponse` (null on every other read) — `/auth/me` resolves it for a `student` via the already-wired `student_repo().get_by_user_id` (tenant-safe: both args from the caller's token; missing-profile / staff / platform → null, never a 500); the shell footer shows it. **(2)** a **last-active-admin guard**: `OnboardingService.set_staff_status` refuses disabling a school's only active admin via a new `UserRepository.count_active_by_school_and_role` (port + adapter + fake) — only for `SCHOOL_ADMIN` + `→disabled` + active target + `count ≤ 1`, after the no-op check; never blocks enabling/teacher-disables; catches "1 active + 1 disabled" (`ValidationError`→400, surfaced as a toast — the FE deliberately does **not** pre-disable the button, an a11y call). **(3)** **shown-once close-guards** — the `InviteResultDialog` (copied?) + bulk-import results (downloaded?) route every close path (Esc/overlay/X/Done) through a `ConfirmDialog` so a stray dismissal can't lose the one-time credential. **(4)** a **resend-invite confirm** when the target is already signed in (`active && !must_change_password`) — resend replaces a working password; awaiting-sign-in/disabled resend freely. Plus a reachable **"Change password"** link in the shell footer. **No migration, no ML change, no new dep, no new permission** (one behavior change: disabling a sole active admin now 400s — a pre-existing route test gained a 2nd admin). BE ruff+mypy+**568 passed / 38 skipped** (+7: 5 last-admin service, a route sole-admin→400, a `/me`-name e2e) + layering; FE lint+tsc+`next build` green; 2× review loop (R1 SHIP no blockers; R2 SHIP → the route 400 test + the a11y BE-guard-+-toast simplification). **Next: BP18c.**
- [0065 — Product Build BP18a: Student credential recovery](0065-product-build-BP18a-student-credential-recovery.md) — the first slice of **BP18 (Account recovery & credential safety)**, the fix for Round-3 **Critical #1** ([0064](0064-product-review-round-3-ux.md), theme J): a student who forgot their password had **no recovery** — the only remedy (delete/recreate) destroyed their photo history (BP8e purges `matches` + the worker skips `completed` media). A **1:1 port** of the BP7c staff/admin resend, **no migration**: `StudentService.resend_invite` (`get_student` tenant-scoped → **404 before any write** → `generate_temp_password` → `users.set_password(must_change_password=True)` → the existing `ProvisionedStudent`; touches **only** the password — no ML delete / profile / enrollment change, so photos + matches survive) behind `POST /v1/students/{id}/resend-invite` (`student:manage`, school from the token, `ProvisionedStudentResponse`); intentionally not active-school-gated (recovery must work regardless of status). FE: a **"Send new password"** action on the student detail (reuses the shown-once `InviteResultDialog`) + a login "Forgot your password? Ask your school" line + a mid-session-expiry cue (`auth-guard` → `/login?reason=expired`, read via `window.location.search` in a `useEffect` — not `useSearchParams`, so `/login` stays statically prerendered). **Honest limit (until BP18d):** a resend kills the old password at login but an already-issued refresh token survives ≤14 days. **No migration, no ML change, no new dep, no new permission.** BE ruff+mypy+**561 passed / 38 skipped** (+5 BP18a incl. an auth-layer login round-trip proving old-pw-dies/new-pw-works) + layering; FE lint+tsc+`next build` green; 2× review loop (R1 no blockers → pre-write-snapshot comment; R2 SHIP → the login round-trip + suspended-school docstring). **Next: BP18b.**
- [0064 — Product review Round 3 (UX-first, all roles) + the BP18–BP25 roadmap proposal](0064-product-review-round-3-ux.md) — a **static, code-grounded, multi-agent** review post-BP9–17 (12 agents: 4 persona walks at first-run + Greenfield scale · a **leads verifier** [25 inherited rough-edge leads → 10 confirmed / 4 partial / **11 refuted** incl. four invented UI elements, recorded so no future round re-chases them] · an **R2 re-tester** [every `product/02` claim → **32 RESOLVED / 5 PARTIAL / 2 UNRESOLVED / 4 PARKED / 0 REGRESSED**] · 6 senior-PM sweeps), every finding file:line-evidenced, absence claims grep-trailed, runtime-feel capped at High as `unverified-runtime`, then a 2× doc loop (3 citation verifiers re-opened ~230 citations — zero findings killed; a cold-read coherence pass). **Two Criticals**: **(J)** student credential loss has **no recovery path** and the only remedy (delete/recreate) permanently destroys the child's photo history (BP8e purges matches + the worker skips completed media — nominated independently by 3 agents); **(K)** a dead-lettered/lost event job strands the event in "Distribution is running" **forever** (no DLQ consumer, re-enqueue refused, zero failure metrics). 18 Highs incl. the inverted student arrival moment (oldest-first + a once-per-session badge), the product misdescribing itself (7 words for one pipeline; "Only you can see these" false; face recognition explained to no one), the under-armed review loop (no reference face; debt invisible at announce while auto-announce defaults on), and the owner running blind (delivery rate = the announce button; `match_corrections` never aggregated; media has no uploader column — a closing window). Design-bar re-judge: Linear delivered · Stripe substantially closed · Pinterest half-closed (grid yes, chrome no). New docs **[`product/06`](../product/06-product-review-round-3-ux.md)** (review, themes **J–Q**) + **[`product/07`](../product/07-improvement-roadmap-round-3.md)** (**BP18–BP25**, recommended order 18→19→21→20→22→25→23→24) + `product-review-r3.html` (exec summary). **Owner scope:** parked items (BP12/15/16, video timeline) **excluded** — cited inline only, never re-ranked; roadmap from new findings only. **Docs only, no code change; the build track stays paused** — the next build starts when the owner picks a phase + re-confirms scope.
- [0063 — Park the remaining backlog (incl. the BP6 video timeline)](0063-park-remaining-backlog.md) — both recommended tracks are done (**BP1–BP8** + the **BP9–BP14 + BP17** Round-2 track). Owner call (2026-07-27): **park everything still open** — the deprioritised **BP12/BP15/BP16** *and* the one concrete deferred feature, the **BP6 video "timeline"** (who appears when, per-timestamp — a net-new `student_media_appearances` read + `GET /media/{id}/timeline`, deferred in 0043) — and keep a **single tracker** of what's skipped so it's visible at a glance. New **[`product/05-parked-backlog.md`](../product/05-parked-backlog.md)** lists every parked item in one place: the parked phases (BP12/BP15/BP16, incl. event-hard-delete + retention folding into BP16), the parked feature (BP6 timeline), the documented scale-up/polish refinements (offset→keyset, analytics snapshot table, async thumbnails, sliding-window rate-limit + nonce CSP, FAISS lease auto-extension, OTel, FE polish), and the pending live-smoke/Docker verification. **Docs only, no code change** (also corrected a stale BP11 header in `04` to ✅ landed). The product build track is **paused** — the next build starts only when the owner picks a parked/new item + re-confirms scope.
- [0052 — Product Build BP8d: Multi-replica enrollment (FAISS write lock, Option B / Redis)](0052-product-build-BP8d-multi-replica-enrollment.md) — the fourth slice of **BP8 (Ops & reliability)**; delivers the "Option B" 0011/architecture §7.4 promised. Enrollment serialized each school's FAISS index writes with an in-process `asyncio.Lock` — the fleet lock **only** under single-replica enrollment (a SPOF). Now the per-school write lock is a **pluggable `WriteLockProvider`** (an adapter seam, not a domain port — the FAISS index is its only user, so `domain`/`orchestration` stay pure): `InProcLockProvider` (Option A, default, the same asyncio.Lock moved out of `IndexCache`) or `RedisLockProvider` (Option B, config-gated `ML_FAISS_LOCK_IMPL=redis`) — a per-school `redis.asyncio` token-checked `Lock` (`SET NX PX` + Lua release) reusing the shared Redis client — so **enrollment can run multi-replica** (reads were already cross-replica-safe via `meta.version` reload). **Fail-LOUD** (the deliberate opposite of BP8c's fail-open rate limiter): a lock-backend outage or a `wait_s` timeout **raises `LockAcquisitionError`** (→ ML 500 → the backend records `enrollment_status=failed`, retryable) — never a silent unlocked write, which would risk a lost enrollment (correctness > availability). The critical section (`load → rebuild → save[meta last] → invalidate`) is unchanged; the lock just wraps it. New `ML_FAISS_LOCK_IMPL`/`_LEASE_S`/`_WAIT_S` settings + a compose comment on scaling; **no default change**. Honest limits (documented): a write slower than `lease_s` (60s) auto-expires the lock under a live holder (loudly logged `faiss_write_lock_lease_lost`; `lease_s` must exceed the slowest write); `wait_s`+enroll must stay under `BE_ML_HTTP_TIMEOUT_S`; the redis lock's thread-local token needs acquire+release on one thread (they are). ML 137 passed (+ gated real-Redis mutual-exclusion/concurrency/wait-timeout + an always-on fail-loud + release-swallow + adapter-uses-provider spy + container selectors) + layering; **no migration, no backend/FE change, no ML-contract change**; 2× review loop (R1 no blocker — cardinal invariant/fail-loud/singleton/no-reentrancy verified; R2 no code blocker → added the coverage). Deferred: **BP8e**.
- [0051 — Product Build BP8c: Rate limiting + security headers](0051-product-build-BP8c-rate-limiting-security-headers.md) — the third slice of **BP8 (Ops & reliability)**; redeems the two items 0029 deferred. Pure hardening, **no migration, no ML change, on by default**. A `RateLimiter` port + FastAPI middleware throttles per a **global** tier + a stricter **`/v1/auth/*`** tier + a per-**`school_id`** tier (verified from the JWT; a forged/unverified claim is never trusted — the backend can't see the client IP behind the BFF, so there's no per-IP tier). Pluggable store — **in-memory** (per-replica) default, **Redis** config-gated (reuses `BE_REDIS_URL`) — both **fail-open** (a store outage never blocks the API). First tier exceeded → **429 + `Retry-After`** + a cardinality-safe `rejections{scope}` metric; `/healthz`/`/readyz`/`/metrics` exempt. Middleware order (outer→inner) = **security-headers → CORS → rate-limit → metrics → routes** (so headers reach the 429/errors, the 429 isn't folded into the HTTP metrics, and CORS preflight is unthrottled); the limiter is built **per-app** (not the process container) so the test suite can't accumulate counts. **Security headers on BOTH** the backend (defense-in-depth: nosniff / frame / referrer / restrictive CSP / HSTS-gated) and the **frontend** (`next.config.ts` `headers()` — the browser-facing set incl. `frame-ancestors 'none'` + a Supabase-scoped CSP + COOP + HSTS; `'unsafe-eval'` dev-only). New `BE_RATE_LIMIT_*`/`BE_SECURITY_HEADERS_ENABLED`/`BE_HSTS_*` env vars. Honest limits (documented): fixed-window 2× burst, the auth tier is a single global bucket (default 300/min; per-IP belongs at the ingress), the `'unsafe-inline'` CSP (nonce-strict is a follow-up), in-memory = per-replica. BE 385 passed (+ gated real-Redis round-trip + always-on fail-open); FE gate green; 2× review loop (R1/R2 no blocker → fixed the redis `aclose` leak, probe exemption, auth default, +4 tests). Deferred: **BP8d–e**.
- [0050 — Product Build BP8b: Access / download audit](0050-product-build-BP8b-download-audit.md) — the second slice of **BP8 (Ops & reliability)**. The backend minted a signed download URL for any entitled caller but kept **no record**; now every entitled download is written to an append-only **`download_audit`** table (migration `0010`), surfaced to school-admins two ways: a per-photo history + a paginated school-wide "Access log". **Trust, not compliance.** Recording is a **separate `POST /media/{id}/download` action** (`GalleryService.record_download`), NOT the `GET` signed-URL mint — the mint is shared with **viewing** (`SignedImage` renders every tile/lightbox/detail off it), so a mere view is never logged; the FE fires the POST **only on the actual save** (fire-and-forget, so the audit never blocks a download), same entitlement gate (a blocked/404 download records nothing). `actor_role`/`event_id` are denormalized (the log survives a deleted account via FK **SET NULL**), `subject_student_id` set only for a student self-download. New **`audit:view`** perm — **school_admin only** (a one-line `ROLE_PERMISSIONS` flip adds teachers later); reads (`AuditService`) compose names in-Python, tenant strictly from the token. Mirrors the BP5 `match_corrections` structure (VO + port + postgres adapter + registry/container). FE: a self-gated `DownloadHistory` panel on the staff photo detail + lightbox, and a `(school)/audit` server-paginated log (nav + `RoleGate` school-admin-only). **No new env var, no ML change.** Migration verified up→down→up on a throwaway DB + a gated real-Postgres round-trip (incl. newest-first ordering); BE 368 passed; FE gate green; 2× review loop (**R1 no security/tenant bug** — added a stable sort tiebreaker; **R2 no blocker** — removed misleading client-side filter chips → clean server pagination) **+ an owner-caught fix**: the audit was first hooked on the shared signed-URL mint, so a *view* logged a download — split into a separate `POST` download action. Deferred: **BP8c–e**.
- [0049 — Product Build BP8a: Failed-photo state + retry](0049-product-build-BP8a-failed-photo-retry.md) — the first slice of **BP8 (Ops & reliability)** (sliced a–e). A photo the ML worker can't process used to look `pending` **forever** (silently skipped); now it's marked **`failed`** — visible + retryable. **Migration `0009`** widens `ck_media_processing_status` to add `failed` (the ML worker writes it, so it must apply before the worker deploys); `InferenceService` marks a photo `failed` on fetch/decode/other error via a new `BackendEventStore.mark_media_failed` (the systemic `EmbeddingVersionMismatch` still aborts the event + marks nothing). Retry = redistribute — the worker re-attempts any non-`completed` photo, and the backend's `process_event` guard was **widened to allow `failed`-only events** (else "Retry failed" would 400). `EventStatusResponse.failed` + FE surface (a warning note + a "Retry failed" button). Cross-service (BE + ML + FE), no ML-contract break. Migration verified up/down on a throwaway DB + a gated real-Postgres `mark_media_failed` round-trip; BE 346 / ML 128 passed; 2× review loop (**R1 caught a blocker** — the enqueue guard; R2 → softer transient-failure copy + 2 test guards). Deferred: **BP8b–e**.
- [0048 — Product Build BP7d-2: Reference-photo set/replace](0048-product-build-BP7d2-reference-photo-replace.md) — the deferred BP7d follow-up that **completes BP7**: an in-place **`PUT /v1/students/{id}/reference-photo`** that sets/replaces a student's reference photo then (re-)enrolls — so a **photoless (bulk-imported) student can enroll** ("Add photo") and staff can **swap a bad photo to fix a failed enrollment** ("Replace photo", closing BP7b's loop). **No migration** (column already nullable from `0008`), no ML change. `StudentService.set_reference_photo` = tenant-scoped `get_student` (foreign→404 **before** the path guard) → the same upload-prefix guard → `StudentRepository.set_reference_photo` → `_run_enroll` (clears the failure reason on success). FE: a `ReferencePhotoDialog` on the student detail (Add/Replace, upload-path memoized), **Re-enroll hidden for a photoless student** (would 400), and the failure-note fix-copy now points at "Replace photo". 345 passed + a gated real-Postgres round-trip; 2× review loop (R1 no bugs; R2 → stale-comment/copy + upload memo + 2 test guards).
- [0047 — Product Build BP7d: CSV bulk student import + student invite model](0047-product-build-BP7d-csv-bulk-import.md) — the flagship "bulk setup": import a class from a **name+email CSV**. **Migration `0008`** makes `students.reference_photo_path` **nullable** (bulk students are created **photoless → pending**, no ML call); students join BP7c's **invite model** — `create_student` **server-generates** the temp password (shared `services/credentials.generate_temp_password`) + the photo is optional → `ProvisionedStudentResponse {student, temp_password}` (shown once, hash-only). `POST /v1/students/bulk` loops the reusable two-write (compensating-delete) over ≤500 `{name,email}` rows **best-effort** — per-row `created`/`duplicate`/`invalid`/`error`, only `created` carries a temp password; batch never aborts; `enroll_student` guards a photoless student (400). FE: a dependency-free CSV parser (`lib/csv.ts` — quotes/CRLF/escapes/**BOM strip**/header detection), a 3-step `BulkImportDialog` (pick→preview→results) + **"Download credentials"** CSV; single-create drops the typed password (server-gen, shown once) but keeps the photo required. **Owner scope: bulk-first** — reference-photo **set/replace** (enrolls photoless students, closes BP7b's replace loop) is **BP7d-2**. Migration verified up/down on a throwaway DB + gated photoless round-trip; 337 passed; 2× review loop (R1 no bugs, security/CSV/migration airtight; R2 → BOM strip, re-pick, SR phase announce, Download-primary hierarchy, +2 regression tests). **No ML change.**
- [0046 — Product Build BP7c: Staff lifecycle + invite model](0046-product-build-BP7c-staff-lifecycle-invite.md) — turns staff/admin provisioning into a proper **invite model** + lifecycle, **no migration** (`users.status` already existed; auth already rejects disabled). Create-teacher/add-admin now **server-generate** a temp password (`secrets.token_urlsafe`, CSPRNG) returned **once** as `ProvisionedUserResponse {user, temp_password}` (drop `password` from the request; plaintext never in list/get/me, never logged, hash-only persisted); **disable/enable** via `PATCH …/{id}` + a new `UserRepository.set_status` (real lockout — auth rejects at login/refresh/`get_current_user`); **resend-invite** via `POST …/{id}/resend-invite`. One entitlement guard `_require_managed_user` (target must match school **and** role, else **404** — blocks cross-tenant, wrong-role, self-action). FE: a shared shown-once **`InviteResultDialog`** (Copy button) + per-row **Resend / Enable-Disable** actions (aria-labelled). **Scope:** students stay caller-supplied (→ BP7d); capacity "of M" is platform-side (BP2), admin `/staff` shows a count; last-active-admin lockout allowed (platform-recoverable, documented). 327 passed + gated `set_status` round-trip + a disabled→login-401→re-enable-200 e2e; 2× review loop (R1 no bugs, security airtight; R2 → per-row aria-labels + resend-revalidate + 2 regression-guard tests).
- [0045 — Product Build BP7b: Reference-photo enrollment quality feedback](0045-product-build-BP7b-enrollment-quality-feedback.md) — surfaces **why** an enrollment failed instead of a bare "Failed". The ML enroll response already carries a per-photo `detail`/status that the backend parsed then **discarded**; now `StudentService._run_enroll` maps it to a closed `EnrollmentFailureReason` `{no_face, ml_unavailable, error}` (`multiple_faces` isn't a failure — the ML enrolls the largest face), persists it (**migration `0007`**: nullable `students.enrollment_failure_reason` + CHECK), **clears it on a successful (re-)enroll**, and exposes it on `StudentResponse`. FE: a reason-specific title+fix `EnrollmentFailureNote` (`role="alert"`) on the student detail + a compact reason on the list. **No ML change.** The cross-service `"no_face"` value is a pinned named constant + tests. **Honest scope:** the fix action — replacing the reference photo in place — is **BP7d**; until then `no_face`/`error` copy steers to delete-and-re-add (Re-enroll retries the same photo, fixing only `ml_unavailable`). Migration verified up/down on a throwaway DB + gated real-Postgres adapter test; 2× review loop (R1 no bugs; R2 → named constant, a contract-pin test, a route serialization assertion, sharper copy).
- [0044 — Product Build BP7a: First-run setup checklist](0044-product-build-BP7a-setup-checklist.md) — slices **BP7 (Onboarding & bulk)** into four sub-phases (checklist · quality-feedback · staff-lifecycle+invite · CSV-import) and ships the first: a server-composed **`setup_checklist`** block on `GET /v1/dashboard` (5 booleans) + a guided **`SetupChecklistCard`** on the FE dashboard that retires once the school has distributed. **Query-only: no migration, no ML.** Only 2 signals are net-new — `has_staff` (the existing `count_by_school_and_role(TEACHER)` method, wired via a new `users` dep) and `has_distributed` (a new `EventRepository.count_distributed` aggregate mirroring BP4's "announced" predicate, status-agnostic, tenant-scoped). The other 3 (`has_enrolled_student`/`has_event`/`has_media`) compose off existing counts (`enrolled > 0`, not merely-added). FE: the checklist supersedes the old `FirstRun`; four **core** steps (enroll→event→upload→distribute) drive progress + the primary CTA, "add a teacher" is **optional/last** (solo admins never need one). Gated real-Postgres coverage for `count_distributed`; 2× review loop (R1 no bugs; R2 fixed D6 CTA wording, a D8 contrast token, and the primary-CTA-on-optional-step). Deferred: **BP7b–d**.
- [0043 — Product Build BP6: Video end-to-end](0043-product-build-BP6-video-end-to-end.md) — surfaces the already-built ML video pipeline in the UI, **FE-only** (no backend/ML change, no migration, no new dep). `SignedImage` gains a `kind`/`asPlayer` **video branch** (full `<video controls>` in the lightbox/detail; a muted `#t=0.1` first-frame **poster** + play badge in grid tiles) rendered off the **same signed URL** the `<img>` used — confirmed inline-served (no `download` disposition) with range requests, so it streams + seeks unchanged. `PhotoGrid` moves from `mediaIds` to **`items: GalleryItem[]` (`{id, mediaType}`)**, deriving index-aligned `mediaIds`/`mediaTypes` for the Lightbox; the **event uploader accepts video** (`mediaTypeOf` by MIME → registers the real `media_type`; `registerMedia`/`RegisterMediaRequest` already accepted `"video"`). Reference-photo upload stays image-only. **Honest limits** (documented): video size bounded by the existing `BE_MAX_UPLOAD_MB` (30 MB) + bucket limit; no stored poster thumbnail; lightbox arrows navigate over video seek. FE gate green; 2× review loop (R1 traced the inline-serve + array alignment, reverted a layout-changing tile class; R2 media-aware a11y/copy). Appearances on a video reuse the **BP5 overlay** unchanged. Deferred: the "who appears when" **timeline** (a new isolated `student_media_appearances` read + `GET /media/{id}/timeline`, corrections-overlaid).
- [0042 — Product Build BP5: Trust & accuracy loop](0042-product-build-BP5-trust-accuracy.md) — turns matching into a **correctable** loop via a backend-owned `match_corrections` overlay (verdict `confirmed`/`rejected`/`added`) keyed on the stable `(media_id, student_id)` — **no ML change, no cross-seam SQL join**. **Reject → hide + block download** (the security-sensitive effective-appearance gate, unit-tested truth table); students **self-serve "this isn't me"**; staff **confirm/reject/undo** + **report-a-miss** (add a missed student → they see + can download) + a **"Needs review (N)"** lane. Three pure overlay helpers (`effective_media_student_ids`/`effective_event_pairs`/`effective_student_pairs`) shared by `GalleryService` (all 6 reads + the download gate), the new `ReviewService`, and (revising BP4) `NotificationService` — so a rejected student is never notified/rostered and an added one is. Migration `0006` (`match_corrections`); dashboard "N to review" becomes `raw − resolved` (clamped); the BP2 list rollups stay **raw** (documented divergence). New `match:review` perm; staff `review.py` + student `me.py` routes. Gated real-Postgres coverage; migration verified up+down on a throwaway DB; 2× review loop (R1 verified the entitlement gate has no bypass; R2 fixed coverage + a11y + the notification-overlay gap).
- [0041 — Product Build BP4: Distribution ("Photos are ready")](0041-product-build-BP4-distribution.md) — the flagship: real *delivery* of photos to students. An authoritative, cross-device in-app **"new photos"** signal (**derived** — no per-student rows, no backend worker, no ML change: announced = `notified_at` set OR (`auto_notify` AND `completed_at`); unseen = no read OR `seen_at` < effective announce; composed in-Python, never a SQL join to the `matches` seam), plus staff **"Notify students"** / an **auto-announce** toggle / a **Notified·Seen roster**, and a **multi-channel notifier seam** (`NotificationChannel` port + `CompositeNotifier` fanning out to a `BE_NOTIFICATION_CHANNELS` comma-list — `log` now, email/whatsapp future drop-ins that compose together or one at a time; best-effort, PII-free). Migration `0005` adds `events.auto_notify`/`notified_at` + a `notification_reads` table, and `set_processing` now keeps `completed_at` **set-forward** on redistribute (a deliberate 0027 tweak). Honest limit: auto drives only the in-app signal (outbound-on-auto needs a future worker). New `notification:send` perm; staff routes on `events.py`, student on `me.py`. FE: a student nav badge + `/me` banner (mark-seen on unmount) **superseding BP3's client `useNewSince`**, and a staff `DistributionCard`. Migration verified up+down on real Postgres; 2× review loop (fixed a badge aria-label + a stale docstring + coverage).
- [0040 — Product Build BP3: Student receive experience](0040-product-build-BP3-student-receive-experience.md) — the student `/me/events` gallery goes **Pinterest-grade**, **FE-only** (no backend/ML; one tiny new dep `client-zip`): real **natural-aspect masonry** (a `variant` on the shared `PhotoGrid`/`PhotoTile` — `MasonryTile` for students, `GridTile` unchanged for staff) with a restrained hover zoom + hover-download; a warm hero ("Only you can see these") + **first-visit welcome** + **"N new since your last visit"** (`useNewSince`, per-user localStorage, rAF-deferred read, settled-non-empty commit); **download-all** as one client-zip (`useDownloadAll`, bounded concurrency, skip-failures, partial-result toast + `aria-live` progress); and a **global `prefers-reduced-motion` guard** in `globals.css` (closing a gap F7 claimed but never enforced). Appearances stay hidden + download entitlement-scoped for students. 2× review loop fixed a `useNewSince` transient-empty bug + the a11y/announcement gaps.
- [0039 — Product Build BP2: List data-richness & scale](0039-product-build-BP2-list-richness.md) — the four admin lists gain **per-row counts + search/filter/sort** (query-only, **no migration, no ML change**). A new **`ListingService`** (isolated, ports-only) zips per-row counts to rows in-Python (no N+1) via new batched aggregates on the existing repos + the `matches` seam (`event_match_counts`/`student_appearance_counts` — only already-declared columns) + `EventMatchCounts`/`StudentAppearanceCounts`/`SchoolRollup` value objects. List-variant schemas (`EventListItem`/`StudentListItem` add flat counts, `SchoolWithRollupResponse` nests a rollup) keep the write DTOs frozen; a new **`GET /v1/schools/{id}/admins`** roster + an additive `UserResponse.created_at`. FE: shared `SearchInput` + `useSort`/`SortableHead` (direction-aware, tie-stable) + the F5 `FilterChips`; students (appearance/event counts + enrollment filter), events (photos/matched/needs-review + status filter), schools (rollup columns + `StatCard` detail + admin roster), staff (added-date). Gated real-Postgres tests cover all six aggregates' SQL + tenant scoping; 2× review loop fixed a sort tie-stability bug + coverage/a11y.
- [0038 — Product analysis + Build BP1: Admin Command Center](0038-product-build-BP1-admin-command-center.md) — the whole-product analysis lands as a durable **`product/`** doc set (`00` knowledge base · `01` lenses/rubric/targets · `03` the phased **BP1–BP8** roadmap; consent out of scope) and **BP1** ships the first build: a read-only **`GET /v1/dashboard`** command center (student/event/photo rollups + needs-attention signals) — grouped/EXISTS/filtered queries on the backend's own rows + the isolated `matches` seam (`MlResultsReader.count_needs_review`, only already-declared columns), composed by a pure `DashboardService`; new aggregate methods on the existing repos + `EventRollup`; new `dashboard:view` perm (school_admin + teacher, tenant from token); the "undistributed" alert counts only **active** events (archived can't be Processed). FE: the placeholder `/dashboard` → a real command center (`StatCard` row, needs-attention alerts, first-run invitation) + **nav information-scent badges** via a shared `useDashboard` (SWR key `"dashboard"`, `enabled` gates off non-staff). **No migration, no ML change**; gated real-Postgres tests cover every new aggregate's SQL + tenant scoping; 2× review loop (fixed the active-only alert + added the gated coverage).
- [0037 — Frontend polish + hardening (Phase F7)](0037-frontend-polish-and-hardening.md) — the final cross-cutting pass (**no backend change**), completing the FE build-out: **`app/{error,not-found,global-error}.tsx`** (the segment boundary uses `reset`, not the `unstable_`-prefixed 16.2 `unstable_retry` — render faults want a re-render; documented) unified with `FullPageError` into one **`FullPageMessage`** (a real `<h1>`); a **mobile nav drawer** (Radix Dialog — the sidebar was desktop-only) with shared `NavList`/`UserFooter`, close-on-navigate + `matchMedia` resize-close, and the redundant mobile logout dropped; an **a11y sweep** — `FilterChips` → a proper **radiogroup** (roving tabindex + arrow nav), error announcement (`EmptyState` gains a `role` prop → `role="alert"` on the 13 error states; `GridSkeleton` `role="status"`; full-page titles → `<h1>`) — redeeming F6's deferred gap; a **consistent-error-UX** audit confirmed complete (401→login, 403/409/502→toast, 404→in-page/router, render fault→`error.tsx`); the **Lightbox** mobile panel now scrolls; the dead `ComingSoon` removed; and a **frontend README**. Deferred: dark-mode toggle, per-page `aria-busy` wrappers, a service-side error logger. 2× review loop **no blockers** (R1 verified the boundary contracts against the *installed* Next 16.2.9 docs).
