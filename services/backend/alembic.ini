# Alembic config for the backend metadata DB.
# The DB URL is NOT stored here — env.py reads it from BE_DATABASE_URL
# (postgresql+asyncpg://...) so no secrets live in the repo. This chain shares the
# database with the ML chain but uses its own version table (see env.py).
[alembic]
# %(here)s = the directory of this ini file, so it works from any CWD.
script_location = %(here)s/src/backend/db/migrations
prepend_sys_path = %(here)s/src
path_separator = os
# Migration filenames: 0001_slug.py (see env.py for revision generation).

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
